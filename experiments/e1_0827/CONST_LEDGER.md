# CONST_LEDGER — e1_0827 상수 대장

- 작성 2026-08-28 · 근거 문서: `Docs/briefs/edge_relabel_brief_v6.md` §3-P1
- 대상: `experiments/e1_0827/code/` 의 신규 라벨링 골격이 실행에 쓰는 **모든** 상수
- 상수 정본은 코드 `code/e1_const.py` 의 `CONSTANTS` 표 하나뿐이다. 이 문서는 그 표의 사람용 판본이고,
  `python3 code/e1_selftest.py --ledger` 가 **양방향**으로 대조한다(코드에 있는데 대장에 없는 것 0건,
  대장에만 있는 유령 이름 0건).
- 상수 지문(fingerprint) `da937f4a3f98f688` (2026-08-28 simple_edge 상수 18개 추가 전: `e1bc1c63af9ffe98`) — 이 값이 바뀌면 상수 세트가 바뀐 것이고, 모든 산출 json 안에
  `const_fingerprint` 로 같이 찍힌다.

---

## 0. 승용 요약 (5문장)

1. 이 골격이 쓰는 숫자는 **62개**이고, 하나도 빠짐없이 아래 표에 값·출처·이유·민감도 계획과 함께 적혀 있다
   (`--scan-consts` 결과 **기록 없이 쓰인 상수 0건**, `--ledger` 결과 **누락 0건 · 유령 0건**).
2. 이 중 **아무것도 채택된 것이 아니다.** 판정에 실제로 영향을 주는 값은 `[임시-결재대기]` 로 찍혀 있고,
   골격이 돌아가기만 하도록 놓아둔 임시값이다 — 채택은 승용 결재다(P1).
3. 유일하게 우리가 정하지 않은 확정값은 낙차 문턱 **0.3 m** 하나뿐이다(브리프 §1 [문헌·기존 확정]).
4. 민감도는 말이 아니라 **실측**으로 붙였다 — 스모크 3프레임을 상수 대안값마다 다시 돌려서 수치가 얼마나
   움직이는지 §3에 인쇄했다. 움직이지 않은 상수(허용오차 `VIS_TOL_M` 등)도 "안 움직였다"를 그대로 적었다.
5. 결재가 필요한 항목은 §4 결재란에 ❓ 번호로 모아 두었다.

---

## 1. 태그 규약

| 태그 | 뜻 |
|---|---|
| `[문헌]` | 출처 있는 논문·표준·발표된 알고리즘 규칙 |
| `[실측]` | 이 저장소의 데이터를 재서 얻은 값 |
| `[도구기본값]` | 라이브러리·도구의 기본 설정 |
| `[기존값]` | 구 코드/렌더러에서 이월했고 출처를 특정할 수 있는 값 |
| `[방법]` | 판정이 아니라 계산·보고 방식을 정하는 값(자릿수, 표 폭 등) |
| `[임시-결재대기]` | **판정에 영향을 주는데 근거가 아직 없는 값.** 골격 실행용 임시값 — 결재 대상 |

---

## 2. 상수 표

### 2.1 이미지·카메라 모델

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `IMG_W` | 1920 | [기존값] | 렌더러 해상도 `variation_kit.py:43 RES_W`. 읽어들인 모든 `*.depth.npy` 의 shape 와 대조 확인 | — (측정으로 확인) |
| `IMG_H` | 1080 | [기존값] | 같은 출처 (`variation_kit.py:43` 의 세로 해상도) | — |
| `PRINCIPAL_POINT_MODE` | `center` | [기존값] | `variation_kit.look_at_rows` 주석 "principal point dead centre of the 1920x1080 frame" → cx=W/2, cy=H/2 | — |
| `PIXEL_ASPECT` | 1.0 | [기존값] | `variation.json` 은 가로 aperture(20.955 mm)와 focal 만 저장한다 → fy=fx(정사각 픽셀). 실측 확인: `2·atan(aperture/2/focal)` 가 저장된 hfov 를 **4.5e-06 deg** 이내로 재현 | — |
| `DEPTH_VALID_MIN_M` | 0.01 | [방법] | 이보다 작은 depth 표본은 무효(근접 클립). 하늘은 이미 `inf` 라 `isfinite` 로 걸린다 | 0.001 / 0.01 / 0.1 |
| `CAM_FRONT_Z_MIN_M` | 0.05 | [방법] | 투영 시 0 나눗셈 방지. 이 앞에 있는 점만 투영 | 0.01 / 0.05 / 0.20 |

### 2.2 카메라 검증 전용 (라벨에 안 들어감)

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `CAMCHK_STRIDE_PX` | 4 | [방법] | 검증용 역투영 픽셀 간격. 보고 전용 | 1 / 4 / 8 |
| `CAMCHK_RADIUS_M` | 6.0 | [방법] | 높이장은 AABB 포락면이라 원거리·건물 화소의 불일치는 카메라 오차가 아니다 → 근거리로 잘라서 같이 인쇄 | 3 / 6 / 10 (셋 다 §3.1 에 인쇄) |

### 2.3 route (a) — 높이장에서 edge 인스턴스

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `DROP_MIN_M` | 0.30 | [문헌·기존 확정] | 브리프 §1 "낙차 위험 문턱 0.3 m", §2 규칙 1. **우리가 못 건드리는 유일한 확정값** | 해당 없음(확정) |
| `R_RUN_M` | 1.0 | [기존값] | 진행거리. `mainrun_0819/code/labeling/labeler.py` 의 `STEP_RUN_M = 1.0` (D10) 이월. 브리프 §2 규칙 1 이 민감도 병기를 **의무화** | 0.5 / 1.0 / 2.0 → §3.2 실측 |
| `HM_STEP_SOURCE` | `heightmap_meta.json:step` | [기존값] | 높이장 격자 해상도는 **씬마다 파일에서 읽는다**(직접 적지 않는다). 실측: 260819_main_on 33씬 전부 **0.05 m** | — |
| `EDGE_WINDOW_SHAPE` | `disc` | [방법] | 격자에서 "진행거리 `R_RUN_M` 안"을 구현하는 이웃 창 모양. `disc`(현행)가 문면 그대로다. `chebyshev`(정사각)는 축 분리가 돼서 ~40배 빠르지만 대각으로 `R_RUN_M`×√2 = 1.41 m 까지 닿는 **과대 근사**, `cross`는 축 방향만 보는 과소 근사. **센서스 트랙의 보고 전용 프로브 `code/p0_edge_distance.py` 는 `chebyshev` 를 쓴다** → 그 도구의 거리 수치는 이 파이프라인과 직접 비교하면 안 된다(요청서: `reports/P0_CONST_ADDENDUM.md`) | `disc` / `chebyshev` / `cross` → §3.5 실측 |
| `BREAK_LOCAL_FALL_M` | 0.05 | **[임시-결재대기]** | 셀 하나 사이(4-이웃)의 낙하량. 이게 없으면 R_RUN 안에서 0.3 m 내려가는 **완만한 경사로**도 턱 선이 된다 | 0.02 / 0.05 / 0.10 → §3.2 |
| `WALK_FOOT_R_M` | 0.30 | **[임시-결재대기]** | 보행 가능성 판정 발자국 반경. 난간 윗면·난간동자·옥상 모서리(높이장에선 이것도 0.3 m 낙차다)를 모집단에서 빼는 장치 | 0.15 / 0.30 / 0.50 |
| `WALK_FLAT_TOL_M` | 0.10 | **[임시-결재대기]** | 발자국 안에서 허용하는 높이 산포 | 0.05 / 0.10 / 0.20 |
| `WALK_SUPPORT_FRAC` | 0.25 | **[임시-결재대기]** | 턱 셀은 발자국이 낙차를 걸쳐서 절대 "평평"할 수 없다 → 대신 발자국의 이 비율 이상이 같은 높이면 보행면으로 인정. 난간 윗면은 0.30 m 원판 안에서 1~2셀 폭이라 여기서 걸린다 | 0.15 / 0.25 / 0.40 → §3.2 |
| `MAX_TRAVERSE_STEP_M` | 0.20 | **[임시-결재대기]** | "로봇이 실제로 서 있는 보행면"을 정하는 flood fill 의 단차 한계. 근거: 건축 계단 챌면 0.15~0.18 m(계단 발판은 전부 도달 가능해야 한다) 대 옥상·파라펫은 도달 불가. **이 로봇에 대한 문헌값이 아니다** | 0.15 / 0.20 / 0.30 → §3.2 |
| `REACH_ANCHOR_MODE` | `largest_ground_component` | **[임시-결재대기]** | 도달 가능성 flood fill 의 출발점. 첫 규칙(`camera_cell_clamped`)에서 **실측 사고가 났다**: 이 코퍼스의 카메라는 높이장 격자 밖에 서는 일이 흔한데(scene02 컷0 eye x = −8.62, x_range [−2.0, 14.0]), 격자 가장자리로 잘라서 앉히니 앵커가 2.84 m 벽에 갇힌 **29셀 조각**에 떨어졌다 — 지면 셀이 83,925개인 씬에서 도달 셀 29개. 지금 규칙은 `cam.ground_z` ±`MAX_TRAVERSE_STEP_M` 안의 **가장 큰 연결 지면 성분**에 앉힌다 | `camera_cell_clamped` / `largest_ground_component` → §3.4 실측 |
| `MIN_INSTANCE_LEN_M` | 0.30 | **[임시-결재대기]** | 연결 성분 길이가 이보다 짧으면 잡티로 버린다 | 0.15 / 0.30 / 1.00 |
| `INT_ASSIGN_INFRAME_ONLY` | False | **[임시-결재대기]** | '속' 픽셀의 주인을 정하는 규칙. False(현행)=순수 최근접 인스턴스 보로노이 → 결합 반경을 지어내지 않아도 되지만, **화면 밖 인스턴스가 속 픽셀을 가져간다**(실측: scene01 컷0 의 e03/e04 가 화면 안 edge 점이 0개인데 85,985 px·45,151 px 를 소유). True=화면 안 점이 하나라도 있는 인스턴스만 소유 | False / True → §3.3 실측 |
| `RDP_TOL_PX` | 2.0 | [방법] | 가시 구간을 `polyline_px` 로 줄일 때의 Douglas-Peucker 허용오차. **저장 압축 전용** — `dist_m`·`src_disagree_px` 는 줄이기 전 점으로 잰다 | 1 / 2 / 4 |

### 2.4 가시성·가림

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `VIS_TOL_M` | 0.15 | **[임시-결재대기]** | 3D 점이 "보인다" = `|z_cam − 렌더 depth| ≤ 이 값`. 참고(채택 아님): 구 `labeler.py` 는 `RIM_TOL_M = 0.35` 를 3×3 창과 함께 썼다. 이 골격은 최근접 1픽셀만 본다 → 두 값은 서로 바꿔 쓸 수 없다 | 0.05 / 0.15 / 0.35 → §3.3 실측 |
| `PRIM_CLASS_RULES` | `code/prim_class_rules.json` | **[임시-결재대기]** | §2 규칙 3(외부 가림물 판정)의 prim 경로 → 분류 표. **일부러 비워서 배포한다** — 규칙이 0개면 모든 가림 prim 이 `unknown`, `occluder.flag` 는 `null`, 프레임은 미해결 목록으로 간다. 채우는 것이 결재(❓B-4) | 규칙 채택 후 재실행 |

### 2.5 route (b) — depth 불연속 (전 프레임 공통 단일 세트)

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `B_REL_JUMP` | 0.05 | **[임시-결재대기]** | 상대 depth 점프: `|Δd| ≥ B_REL_JUMP·d`. 0.3 m 단차가 멀어질수록 depth 차이가 줄기 때문 | 0.02 / 0.05 / 0.10 → §3.3 |
| `B_ABS_JUMP_M` | 0.10 | **[임시-결재대기]** | 같은 판정의 절대 하한: `|Δd| ≥ max(B_ABS_JUMP_M, B_REL_JUMP·d)`. 근거리 텍스처에서 상대항만으로 터지는 것 차단 | 0.05 / 0.10 / 0.30 → §3.3 |
| `B_MIN_COMP_PX` | 20 | **[임시-결재대기]** | 세선화 후 연결 성분 최소 픽셀 수 | 10 / 20 / 50 |
| `B_THIN_METHOD` | `Zhang-Suen 1984` | [문헌] | T. Y. Zhang & C. Y. Suen, *A fast parallel algorithm for thinning digital patterns*, CACM 27(3):236-239, 1984. 발표된 규칙이라 자유 파라미터가 없다. **직접 구현**한 이유: 저장소가 의무화한 `PYTHONNOUSERSITE=1` 환경에서 `skimage` 가 import 되지 않는다(실측) — route (b) 상수가 "user-site 패키지가 보이느냐"에 의존하면 안 된다 | 해당 없음 |
| `ZS_B_MIN` | 2 | [문헌] | Zhang-Suen 조건 (a) 하한 | — |
| `ZS_B_MAX` | 6 | [문헌] | Zhang-Suen 조건 (a) 상한 | — |
| `ZS_A_TARGET` | 1 | [문헌] | Zhang-Suen 조건 (b): 이웃을 한 바퀴 돌 때 0→1 전이가 정확히 1회 | — |
| `B_THIN_MAX_ITERS` | 64 | [방법] | 반복 안전 상한. 알고리즘은 변화가 없으면 스스로 멈추고, **실제 도달 반복 수**를 진단에 인쇄한다(스모크 3프레임 실측: 7·8·9회) | — |

### 2.6 (a)–(b) 대조

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `CMP_METHOD` | `nearest-point EDT` | [방법] | 인스턴스별로, **보이는** (a) 투영점마다 가장 가까운 (b) edge 픽셀까지의 px 거리(`scipy.ndimage.distance_transform_edt`). {평균·중앙값·p90} 으로 보고하고 **합격선은 두지 않는다**(브리프 §4 Phase 1) | 해당 없음 |

### 2.7 레코드 덤프(결정론)

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `JSON_DECIMALS` | 4 | [방법] | 정본 json 고정 소수 자릿수. 4자리 = 미터로 0.1 mm, 픽셀로 1e-4 px — 여기 나오는 어떤 물리 척도보다 작고, 부동소수가 두 번 다르게 찍히지 않을 만큼은 크다 | 3 / 4 / 6 |
| `JSON_SORT_KEYS` | True | [방법] | 키 정렬 + 개행 종료 + `ensure_ascii=False` → 같은 입력 두 번이면 바이트 동일 | — |

### 2.8 시각화·출력(측정값에 영향 없음)

| 상수 | 값 | 태그 | 용도 |
|---|---|---|---|
| `OVERLAY_LINE_PX` | 3 | [방법] | 오버레이 선 두께 |
| `OVERLAY_FONT_PX` | 18 | [방법] | 오버레이 주석 줄 간격 |
| `OVERLAY_PALETTE` | 8색 RGB | [방법] | 인스턴스별 색. 밝은 광장/어두운 지하도 양쪽에서 구분되게 고름 |
| `SORT_KEY_DECIMALS` | 6 | [방법] | **정렬 키에만** 적용하는 반올림 — 좌표가 같은 두 인스턴스의 순서가 실행마다 뒤집히지 않게. 저장값은 건드리지 않는다 |
| `PRINT_DP_FINE` | 6 | [방법] | 콘솔 자릿수(잔차 규모) |
| `PRINT_DP_COARSE` | 4 | [방법] | 콘솔 자릿수(미터 규모) |
| `PRINT_DP_SHORT` | 2 | [방법] | 콘솔 자릿수(보고 줄) |
| `CENSUS_TOP_N` | 30 | [방법] | `--prim-census` 가 콘솔에 찍는 prim 개수. 전수는 항상 `prim_blocker_census.json` |
| `SENS_FRAME_COL` | 28 | [방법] | 민감도 표의 프레임명 칸 폭 |
| `SHA_PREFIX_LEN` | 16 | [방법] | 지문으로 인쇄하는 sha256 앞자리 수 |

---

### 2.9 simple_edge.py — 단순 라벨러 (2026-08-28 추가)

`code/simple_edge.py` 는 "내가 선 면에서 낙차 위험이 시작되는 지점을 **실선 하나**로, 보이는 낙차면을
**마스크 하나**로" 만 만드는 별도 모듈이다. 판정 상수 4개는 **새로 만들지 않고 기존 행을 그대로 재사용**한다:
`DROP_MIN_M` 0.30 [문헌·기존 확정] · `R_RUN_M` 1.0 [기존값] · `WALK_FLAT_TOL_M` 0.10 [임시-결재대기]
(사양서가 말한 FLAT_TOL_M 이 이것이다) · `RDP_TOL_PX` 2.0 [방법]. 아래는 이 모듈이 **새로 더한** 값뿐이다.

| 상수 | 값 | 태그 | 왜 이 값인가 | 민감도 계획 |
|---|---|---|---|---|
| `DEPTH_MAX_M` | 60.0 | [방법] | 이보다 먼 depth 화소는 평탄/낙차 판정 전에 버린다. 안 버리면 내가 선 면 성분이 지평선까지 달려가 0.10 m 평탄 허용오차가 무의미해진다. 높이장을 뽑은 탐침 상한(`variation_kit.AabbPrefilter.ground_z(top=60.0)`)과 같은 값 | 30 / 60 / 120 |
| `MIN_EDGE_PX` | 30 | [방법] | 추적된 위험선 폴리라인이 이 픽셀 길이보다 짧으면 얼룩으로 보고 버린다. `MIN_INSTANCE_LEN_M` 의 화면좌표 짝 | 15 / 30 / 60 |
| `EDGE_BRIDGE_PX` | 5 | [방법] | 위험선을 3x3 로 이만큼 팽창시킨 뒤 어떤 낙차 성분이 닿는지 묻는다. 챌면(riser)은 선 면도 낙차면도 아니어서 그 틈을 건너기 위한 값 | 2 / 5 / 10 — **§3.6 에서 실측: 5 는 부족했다** |
| `BRIDGE_SWEEP_PX` | [2, 5, 10, 20, 40, 80] | [방법] | `--bridge-sweep` 가 6프레임을 다시 돌리는 `EDGE_BRIDGE_PX` 값 목록. 보고 전용 — 납품 라벨은 항상 running value 를 쓴다 | — (그 자체가 민감도 측정) |
| `SIMPLE_SEED_STRIP_FRAC` | 0.08 | [방법] | 카메라가 딛고 선 화소를 찾는 화면 아래 띠의 높이(프레임 비율) | 0.04 / 0.08 / 0.15 |
| `SIMPLE_SEED_HALF_W_FRAC` | 0.15 | [방법] | 같은 띠의 중앙 패치 반폭(프레임 비율). 지금은 보고용으로만 쓰이고 선택은 아래 띠 전체에서 한다(§3.7) | 0.08 / 0.15 / 0.30 |
| `SIMPLE_EDGE_RGB` | [255, 120, 0] | [방법] | 위험선 **단일** 색(주황). 미관 | — |
| `SIMPLE_MASK_RGB` | [220, 30, 30] | [방법] | 낙차 마스크 채움색. 미관 | — |
| `SIMPLE_MASK_OUTLINE_RGB` | [110, 0, 0] | [방법] | 마스크 가장자리 1 px 진한 빨강. 붉은 벽돌 포장 위에서도 범위가 읽히게. 미관 | — |
| `SIMPLE_MASK_ALPHA` | 0.35 | [방법] | 마스크 불투명도. 아래 포장 질감이 비쳐야 사람이 판정할 수 있다. 미관 | — |
| `SIMPLE_CAPTION_PX` | 26 | [방법] | 오버레이 캡션 글자 크기. 반려된 스모크 오버레이의 `OVERLAY_FONT_PX` 18 px 는 읽히지 않았다. 미관 | — |
| `SIMPLE_FONT_PATH` | `/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc` | [도구기본값] | 캡션·컨택트시트 라벨용 시스템 Noto CJK. 없으면 PIL 기본 비트맵으로 자동 강등 | — |
| `SHEET_TILE_W_PX` | 640 | [방법] | 컨택트시트 타일 한 장의 폭. 3 x 640 = 1920 px = 화면 하나 | — |
| `SHEET_JPEG_QUALITY` | 85 | [방법] | 컨택트시트 JPEG 품질. 미관 | — |
| `SHEET_LABEL_PX` | 22 | [방법] | 타일 위에 한 번 찍는 씬 이름 크기. 미관 | — |
| `SHEET_BG_RGB` | [18, 18, 18] | [방법] | 컨택트시트 배경(어두움). 미관 | — |
| `SHEET_MAX_MB` | 1.5 | [방법] | 컨택트시트 크기 예산. 달성값을 인쇄해 대조한다(실측 0.31 MB) | — |
| `BYTES_PER_MB` | 1048576 | [도구기본값] | 인쇄용 1 MiB | — |

## 3. 민감도 — 실측 (스모크 3프레임)

측정 대상 프레임(`code/smoke_frames.txt`):
`260819_main_on/scene01/L0__s20260819__0000` · `.../scene02/...0000` · `.../scene03/...0000`
재현: `python3 code/e1_sensitivity.py --key <상수> --list code/smoke_frames.txt [--full]`

### 3.1 `CAMCHK_RADIUS_M` (카메라 검증 반경) — 3값 전부 인쇄

`|dz|` = 렌더 depth 를 역투영한 3D 점의 z 와 씬 높이장의 차이, 미터.

| 씬 | 전체 median / p90 | r=3 m | r=6 m | r=10 m |
|---|---|---|---|---|
| scene01 | 0.000090 / 0.489629 (n=53,696) | 0.000061 / 0.000115 (n=30,301) | 0.000062 / 0.000122 (n=34,822) | 0.000070 / 0.186885 (n=39,786) |
| scene02 | 0.805111 / 2.807666 (n=28,935) | 표본 0 | 표본 0 | 1.969598 / 2.839816 (n=13,318) |
| scene03 | 0.000201 / 0.501725 (n=25,575) | 표본 0 | 0.000176 / 0.206059 (n=22,319) | 0.000191 / 0.196481 (n=24,334) |

읽는 법: **근거리 중앙값이 0.1 mm 미만**이면 카메라 모델이 맞은 것이다(scene01·scene03).
scene02 의 0.8 m 는 카메라 오차가 아니라 **그 씬 높이장이 AABB 포락면이라 지하도 개구부를 덮어버린 것**이고,
p90 꼬리(0.19~0.50 m)는 난간·건물 AABB 가 실제 표면보다 위에 있는 화소들이다 — §4 ❓B-1.

### 3.2 route (a) 상수 — 인스턴스가 몇 개 나오나

`comps` = 연결 성분 수, `len_m` = 턱 선 총 길이(격자 셀 × 0.05 m).

| 상수 | 값 | scene01 edgecells/comps/len_m | scene02 | scene03 |
|---|---|---|---|---|
| `R_RUN_M` | **0.5** | 534 / 6 / 26.70 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **1.0** (현행) | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **2.0** | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| `MAX_TRAVERSE_STEP_M` | **0.15** | 534 / 6 / 26.70 (도달 셀 17,224) | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **0.20** (현행) | 750 / 8 / 37.50 (도달 셀 93,090) | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **0.30** | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| `BREAK_LOCAL_FALL_M` | **0.02** | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 761 / 41 / 38.05 |
| | **0.05** (현행) | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **0.10** | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| `WALK_SUPPORT_FRAC` | **0.15** | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 699 / 39 / 34.95 |
| | **0.25** (현행) | 750 / 8 / 37.50 | 0 / 0 / 0.00 | 691 / 41 / 34.55 |
| | **0.40** | 738 / 8 / 36.90 | 0 / 0 / 0.00 | 671 / 41 / 33.45 |

읽는 법(있는 그대로):
- `R_RUN_M` 0.5→1.0 에서 scene01 이 6→8 인스턴스로 늘고 1.0→2.0 은 변화 없음. **0.5 와 1.0 사이가 실제 갈림길**이다.
- `MAX_TRAVERSE_STEP_M` 0.15→0.20 에서 scene01 의 **도달 셀이 17,224 → 93,090 으로 5.4배** 뛴다.
  이 씬 계단 챌면이 0.15~0.20 m 사이라는 뜻이고, 이 상수는 **가장 민감한 임시값**이다.
- scene02 는 어떤 값에서도 0 — 상수 문제가 아니라 높이장에 낙차가 없다(❓B-1).

### 3.3 하류 상수 — 레코드에 실제로 닿는 값 (`--full`)

| 상수 | 값 | scene01 tier/edges/int_px/미해결 | scene02 | scene03 |
|---|---|---|---|---|
| `VIS_TOL_M` | 0.05 / 0.15 / 0.35 | 세 값 모두 VE_raw / 5 / 157,296 / 2 | 세 값 모두 NEG / 0 / 0 / 0 | 세 값 모두 VE_raw / 5 / 348,120 / 2 |
| `B_REL_JUMP` | 0.02 / 0.05 / 0.10 | 변화 없음 (dev median 1.0 px) | 변화 없음 | 변화 없음 |
| `B_ABS_JUMP_M` | 0.05 / 0.10 / 0.30 | 변화 없음 (dev median 1.0 px) | 변화 없음 | 변화 없음 |
| `INT_ASSIGN_INFRAME_ONLY` | False (현행) | VE_raw / 5 / **157,296** / 2 | NEG / 0 / 0 / 0 | VE_raw / 5 / **348,120** / 2 |
| | True | VE_raw / **3** / **65,823** / 2 | NEG / 0 / 0 / 0 | VE_raw / 5 / 348,120 / 2 |

"변화 없음"이 진짜인지 따로 확인했다(수리하지 않고 원인을 잰 것):
- `VIS_TOL_M` 을 **1e-06** 까지 낮추면 scene01 e00 의 가시점이 40 → **0** 으로 무너지고, **1.0** 으로 올리면 41 이 된다.
  즉 0.01~0.35 구간이 **평평한 고원**이다 — 렌더 depth 와 기하의 일치가 ~1e-4 m 인데 가림은 미터 단위라
  그 사이에 아무것도 없기 때문이다. 상수가 무시되는 게 아니라 **이 씬에서 둔감한 것**이다.
- `B_REL_JUMP`/`B_ABS_JUMP_M` 은 route (b) 자체를 크게 흔든다(scene01 점프 픽셀 **129,269 → 66,466 → 48,508**,
  성분 **62 → 42 → 27**). 그런데도 e00 의 (a)–(b) 이탈은 세 세트 모두 mean 0.9 / median 1.0 / p90 1.0 px 로 같다.
  → 세 세트 모두 같은 턱 선을 1 px 안에서 잡고 있다는 뜻. **파일럿에서 전 씬으로 다시 재야 하는 항목**이다.

### 3.4 `REACH_ANCHOR_MODE` — 이번 Phase 0 에서 실제로 잡은 결함

| 값 | scene01 도달셀 / 턱셀 / 성분 | scene02 도달셀 / 턱셀 | scene03 도달셀 / 턱셀 / 성분 |
|---|---|---|---|
| `camera_cell_clamped` (첫 규칙) | 93,090 / 750 / 8 | **29** / **0** | 34,552 / 691 / 41 |
| `largest_ground_component` (현행) | 93,090 / 750 / 8 | **83,896** / **0** | 34,552 / 691 / 41 |

읽는 법:
- 앵커 규칙을 고치자 scene02 의 도달 지면이 **29 → 83,896 셀**로 정상화됐다. scene01·scene03 은 **아무 변화 없다**
  (원래 앵커가 제대로 앉았던 씬).
- **그런데 scene02 의 턱 셀은 여전히 0이다.** 즉 scene02 가 NEG 로 나오는 이유는 앵커 결함이 아니라
  높이장에 낙차가 없다는 것(❓B-1)이 맞다 — 결함을 고치고 나서도 결론이 그대로라는 것을 확인한 셈이다.
- 전체 파이프라인(`--full`)에서는 세 프레임 모두 tier_now·edges·속 픽셀이 동일하다.

### 3.5 `EDGE_WINDOW_SHAPE` — 창 모양이 얼마나 바꾸나

| 값 | scene01 낙차≥thr / 턱셀 / 성분 | scene02 | scene03 낙차≥thr / 턱셀 / 성분 |
|---|---|---|---|
| `disc` (현행) | 17,398 / 750 / 8 | 10,168 / 0 / 0 | 20,292 / 691 / 41 |
| `chebyshev` | 17,586 / 750 / 8 | 10,184 / 0 / 0 | 20,624 / 691 / 41 |
| `cross` | 16,797 / 748 / 8 | 10,024 / 0 / 0 | 19,064 / 679 / 41 |

읽는 법: 낙차 셀 수는 ±3% 안에서 움직이지만 **턱 셀·성분 수는 사실상 안 변한다**(scene01 750/750/748, 성분 8/8/8).
격자 창 모양은 이 코퍼스에서 둔감한 축이다 — 다만 `chebyshev` 는 정의상 대각 1.41 m 까지 닿으므로,
**"진행거리 1.0 m"라는 문장과 정확히 일치하는 것은 `disc` 뿐**이다.

---

### 3.6 `EDGE_BRIDGE_PX` — 실측: 5 px 는 챌면 틈을 못 건넌다

재현: `python3 code/simple_edge.py --bridge-sweep` · 값은 `mask_area_px`.
"틈"은 위험선에서 가장 가까운 낙차 화소까지의 거리(px, EDT 실측).

| 프레임 | 틈 px | 2 | 5 (running) | 10 | 20 | 40 | 80 |
|---|---|---|---|---|---|---|---|
| 260819_main_on/scene01 | 1.0 | 165258 | 165258 | 165258 | 165258 | 165258 | 165261 |
| 260819_main_on/scene03 | 1.0 | 152033 | 152033 | 152033 | 152033 | 152033 | 152033 |
| 260819_main_on/scene09 | 32.0 | 0 | **0** | 0 | 0 | 1528329 | 1528329 |
| 260819_main_on/scene18 | 1.0 | 769172 | 769172 | 769172 | 769172 | 769172 | 769172 |
| 260824_v3w3_extbase_A/sceneH1 | — (위험선 0) | 0 | 0 | 0 | 0 | 0 | 0 |
| 260819_main_on/sceneD2 | 44.1 | 0 | **0** | 0 | 0 | 307820 | 307820 |

읽는 법: 6프레임 중 4개는 틈이 1 px 이라 값이 무엇이든 같다. 낙차가 **경사/원거리 챌면**으로 나타나는
scene09·sceneD2 는 틈이 32.0 / 44.1 px 이라 5 px 팽창으로는 못 건너고, 낙차 바닥이 **눈에 뻔히 보이는데도**
마스크가 비어 tier 가 V 가 아니라 E 로 찍힌다. 씬별 조정은 하지 않았다 — running value 5 를 그대로 납품하고
`overlays/_simple/contact_sheet_EDGE_BRIDGE_40px.jpg` 에 40 px 일 때의 그림을 따로 붙여 대조만 시킨다.
채택은 결재(❓S-1).

### 3.7 내가 선 면의 씨앗 규칙 — sceneD2 에서 실측한 결함

사양서의 "화면 아래-**중앙**에 연결된 성분"을 문면 그대로 쓰면 sceneD2 에서 5,045 px 짜리 덩어리가 뽑힌다.
이 씬은 카메라가 도랑 턱에 서 있어 화면 아래-중앙이 이미 도랑이고, 그 안에 지면 높이의 작은 덩어리가
하나 떠 있기 때문이다. 사람이 딛고 선 면은 도랑을 둘러싼 **960,320 px 마당**이고, 도랑 림(위험선)을 지닌
것도 그쪽이다. 그래서 씨앗을 **아래 띠 전체에서 가장 큰 성분**으로 바꿨다(전 프레임 동일 규칙, 씬별 조정 아님).
나머지 5프레임은 두 규칙의 결과가 같다 — 각 json 의 `seed_centre_area_px` 와 `surface_area_px` 로 대조 가능.

## 4. 결재란 (❓)

| 번호 | 항목 | 지금 상태 | 담당 |
|---|---|---|---|
| ❓B-1 | scene02 처럼 **높이장(AABB 포락면)이 개구부를 덮어 route (a) 가 낙차를 못 보는 씬**의 처분 — 재렌더로 높이장을 다시 뽑을지, (b)만으로 갈지, 모집단에서 뺄지 | 실측만 함(scene02 = NEG 0 인스턴스, RGB 에는 구덩이가 명백) | 승용 |
| ❓B-2 | 계단은 **챌면(nosing)마다 인스턴스 1개**가 나온다(규칙 1의 문면 그대로). 이대로 둘지, 맨 위 턱 하나로 합칠지 | 현행=문면 그대로. scene01 8성분·scene03 41성분 | 승용 |
| ❓B-3 | `MAX_TRAVERSE_STEP_M` 채택값 (0.15 / 0.20 / 0.30) — §3.2 에서 가장 민감 | 임시 0.20 | 승용 |
| ❓B-4 | `prim_class_rules.json` — 어떤 prim 경로가 '자기가림(=E)' 이고 어떤 것이 '외부 가림물(=H)' 인가. 스모크에서 실제로 막은 prim: `/World/Scene01/UpperPlaza`(184표본, 브링크로 보임 → self 후보) · `/World/Scene03/Bollard_0`,`Bollard_1`(각 7표본, 볼라드 → external 후보) | 규칙 0개, 전부 `unknown`, flag=null | 승용 |
| ❓B-5 | `INT_ASSIGN_INFRAME_ONLY` — 화면 밖 인스턴스가 속 픽셀을 소유해도 되는가 | 임시 False(소유 허용). True 로 바꾸면 scene01 속 픽셀이 157,296 → 65,823 | 승용 |
| ❓B-6 | `VIS_TOL_M`·`B_REL_JUMP`·`B_ABS_JUMP_M` 채택값 — 스모크 3프레임에서는 둔감했다. G1 파일럿(4씬 전 프레임) 분포를 보고 정할지 | 임시 0.15 / 0.05 / 0.10 | 승용 |
| ❓B-9 | `REACH_ANCHOR_MODE` 채택값 — 현행 `largest_ground_component` 로 확정할지 | 첫 규칙에서 scene02 도달 셀 29(결함). 고친 뒤 83,896 | 승용 |
| ❓B-7 | `WALK_FOOT_R_M`·`WALK_FLAT_TOL_M`·`MIN_INSTANCE_LEN_M`·`B_MIN_COMP_PX` — 스모크에서 단독 스윕을 아직 안 돌린 임시값 4개 | 임시값 유지, 파일럿에서 스윕 예정 | 승용 |
| ❓S-1 | `EDGE_BRIDGE_PX` 채택값 — 5(현행) 인가 40 인가. 5 는 §3.6 실측에서 6프레임 중 2개의 낙차 마스크를 통째로 비웠다 | 임시 5 유지 + 40 일 때 그림 별첨 | 승용 |
| ❓S-2 | sceneH1 처럼 **난간·파라펫이 막아선 낙차**를 NEG 로 둘 것인가. 단순 라벨러는 정의대로 NEG 로 찍는다(내가 선 면의 경계 1 m 밖은 벽 위쪽이라 낙차가 아니다) | 정의대로 NEG | 승용 |

---

## 5. 검증 명령 (그대로 복붙)

```bash
cd experiments/e1_0827
export PYTHONNOUSERSITE=1
python3 code/e1_selftest.py --scan-consts --schema --ledger     # 상수 0건 · 스키마 0건 · 대장 0건
python3 code/e1_sensitivity.py --key R_RUN_M --list code/smoke_frames.txt
python3 code/e1_sensitivity.py --key VIS_TOL_M --list code/smoke_frames.txt --full
python3 code/simple_edge.py --scan-consts                       # 단순 라벨러 상수 0건
python3 code/simple_edge.py --batch --contact-sheet             # 6프레임 + 컨택트시트
python3 code/simple_edge.py --bridge-sweep                      # §3.6 표 재생성
```
