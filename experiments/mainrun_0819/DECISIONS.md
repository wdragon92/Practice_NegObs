# DECISIONS.md — 자율 판단 기록 (mainrun_0819)

- **D1 (21:55)**: DATA_CANDIDATES 전수 조사 결과 사용 가능한 전체 학습 데이터셋 없음
  (dataall 비어 있음 — 커밋 36037ea "미렌더 정정" + 8c814db 큐 "재렌더").
  → 금야 `run_data_render.py`로 신규 렌더. 근거: 사용자 구두 지시 "컷들도 너가 적당한
  위치에서 잡아도 되고, 알아서 잘 활용" + brief 임무(훈련까지). 구세대 2607xx 라운드는
  W4~W6 수리 이전 룩이라 배제.

- **D2 (22:35) 렌더 계획**: 33씬 전부 × conds L0,L5,L7(check_data_run EV쌍 게이트 충족;
  거부 5쌍은 선언 치환) × 8캠 × 2팔(on/off) = 씬당 48컷, 총 ~1584컷 ≈ 2.5-3.5h.
  seed 20260819 고정 → 양팔 카메라 포즈 바이트 동일 = 진짜 트윈 쌍. 라운드
  260819_main_on / 260819_main_off. 씬 단위 flock -o -w 3600 -E 201 루프
  (run_260817_w4_regfix.sh 패턴), stamp_round --capture-env 동봉(8c814db 구멍 대응).
  spp 결함은 데이터 채널에 무해(PT_FAST 하드코딩) — 야간 수정 안 함.
- **D3 (22:35) 폴라 그리드 = PROVISIONAL-GRID-V0 선언**: 격자 상수 [코드 미발견]
  (브리프 A2의 "코드가 진실" 전제가 리포 실태와 불일치 — spec §281의 15는 레이팬).
  브리프 문면상 파생 금지 → 훈련 보류이나, 당일 사용자 구두 지시("알아서 잘 활용",
  실험 진행 의사)를 우선해 **리포 앵커 값으로 잠정 그리드를 선언하고 훈련을 진행**,
  아침 1순위 결재로 상정. 상수: 방위 5섹터 A–E(화면 좌→우) 균등 12.44° over
  [+31.1°,−31.1°](= 명목 hfov 62.2°), 거리 밴드 1/2/3 = [0,2)/[2,5)/[5,12) m
  (grid_views d{2,5,10} + 샘플러 d최대 12 앵커). 셀 인덱스 = band*5+sector.
  라벨·모델·결과 전부 `PROVISIONAL-GRID-V0` 태그, gridspec_v0.json 파라미터화로
  아침 확정 후 원커맨드 재파생·재훈련 가능(재훈련 비용 ~30분 실측 근거).
- **D4 (22:35) depth 사이드카**: run_data_render.py에 opt-in 패치(env
  NEGOBS_DATA_SIDECARS=1, 기본 OFF=기존 동작 바이트 동일). replicator
  distance_to_image_plane → <컷>.depth.npy(f16). Depth U-Net 팔 + 티어 파생 +
  G5 교차검증의 공통 전제. 1컷 GPU 검증 후 본 렌더.
- **D5 (22:35) 높이맵 사이드카**: 씬·팔당 1회 AabbPrefilter.ground_z 격자 샘플
  (+_solid_at 오라클 s06/s08/s11/s12/s19·s07 path_z 반영, s05 오라클 부재 → GT 의심
  플래그). 폴라 GT = 높이맵 낙차 footprint(컷별 기준지면 대비 ≥0.3m)를 카메라 폴라
  그리드에 투영. hazard-off 팔은 footprint 자연 소멸 → G2 자동 충족 구조.
- **D6 (22:35) V/E/H 파생**: A3의 semantic 계측기 미구현(SPEC_CONFLICTS 기재) →
  depth 재투영 기반 파생으로 대체: int_px(기준지면 −0.15m 이하 & footprint 내 픽셀 수),
  edge_ratio(footprint 근경계 폴리라인 투영점의 depth 불연속 가시율). strict-H =
  int_px==0 AND edge 가시 0. τ_int 후보 {1,50,200}px·τ_edge {0.02,0.05,0.10} 민감도
  표. tier_source=derived-depth-v0. 유리 가림(A3④) 판별 불가 → 해당 씬 아침 육안
  플래그(GAP_REPORT).
- **D7 (22:35) 분리**: 리포 고정 씬 분리(variation_kit 23/5/5, 하드네거 전부 train)는
  디렉터리 배치용으로만 두고, 실험 분리는 브리프 Phase 3 기준으로 manifest 재분리
  (씬 단위+토글 동반). SPLIT_PROPOSAL.md에 리포 분리와의 차이 명기.
- **D8 (22:35) 기타**: hflip 기본 OFF(폴라 빈 순열 필요— 헬퍼만 구현), 페어드
  부트스트랩 자체 구현(SR_HPE에 부재 확인), Grad-CAM 코드 부재 → 보너스는 수동
  구현 시간 남을 때만.

- **D9 (23:05) 렌더 패치 GO + 본 런 개시**: 프로브 검증 v1~v5 전부 PASS(depth 정합
  0.13% 평균·낙차 재구성 19mm·높이맵 off팔 0셀·트윈 바이트 동일·게이트 무영향·기본
  경로 무변경 실증). 발견된 잠재 결함: `del math` UnboundLocalError(HEAD 기존) —
  replicator 활성 시 SIGSEGV로 씬 resume 장부 파괴 → 사이드카 팔 한정 os._exit(0)
  우회(기본 경로 무수정). 실측 3.8s/cut → 양팔 ~3.0h 추정. 주의 이월: ①off팔 31/33
  미실행 분기(스크립트가 실패 수집·재개 안전) ②높이맵 x[-2,14] — 카메라 기준지면은
  cam.ground_z 사용(라벨러 이미 그렇게 설계) ③sceneD1 off팔은 yard 드레싱 동반 제거
  — 팔 식별 단서 후보로 shortcut 감사 플래그 ④N1~N5는 양팔 모두 GT 0(off는 착시
  요소 제거) — off팔을 추가 네거티브로 취급 금지.

- **D10 (00:02, 사용자 지시 반영) footprint v2 = on/off 높이맵 차분 + 스텝 게이트**:
  기존 v0(컷별 cam.ground_z 단일 기준면)는 경사 씬(N4 램프·s03/s04/s17 뚝방)에서
  원거리 지면이 기준면보다 자연히 0.3m 이상 낮아져 GT 과대(가짜 양성) 오류 모드 확인.
  v2: footprint = {z_off(x,y) − z_on(x,y) ≥ 0.3} (off팔 = 반사실적 보행면) AND 근경계
  스텝 게이트(z_on이 수평 ≤1.0m 내 ≥0.3m 하강 — 계단·수직 낙차 통과, 완경사 램프 배제
  → N4 하드네거티브가 자동으로 GT 0). int_px 기준면도 z_off(x,y)−0.15로 교체(경사 강건).
  검증: synth 재통과 + 실프로브 재검 후 적용. 라벨은 전부 재파생(원본 무접촉).
- **D11 (00:02, 사용자 지시) 신규 상수 3건 근거 기록**:
  ① 내부면 마진 −0.15m = 코퍼스 지면 변위 상한(ground_kit delta_max 실측 0.113m,
  scene02 로그) 위 + 위험 문턱 0.3m의 절반 아래 — 지면 텍스처/변위 노이즈와 실제
  내부면을 분리하는 중간값. ② 림 depth 허용 ±0.35m = f16 depth 양자화(≤16m에서
  ~0.016m) + 높이맵 5cm 격자×경사 + 립 1px 폭의 3×3 창 표본화를 흡수하는 실측 안정값
  (프로브에서 0.175 검출 성립) — 단 0.3m 최소 낙차에서 립/바닥 혼동 여지 있어 v0
  잠정치로 명시, 아침 민감도 스윕 후보. ③ 립 표본 200점 = 5cm 격자에서 ~10m 립과
  1:1 밀도, 프레임당 전체 스택 0.24s 실측 유지 상한.
- **D12 (00:02, 사용자 지시) 감사 오버레이 층화 강화 + 예비 수 조준 수정**:
  audit_samples 30장에 원거리(cam.d>8m) E 판정 프레임 ≥4장 쿼터. strict-H 보강
  예비 렌더(+8캠)는 "전체 H 상위 씬"이 아니라 **test로 배정될 H 상위 씬** 조준으로
  변경(헤드라인 지표는 test H 표본 수가 지배) — 명령어만 준비, 실행은 아침 결재 후.

- **D13 (00:25, 사용자 지시) 스텝 게이트 수평창 1.0m = 신규 상수 4호, 근거**:
  분리 대상 두 부류의 기하 중간값 — 계단(건축 기준 챌판 0.15-0.18m × 디딤판
  0.26-0.30m → 0.3m 하강이 run ≤0.6m 안에서 발생; 수직 낙차는 즉시)은 40%+ 여유로
  통과, 정상 램프(접근성 기준 1:12≈4.8°, 가파른 보행로 ~15%≈8.5° → 0.3m 하강에
  ≥2.0m 필요)는 2배 여유로 배제. 1.0m = 높이맵 5cm 격자의 정수 20셀 창(표본화 안정).
  검증 의무: batch1 비계단 낙차 4씬(D1 dock·D2 opening·D3 channel·D4 platform)의
  씬별 footprint 면적(게이트 전/후)으로 오배제 없음을 실증 — 해당 씬 높이맵 착지
  즉시(~01:00-01:30) 실행, 결과는 채팅+STATUS 보고. 이론 예상: 4씬 모두 수직벽
  프로파일이라 통과(유일 위험 프로파일은 완경사 측벽 배수로인데 D3는 콘크리트
  수직벽 채널).

- **D14 (00:42, 사용자 지시) 게이트 배제분 보존 + G2 씬 단위 HOLD**:
  ① 검증이 라벨링 전 미완이어도 라벨링 진행. ② 스텝 게이트 배제 셀은 무손실 보존 —
  프레임별 `polar_gt_pregate`(게이트 전 raw footprint로 계산한 15셀 GT)와
  `gate_excluded`(게이트로 꺼진 셀 인덱스 목록) 병록, 씬 레벨 배제 높이맵 셀 수 병록.
  훈련 GT = 게이트 후 버전 유지. 아침에 게이트 반전 결정 시 **재파생 없이 필드 스왑**
  으로 즉시 전환 가능. ③ 씬별 배제 셀 수 표 = GATES_REPORT + STATUS 전재.
  ④ batch1 비계단 4씬(D1~D4) 중 G2(on팔 양성 0)에 걸리는 씬은 해당 씬만
  `PROVISIONAL-HOLD`: manifest에 플래그 유지하되 train/val/test 전 분할에서 제외
  (오염 방지 — test 포함 시 헤드라인 지표 왜곡). 게이트 채택/반전은 아침 결재.

- **D15 (01:10) D씬 검증 결과 조치**: ① D1/D2/D3 게이트 무손실 통과(배제 0셀) —
  스텝 게이트 오배제 위험 실측 해소. ② D4 = 높이맵 계측기 한계(밀폐 실내 — 상공
  레이가 지붕에 차단, footprint 게이트 전부터 0) → D14④ 그대로 PROVISIONAL-HOLD.
  ③ 일반화 정책: 전체 라벨링 후 씬별 max_diff를 문서 낙차표(dropoff_cue_matrix
  §96-127)와 대조, on팔 양성 0 또는 max_diff < 문서 낙차의 50%인 씬은 동일 HOLD
  (의심 라벨의 훈련·평가 오염 방지 우선, 나머지로 훈련 진행). ④ D3 트윈 포즈
  불일치 15/24컷(기하 차이→카메라 기각 재추첨 분기) — 훈련 무영향, 트윈 on−off
  분석만 포즈 일치 컷 필터 + 씬별 매칭률 보고.

- **D16 (01:35) 렌더 수용 판정**: 코퍼스 1584컷 완성(792+792, 치환 포함 씬당 24컷
  정확). check_data_run FAIL 6/5건의 원인 분해 — 노출 위반(luma 49·dark 48)은
  **48건이 sceneD4 단독**(밀폐 실내 × data 채널 고정 주광 노출 → 평균 휘도 0.5,
  r_reject 1.0). D4는 높이맵 사각·암흑 노출·기각 부기 3중 사유로 HOLD 확정(D14④).
  잔여 위반: scene19 1컷 luma 28.3(경계 미달 → manifest 제외, notes 기록),
  scene09 clip 1.4~1.5% 2컷(경미, 유지+기록). EV 순서/sunless 라운드 통계 FAIL은
  D4 오염+치환 씬 쌍 구조 변화 기인으로 판정 — 프레임 차단 없음, 아침 보고.
  s/cut 게이트는 사이드카 비용으로 예상된 advisory. scene11 off 1컷 depth 결손 —
  라벨러가 관용(raw_vis=None), off팔이라 무영향.

- **D17 (02:05) 구조 1차 결과 조치**: s02/07/08 융합 복구 채택(문서비 1.00~1.43×,
  s08은 on=aabb/off=fused 혼합 페어링 — on팔 상공 레이가 depth와 중앙값 7mm 일치
  검증됨). s06 = 위험이 전 컷 시야 밖(양팔 0.1퍼센타일 z −0.14) → 라벨 정상, 유지.
  **s11·s13·s19 = on팔 GT 오라벨 위험**(s11/s19: 토글이 카메라 지면 높이 자체를
  변경해 트윈 전제 붕괴·데크 위 시점의 낙차 미라벨 / s13: 보행 근경계가 융합
  커버리지 밖이라 게이트 전멸) → **extra_holds.json으로 양팔 HOLD**(오라벨 양성
  누락을 학습하면 "데크 엣지=무위험" 오염). off팔만 살리는 안은 A6 씬 동반 원칙
  위배로 기각. s12(0.19×, strict-H 18)·s16(0.12×, H 12) = 과소 footprint 확정 →
  융합 수리 2차 즉시 실행(헤드라인 H 75 중 30 보유). 2차 후 라벨 동결, 잔여 의심은
  아침 목록.

- **D18 (02:30) 분리 v1 확정(PROVISIONAL)**: 1차 산출이 strict-H 최다 2씬(s09+s14=33)
  을 val로 소모(train H 0·test H 12)하는 결함 — val의 "strict-H 선호" 사양 자체가
  오류였음을 확인, val은 H-금지(+양성 V/E 보유 필수)로 수정(PROOF-9 재정의). 추가로
  test 통계력을 위해 --force-test 플래그 신설, H 최다 s14(18)를 test 고정(PROOF-7b) —
  근거: 헤드라인 = test H recall의 CI 폭(12→21프레임), s14 = 평탄연속 착시 대표씬으로
  정성 패널 가치. 최종: test {05,07,14,15,18,C2,N3} H21 / val {10,D3} H0 / train 20씬
  H24. 아침 승인 대상 명기.

===== DAYRUN 0820 (지시서: Docs/experiment/DAYRUN_BRIEF_0820.md, WORKDIR experiments/dayrun_0820) =====

- **D19 (08-20 15:16) 작업 기본값 채택 + 실행 판단**: 브리프 확정사항(그리드 V1 20칸
  중첩·E 승격·YOLO 4행·amodal 파생·발전서사)과 작업 기본값(게이트 유지·HOLD 유지·
  G5 참고지표 강등·유리씬/C2 육안 이월·광도증강 보류) 전부 채택, DAYRUN_REPORT 일괄
  승인란에 정리 예정. 실행 판단: ① 보강 캠 편향 기제 = 샘플러 opt-in 오버라이드
  (H보강: d∈[6,12]+h∈[0.25,1.0] — 저고도·원거리일수록 가림 기하 최대 / E보강:
  d∈[6,12]+h∈[1.2,1.9] — 높은 시점은 원거리에서도 림 가시). ② 보강 렌더 conds는
  본 코퍼스와 동일 L0,L5,L7 유지(치환 규칙 동일). ③ E보강 train 씬 선정은 Phase 1의
  희소 셀 표 + 씬별 E 후보(원거리 림 가시 가능) 데이터 기반, 렌더 벽시계 ~2h 캡.
  ④ GPU 순서: 보강 렌더 → 9런 재훈련 → YOLO/aux(밤). ⑤ gates.py: G5는 밴드별 참고
  지표(합불 제외)로, G2의 scene06은 "설명된 0(시야 밖, D17)" 주석 처리 — 총평
  "GATE FAILURE" 문구를 참고 판정 체계로 정정(브리프 기본값 이행).

- **D20 (16:00, 진단 반영) 트윈 페어링 허용오차 + C2 판정**: 진단 ③(c)가 트윈 배제
  183쌍의 실원인을 규명 — 카메라 재추첨이 아니라 **토글이 지면 데이텀을 바꿔 ground_z만
  이동**(C2는 낙엽 둔덕 삭제로 0.11m). v2 평가부터 트윈 페어링에 |Δground_z|≤0.15m
  허용오차 채택(배제 열 유지·보고) — 배제 51쌍 회복, mainrun V0 수치는 불변 보존.
  C2 오경보 = 0.3m 혼동이 아니라 **외관 맥락 환각**(off팔 지면 평탄 16mm 실증, 발화
  0.98까지) + far밴드 사전확률 0.61 증폭 — V1 밴드 분할·bias init의 수치 근거.
  C2 씬 결함(토글이 둔덕·난간까지 제거 — off팔 외관 비보존)은 씬 코드 수정 사안이라
  선신고 대상으로 DAYRUN_REPORT 승인란에 제안만(금일 수정 안 함).

- **D21 (16:30) 보강 렌더 목록 확정 + P1 부산물 채택**: H = {s14,s15(test), s09,s17(train)}
  ×저고도·원거리 밴드. E = test 위험 5씬{05,07,14,15,18} + train 후보 상위 9씬
  {C4,s20,C1,s09,s17,s03,s04,s12,s08}(P1 전환성 랭킹; D1 0.42 제외) = 14씬, 계획
  ~100분(캡 내). P1 부산물: ①gates far-E 쿼터 픽스 버그 수정 ②D14 반전 원장 정정 —
  V1 기준 48프레임/147셀이 정직한 수치(V0의 조대 band3가 배제를 은닉) → 승인란
  수치 교체 ③G5 밴드별 단조(0.03/0.48/0.60/0.70) = 강등 정당성 정량 확보
  ④val 1A셀 양성 0 플래그 — split v2 검토 시 확인.

- **D22 (17:10) YOLO 트랙 준비 완료 + 오라클 천장 채택**: amodal 마스크 552장(V 프레임
  가시낙차 95.8% 피복+가림 너머 연속 검증), ultralytics 8.4.123 전용 venv(cu130 휠 —
  금야 GPU 호환 확인). det2cell 어댑터의 오라클 천장 실측(GT 박스를 conf1.0으로 주입):
  frame-det 0.404 / V 0.514 / E·H 0.000 / cell 0.074 — **비가시 낙차에서 YOLO 행의
  0은 구성적 상한**임을 표에 명기(하단변 샘플 밀도 33점까지도 불변). τ 규율:
  predict 0.05 바닥 + eval 스윕 일원화(이중 임계 방지). 마스크는 그리드 비클립
  (브리프 충실; --clip-grid 옵션 보존, 면적차 ~5%).

- **D23 (16:45) 보강 렌더 1차 무효 — 팔 충돌 버그**: 라운드명이 boost 리스트별로만
  분리되고 팔(arm)이 빠져, seed 동일 → 파일명 동일 → **off팔이 on팔을 덮어씀**
  (내 라운드명 지정 미비가 원인). 수리: run = 260820_boost_{h,e}_{on,off} 4라운드
  체계 + guard 갱신 후 전체 재렌더(~53분). 충돌 1차 산출물(260820_boost_h/_e =
  off팔 생존본)은 무수정 존치·supersede 문서화, 라벨링 대상에서 제외.

- **D24 (18:50) E 수율 미달 → E2 라운드 + frame_id 유일화**: 고시점·원거리 E밴드는
  E 12·V 147 산출 — 오판정: 높은 눈은 내부면까지 보여 V가 됨. E의 기하 조건 =
  낮은 눈(그림자 길이↑) + 중거리 + 립 시야 무가림 → E2 밴드 d[4,9]×h[0.3,0.9],
  개방형 위험 씬 8개(s05,03,04,20,12,18,C4,07), seed 20260821(파일명·추첨 독립),
  라운드 260820_boost_e2_{on,off} (~40분). h/e 리스트 겹침 씬의 파일명 동일로
  병합 frame_id 충돌 192건 발견 → 병합 시 파일 토큰에 '::<round>' 접미사(파서
  split('/',2) 안전 확인). 큐 개시는 E2 병합 후(~19:40)로 조정.

===== NIGHTRUN 0820 (지시서: Docs/experiment/OVERNIGHT_BRIEF_0820_v1.md, WORKDIR experiments/nightrun_0820) =====

- **D25 (23:45) 스위치 확정 기록**: APPROVE_BATCH_DEFAULTS=YES — DAYRUN_REPORT ④의
  #1~#6·#8 기본값 사용자 확정(그리드 V1·D19 기본값 5건·val scene20·보강 수용·트윈
  허용오차 0.15·YOLO 천장 해석·팔충돌 supersede). **④-7(C2 씬 토글 비보존) 승인 완료**
  — RUN_DRESSING_CONTROL=YES로 sceneC2·N3 외관보존 off팔 재렌더 트랙 실행(별도
  디렉토리·기존 팔 무수정·on팔 해시+포즈 게이트 후 평가). RUN_HOLE_PROBE=YES —
  hole 제로샷 프로브(평가 전용, 훈련 편입 금지, experiments/probe_holes_0820/ 격리).
- **D26 (23:45) 시작 점검 통과**: 코퍼스 2832 확인·split_v2_full sha 9bc44d4efbe6e83e·
  test 7씬 D18 동일·디스크 258G. GPU는 사용자 baseline 활성 → §2 프로토콜(10분 폴링,
  2회 연속 무점유+lock 확인 시 개시, 05:00 데드라인) 가동. CPU 트랙 B1~B6 즉시 병행.

- **D27 (00:20) B4·B5 착지 — 헤드라인 방어 성립**: 트윈 허용오차 층(54쌍)에 **H·E
  티어 쌍이 0개** → exact-only H Δ = 발표치와 4소수점 동일(rgb .285/depth .407/b2
  .110, 9개 CI 전부 0 배제) — D20 채택이 헤드라인을 부풀리지 않음이 항등적으로 증명.
  단 all-tier Δ는 +0.041(rgb) 부풀림(허용층이 s07·C2 집중, C2 몫은 dressing 혼재 —
  각주 초안 TWIN_STRATIFICATION §3, C2 대조군 트랙이 이 수치를 겨냥). B4: 걸침이
  miss를 늘린다는 증거 없음(8m 교차는 오히려 3/3 시드 miss 감소), 8m 경계 최다 교차
  (52-65%), 점유율 — band1 발화 셀의 71%가 실통행 가능(과차단, 조기경보 표현 프레임)
  · 0.5-1.5m hole은 band3b 셀의 1-3%뿐(C1 해석 지침).

- **D28 (00:45) B1·B2 착지 — 논문 서술 교정 3건**: ① Depth 하락 = 100% 구성 변화
  (동일 v1 H프레임에서 v2-Depth 0.714→0.857 상승; 하락 축은 밴드가 아니라 cam.d —
  d≥9에서 Depth H 0.000/전시드, RGB 0.587 유지). **H 주장은 3b 한정으로 서술**(3a
  H 트윈 Δ −0.031 CI 0 포함 — 3a로 일반화 금지). ② ρ 결합은 "절반으로 축소, 제거
  아님"(0.963→0.848, FA/prior 0.377→0.234). ③ FA 지리 이동: RGB의 C2 환각 붕괴
  (0.875→0.069)·신규 1위 s15(0.699)·광역 저진폭 발화로 총 FA 상승 — Depth는 C2
  집중 심화(76.1%) → **C2 대조군 평가는 Depth 확장 정당화**(브리프 "여력 시" 조항
  발동). 원장: narrative/diag_v2/DIAG_V2.md.

- **D29 (01:15) C1 프로브 준비 완료 + 원장 주입 방식**: 씬 3종(근거리 V/E·통로보존
  E·차폐 H-구성 — 여유마진 0.36m 설계) CPU 게이트 47/47 PASS. **함정 발견·회피**:
  variation_kit AZ_LEDGER에 소스 레벨로 3행을 추가하면 split_map 셔플이 밀려 코퍼스
  19/33씬의 분할 디렉토리가 이동(절대규칙 1·2·4 위반) → 런타임 주입(split_map 캐시
  후) + 33씬 SPLIT_GUARD 이중 검증으로 해결, 프로브는 4번째 split "probe"로 격리.
  클래스는 sceneD2 차용(C'), L0/L5/L7 무거부. 렌더 추정 12-20분(브리프 30-60 대비).
  트윈 tol 1e-6 고수(배제 발생 시 그 자체가 발견). b2 평가는 HF_HUB_OFFLINE=1 필요.

- **D30 (01:40) C2 대조군 준비 완료**: keep_dressing opt-in 수술(C2 5개소·N3 1개소,
  플래그 부재 시 도달불가 증명 + 스모크 6/6). **D17 함정 정면 해결**: 구 off팔의
  ground_z 이동 원인 = build_flat_fill의 복도 관통 슬래브(+16mm 스킨) — 신 팔은
  이를 호출하지 않아 카메라 데이텀이 on팔과 정확 일치(tol 0 페어링). 동결 라운드
  10개 해시 스냅샷 선기록. 리허설 실측: C2 구off FA 0.208/Δ 0.476·N3 구off FA
  1.000/Δ 0.031(s42). GPU ~10분 패키지. 리스크 명시(3중 차이 패키지 해석·난간
  x2.2m 삼킴·mound B 0.20m 마진·N3=널 컨트롤).

===== WEEKEND 0823 (지시서: WEEKEND_BRIEF_0823 v2.0 + PS_0823, WORKDIR experiments/weekend_0823) =====

- **D31 (일 03:15) 주말 계획 + 사용자 채팅 지시 반영(브리프 비절대화)**: 채팅 룰링 —
  "지시서에 얽매일 필요 없음, 목표는 cue 기반 negobs 탐지·학습 가능성 증명, 계속 점검·
  검토하며 방향 잡기, Opus 레드팀 단계별, U-Net은 최소치 — 유용·실효 모델 추가 가능".
  이에 따른 내 판단 4건:
  ① **CUE-OFF 대조(GPU-2)를 본 창의 과학적 중심으로 격상** — 북극성 직결(트윈 Δ는
  "위험에 반응"까지, cue-off가 "단서로 반응"을 완성). {위험+단서/위험−단서/무위험+단서}
  삼각 확장을 기본 실행(여력 조항 → 기본).
  ② **신규 모델 트랙 신설**(채팅 지시): smp+timm 백본로 레시피 v2 그대로 —
  후보 tu-convnext_tiny U-Net·resnet50 U-Net·DeepLabV3+(r50)·(검토) DINOv2 frozen+linear.
  RGB 위주 3시드, 레드팀 검토 후 2-3종 확정. 근거: 동일 하네스 플러그인(실효성) +
  "cue 학습이 아키텍처 일반적인가"(유용성).
  ③ **Gazebo 신규 설치 생략** — Baseline_NegObs의 기존 ROS2 Humble+Gazebo 환경 재사용
  (읽기+자체 월드 파일만, env_isaaclab 무접촉 = 격리 취지 충족, 설치 리스크 제거).
  ④ V2S 재라벨·게이트(CPU ~30분) 동안 GPU-1(Depth-C2 신off, 분 단위)을 선실행 —
  락 순서의 취지(장시간 점유 우선권) 유지하며 유휴 제거.
  레드팀 3파(초기: §12.3+V2S 기준+본 계획 / 중기: C2·3a 가설+cue-off 설계 / 말기:
  헤드라인·한계·보고서) 편성. 커밋 후 push 상시(공개 repo 규율).

- **D32 (일 03:50) GPU-1 완료 — C2 판별 1차**: Depth 신off C2 FA .750/.500/.750
  (구off에서도 집중 발화였음) → 장식 세대와 무관하게 지속 = **C2 고유 기하/depth
  통계 요인 우세**(사전등록 분기). 낙엽 산포 카드·헤지/수목 라인·상부 경로 기하가
  용의선상, 조명 요인은 depth 무관으로 기각. N3 Depth 신off .00-.13 = 벽화 무반응
  널 일관 ✓. RGB(.681)와 Depth(.667)가 다른 이유로 같은 씬에서 발화하는 구도 —
  C2 root-cause 트랙에 인계.

- **D33 (일 04:10) V2S 게이트 전부 PASS → GPU 9런 개시**: 티어 5범주 양 뷰 동일·
  폴딩 0위반(pregate 포함)·flip-fold 가환 2000표본 증명·스택 40칸 CPU 종단 green.
  하드코딩 실버그 3건 수리(synth 섹터 인덱스·det2cell 리터럴 2) + 진단 스크립트
  4건 잠재 블로커 보고(§12.6-3 전 처리 필요). split은 씬 파티션이라 원본 재사용
  (재생성 시 test 변동 위험 — 채택 기준 ① 붕괴 방지). YOLO 재매핑 선완료:
  프레임 지표 V1과 비트 동일(E/H=0 유지 — §12.5-(B) 실증), cell-F1 반감은
  det2cell 구조 상한으로 계보 명시.

- **D34 (일 04:25 — V2S 어떤 런도 완료 전 타임스탬프 고정) V2S 판정식 개정(레드팀 R2)**:
  기존 사전등록 기준이 V2S에 구조적 불리(칸 2배→FA 기계적 상승·cell F1 희석) +
  "시드 산포 내"는 시끄러운 팔 유리 역인센티브. 개정: **판정은 폴딩 채널**(V2S 섹터쌍
  max → 20칸 등가)에서 3지표(H recall·FA·트윈Δ)×3모델 전부에 대해
  `mean_s d ≥ −0.05 ∧ min_s d ≥ −0.10 ∧ paired-bootstrap CI_lo ≥ −0.05` (d = V2S폴딩 − V1).
  + **네이티브 이득 필수**: 섹터 국소화 오차 중앙값 ≤ 0.80×V1 (시드 2/3). + 신설 게이트:
  V2S val 40칸 각각 양성 ≥3(위반 시 해당 셀 목록 보고). ④(비용 대비 이득)는 사전등록
  밖 승용 결재 게이트로 분리. hflip 순열 우려는 기해소 확인.
- **D35 (일 04:25) 레드팀 1파 수용 계획**: R1-F1(FA-matched 열 신설·주장 하향) ·
  R1-F2(씬 단위 정직화: 씬별 H/off-FA 병기, H·E CI 철회 또는 클러스터 CI) ·
  R1-F3(YOLO 서술 어댑터 한정 교체 + 어댑터 프리 검출 지표 신설) · R2 트윈-조건부
  recall 표 정본화 · R1-F7 H쌍 |on−off| 픽셀 차분 감사 · R3 문서 정정 2건 + **공개
  repo에서 runs/ 산출 md·json이 gitignore로 배제된 치명 갭 즉시 수리**. CUE-OFF
  설계에 플라시보 팔+포즈 5키 게이트+판정식(Δ_cue−Δ_placebo ≥ 0.15 ∧ 3/3 동부호)
  사전등록 편입. §12.3은 R2 수정판(밴드=실측 거리·N-cue 대조컷·AUC≥0.80 등)으로
  프로토콜 명문화. τ_edge "9조합 무민감" 근거는 공허(이봉 분포) — 정직 문장 교체.

- **D36 (일 04:00) 단서 감사 착지 + CUE-OFF 최종 설계**: 관찰 층화 = 사전등록 기준
  미충족(부호 불일치·bare n=9<10·씬+밴드 고정 시 격차 흡수) — 정직 기록. 확정 사실:
  H 243 전수 자기가림(맨-가림 부재 → 논문 서술 교정) · bare-H 18/18 = h0.3 시점 속성 ·
  Depth H는 가드 씬 집중(.706/.133) · s14 가드 구조물이라 토글 불가(test H 60%
  절제 불능 — CUE-OFF는 비-test 3씬으로). 설계: s12(주)+s20(외과적)+s17(무가드 대조),
  4팔(A 위험+단서/B2 가드만 off/B1 단서 전off/C 무위험+단서 — KEEP_DRESSING 포팅을
  격리 사본으로) + **R2 플라시보 팔 추가**(비단서 오브젝트 유사 픽셀질량 제거 — 씬별
  가능성 구현 에이전트 판단, 불가 시 한계 명기) + 판정식 사전등록(Δ_cue−Δ_placebo
  ≥0.15 ∧ 3/3 동부호). 게이트: 포즈 eye<1e-6·A/B GT 바이트 동일·1프레임 실렌더 스모크.
  렌더는 V2S 큐 종료 후(스모크만 사이 슬롯 허용).

- **D37 (일 04:20) Gazebo 정찰 착지**: Classic 11+Humble, 절대경로 world 스왑 실증,
  격리 PASS(conda 무접촉 검증), 캡처 도구 자작(cv_bridge 회피). 월드 4종(계단 0.72m·
  플랫폼 0.80m × 트윈 ctrl — ctrl은 신off형 FA 프로브 겸용), 카메라 9프리셋을 코퍼스
  grid_views와 자릿수까지 정합. infer 필수 플래그 검증(--grid V1·--fit squash — letterbox
  회색 패드는 콘크리트로 오독됨). 한계: 4월드 전부 V티어(그림자존 짧음) → H(가림물)·
  E(협폭 트렌치) 월드 추가 지시. 캡처(GPU-6)는 CUE-OFF 이후 슬롯.

- **D38 (일 04:40) 3a·C2 감사 착지**: ① 3a Δ≈0 = 시점 무관 균일 널(포즈-클러스터,
  단일 포즈 제거 시 +0.002; V티어는 +0.405로 살아있음 = "단거리 차폐"의 성질) →
  결재 #4 예비판정 = **안A "3b 한정 유지"**(양안 문장 완비, GPU-5 렌더 불요·제로렌더
  대안만 저녁 배치). 신규 한계 문장: 3b 내부도 씬 집중(s15 3b CI∋0·h_lo 클러스터
  CI∋0). ② C2 3중 확정: Depth FA = 구별입력 1~3포즈(s43은 1포즈=100%) → 보고는
  distinct-input 레벨로 / dressing 귀속 Depth 44.4% vs RGB 76.7%(브리프의 판별
  논리 교정: depth에겐 dressing이 곧 기하) / hgrad_mean 예측자(d=1.98, 신off ×3.0
  상승 사전 예측 적중). 씬 수정 제안은 아침 결재란으로(수정 없이도 보고 레벨 교정으로
  캐비앗 처리 가능이 예비 판정).

- **D39 (일 04:55) 실측 프로토콜 R2판 착지**: PROTOCOL_SHOOT 개정(20칸·16:9·캘리브컷·
  피치 [-15,-3]) + REALWORLD_GRID_PROTOCOL 신설(§12.3 명문화) + README_PILOT.
  AUC 사전등록 고정(중앙값 시드, ≥0.80 진행/0.60-0.80 보류/<0.60 중단 — 결과 전 고정).
  파일럿 재배분 V4/E4/H4/Nturn4/Ncue4. 결재란 후보 2건 신규: ① sites.csv azimuth
  부호 반전(촬영 전 1회 정정 필요) ② 규정 포즈에서 band1 프레임 밖 — 실측 관측
  격자는 15칸(문서 명시 완료, 논문 방법 절 반영 필요).

- **D40 (일 05:10) Gazebo H/E 월드 착지**: gz_drop3(H 7/7 프리셋 — 완전가림이라
  트윈 픽셀 동일·Δ=0 구성적 → 순수 단서-발화율 측정 + 프레임 diff=누출 QA) ·
  gz_drop4(E 2/7 — 역산: d≤2에서 E는 개구 1-3cm=균열이어야 성립 → "E 연약"의
  기하적 독립 확증). τ_int=50은 DS=4 그리드 기준(풀해상도 800px) 정정 반영.
  8월드 sdf 8/8 PASS·기존 4월드 바이트 불변·캡처 216프레임 계획. E 통계는 2컷이라
  금지(far-standoff 서술만).

- **D41 (일 05:30) 레드팀 대응 산출 착지**: ① FA-정합 표 — 전 지점 Depth≥RGB,
  하향 문장 채택("RGB는 FA 10% 예산에서 은닉 프레임 1/3 회수, 탐지기 기준선의
  트윈-조건부는 −0.010"). ② 트윈-조건부 정본화 — Depth H는 96/96 위험-조건부
  (off 발화 0), RGB는 44.7% 무조건부. ③ YOLO "구성적 0" 철회 → 이중 상한(어댑터
  0.000 / 어댑터-프리 위험-조건부 +0.038 vs V +0.337) + amodal 박스의 상 미결정
  한계 자인. ④ **F7: "완전가림(fully occluded)" 용어 폐기** — 96쌍 전부 픽셀 차
  (s14 7.6%/노이즈 ×3140; 토글이 어깨 지형·파라펫까지 제거), strict-H는 "내부면·림
  기여 0"으로 정확 서술하고 트윈 Δ는 "위험 제거의 총 시각 결과(간접 포함)에 대한
  반응"으로 재프레임(PS §1-3 예견 프레임). 픽셀 잔차와 트윈 Δ가 같은 씬을 지목
  (독립 측정 일치) — Depth는 잔차를 읽음(6/6 양의 상관), RGB는 비일관. ⑤ aux
  헤드라인 효과 클러스터 CI 사망 → SEED_TABLE §5.2 철회 부기 완료(append).
  ⑥ N3 널 교정은 팔 한정(Depth 진짜 통과·RGB 포화). 잔여 리스크 RR1-RR9 원장화.

- **D42 (일 05:50) CUE-OFF 구현 완료 + 코퍼스 결함 발견(상향)**: 플라시보 실측 —
  s17 주 레그 승격(아파트 4동 제거 = B1 제거량의 18×, GREEN)·s20 플라시보 불능
  정직 선언(전 소품 0px)·s12 강등(3.2× 과소, 비승격 2차). datum 수리 2건(s20 밴드
  8.5mm 재건·s12 오라클 hazard-blind 함정). 스모크 s12/s17 DATUM OK, s20은 V2S 락
  대기로 보류(분석 검증 대체, 다음 슬롯 실행). **결함 G7**: boost_* 라운드 미융합
  높이맵 → 오라클 6씬의 boost GT가 빈/조각 footprint 기준(s07 test·s08 val 0셀,
  s12 H48이 50셀 기준) — 영향 분석: 해당 프레임 대부분 none_in_fov로 빠져 recall
  분모 미오염이나 on-neg cell FPR 일부 과대 가능. 동결 무수정, 정량화+처분은 아침
  결재란(수정 = 코퍼스 v3 = 재라벨·재훈련 함의). 렌더 53분+평가 77-115분 견적,
  V2S 종료 후 즉시 체인.

- **D43 (일 06:20) V2S 채택 판정 = ROLLBACK(사전등록)**: 폴드 게이트 PASS(146,880
  비교 불일치 0) 후 D34 9셀 중 6 PASS — ① 위반(rgb H 폴딩 −0.306 mean·트윈Δ CI 2건)
  + 국소화 이득 공허 통과(중앙값 0/0 항등) + test 측방 씬 0(구조적) → 롤백. 실이득
  (FA↓·통로 3/3·ep1 붕괴 0/9)은 기록하되 각 분해능 귀속 불가. V1 정본 유지, V2S는
  §12.5 연장 (1) 복귀 + 재도전 설계조건(측방 씬의 test 편입이 선결) 보존. FA-매칭
  렌즈에서 rgb 격차 −0.306의 상당분은 캘리브레이션 아티팩트이나 시드 산포 ±0.4로
  판정 불가 — 지식으로만 기록.

- **D44 (일 06:35) CUE-OFF post 게이트 G2 오탐 판정**: s12 P-vs-A 높이맵 차이 42셀은
  전부 x∈[−2,−1.9]·y∈[−3.65,−2.55](제거 소품 자리) ±2mm — 낙차값 중첩 0·카메라
  스트립 밖·drop 기하 불변. 게이트가 전역 비교라 소품 셀 변화를 위반으로 오분류
  → 오탐 판정, 근거 수치와 함께 진행(향후 게이트는 hazard footprint+스트립 스코프
  권고). 평가 체인 개시.

- **D45 (일 06:40) 신규 모델 트랙 확정(사용자 채팅 지시 이행)**: 인코더 축만 격리 —
  tu-convnext_tiny U-Net + resnet50 U-Net × RGB 3시드(레시피 v2 그대로, --grid V1).
  DLv3+ 제외(디코더 변경이 인코더 효과와 교란 — 과학적 축 오염). 비교는 R1-F1
  규율(FA-매칭 병기) 준수. 슬롯: CUE-OFF 평가·Gazebo 캡처 후 저녁. 질문: "cue 학습이
  백본 용량·계열에 일반적인가".

- **D46 (일 07:20) CUE-OFF 판독 — 주말 중심 결과**: C팔(단서만) RGB/B2 FA 1.000 ·
  B1(단서 전제거) RGB Δ+0.375(s44 +0.875)/B2 +0.972 붕괴 · B2(가드만) RGB Δ 0.000
  (가드 단독 기여 0~24%) · 플라시보 Δ 0.000 · Depth 전 씬 Δ≈0. 사전등록 1차 판정 =
  SHORTCUT 다수+UNDECIDED, 강한 CUE-EVIDENCE는 반보수 2차 레그만(비승격 — 규칙 준수).
  종합 명제: **"단서 연합이지 단서 추론이 아니다"** — 매트릭스 패밀리②의 인과 실증,
  9월 탈상관 재훈련의 실험적 근거 완성. 논문 중심 문장 재편 재료. VOID 1계보
  (paired-H 0) 규칙대로 폐기.

- **D47 (일 08:00) 융합·광도 착지**: 융합행 — E/H +0.000(3시드 바이트 동일, 상보성
  불성립)·V +0.026(산포 1/6)·교환비 14구조:11신규FA. 실발견: U-Net band1 발화 0
  (3시드 0/4080) — band1 열은 YOLO 단독, 한계 문장 신설. 위치 권고 = 부록(결재란
  형식 기재). 광도 — 「광도 증강 불요 근거」 채택(최대 −0.044 < 기준 0.1), 암부는
  τ 이동만(완전 회복), 과노출은 하이라이트 클리핑으로 비가역 → 파일럿 수칙 P-1~P-3
  (과노출 재촬영·EV 기록·FA 먼저 판독) PROTOCOL 반영 대기.

- **D48 (일 08:20) Gazebo 교차-시뮬 착지**: 캡처 216/216·누출 검증 정밀 통과(결정적
  렌더러 실증, drop3 핏 밴드 0px — H 성립, 데크 테셀레이션 잔재만 정량 기록).
  제로샷: 트윈 판별 붕괴(rgb Δ +0.019)·티어 사다리 역전(4/6 H>V — 단서 목록 추종)·
  **단서-단독 발화 교차-렌더러 재현**(drop3 ctrl rgb .524/b2 .905 ↔ CUE-OFF C팔 1.0,
  같은 방향). 안전 주장 경계 명시(0픽셀 발화·사다리 역전만; 트윈 붕괴 자체는 sim2sim
  교란). 잔여: depth 팔 부재(월드 --depth 미빌드)·섹터 부호 미해결·b2 3줄 심.

- **D49 (일 09:00) 레드팀 2파 — D46·D44·D48 정정 및 수리 지시**: ① D46의 "C팔 FA
  1.000"은 **scene12 한정**(s17 .042/s20 .36 — 씬별 C수술 광학 크기와 단조: 36.6%→1.0
  /13.1%→.36/1.0%→.04)이며 "단서 어휘 반응" 해석 철회, 1차 판정 census 정정(CUE 0/
  SHORTCUT 5(검정력 없음: 0-flip 상한 .117>.10)/UNDECIDED 7). ② D44는 G2 위반 8건 중
  1건만 검사한 과잉 일반화 — 인정·정정(s20 B1/B2 채점표 상이 +8% 실증). ③ D48
  "4/6"→**5/6** 정정. ④ 플라시보 질량은 AABB가 아니라 실렌더 픽셀차 기준으로 전면
  재산정 필요(그림자·GI 누락 — F7 예고 함정; s20 "0px"는 투영 버그, 실측 0.91× 최적).
  수리 지시: (a) s20 재렌더(+cams, paired-H≥10)+플라시보 복원 → b2 레그 1차 CUE
  EVIDENCE 후보(+0.667, 3/3) 검증 (b) G4/G5/G7 실행 + G2 8건 전수 판정 (c) same_sign
  조작 정의 수정 + PREREG 수정 A2로 재판정(사후 수정 정직 표기) (d) R6 79문장 일괄
  적용. 완료 전까지 CUE-OFF 판정 인용 금지.

- **D50 (일 11:30) CUE-OFF 수리 완주 — census v2 확정**: 1차 CUE EVIDENCE 0 /
  SHORTCUT 2 / NO-EFFECT 6 / UNDECIDED 4 / VOID 18 (v1 SHORTCUT 8건 전소멸 — 술어
  양방향 결함). 실렌더 질량으로 전 레그 보수성 부정(플라시보 역전: s12 b2에서
  플라시보 Δ+0.222 > 단서 Δ+0.028 = 8×). s20fix: paired-H 6→27, b2 레그는 A2-13
  (눈가림 사전 룰링)으로 VOID — 2차 A-GT 판독 +0.506(3/3, R4 예측 재현·비승격).
  s20 "0px"는 투영 버그가 아니라 참(D49④ 정정) — 오류는 §3.3의 추론(팔이 다른
  집합 제거). G2 8건 전수: 6 오탐/2 실질-비물질(D44는 s12 한정 정당·일반화만 과실).
  G4 최초 실행: V→H 18/24(twin·s12·e2 B1) 등 미검증 기본값들 실측. **s12 strict-H
  48프레임 = 교정 footprint에서 V티어**(H 지위는 라벨 조각 아티팩트 — G7 처분과
  결합). 관리 실패 2건 자기 보고(PREREG mtime·게이트 파일 덮어씀 — 재구성 표기).
  논문 위치: 개입 장치는 중립 결과 — 기여는 장치·수리 사가·진단 수렴으로 서술.

- **D51 (일 11:45) 신규모델 판독**: resnet50 H .469/.240/.219(FA .19-.34) ≈ resnet34
  동급(용량 무익 — 인코더 축 결론). convnext_tiny는 τ0.5 퇴행 작동점(H .63-1.0이나
  FA .73-1.00, s43 전지표 1.0 = 상시발화) — 선택지표(0.5·F1+0.5·H)가 고FA 체제로
  수렴한 것으로 추정, FA-정합 분석 필수·무정합 인용 금지. 표 위치: 부록 후보
  (FA-정합 계산은 조립 단계 위임). 아키텍처 일반성 질문의 답: "연합 행동은 백본
  불문 — 강한 백본은 연합을 증폭"이 잠정 서술.

- **D52 (월 조립) 신규모델 FA-정합 판독 — D51 정정 + 아침 보고 착지**: D51의 지시
  ("무정합 인용 금지·FA-정합 필수")를 이행. 규칙은 R1-F1(`rt_response/code/f1_fa_matched.py`)
  과 동일한 exact-quantile τ. 결과: **두 신규 인코더 모두 FA-정합 전 지점에서 resnet34
  기준선 아래** — resnet50 Δ −0.250/−0.215/−0.212/−0.188, convnext Δ −0.372/−0.323/
  −0.253/−0.212 (FA .359/.20/.10/.05). ⇒ **D51의 "resnet50 ≈ resnet34 동급"은 정정**
  (τ0.5 무정합에서만 비슷해 보였음). convnext는 τ0.5 H .823이 **FA 0.909의 상시발화**
  이며, 더 근본적으로 **3시드 중 2시드가 상수 출력으로 붕괴**(off-max 폭 s42 8e-5·
  s43 2e-4, 셀별 std ~1e-5; s43은 epoch 1 체크포인트 선택) = 아키텍처 결과가 아니라
  **훈련 실패**. 원인: 선택 지표 `0.5·val_f1 + 0.5·val_H_recall`의 H 항 분모가
  **val strict-H 6프레임**이라 상시발화에 1.0을 공짜로 준다 — 이 선택식은 **레시피 v2의
  것이고 본 표 resnet34도 동일 사용** → 신규모델 결함이 아니라 레시피 약점, **본 표에도
  해당되는 캐비앗(C3)으로 승격**. D45 질문의 답: "이 레시피에서 인코더를 바꿔 얻는 것은
  없다 — 표는 resnet34 3행 그대로". 산출 `experiments/weekend_0823/newmodels/FA_MATCHED.md`.
  아침 보고 `experiments/weekend_0823/MORNING_REPORT_0823.md` 착지 — 결재 12건 상신.

===== V3 창 (지시서: V3_BRIEF_0823 + DZ=V3_DESIGN_0823 v2 + PS_0823_v2, 루트 experiments/v3_0823) =====

- **D52 (일 오후) V3 창 개시 — 이해 검산 + 질문 4건 상신**: 3문서 정독 완료. 패러다임
  독해(3원인↔3치료 짝, 4층=데이터·지도·계기판 원리, (A,C)쌍 H판별 근거=지형 연속성
  증거라는 해석 포함)를 채팅으로 검산 요청. 사용자 질문 4건: Q1 v3 훈련 코퍼스에서
  기존 씬 지위(안2 권고: A+신off C 편입·구off 폐기·B/D는 신규 공급) / Q2 구off의
  D팔 재활용 가부 / Q3 GPU 점유 상한(사용자 일정) / Q4 CPU-7 범위. **P-5(렌더 계획)만
  질문에 게이트** — P-1~P-4·CPU-3·CPU-4는 무조건 필수라 즉시 착수.

- **D53 (일 오후) P-1 완료 — census "불일치"는 불일치가 아니었다**: MR23의 30칸과
  CUEOFF_RESULT_v2의 24칸은 경쟁이 아니라 **포함 관계** — 24 = 30 − s20fix 수리 라운드
  6칸(v1 판독 07:20 시점 PNG 0장, 전부 VOID). 정본 = **primary 전블록 30칸**(사전등록
  모집단이 s20fix를 A2-10으로 렌더 전 등록) + A2-7 접은 21칸 병기 의무. 인용 규약 신설:
  **leg+접기여부+n 필수, 맨숫자 인용 금지**(같은 문서 안에 n=24·VOID 12가 각각 두 뜻으로
  출현). `experiments/v3_0823/ACCOUNTING.md` 신설(§1 회계, §2 DZ §12 9선 등재, §3 상시
  모집단) + 정정 배너 2건 + 구판 PS 대체 배너 1건 append. **신규 실측**: test-core 816프레임
  중 480(5씬×96)이 boost 라운드 = G7 노출면이 test-core GT의 59%를 관통 — §12-1(교정 GT
  재채점)의 무게 실증. 미결: 단서 존재 임계 k(지도 착수 전 게이트 고정), none_in_fov 분모
  구멍의 v3 처리(ACCOUNTING §3.4). 부수: MR23 작성·창 줄의 08-24 잔존 2곳을 기결재 시점
  수정의 잔여분으로 정정(줄 수 불변, 인용 행번호 무영향).

- **D54 (일 오후) CPU-3 완료 — FA 현실 조사 + FA 가족 5종 예비 룰링**: `v3_0823/FA_REALITY.md`
  (출처 37건). ① DZ §8.1의 FA 가족 4종(장식/지형통계/조명/경계칸)은 리포 전체에 정의가
  없음을 실측 — 예비 룰링으로 **5가족 분리**(경계칸형='칸' 형태소대로 그리드 가장자리 칸
  인공물에 예약, 내용 가족은 대리선형 신설, 다중 소속 허용). 최종 채택 승용, CPU-2 센서스가
  이 5가족으로 설계. ② 갭 지도 상위: 맨홀(빌더 기존재·에셋 0)·수목보호격자·**cue_shadow_caster
  토글 부재**(그림자⊥위험 독립 강제가 현재 구현 불가 — v3 씬 표준 장비에 추가)·줄눈/신축이음·
  노면표시 — 상위 6 중 4가 "에셋 있음, 제어면 없음" = 토글 승격 작업. ③ **젖은 노면은 갭이
  아니라 함정**: RTX 센서 계통이 물을 불투명 취급(이슈 #547) → RGB/depth 팔 물리 불일치
  교란. depth GT 검산 없인 채택 금지. ④ N-cue 사다리 12종(★1~5) — 핵심 논거: 교통약자법
  시행규칙이 턱낮춤(≤2cm)+점형블록 동시 의무화 = **법이 N-cue 분포를 실세계에 보장**(ADA
  동형). ⑤ 논문 탄약: Matthies&Rankin'03이 우리 단서 채널을 선제 기각하고 열화상으로 우회
  ("조명 의존·오경보") = 본 연구가 앉은 구멍의 문헌적 좌표; RELLIS-3D/ORFD에 음의 장애물
  클래스 부재. ⑥ 파일럿 Ncue 컷 6종(실측 높이 기록 의무 포함).

- **D55 (일 저녁) P-2 완료 — G7 수리: 결함은 5쌍·3씬, s12 48/48 H→V (D50 재현)**: 전 26개
  (밴드,씬) 쌍 전수 스캔 — 미융합 결함은 boost_e×{s07,s08,s12}+boost_e2×{s07,s12}뿐,
  boost_h 무결함, 재렌더 불요(융합 입력 전량 존재). 심링크 섀도 트리로 동결 트리 무접촉
  수리, 대조군 816프레임 바이트 동일 입증. 교정본 2벌(정본 B=본 라운드 융합 참조 240프레임
  교정 / 민감도 A) — `dataset_manifest_v2corr.json` 정본. 티어 이동: 코퍼스 strict-H
  243→195(전량 s12), **test H 96 불변**, 그러나 **test V 180→219(+21.7%)·recall 분모
  327→372** — "분모 미오염"은 H행 한정 참(PS v2에 정정 배너). 게이트: 순진한 "아티팩트 0"
  검사는 결함본도 통과함을 명시하고 반대-instrument footprint 재측정으로 대체 — 잔존
  strict-H 12쌍 전부 FLAG 0, 진짜 H는 ratio≥1.0(은닉의 물리 서명). 부수 정정: D42의
  "오라클 6씬"은 실측 3씬/5쌍(인용 수치 자체는 정확). 다음: v2 전 체크포인트 교정 GT
  재채점(§12-1).
- **D56 (일 저녁) P-3 완료 — 커버리지 감사: "가용 3씬"의 진짜 병목은 배선이 아니라 strict-H**:
  33/33씬이 제거형 토글 ≥1 보유(주말 인벤토리의 dead 27은 cfg.get() 미포착 버그로 11칸
  과소 — 실제 22). 필터 체인 33→strict-H 6씬→비test 4→s09 탈락(강단서가 토글 미부착)→3,
  사후 판정으론 **실효 1씬(s17)**. s14 가드 구조물 확정(parapet이 무조건 빌드). 13키 중
  5키(shadow_line·far_side_visible_depth·water_surface·specular_change·edge_line_contrast)는
  토글 전무 — B팔 조작 가능 키는 R/Ta/N/T/Sg/V뿐. **cue_material_break가 유일한 전코퍼스
  청정 B팔 레버**(24/32씬 재질 재바인딩만, 높이맵 비트 동일 보장; 예외 8씬 중 s05
  LipCurb·D4 Facade_가 구조물 최상위 의심). **v2 off팔은 비균질 세대** — 8씬 단서
  보존(≈C)/25씬 동시 소멸(≈D), ACCOUNTING §4.1 정정. P-5 결정 입력: 기존 33씬으로
  test-ext 불가(4팔 무수정 성립 8씬이 전부 v2 학습/시험 이력) + 4팔+strict-H 조합 0 →
  **paired-H≥10은 전량 신규 씬**. 기존 씬 C팔화는 keep_dressing 포트 25씬×0.5-1h(CPU).
  구조물/장식 렌더 판별 큐: 고우선 33쌍 + 저우선 39쌍.
- **D57 (일 저녁) CPU-4 완료 — 가정 대장 73행 + 신규 결함 4건**: `experiments/v3_0823/
  ASSUMPTION_LEDGER.md`. 소스 6종(PS v2·DZ·MR23·D1–D56·METRICS·RESULTS_DRAFT) + 코드
  12파일 정밀 대조로 설계 선택 73행 축조(GRID 8·LAB 16·DATA 12·MOD 13·EVL 11·SCOPE 7·
  V3 6). 회계: **계승·미검토 19행(순수 13)** · 훼손 31 · 지지 27(양쪽 4) — 근거 공백이
  가장 몰린 곳은 **MOD(8/13)**, 훼손이 가장 몰린 곳은 **LAB(9/16)**. **신규 발견 4건**:
  ① 트윈 tol 문서-코드-산출물 3자 불일치 — 문서는 `|Δground_z|≤0.15`인데 코드는 5키에
  스칼라 1개(`twin_analysis.py:56`), 게다가 `run_queue_v2.sh:126`은 `--tol 0.15`이나
  출하 `runs/*/twin/twin_analysis.md:4`와 `METRICS.md:966`은 `1e-06` → **논문 트윈 수치의
  생성 설정 미확정, v3 착수 전 재현 1회 필요** ② `none_in_fov`가 **게이트 후** GT로 판정
  (`labeler.py:526`) → 스텝게이트 배제분이 "위험이 그리드 밖"이라는 다른 원인의 이름으로
  분류 ③ 선택지표 분모 이중 정의 — `n_val_strict_h`(`:158-161`, toggle 검사 有) vs
  `h_frame_recall`(`:235-244`, 검사 無) → P-4 새 식이 이 불일치를 승계하면 수리가 헛돔
  ④ 게이트 번호 충돌 — 라벨러 G1–G5(`gates.py:2`, G5는 참고)와 CUE-OFF G0–G7
  (`PREREG_CUEOFF.md:224-230`)에서 **같은 G2/G4/G5가 다른 뜻** → 접두어 분리(LG*/CG*) 권고.
  레드팀 2세션 과녁 분할 편성(A: 근거 없는 상수 19행 / B: 훼손된 가정↔v3 처방의 짝맞춤).
  집필 중 착지한 D53–D56을 9행에 반영(LAB-10·12, DATA-09, SCOPE-04, EVL-09, V3-01·02·03·05).
  미이행 자인: V3_BRIEF §6-1 정성 패널 — 문서 트랙이라 원재료 없음(면제 주장 안 함).

- **D58 (일 밤) 승용 회신 v2 — 검산 채택 + Q1~Q4 룰링 + P-5 해제** (원문: 08-23 채팅, Claude AI 정리):
  ① **(A,C) 지형 연속성 독해 채택**(F7 간접 신호의 설계 승격) + 정밀 조건 3: ⓐ 티어 순도
  (프레임별 기계 술어만, A팔 렌더 후 티어 재판정 게이트) ⓑ **모서리 소속 게이트**(A팔 H의
  가시 모서리는 가림체 기하 소속, 낙차 림·내부 0px를 ID 마스크로 기계 검사; 판정 불가는
  경계 버킷 격리 — H로 안 셈) ⓒ 현실 대조 정성 패널(실측 H 규정 컷 vs sim A팔 H 컷).
  v1 부칙 유지: 어휘 규율("가시적 지형 결과에 반응") · 쌍별 A−C 광학차 로그(≈0 쌍은
  정보량 0 층화·무벌점) · C팔 게이트(단서·장식 마스크 불변 해시 + 변경 픽셀 지형 국소성).
  ② **Q1 안2 채택**: 기존 씬 = A + 신세대 C팔 재렌더 편입(구off 폐기), B/D는 신규 공급,
  |r| 실측 관리. 부칙: 팔×씬출신 상관 병기(신규 교란 감시) + P-3 '장식' 씬은 B팔도 제작.
  ③ **신규 씬 품질 원칙**: 사실화 파이프라인 표준 전부 승계(생활권 유형·조립 공식·HDRI/
  재질/props). 추상 기하는 진단 프로브 전용. N-cue는 현실 맥락이 성립 조건. **절약은
  보고·컨텍스트 위생에서, 씬 품질에서 하지 않는다.** ④ **Q2 구off→D팔 재활용 승인**,
  조건 3: 게이트 통과분 · 제거 범위=현행 cue_* 일치 씬만 · 계기판/test-ext D팔은 신규 렌더.
  ⑤ **Q3 GPU 프리런**(예약 없음, flock 유지, 개입 필요 시 프롬프트 요청 후 대기 정상).
  ⑥ **Q4 = ⓐ+리소스 추출**: submission_0830/ = INDEX.md(중요·누락위험 메모 열) + 표·패널
  규격본, 산문은 Claude AI 기본, 문장 조각은 색인 표시로 이원화 방지. ⑦ **운영 수칙(자제
  원칙)**: 대용량 원문 반입 자제(스크립트 집계 기본) · 재정독 대신 착지 기록 참조 ·
  서브에이전트 요약 우선 · **Opus 위주 활용, Fable은 고도 판단만(토큰 압박), 내부 영어**.
  ⑧ **P-5 게이트 해제** — 렌더 계획 착수. ⑨ ⓑ의 파생: ID 마스크(세그) 사이드카가
  load-bearing으로 승격 — §12-5 k 판정과 모서리 소속 검사가 공히 세그를 요구, P-5에
  세그 사이드카 실증 스모크 포함.

- **D59 (일 밤) v2 재채점 완료 — 무결성 4/4, 공표 델타는 42프레임 유입 하나로 전량 설명**:
  9런 GPU 2분33초. ① 예측확률 비트 동일 9/9(픽셀 불변이 모델 출력층에서 확인) · **H행
  완전 불변**(frame/cell recall·twin ΔH 전부) · off팔 FA Δ=0. ② σ 초과 이동 2건뿐(depth
  cell_precision +0.051=1.65σ / H_weak σ=0 퇴화라 인용 금지). V recall 이동은 전부 σ 이내
  — 단 rgb σ=0.204라 "판정 불가"로 서술. ③ 기전 폐쇄: 구 분모 327 위 재채점 = 공표치와
  소수 4자리 동일 → 델타 100%가 scene07 boost 42프레임 유입(V39+Hw3). ④ **FA-정합 생존**:
  4지점 H recall 소수 3자리 불변(τ는 off팔, H 96과 G7 결함면 교집합 0), Depth≥RGB 유지·
  격차 확대. ⑤ C/D 분해 최초 산출: FA_C>FA_D 3모델 성립하나 rgb는 sceneN3 전담(제외 시
  부호 반전) — 방향 증거 전용. ⑥ 트윈 재층화 CPU 완료: "Depth H 96/96" 생존, V kept
  165→177. ⑦ **정정**: D55·ACCOUNTING §4.2의 "none 36/분모 372"는 오케스트레이터 산술
  오류 — 정답 **39/369**(§4.4 정정, 이후 분모는 스크립트 산출만). 산출물 V2_RESCORE.md
  §6.2-6.3에 공표 문서의 이동 줄/불변 줄 전수 색인(편집 없음). 후속 큐: YOLO 3런·
  rgb_s42_aux 재채점(소과제), 트윈 tol 확정(§4.3 #1 — 즉시 착수).

- **D60 (일 밤) CPU-2 완료 — FA 센서스: "FA는 씬이 아니라 그리드에 고정"**: 6,925사건
  (OFF 2,212/ON_NEG 4,713), 공표 대조 27/27 일치, GPU 0. ① **최대 소득 = 융합 여유**:
  OFF층 시드-다수결 FA의 3모델 동시 발화 0/362, **rgb∩depth 정확히 공집합**(Jaccard
  0.000), 88.4%가 단일 아키텍처 고유 — FA 절감형 융합의 실측 근거. ② none_in_fov가
  ON_NEG 사건의 56.7%(lift 1.92×, s07+N3 집중) — 분모 구멍의 실물량. 단 **구 GT 측정**:
  s07 39프레임이 교정 GT에서 양성 전환되므로 교정 재생성 필수(동일 에이전트 속행 지시).
  ③ **가족 판정**: 장식형 기각(국소화 2씬 lift≈1.0 — CUE-OFF 0/30과 정합), 대리선형
  OFF 무효과(N3 역방향), 조명형 U자 비단조; 단조인 것은 칸내 휘도 산포(34×) = 광학
  변화량 — CUE-OFF 독법을 제3계기로 재확인. ④ **경계칸형 분해**: 방사 절반(3b밴드
  lift 2.7~3.3)은 R^1.98 법칙(원거리 소실 기하의 거울상 = 지형통계형으로 흡수 권고),
  방위 절반(A/E섹터)은 lift 0.36~0.72로 역방향 — D54 5가족 스키마는 유지하되 보고 시
  경계칸형을 지형통계 방사 하위형으로 접는 수정 예비 룰링(최종 승용). ⑤ 카메라 6m 후퇴
  검정: 발화 밴드 불이동(기울기 −0.06~+0.39, 내용추종이면 ±1) — s05는 씬 밖 밴드에
  발화. ⑥ convnext 상수출력이 최대-사전 칸(중앙섹터×최외밴드)에 정확히 앉음 = 비계획
  양성 대조군. ⑦ N-cue 표적 규칙: 시드 ≥2/3 지속 사건만. ACCOUNTING §4.5 등재(구 GT 태그).

- **D61 (일 밤) 트윈 tol 포렌식 종결 — "3자 불일치"는 불일치가 아니었다**: v2 출하
  twin_pairs.csv가 `--tol 0.15` 재실행으로 **바이트 동일 재현**(rgb/depth_s42 md5 일치),
  출하 v2 헤더도 0.15 명기. 원장의 "1e-06 헤더" 인용은 ① pre-D20 v1 런 파일 ② 홀 프로브
  (eval_probe.py 설계상 1e-6 하드코딩)의 **오인용** — v1 파일은 tol 줄이 4행, v2는 5행
  (인용 행번호가 단서였음). 코드 경로: 헤더는 실제 인자를 인쇄(:123→:56 동일값) — 헤더
  버그 없음. 독립 408쌍 층화가 METRICS §N.1 정확 재현: EXACT 312(H96·E45)/TOL 54(V33·
  기타21·**H0·E0**)/EXCL 42 → D27 항등 재확인, H/E 주장 tol 불변. 공표 수치 이동 **0**.
  잔존: 5키 혼합 단위 스칼라 tol은 잠재 결함(ground_z 외 Δ=0.0) — v3 코드에서 키별 tol로
  정리 권고. ASSUMPTION_LEDGER 결함①/LAB-13 철회 배너 append. 산출: TWIN_TOL_RESOLUTION.md
  + ACCOUNTING §4.6(에이전트 직접 append). **P-1 census 건에 이어 두 번째 "불일치가
  오인이었던" 사례** — 교훈: 파일 계보(런 세대) 확인 없는 수치 인용이 유령 결함을 만든다.

- **D62 (일 밤) RT-A 착지 — 차단급 4건·신규 행 31건·실측 반전 2건**: `redteam/RT_LEDGER_A.md`.
  차단급: ① EVL-12 FA-정합 축(frame any-fire)이 Depth 칸당 부담 은폐 → 양축 병기 규약
  (ACCOUNTING §4.7, 재작도 즉시) ② LAB-19 void=음성 인쇄(on팔 42.4% 프레임) → v3 라벨
  사양 gt_void 분리, P-5 주입 ③ LAB-26 check_twins가 트윈 정의 4번째 자리(ground_z
  미검사) ④ SCOPE-08 추론 hfov 69° vs 훈련 62.2° — 파일럿 AUC 오독 위험, 촬영 전 수리.
  실측 반전: LAB-01(0.3m)은 hz 0.20-0.40 전구간 분모 불변으로 **강등**하되 (hz×STEP_RUN_M)
  결합 벼랑(hz 0.5에서 s14 GT 전멸, test H의 60/96) 신규 행 / LAB-07 τ_edge는 죽은 상수,
  대신 τ_int가 H_weak 보고 분모를 조용히 줄임(eval TIERS에 H_weak recall 행 부재) 발견.
  31행은 RT-B 착지 후 대장에 일괄 append 예정. §4.2 오기 독립 재확인(§4.4로 기종결).

- **D63 (일 밤) 센서스 교정 GT 재생성 — none_in_fov 헤드라인 자기 격파, off팔 소득 전부 생존**:
  ON_NEG 분모 5,469→5,304 · 사건 4,713→3,894(−819, 전량 scene07) · **none_in_fov 비중
  56.7%→17.5%, lift 1.91→1.19** — "분모 구멍이 on팔 FA 절반" 주장은 결함 GT의 아티팩트로
  판명, 에이전트가 자기 권고 N4를 자진 강등(잔여 39프레임의 규약 정리 항목으로 유지,
  labeler.py:526 결함은 별개 존속). OFF층 2,212행은 두 GT에서 **행 단위 동일**(가족
  플래그·method 포함 0행 차이) 실증 — R^1.98·그리드 고정·융합 0중첩·장식형 기각 전부
  생존. 대리선형 ON_NEG lift 2.18→1.60(bleed 가드 제외 88→184 — 진짜 립을 오계상했던
  것의 정정, 약가족 결론 강화). 부수: 에이전트가 자기 코드의 하드코딩(none=81) 결함을
  발견·수리(구 GT에선 우연히 정답). 검증: 덤프가 정본 B임을 tier 전수로 확인, 구 GT
  산출물 7종 md5 불변. **ACCOUNTING §4.7 번호 동시 append 충돌** — 센서스(423행)와
  RT-A(471행)가 같은 번호 사용, 후자를 §4.8로 개번(D62의 "§4.7" 인용은 §4.8로 읽을 것).
  이후 ACCOUNTING append는 오케스트레이터 직렬화(에이전트는 제안만).

- **D64 (일 밤) RT-B 착지 — 짝맞춤 7봉합/12부분/12무처방, 최중대 공백 5**: `redteam/
  RT_LEDGER_B.md`(834행). ① **계기판①에 FA-정합 부재**(자기 법 위반) → 병기 채택(§4.9-2)
  ② **val strict-H≥30 공급원이 사양에 없음**(§4.2 H는 전부 test-ext, 유용 시 §12-9 위반)
  → P-5에 훈련측 paired-H 씬 신설 주입 ③ |r| 계기가 퇴화를 성공으로 읽음 → 보고 규약
  3종(§4.9-3) ④ (A,C) 데이텀 게이트 부재 → VG-datum(§4.9-5) ⑤ σ=range/2 15% 과소 →
  ddof=1(§4.9-1). **N-5**: 본표 9런 전부 aux OFF 실측 — DZ §0.1 전제 거짓, GPU-5 표본
  n=1, aux on/off 결재 상신(§4.9-4, 권고: OFF 유지+격리 런). 5선 재판: 동의 3(MOD-07
  강화·GRID-02 훼손 승격 실측·EVL-07 최저가 수리)/기각 1(LAB-13, D61과 독립 동일 결론)/
  강등 1(LAB-01 2→5위, A/B 무해). 결함 처분 재판 4건 전부 불충분 판정 — ③의 진짜 위험
  (v3판: 무시 마스크 프레임 발화로 선택) P-4 주입. 과녁 분할 맹점 자인: 지지 27행을
  아무도 안 봄 — 빠진 5선급 4개가 전부 거기서 나옴. 트윈 tol 관성 확정 비판은 수용
  (판정 아닌 관성이었음을 D61에 소급 주석으로 남김이 옳으나 D61은 불변 — 본 행이 그 주석).

- **D65 (일 밤) EVL-12 칸축 재작도 — 단위가 우열을 뒤집는다**: `redteam/EVL12_CELL_AXIS.md`.
  ① FA-정합 "Depth≥RGB"는 칸축에서 **강화**(Δ +0.32~+0.43, 3/3 시드 전 지점) — 축 선택이
  결론 부호를 바꾸지 않음(양축 병기 규약 §4.9-2의 실증). ② **최대 인용 결과**: τ=0.5에서
  프레임축 RGB .688>Depth .438 ↔ **칸축 Depth .537>RGB .344** — any-hit(EVL-08)+any-fire
  (EVL-12) 중첩이 "정답 1칸+여분 살포"에 상을 주고 RGB가 정확히 그 행동. ③ 레드팀
  자기정정: 피해 방향 추정 오류(any-fire는 Depth에 불리 — 칸/FA프레임 4.29로 경보 뭉침),
  실질(단위 미유도·하중)은 유지; 은폐 실재(프레임 FA .359에서 Depth 칸 FPR이 RGB 2.34배).
  ④ 교정 GT 영향: 본 표 불변(H/E/off 바이트 동일 — 우연 아닌 구조), V행만 이동(RGB
  −0.044>Depth −0.012, 격차 확대 방향) — V 인용 표는 교정 GT 재발행 필요. ⑤ MAP-C(칸축
  운용점 4종) 공표량만으로 결정적 등록. 논문 함의: "RGB .688" 서술에 양축 각주 의무
  (백업본 INDEX 누락위험 메모행).

- **D66 (일 심야) P-5 완료 — 렌더 계획 v3 + 스모크 통과 + B팔 레버 교체**: `RENDER_PLAN_V3.md`
  (949행)+`render_plan_v3.json`+`build_render_plan.py`(수치 단일 원천, 26/26 교차검산).
  총괄: 씬 41행(기존 22+신규 19), 훈련 5,064프레임(A1176/B1152/C1368/D1368)+test-ext
  1,728(9씬 4팔)+진단 96, 신규 6,192컷, GPU 10.0h(p90 13.2h), 디스크 52.9GB. ① **per-key
  r이 설계를 바꿈**: D58 Q1 문자 그대로(B/D 신규 씬 전용)면 팔×출신 r=1.000 완전 교락 +
  material_break 단독 B팔이면 5/6 키 r≈0.56 → **B팔 레버 = 최대 제거(VG-CLS 통과 키 전부,
  material_break는 보장 바닥)** 채택, 전 키 ≤0.052, +152컷. ② **세그 스모크 PASS**:
  instance_id_segmentation은 Semantics 프림 불요(46씬 전부 미저작 — semantic 계열이면
  전부 배경 반환됐을 것), 프림 경로 직접 반환·48KB/컷·추가 렌더 비용 0, §12-5 k와 모서리
  소속 게이트 구현 가능. run_data_render.py에 NEGOBS_SEG_SIDECAR=1 옵트인 패치(기본 경로
  불변 검증). ③ 주입 4건 흡수: sceneH6/H7 신설로 val strict-H 기대 58.8(요구 30, 60%
  수율에도 37.4) · gt_void 채널(평가 분모 v2 동일 유지) · VG-datum 3단 보고+188컷 예비 ·
  LG/CG/VG 대조표 §6.5. ④ 구off 재활용 실익 96컷뿐(HZ 결속 s02·s03만 — HZpart는 "C와 D
  사이"로 D 불가, §4.1 비균질의 정체 규명) — VG-04 통과 시 선택 경로. ⑤ CUE_COVERAGE의
  컷당 시간 25% 과소·씬당 시간 2.5배 과대(상쇄로 총계는 근사) 정정. **채택(무조건 필요분
  즉시 착수)**: W0 분류 프로브(312컷) · 신규 씬 A안 착수(전 시나리오 공통 필요) ·
  sceneH6/H7 우선(P-4 전제). **결재 대기 4건은 채팅 상신**: 씬 범위 A/B/C안 · 정본 씬
  keep_dressing 15씬 수정 방식 · GPU 상한 20h · aux on/off(N-5).

- **D67 (일 심야) CPU-6 완료 — 논문 골격 5종 + 원장 충돌 8건 색출**: `experiments/v3_0823/
  paper/` (서론 7비트+Fig1 사양 / 버전 여정+철회-생존 문장 15행 대조 / 근거리 통일 문단 /
  A/B 표 틀 6종 전칸 공란+판정 규칙 / 색인+이원화 방지 5규칙). 충돌 8건 중 실무 직결분:
  ① **Matthies&Rankin "열화상 우회" 인용문은 리포에 축자 존재하지 않음**(5단어 단편+
  의역뿐, 축자 인용은 2007 SPIE 것) — 인용부호 인쇄 전 원문 1회 대조 의무(백업 INDEX
  재발행 목록행) ② 칸/FA프레임 추정자 이원(시드-풀 4.29 vs 시드-평균 5.50) — 정본 룰링
  대기(권고: 풀 방식, 센서스 사건 단위와 정합) ③ σ 표기 마찰: 구 표는 range/2 인쇄,
  판정은 ddof=1(§4.9-1) — 캡션에 방식 명기 의무 ④ P4_SELECTION이 집필 중 착지: 재현
  검증이 모델 평균 PASS / rgb_s42 단독 2.63σ FAIL — §2-7이 판정 단위(모델/런)를 미정의,
  P-4 검수 시 판정. 기록만 하고 수치 비흡수(승인 전 문서 원칙 준수 확인).

- **D68 (일 심야) P-4 완료 — 선택식 수리·재현 검증, b2 재현 불능 발견**: `P4_SELECTION.md`.
  ① 에폭 체크포인트 부재(best/last만)나 per-epoch val metrics.csv(430에폭, val_fpr 포함)+
  선택 시점 val 확률 덤프 존재 → 재선택 CPU, GPU는 신선택 에폭 실체화만(14분). ② 신식:
  S=(1−β)·F1+β·(h+1)/(n_H+2)−1.0·FPR, **β=n_H/(n_H+30)** — n_H=30에서 구 0.5/0.5 복원
  (가중 유도 완결, n₀=30은 차터 자체 목표치), Laplace 평활 H항, VG-const 게이트(붕괴
  1.92e-4↔정상 0.348의 로그 중점 1e-2), 무시 마스크 3층 규약(손실=마스크/선택=마스크/
  평가=비마스크) 참조 구현 자가시험 6/6. **"전부 발화"가 구식 283/430 에폭 승 → 신식
  0/430**. ③ 재현: depth 3시드 델타 정확히 0 PASS · rgb 모델 평균 PASS(max 0.91σ)/
  rgb_s42 단독 ep9→10 H −0.427=2.63σ FAIL — §2-7 판정 단위 미정의로 양측 인쇄, 결재
  상신. 기전 진단: 신선택이 FA-정합 4지점 전부 우위(+0.073~+0.125) = 구선택의 문턱 착시
  교정. **Claude 예비 판정: 모델 수준이 유효 단위**(σ 잣대 자체가 시드=모델 산포) + 단독
  델타는 투명성 인쇄 — 최종 승용. ④ **b2 재현 불능**(SegFormer 비결정성, 에폭 1부터
  발산; rgb는 24/24 비트 동일 재현 대비) — b2 선택은 재검증 불가 한계로 등재, σ 기반
  주장에서 b2 제외 권고. ⑤ P-5와의 게이트 ID 충돌 자체 회피(VG-const/VG-denom 개명).
  결재 7건은 P4_SELECTION 결재란 + 마일스톤 보고서로 통합.

- **D69 (일 심야) CPU-7 완료 — 백업 패키지 + 누락위험 5·인용 사고 후보 12**: `submission_0830/`
  28파일(INDEX 81자산행: 제출가능 68/재발행 9/로컬 전용 4, 규격 표 15, 조각 12 이원화
  플래그). 누락위험 상위: ① **v2 정성 패널 119장 로컬 전용**(`**/runs/**/*.png` ignore —
  헤드라인 정성 예시를 공개 리포에서 조달 불가) → 선별 컷 추적 경로 복사 트랙 즉시 편성
  (Q4 리소스 추출 권한) ② aux OFF 사실(F09 조각) ③ 단위 반전 양축 의무(F08) ④ hfov
  69°/62.2° 차단급+실측 0장 ⑤ 분모 정합 2쌍(두 파일 동시 수정 필요, 372→369 포함).
  인용 사고 후보 12건 INDEX §5 수록 — 대표: b2 "46.7%"는 원장에 없는 수치(상대 하락폭,
  인쇄치 0.305) · "R² 법칙" 표기 금지(적합도 통계 부재, R^1.98 지수만) · 4.29/5.50
  라벨 의무. 판단 지점 3건(A-3 부록 재채점 / C-6 패널 공개 경로 / gazebo 45MB blob)은
  마일스톤 결재란에 병합. 채택: C-6은 선별 복사로 즉시 해소(패널 트랙), A-3은 GPU 유휴
  시 2분짜리라 재채점 실행 예정(태그 제거가 각주보다 싸다).

- **D70 (일 심야) 마일스톤 보고서 — 선행 국면 완주 선언**: `MILESTONE_P1P5.md`(360행).
  통합 결재란 **22건**(채팅 대기 4 + P-4 7 + 가족 분류 + 추정자 + 원장 스윕 6 + CPU-7 3).
  조립 중 원장 모순 7건 §7 보고 — C1~C3(RENDER_PLAN 문면 vs json)은 json 단일 원천
  원칙으로 정정 배너 봉합, C4는 append-only 의도 형태(잔존 파생 2건은 대장 통합 갱신
  시), C5(토글 무 키 6 vs 5)는 결재 19로 상신, C6 반올림, C7 기지(D63). 다음 국면 =
  씬 빌드/렌더(W0·H6/H7 진행 중) + PREREG_V3 작성(§4.9 채택 규약 반영) + 대장 통합
  갱신(RT 31행+N-5~8+등급 반전) — 전부 재개 패키지 §6에 순서 명기.

- **D71 (일 심야) 패널 트랙 완료 — 누락위험 1 해소 + §6-1 의무 개시**: Part A `submission_0830/
  panels/` 24장 6.29MB(복사·축소 전용, 합성 0 — H hit/miss 18·aux 1·off FA 3·Gazebo 2,
  수치 열은 교정 GT 원장 재독; **의도적 짝**: s14 L5 0006 프레임을 rgb-s42/44·b2-s42가
  동시 0.00 miss — "FA는 안 겹치는데 실패는 겹친다" 반례 확보). Part B `experiments/
  v3_0823/panels/` 4장(FA 가족 예시 — 발화 칸 지면 쐐기 되투영 9/9 · 칸축 반전 · 재채점
  델타 · 융합 여유; 숫자는 JSON 계산 후 md 3자리 대조 게이트, 어긋나면 AssertionError
  정지). 기록된 구멍: CUE-OFF 팔별 스트립(원자료 렌더는 로컬 존재 — CPU 합성 가능 후속),
  YOLO test-core hit/miss(생성된 적 없음), aux 마스크/v3-B/2×2(자료 부재). §6-1 필수
  7종 중 1완료/2착수가능/3자료부재/1기존자산. INDEX §4·§6-C6 포인터 종결 처리(본 행).
  재현 드라이버 2종 code/ 등재.

- **D72 (월 새벽) W0 완료 — 분류 39쌍 + 참조 비대칭 발견, D팔 선렌더 채택**: 렌더 232컷/
  GPU 0.64h(컷당 9.93s — 4컷 프로세스의 부트 비상각, 모델 예측 11.1s보다 빠름), 씬 로드
  실패 0, 토글 발화 39/39 실증. 판정: 장식 17/구조물 6/**판정불가 16** → 토글 금지 23쌍.
  ① **하중 발견**: z_off 참조(260819_main_off)가 단서-비대칭(HZ 결속만 제거·free 결속
  보존) → VG-01 통계가 "단서가 낙차를 바꿈"과 "참조의 편측 단서"를 혼동(지문: Δfootprint
  =Δheightmap 정확 일치, s01 +9,021=9,021). 보조 검정(기하 교차)은 반대 맹점(낙차 위
  돌출 단서). 16쌍 불일치 → dressing 장식 확정이 3/19뿐 → **V키 r=0.2366>0.2(VG-09
  위반)**; 분쟁 16쌍 장식 해소 시 0.1294. ② **처방 채택(W0-1)**: W1 렌더 순서 D팔 선행
  (VG-CLS 무의존, keep_dressing 결재 무관) → D를 z_off로 6초 CPU 재판정 → B/C 착수.
  ③ 부수 채택: scene06 B팔 보류 해제(3키 전부 장식 실측) · 프림 생성형 material_break
  4씬 전부 장식(T 레버 안전 범위 확장). ④ VG-datum 사전검사: 쌍 생존 38/39(97.4%) —
  **scene02 dressing Δground_z 2.840m**(카메라 데이텀 스트립 정중앙에 드레싱 프림,
  v2 183쌍 사고와 동기전·구C2의 25배) = 씬 국소 결함, dressing 제거 팔에서 VG-datum
  자동 격리 + 수리 제안은 결재 큐(정본 씬 수정). 17/19 dressing 쌍은 데이텀-정확.
  ⑤ 공짜 게이트: W0 A팔이 코퍼스 A팔과 sha256 19/19 동일 — 판정이 W1에 직이전.

- **D73 (월 새벽) sceneH6/H7 빌드 완료 — val H 공급 83.4(요구 30×2.78), 기하 수정 0회**:
  `scenes/main/sceneH6_berm_levee2.py`(둔덕형·제방수변, 낙차 2.80m, 종단면 스침각 부등식)
  ·`sceneH7_bend_walk2.py`(복도 굴절형·보도, L형 옹벽, 평면 t_y≤t_x). 실측 strict-H
  수율 7/8=0.875(계획 가정 0.60의 1.46배) → 외삽 H6 46.9+H7 30.5+s20 → **val ≈83.4**,
  수율 40%로 붕괴해도 ≥30 유지. 설계 교정 1건: 은닉을 묶는 건 내부 조건이 아니라 **림
  조건**(화강암 코핑 엣지의 깊이 불연속이 계단 노징보다 큼 — s14 인용 예측이 틀림, 씬
  CPU 모델을 림 기준으로 교체·양 모델 병기). 게이트: VG-datum 정확 32/32 · VG-void
  1.0 전 팔 · **VG-06 모서리 소속: strict-H 전 프레임에서 위험 구조물 기여 0px**(신선한
  per-cut ID 마스크 실측) · 표준 장비 16키 사문 0 · CUE_CLASS 선등재. 잔여 사용자 게이트:
  **룩체크 R7**(대표 렌더 채팅 발송). ① **ID 마스크 사이드카 결함 발견**: idseg_fetch
  ="t0" 고속 경로가 다중 컷 라운드에서 stale(8컷 전부 바이트 동일; P-5 스모크는 1컷이라
  못 봄) — --seg-strict 강제 스텝으로 8/8 상이 검증, 비용은 노이즈 이내("추가 비용 0"
  결론 생존, t0 단만 오류). 검출기: variation.json의 idseg_n_ids 상수성. W1-D에는
  저영향(D팔은 단서·위험 무라 세그 비하중) — 주입으로 처리, 이후 웨이브는 strict 경로
  의무. ② 정본 문서 1건 가산 수정: placement_rules_v1.yaml +6행(볼라드 캐리어 레지스트리
  등재 — 미등재 시 린터 하드에러; 35씬 린트로 가산성 검증, 기존 ERROR 16은 전부 구 씬).

- **D74 (월 새벽) W1-D 완료 — 전 음성 PASS·재판정 종결·VG-09 통과, B 웨이브 GO**: 912컷/
  실패 0/GPU 0.571h(컷 2.26s — D팔은 프림 대폭 감소로 2.6배 저렴), 디스크 7.5GB. ① 전
  음성: 38/38 씬×밴드, 잔존 셀 0/1,658,351 — 검정 교체 1회(구off 참조 검정이 s04 오탐
  → D 자체 높이맵 링-중앙값 검정으로 대체, D는 무단서라 건전). 구off 스캔의 s N4 12프레임
  양성은 W0 예측 Δcue 지문 그대로. ② 데이텀 격리 3: s02(기지)+**s10 0.25m(신규)+
  sN1 12.8m(신규 — 구C2의 112배)**, 전부 데이텀 스트립 위 프림 1캠 — 씬 수리 결재 큐
  병합. 쌍 생존 92.1%(v2 77% 대비). ③ 재판정: 분쟁 16→10(8 장식·1 구조물 해소), 금지
  23→**15**, **V키 r 0.2366→0.1824 ≤0.2 — VG-09 전 6키 통과**. 잔여 10은 참조 교체로
  불가(fp_A 자기 소거 맹점) — 세그 3차 검정 필요, A/C 웨이브 strict 세그에서 무료 해소
  예정, 그때까지 금지 유지(보수). ④ 결함 3 수리: 프림 하한 가드 재보정(28프림 s02 오격리)
  · 세그 staleness 확정+strict 경로 NEGOBS_SEG_STRICT=1 이식(152 stale 컷은 D팔 비하중
  문서화, A/B/C 의무) · D팔에도 융합 필요 발견(s08 발자국 0 붕괴 방지, 7 사이드카,
  기록된 불일치 3-4자리 재현) + W0 검정2 계기 불일치 8쌍 격리 원장. ⑤ **차단 결정
  판정(Claude)**: s03/s04/s10 무레버 → B≡A 바이트 동일 168컷은 **스킵**(중복 프레임은
  학습 이중 가중 해악, r 불변 — removable() 기모델링) — 3씬은 부분 2×2(A/C/D), ACCOUNTING
  팔 행렬 각주 예정. 계획 이탈 2 문서화(밴드별 라운드 트리 — D23 교훈 · s02/s03 신규
  렌더 — 재판정 자기 무효 방지). 누적 GPU ≈1.3/20h.

- **D75 (월 아침) W1-B 완료 — 중앙 게이트 통과·T키 위반 발견·수리 확정**: 648/648컷(스킵
  168 명시), GPU 0.659h, 데이텀 격리 0(기지 결함 씬은 구조적 부재). ① **A/B GT 동일성:
  polar_gt 비트 동일 648/648** — 장식 분류의 경험적 백스톱 성립. T3 격리 4(s12/s16,
  융합 계기 씬 scene-level cells_raw만 상이). 정확성 수리: 게이트 z_off를 구off→**D팔**
  로 교체(구off 참조는 유령 발자국 4씬 제조 — W1D §6.3 수치 정확 재현으로 검증) — D-선행
  순서의 추가 배당. ② 세그: 648컷 전부 orch 경로·stale 서명 0. **VG-03 술어 교정**:
  idseg 해시 비교는 오설계(ID가 평가마다 재부여·동일 포즈는 마스크 공유가 정상) →
  프림-경로 정규화 + masks≥poses로 확정. 실결함 1컷(s20 E2 L7 cut0, 직전 마스크 승계 —
  결정론적, strict 생존) 격리. ③ **T키 VG-09 위반(gated r 0.3100)**: hazgate.py가 if문만
  파싱해 삼항 읽기(mtl = A if cfg[k] else B) 20건을 "미배선"으로 오기록 → W0 범위→재판정
  →레버표→레시피로 승계된 계보 결함. CUE_COVERAGE §0-3은 처음부터 옳았음(32/33 토글).
  수리 = 12씬 B팔에 material 재바인딩 레버 추가 재렌더(~0.6 GPU-h, VG-01 안전 — 재질
  전용) → 수리 후 T 예측 0.0442. **채택: 보충 웨이브 W1-B2 즉시.** D74 ⑤ "r 불변" 주장
  정정(판정은 유지, r은 Δ+0.026 이동). ④ C 선행요건 3: C-1 keep_dressing 0/15(결재 대기)
  · **C-2 A팔 idseg 0/816** — VG-06/k 차단 → 세그 백필(W0의 sha256 재현성 19/19 활용:
  재렌더→바이트 동일 검증→idseg만 채택·PNG 폐기) 채택 · C-3 (A,C) GT 계기 룰링(융합
  시점 의존, s12/16 교훈 — PREREG 전 확정 필요). 부수: B 러너 --no-resume 수리(실패
  유닛 무렌더 재개 버그).

- **D76 (월 오전) test-ext 1차 빌드 — sceneH1/H2 전 게이트 통과, VG 정의 결함 4건 수리**:
  `scenes/main/sceneH1_berm_levee.py`(둔덕형·제방, 낙차 2.57m, strict-H 14/16=0.875,
  paired-H 14)·`sceneH2_landing_campus.py`(계단참형 — v3 3계열 첫 구현, 캠퍼스, 1.44m
  다단(참→4단→참→4단→선큰), strict-H 12/16, paired-H 12). non-H 4프레임 전부 설계
  예측대로의 V(모형 오류 0), 근거리 대조 V 4/4(퇴화 아님). 게이트: VG-01 0/91k·0/72k ·
  VG-02 0·0 · 데이텀 정확 48/48 · **VG-06 26개 H 프레임 전부 가림체>0px·낙차 구조물
  0px** · VG-07 (A,C) 광학차 정보량-0 쌍 0 — **H2 중앙값이 H1의 3.2배 = 계열별 층화
  필요의 실측 근거**(계기판① 설계 입력). 개정: H1 3건(look 2 + H 프레임 단서 0px →
  관리 램프·줄눈·점형블록 추가, heightmap 바이트 불변 검증) · H2 기하 1회(기단 상면이
  채움면보다 낮아 반사실이 뒤집히는 결함 — 계단참형 고유 함정 기록). **VG 게이트 정의
  결함 4건**: §6.1이 계수 영역 미명시(VG-01/02=발자국 안, VG-06=격자 안, VG-08=포즈
  단위) — 정정 전 넷 다 정상 씬을 실패로 오독, h12_gates.py에 수정 판정 구현·PREREG에
  정의 반영 필수. placement_rules +4행 가산(전수 린트 불변). 226컷/0.37 GPU-h. 판정:
  W3 본렌더 투입 가능, R7 룩체크 선행(채팅 발송). 사용자 대기 소항목: H2 판석 휘도·
  H1 줄눈 폭 60mm·실측 H 촬영분(§6.6 현실 대조 패널).

- **D77 (월 오전) PREREG_V3 초안 완성**: `PREREG_V3_DRAFT.md`(734행, sha256 b0c4c2b5…).
  구조: 가설·인과 진술 템플릿(창발/미달 양방) / 계기판 3종 조작 정의(무대 test-ext 4팔
  전용, 운용점 = τ_op 참고 + 프레임축 4점 + 칸축 MAP-C 4점, T-scaling 병기, ① 3계열
  층화+영정보 격리, ③ FA_C−FA_D 분해+void 격리) / 판정 = 방향+σ(ddof=1) with 창발
  3/3·부분·미달 정의, 예비 레버 1개 / 구조 게이트 절대치(VG-const 1e-2 유도, VG-1ep
  신설 제안, as-built 계수 영역 전문) / 금지 17항 / **PENDING 24건**(MILESTONE 연결
  15 + 비연결 9, 본문 사용 지점 인라인 표식 — 미해소분은 판정에서 제외 조항) / 동결
  절차(cp→sha256→ACCOUNTING). **C-3 제안**: C팔 GT = 사양 상수(전 칸 음성) 고정,
  C 높이맵은 A 계기 상속·독립 재융합(공유 금지), 역할은 (A,C) 발자국 z_off 참조 한정 —
  융합 시점 의존은 라벨이 아니라 VG-01-AC 게이트에만 나타나고 불일치 (밴드,씬)은 계기판①
  모집단 제외+개수 인쇄. z_off 권위 쌍별 명문화((A,B)→D, (A,C)→C). 주의점 2 해소:
  τ=0.5 이중 규정은 대상 상이(계기판/P-4)로 교차 인용 금지 등록 · w1b_fuse_audit rule
  문자열과 실행값 불일치는 실행값(독립 재융합)을 정본으로. 동결은 결재 해소 후.

- **D78 (월 낮) test-ext 2차 빌드 — H3·L1 완성(A안 6/8), 계기 결함 3건 추가 수리**:
  `sceneH3_bend_walk.py`(복도 굴절형, strict-H 0.750·paired-H 12 ✅, 조도 개정 2회
  73.1→33.3% — GT 중립 증명: tier/polar_gt/발자국 16/16 불변)·`sceneL1_lateral_canal.py`
  (측방 수로, V 16/16, **섹터 분포 실측 A+E 0.667/C 0.033·질량 전부 3a/3b** — V2S 재론
  측정 기반 개시). ① **VG-01 판정층 = polar_gt로 승격**(4단 신설 hm_gt_equal): 계단 위
  공중 난간이 높이맵 611셀을 흔들어도 polar_gt 비트 동일 — cue_railing 레버 유효 유지,
  **동일 잠복 사례가 sceneH7 B팔(미렌더)에 존재** 예고. ② VG-02 부호화(z_D−z_C≥depth —
  C가 D 위인 126셀은 보존 드레싱이지 구멍 아님). ③ **라벨러 D10 스텝 게이트의 측방 사각**:
  _outward() 8방위 양자화가 폐형 임계 d≤−x₀+√3·y_lip=8.13m 밖 축평행 립을 미탐 →
  cells_kept 0 = 거짓 음성 GT(실측 d 8.50/8.69 재현). 씬측 우회(횡단 암거 2 —
  none_in_fov 0.25→0.000) 적용, **근본 수리(성분-경계 스캔)는 정본 라벨러 수정이라 결재
  상신**(sceneL2에도 적용됨; v2 GT 소급 불변 조건 필수). H1/H2 게이트 교정 후 재주행
  판정 불변(회귀 안전). GPU 0.45h. R7 잔여 레버 2건(H3 암부 tint·L1 휘도)은 룩체크
  회신에 병합.

- **D79 (월 오후) W1-B2+세그 백필 완료 — 전 6키 r 통과, PNG 동일성 전제 정정**: ① B2 576컷/
  0.62 GPU-h: hazgate --mode {ifonly,full}(구 동작 39/39 바이트 재현·수리는 별도 파일),
  맹점 23쌍 독립 재유도, 재질 재바인딩 13사이트 기계 검증. **최종 r: R .1955/Ta .1253/
  N .0364/T .0692/Sg .0106/V .1866 — 전 키 ≤0.2**(T as-built .0442, W1B 예측과 자릿수
  일치). VG-01 polar_gt 576/576 비트 동일, 격리는 W1-B와 동일 4개(s12×3·s16 — T 무관,
  융합 기지 이슈). ② 세그 백필 816컷/1.39 GPU-h, 0 실패: **"PNG 바이트 동일" 기준은
  불만족 가능**(무변경 대조군도 PNG 0/76 — PathTracing RGB 비트 비재현; W0의 19/19는
  높이맵 sha였음, D74·D75 전제 정정) → **기하 대응 기준**(높이맵 sha ∧ n_prims ∧ 포즈
  Δ=0 ∧ 컷별 depth sha) 채택·상신(B2-1), 마스크는 기하+카메라 종속이라 프림-경로 내용
  15/15 동일 검증. 설치 29/34 유닛 696 사이드카(가역, 파일별 원장), 정본 픽셀 무접촉.
  seg 불가 5유닛(s08 depth 씬 고유 불일치 2 + stale 3). ③ **stale 결함은 조건-경계
  결정론이 아님**(신규 5건 전부 조건 내부·위치가 라운드 간 이동, 0.15-0.37%) → 렌더러
  seg 경로 내 컷별 검증+재페치 수리가 **C 웨이브 전 필수**로 승격(옵트인 분기 내 수정,
  기본 경로 불변). ④ C 상태: C-2 실질 해제(696/816), C-1 결재 대기, C-3에 s17 base
  계기 불일치 신규 데이터점. 누적 GPU ≈5.5/20h.

- **D80 (월 저녁) A-3 완료 — 부록 인코더 6런 교정 GT 재채점, 구 GT 태그 해제**: 가중치
  전부 디스크 존재(재훈련 0), GPU 2분55초, 씬 빌더와 flock 충돌 0. 게이트 4/4(확률
  비트 동일 6/6·H행 완전 불변·off FA Δ=0·구 분모 대조 소수 4자리 동일 — 정본 9런과
  같은 "분모 성장 단일 기전"). σ 초과 2건 전부 기지 기전(convnext cell_precision
  1.39σ = depth와 동일 FP→TP 재분류 / resnet50 H_weak 퇴화 인용 금지). **FA-정합
  부록표 H열 전 지점 불변** → 부록 서술 무수정 생존. census 부록 계층 재생성 가능
  (가드 해제 한 곳 — 후속 큐). V2_RESCORE §7 append(§0-6 무수정), INDEX A-3 종결.
  잔여 구 GT 런: YOLO 3 + rgb_s42_aux 1(15런 밖) — 동일 에이전트 속행.

- **D81 (월 저녁) 최종 재채점 배치 — v2 계열 19런 교정 GT 정렬 완결**: YOLO 3(가중치·동결
  예측 존재, det2cell만 재실행 — GPU 불요 25s)+rgb_s42_aux(19s). 게이트 5/5, **H행 4/4
  불변**(YOLO 0.000 유지·aux .7188 유지). σ 초과 1: **YOLO frame_recall_V −0.0267=1.11σ —
  19런 중 V가 σ를 넘은 유일 사례**(σ가 rgb의 1/9로 작아서; 신규 39 V프레임 0/39 적중,
  cell_precision Δ 정확히 0 = 발화 칸과 신규 양성 무교집합). 어댑터-프리 F3 재계층화:
  **H행 전 열 완전 불변**(.066 문장 수정 불필요 — 어댑터 천장 주장 양 경로 생존), V 하락
  ·none 분모 축소 상승은 분모 병기로 처리. **부수 소득**: compare_aux_vs_base 교정판에서
  공표 cell_f1 CI [+0.0001,+0.0786] "0 제외"가 [−0.0057,+0.0708] "미제외"로 반전 —
  D41 철회의 프레임-i.i.d. 독립 재확인(경계선 하한의 정체 노출). V2_RESCORE §8 append
  (965행). **test-core 위 v2 19런(정본9+부록6+YOLO3+aux1) 전부 교정 GT 기준 — 구 GT
  태그 잔존 0.**

- **D82 (월 밤) 승용 결재 회신 — 전 항목 처분 확정 + 룩체크 R7 지적 1건**: ① A안 채택+B안
  병행 설계. ② **additive 옵트인 키 채택**(기본값 무변경 해시게이트 증명 조건) + 데이텀
  3씬(s02·s10·sN1) 수리 + 라벨러 측방 사각 근본 수리(성분-경계 스캔, **v2 GT 소급 불변
  검증 필수**) 동일 묶음 승인 — **C 웨이브 게이트 해제**. ③ v3-A aux OFF + 격리 aux 런
  1개. ④ 신 선택식 채택, 재현 판정 단위=모델 수준, b2 비결정성=한계 각주. ⑤ **gazebo
  frames 리포 제외** — sha256+재생성 스크립트만. 나머지 17건 규약성=권고안 채택(C-3
  AC-INSTR-1, FA 5가족+경계칸 접기, 추정자 시드-풀, σ ddof=1 등 확정). **룩체크**:
  "점자블록 두 줄" 지적 → ⓐ 단서 설치 = 실제 설치 기준 준거 원칙(점형=경고/선형=유도
  역할 구분, 위험 경계 전 점형 1밴드, 근거 조항 씬 사양 명기; sceneH1 마루 2밴드 재배치;
  규정-정합 자연 요소 유지) ⓑ H 정보량 부족은 시점·기하·규정-정합 요소로 해결 기본
  (증량은 타당성 근거+ⓒ 감사 통과 조건) ⓒ **cue-extent 감사 신설**(씬별 단서 픽셀
  면적·개수 vs 위험 상관 = 존재 |r|의 면적판, N-cue 밀도 대칭 포함) ⓓ 신규 씬 전수
  설치-규정 감사→수정 재렌더 후 본렌더. 잔여 10장 통과. 부가 지침: 규정 주준거하되
  규정 미적용 시점의 현실 환경 상실 금지, 타당성 기반 자율 시도 허용.

- **D83 (월 새벽) cue-extent 감사(D82 ⓒ) 계기 완성 — "면적 상관의 64%는 C팔 부재"**:
  `CUE_EXTENT_AUDIT.md`+드라이버(재생 훅 포함, CPU 80초). ① as-built 면적 r_pb 0.4135
  — 단 C팔 0 상태의 수치이며 κ 투영으로 C 완비 시 **0.1495**, 2×2 완비 신규 씬 실측
  **r=−0.112 통과**(귀무 q99 0.1009) → C 웨이브가 곧 처방(RB-1). 최악 키 V dressing
  0.366; **T는 면적 계기의 원리적 사각**(재질 재바인딩 — 존재 |r| 0.0692만이 근거,
  "면적-불가시 키" 표기). ② 밀도 대칭: 정본 33씬 A↔N-cue **대칭**(비 0.966) · 씬별
  A↔C 소수 둘째 자리 일치(최강 증거) · **신규 H씬은 역방향 비대칭**(10.4% vs 28% —
  "저밀도=위험" 역지름길 감시 필요, 규정 감사와 상호작용: H1은 줄고 H2는 늘어 부호
  반대 실측). ③ **부산물: scene02 출구 표지 전 팔 불소등**(B2 Sg r 0.0106의 전제 붕괴
  → VG-09 재검토) + 상수 단서 프림 90건 원장(절반 오귀속·절반 진짜 제거불가 — B팔은
  완전한 "무단서 팔"이 아님, 서술 규율에 반영). ④ 문턱 제안 VG-cuex |r_pb|≤0.1009
  (씬내 셔플 q99)+Cliff δ≥0.147 — PREREG PENDING으로 편입(적용은 동결 시). ⑤ 처분:
  RB-1 채택(진행 중) · scene02 표지 가드 배선 수리+해당 씬 B 재렌더는 **C 착지 후
  재균형 배치로 묶음** · RB-2(무레버 3씬 B 렌더)는 **보류** — 무레버면 B팔도 단서
  만재라 면적-r 개선에 무효, 배선 수리 후 재평가 · RB-4(C1·C4 keep_dressing)는 C
  웨이브 15씬 목록과 대조 확인 · H6/H7 (A,C) 대칭 미측정분은 규정 감사 재프로브
  (strict)가 자동 공급. N9/N11 4팔 라운드+라벨 존재 확인(빌더 보고 대기).

- **D84 (월 새벽) N9·N11 완성 — A안 8/8, 규정 주입 실시간 흡수**: `sceneN9_busstop_tactile.py`
  (FA_REALITY L6 ★3, 승강장 경고블록)·`sceneN11_ground_pattern.py`(L10 ★4 지면 문양
  삼중주+얕은 함몰). ① 팔 매핑: 함정 낙차(연석 0.200/식재대 0.150m)가 전부 임계 미만
  → **네 팔 전부 무낙차 사실 판정**, 계기판③ 대조축 = (C,D). ② 게이트 전항: all-negative
  4팔×16프레임 cells_raw 0(D78 대응 — 카메라 무관량으로 판정), 단서 픽셀 A/C p50 60만
  vs B/D 정확히 0, (C,D) 광학차 (A,C)의 30~40배·정보량0 쌍 0, VG 전항 통과. ③ **규정
  위반 2건 자체 적발·수리(R3)**: 점형 12.40m 승강장 제식→별표2 승차 위치 1밴드 1.80×
  0.60m(배면 0.30m 이격) · 점형 도트 2줄 유도로→선형블록 1줄(전용 재질) · N11 볼라드
  이격 0.30m. 별표2 제7호의 종방향 열 미적용/횡단 열 의무 구분 사례쌍 문서화. ④ 밀도
  실측: N-cue 면적 r **음수**(-0.63~-0.71, N-cue가 큼 — 초과분 75%가 정체성 드레싱,
  규정 시설물 2%). 원경 층수 현실화(R4)는 무효과 실측(보행 화각 밖) — **유효 레버 =
  측방 이격·수평 폭** 확정, 정체성 훼손 전에 중지·상신(§13-10-11). ⑤ h12_gates ncue
  모드 확장(1,044행), placement_rules 2행(전수 린트 39씬 불변). 706컷. 파일 충돌 1건
  보고됨(cue_extent_audit.py — 정본 1,840행 버전 생존 확인). **다음 게이트: 룩체크 4차
  (N9/N11) + 규정 감사 스코프에 N9/N11 검증 행 추가 → 통과 시 W3 본렌더.**

- **D85 (월 아침) 설치-규정 감사 완료 — 6씬 수정·수율 무이동, 인용 정정 7건**: `CUE_REGULATION_
  BASIS.md`(리포 출처 우선, [확인]/[웹] 행별 태그)+`REG_AUDIT.md`. ① H1 진짜 결함 =
  **이격 상이한 밴드 2개**(한 밴드 내 2줄이 표준 — 지침 6.5.2 세로폭 30-90cm·60 표준,
  6.6.7 방향별 60/30) — 코핑 밴드 삭제·마루 밴드 이격 0.30 정정, 점형 면적 8.72→1.38m².
  ② **공통 결함 = 0.40m 세로폭(시공 불가)** → 전 씬 0.60m. L1은 이격 0+잔디 위 시공까지
  3중 정정. N9/N11 검증 행: 재렌더 요구 위반 0(독립 유도 결과와 스펙 바이트 일치).
  ③ **의도적 미수정 7건**(볼라드 전면 점형 부재 등) — 한시련 실측(점자 적정 4.0%·볼라드
  부적정 96.0%)+리포 자체 규칙 근거: 채우면 규정 준수가 아니라 단서 증량(D82 ⓑⓒ 위반).
  ④ 인용 정정 7: 난간 1.10m 근거 교체(건축법 §40 아님) + **하천 제방·옹벽·개거는 난간
  조항 자체가 없음 실증**(하천법·KDS 전수 0건) — 그 난간들은 관행 = "난간=낙차" 제도
  연결을 코퍼스가 institutional하게 끊는 논문 재료. ⑤ 재게이트 6/6 통과·수율 Δ0(H1
  밴드 삭제에도 cue_px 최소값 바이트 동일 = 과잉분 정보량 0 실증), H6/H7 per-cut 마스크
  최초 확보(구 probe_C stale 해소). ⑥ 진단 정정 2: H3 밝기 레버 미약(무조명 면 —
  재질로 불가, 조명/시점 결정 필요) · L1 하이라이트 지표 오설계(포화 황색을 백색으로
  오계상 — min-channel 기준으로 L1 무위반, 진짜 순백 대면적은 H2 판석 92.7%+**H7 Walk
  93%(유일한 무틴트 재질 M["paving"] — 재질 3단 규정 이탈)**). ⑦ 계기 결함 2(h67 러너
  SEG_STRICT 부재 + 기존 출력 디렉토리가 seg 재생성 차단 — 렌더러측, w1c 소유로 이관).
  GPU 1.39h. **채택(Claude)**: 백색 지표 = min-channel 공식 교체 · H7 paving 틴트 수리
  (규정 정합 — 표준 이탈의 교정) · H2 판석 톤 완화 · 0.60m 기본값. **R7 잔여(승용)**:
  H3 조명/시점(예비 권고: 음지 골목의 현실성 수용) · 씬별 0.60m 개별 번복권.

- **D86 (월 오전) 규정 트랙 마감 배치 — 백색 지표 공식화·틴트 수리·조항 업그레이드**:
  ① h12_gates에 gate_white() 신설(공식 = min-channel>0.8 전면+하부 2/3, 구 지표는
  [deprecated] 1회 병기; REG_AUDIT §6.1과 4자리 재현 — h67 행은 A+C 측정면이었음 규명).
  L1 재판정: 무위반 확정(레버 미발화). ② sceneH7 paving_tint 신설(형제 씬 H3 값 정합)
  — 백색 p50 0.0703→**0.0024**(−96.6%), 귀속 검증 Walk −97.5%·타 프림 ±80px. sceneH2
  plaza 단일 계수 0.8919 스케일(색상비 4자리 보존) — 백색 총픽셀 −41.6%, 잔여 87.5%는
  광장 자체라 추가 축소는 리스타일 필요 → OPEN. ③ 재프로브(reg2 라운드, 21분): 수율
  H7 0.875·H2 0.750/paired-H 12 동일(같은 프레임까지), cue_px 바이트 동일, VG 전항.
  ④ N9/N11 문서 항목: 별표 인용 정정(점자→교통약자법 별표1, 볼라드 점형→보행안전법
  별표1 제10호 바 — 구 별표2 7호 이관 확인)·선형 30cm 이독 기록·펜스 근거 교체(90cm
  기준, as-built 적합). 밴드 깊이 통일(N9 0.60/N11 0.30)은 기하 변경이라 OPEN. **규정
  감사 트랙 완결 — W3 본렌더 게이트 개방**(R7 잔여: H3 조명 예비수용·N9/N11 4차 회신
  대기, 무응답=통과 관례).

- **D87 (월 낮) W3 완료 — test-ext 무대 완성 + 룰링 3건 예비 판정**: 1,152컷(6씬×4팔×48)/
  GPU 1.44h, 계획 9씬 대비 −576은 B안 잔여(H4·L2·N12 — 파일 미존재) 정직 명기. 게이트
  48/48: VG-01 발자국 불일치 0·데이텀 격리 0·VG-08 stale 0·void 1.0 전면·paired-H
  H1 33/H2 18/H3 21(전부 하한 초과), 프로브 수율 포즈 수준 정확 재현, L1 측방 0.672
  (n=48). N씬 전 팔 all-negative. ① **예비 룰링(H3 무정보)**: (A,C) 24/24 무정보는
  완전 은닉 설계의 성공 증거 — D58 영정보 층의 취지(무벌점)대로 **"완전 은닉 층" 분리**:
  계기판①에서 삭제가 아니라 별도 행(정의상 측정 불가 — 단서 동일+반사실 불가시 →
  올바른 모델은 A/C 동등 발화가 정답), 보고량은 Δp 분포+보정 정합. (C,D) 6.86으로
  건강 — 용량-반응은 정상 측정. ② **VG-ver 신설 채택**: 프로브 라운드 1건(h3l1reg/H3)이
  씬 편집 00:59:59를 팔 사이에 걸침 — (A,C) 목적 격리, 씬 파일 해시를 라운드 메타에
  스탬프하는 버전-일관성 게이트 채택. ③ VG-06 공허 통과(양성 0 밴드) → "n/a(양성 0)"
  표기 채택(D76 가족 5호). ④ 러너 검증기 자기 결함(컷 수 기준 seg 판정) 자체 수리 —
  정본 게이트가 옳았음. ACCOUNTING 후속 append: test-ext 모집단 행(P-17 경유 태그).
  v2 zero-shot 재채점 입력 준비 완료(§2-8) — H3 층 룰링 선행 조건은 본 ① 예비 판정으로
  충족, AC-INSTR-1 C3-2 소비자 규칙 명기.
