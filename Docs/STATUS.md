# STATUS — GT-89만 유지 · GT-90~93 전면 revert(판독 오류) · 유리 금지 룰링 (2026-08-10)

직전 판(GT-72~88)은 git 이력 참조. 이 판 = 08-10 사용자 정정 반영.

## 한 줄
**GT-89 LANDED 유지**(구형 23씬 선부팅 SMOKE 게이트 + 캡처 대기 160, 해시 비트동일 증명) ·
**GT-90~93 은 착지 후 전면 revert**(f40e13c — 4씬 해시 라운드 이전과 비트동일 복원 확인).
원인 = 오케스트레이터 판독 오류: s03 답변("What is that shape..")은 **차폐 38.9 % 의미 설명
요청(질문)**이었는데 기각으로 오인해 곡선 고가를 롤백했고, s06·s11·s16 구현도 의도 불일치.
**유리 룰링(08-10)**: GT-73 확산 기각 + **유리 소재 전면 사용 금지**(투명 재질은 NegObs 경계
hidden/visible 판정을 모호하게 함). 다음 작업은 사용자가 **다른 세션에서 재지시**.

## 읽는 순서 (신규 합류)
1. 이 파일 → 2. `briefs/process_spec_v1.md` → 3. `audit_v4/gt_changes_w3.md` 행 45~49
   (45=GT-89 LANDED · 46~49=**WITHDRAWN — 구판독으로 재착지 금지**)
→ 4. `reports/critique_intent_map_v1.md`(비판-의도 지도 — 수정 전 필독)

## 현재 상태
- 씬 상태 = **GT-72~88 착지 직후로 복원**(scene03 곡선 고가 + 선언 FAIL 38.9 % 포함 — 이 FAIL은
  사용자 룰링 귀속으로 유효한 선언 상태이지 결함이 아님).
- **검수 대기 2건(원복)**: `look_check/_review/260806_w3_s13fix7/`(scene13 — 유리 투명화 포함,
  단 유리 자체는 08-10 기각 룰링 — scene13 파일럿 처리 재지시 대기) ·
  `look_check/_review/260806_w3_fixqueue2/`(15씬 210컷).
  `_review/260807_w3_fixqueue3/` 는 **revert 된 상태의 렌더** — 참고용 보존, 검수 대상 아님.
- **결재**: ① 유리 — **기각 확정(유리 소재 금지)** ② 헤지 밀도 확산(GT-71) ③ C2 대안 3안
  ④ 건물 사실성 서베이 — ②③④ 대기 유지.
- **결정 대기(설계) 원복**: scene16 하부 유효고 1.75 m(원장 행 35) · scene16 x=0 종단 포스트 ·
  scene03 차폐 38.9 %(설명 전달됨 — 사용자 판단 대기).
- GT-89 잔여 질문: "old skins"가 비주얼 의미였다면 별도 웨이브(확인 대기).

## 다음 (차기 세션 — 사용자 재지시 기준)
1. s03·s06·s11·s16 재지시 수령 → 새 행으로 선신고(46~49 구판독 재사용 금지)
2. scene13 유리 파일럿 처리 방침(롤백/불투명 폴백) 재지시
3. 결재 ②③④ 응답 → 확산/채택 라운드
4. W4 GT 재판정 → W5 생산 렌더

## 규율
`process_spec_v1.md` §2 정본. floor 4종 / 같은 prim A/B / 선신고 / flock(-o) / gallery --out.
**GT-89 착지로 33씬 전부 SMOKE 안전** — 구형 씬 특례 폐지. 캡처 대기 `NEGOBS_CAPTURE_WAIT`(기본 160).
**신규(08-10)**: 유리·투명 소재 금지 · 짧은 검수 답변이 질문인지 지시인지 애매하면 **설명 먼저,
기하 변경은 명시 판정 후**.
