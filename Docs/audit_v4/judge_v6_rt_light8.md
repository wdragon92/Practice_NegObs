# v6 RT 판정 — 경수정 검증 8씬 (01 / 02 / 09 / 14 / 16 / 19 / 20 / 21)

판정 2026-07-27 · v6 RT 판정관(경수정 검증 담당) · **파일 수정 없음**(판정 전용)
입력 : `look_check/sceneNN/v6_rt/rt_noon_*.png` (RayTracing, noon, 1920×1080, 13~16컷/씬)
대조 : `look_check/sceneNN/v5_pt/` (직전 PT 파이널)
근거 : `Docs/audit_v4/user_feedback_v5_1.md` §1~9 · 픽스로그 `fixlog_VC.md` / `fixlog_VD.md` / `fixlog_N4.md`
회귀 감시 : `judge_v5_pt_01-05.md` · `judge_v5_pt_09-18.md` · `judge_v5_pt_16-21.md` (직전 전부 합격, 19만 "잔여")

## 판정 요약

| 씬 | 직전(v5_pt) | **v6 RT 판정** | 한 줄 |
|---|---|---|---|
| 01 campus_stairs | 합격 | **조건부** | 벤치 앵커링·나무 v2·사인 정리 전부 반영. 잔여 백색 볼라드 6본이 §2/§3/§4 동시 위반 |
| 02 underpass | 합격 | **합격** | D6 게시판 삭제 + 입구 사인 진입부 측면 지주식 이설 확인, 보행축 침범 0 |
| 09 ghat_riverfront | 합격 | **재수정** | 호수공원 재해석이 **어느 컷에서도 판독되지 않는다** — "의도 불명" 미해소 |
| 14 grandstair_illusion | 합격 | **조건부** | 스텝 파라펫 소멸·축선 정리·착시 유지 성공. 참 이음부 쐐기 슬릿 + 깃발 전경 침입 잔존 |
| 16 canopy_shadow | 합격 | **합격** | 인방 교체로 부유 청색 패널 소멸(문형 폐합), 볼라드 규정 배치 확인 |
| 19 fan_winder | 잔여 | **재수정** | 재조준 2컷(`roof_skyline` / `radial_nosing`) **둘 다 실패** — v5 잔여 그대로 |
| 20 diagonal_oblique | 합격 | **합격** | 사선 에지 볼라드 열 폐기 + 규정 배치, 은닉·에지 무결 |
| 21 monumental | 합격 | **합격** | 중앙축 비움이 그리드·미장센 전 컷에서 확인 — 위엄 지시 달성 |

**합격 4 / 조건부 2 / 재수정 2.**

### 전 씬 공통 확인 (v5.2 §7·§8)
- **점자블록(황색 점형·선형) — 8씬 전 컷 부재 확인.** 01 `h0.9_d5`·`edge_closeup`, 02 `pit_edge`·`h0.3_d10`,
  16 `h0.3_d10`(v5 잔존 "황색 점자띠 1줄"까지 소멸), 20 `along_diagonal`, 21 `oblique` 전수 육안 확인.
  21 의 황색은 `cue_nosing`(단코 띠)이며 `cue_tactile=False` 코드와 정합.
- **임의 경고 팻말(추락주의·계단주의) — 8씬 전 컷 부재 확인.** 잔존 사인은 02 `sign_exit`(출구 →)·
  지하도 입구 사인, 16 `sign_exit`, 21 `sign_info`, 09 `sign_info` 뿐 = 실존 관행물.
- **255 클리핑** 전 55컷 0.00~0.03 %(01 `amphi_view` 0.031 %, 02 `pit_edge` 0.015 % — 하늘/광원). 규칙 내.

---

## scene01_campus_stairs — **조건부**

검수 컷 : `beauty_overview` / `amphi_view` / `lower_lookback` / `edge_closeup` /
`preset_h0.9_d5` / `preset_h0.3_d10` (+ 400 % 크롭 2)

### 1) 피드백 반영 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **벤치가 허허벌판 → 수목·화단 앵커 옆 재배치** | **해소** | `beauty_overview` 우하단·우중단 벤치 2기가 화단 캡 외면에 밀착(이격 0.17~0.37 m), `h0.9_d5` 좌·우 벤치도 각각 화단 옆. yaw 지터로 축과 어긋난 것이 육안 확인된다. 광장 한복판 단독 벤치 **0기**. |
| **나무 v2(수형 결정적 변형)** | **해소** | `h0.9_d5` 좌우 가로수가 수관 폭·블롭 배열·줄기 기울기가 서로 다르고, 줄기가 2단 테이퍼(하부 굵고 상부 가늘게)로 읽힌다. **지지대(stakes) 0본** — v4-B3 회귀 없음. |
| **게시판 대칭쌍 → 비대칭 가장자리** | **해소** | `lower_lookback` 중앙 상단 1매(건물 캐노피 앞 북단)만 프레임에 들고, 구 (−14, ±2) 대칭쌍은 소멸. |
| 자전거 거치대 비등간격 | 해소 | `lower_lookback` 좌측 4기 간격이 서로 다르다(생울타리 옆). |

### 2) 은닉·회귀
- **grazing 은닉 유지 ✓** — `h0.9_d5` / `h0.3_d10` 4단 0.6 m 낙차 완전 은닉, 상·하부 광장이 연속면으로 이어진다.
  v5 에서 단서였던 황색 점자띠가 사라지고 **차콜 그래나이트 밴드**만 남아 은닉이 오히려 강해졌다.
- `amphi_view` 3티어 목재 스트립 좌석 판독 유지, `edge_closeup` 상단 에지 포장 연속 정상.

### 3) 결함 — **[중] 백색 볼라드 6본** (조건부 사유)
- 실물 : `PARAMS["bollards"] = x 13.2, y −6..6, step 2.4, r 0.06, h 0.75, M["rail"]`.
- `h0.3_d10` 400 % 크롭 및 `beauty_overview` 우상단 크롭에서 **잔디–보도 경계를 따라 순백 원통 6본이 등간격**으로 늘어선 것이 확인된다.
- §2 위반 3건 : 높이 0.75 < 규정 0.80 하한 / 지름 0.12 < 0.10~0.20 하한 근접·반사띠 없음 / 간격 2.4 m ≠ 1.5 m.
  §3 위반 : 등간격 정렬. §4 위반 : 순백(스테인리스 재질 유용). **§2 허용 지점(보도-차도 접점·램프/광장 진입부·계단 전면) 어느 것도 아닌 잔디 경계** — 차량 진입 근거 0.
- v5.1 §2 는 "장식적 볼라드 열 전면 제거(18 등)"를 전역 규약으로 명시했고 18/14/16/20/21 은 이행됐으나 **01 만 범위에서 누락**됐다.
- **수정안(택1)** : ① 6본 전량 삭제(권장 — §6 비움 기본값) ② 존치 시 `build_bollard_std` 준용으로 h 0.90 · d 0.15 · 간격 1.5 · 상단 백색 반사띠 · 본체 톤 0.55~0.65 로 교체하고 위치를 계단 진입 전면으로 이설.

### 4) 잔여 (경미·비차단, 전부 기존)
- `lower_lookback` 게시판 판면이 **백색 무지 대면적**(§4 순백 저촉) — 텍스처 or 톤 0.7 하향 권고.
- `beauty_overview` 지평 갭의 **원경 경계벽 무텍스처 단색 회색 판** — v5 그대로.

---

## scene02_underpass — **합격**

검수 컷 : `approach` / `beauty_overview` / `pit_edge` / `inside_looking_up` / `preset_h0.3_d10`

### 1) 피드백 반영 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **D6 노선도 게시판 삭제(개수 축소)** | **해소** | `beauty_overview`(구 게시판 위치 정면) 및 `pit_edge` 에서 패널 소멸 확인. 진입부 사인 총 **2매**(입구 사인 + 출구 표지). |
| **D4 입구 사인 이설**(보행축 침범 → 진입부 측면 지주식) | **해소** | `inside_looking_up` 개구 상단 **우측 가장자리**에 감청 소형 패널로만 걸린다. 구 "기둥에 매달린 갠트리"처럼 보행축(y=0) 위를 가로지르는 인상 소멸. `approach` 정면 프레임에는 아예 들어오지 않는다. |
| 경고 팻말 제거(VD) | **확인** | `sign_caution_step` 소멸, `sign_exit`(출구 →) 1매만 터널 입구에 잔존 — 실존 관행물. |

### 2) 은닉·회귀
- **grazing 은닉 유지 ✓** — `h0.3_d10` 대형 개구 완전 은닉(난간 상부·파라펫만 부유), 포장 연속.
- `inside_looking_up` 20단이 **황색 노징 띠로 개별 분절** 유지, 사면 오독 없음.
- 터널 암부 : RT 기준 `inside_looking_up` 평균 58.8 · dark 38.1 %, `pit_edge` 평균 109.7 · dark 4.6 %.
  v5 에서 광량 45000 상향의 효과는 PT 전제로 검증됐고, RT 값도 순수 검정 0 %·그라디언트 성립 — **회귀 아님**.

### 3) 잔여 (경미)
- 볼라드 4본(−1.0, ±4.3/±6.0)이 `beauty_overview`·`h0.3_d10` 에 회색 원통으로 남는다. fixlog_VC 메모5 가 명시한 **범위 밖** 항목 — §2 소급 시 규격화 대상.
- 입구 사인·게시판 판면 백색 무지(기존).

---

## scene09_ghat_riverfront — **재수정**

검수 컷 : `ghat_walk` / `across_river` / `from_river` / `waterline` / `preset_h0.9_d5` / `preset_h0.3_d10`
(+ 400 % 크롭 2 · manifest 뷰 좌표 역산)

### 판정 사유 — **"의도 불명" 미해소** (본 라운드 최우선 확인 항목)
N-4 가 넣은 재해석 요소(정자·데크·오리배·갈대·이끼)는 **코드에는 있으나 어떤 컷에서도 "호수공원"으로 읽히지 않는다.**

| 요소 | 프레임 노출 | 판독 |
|---|---|---|
| 수변 정자(사모정) | `from_river` / `across_river` 상단 우측, 화면 폭의 약 6 % | **실패** — 기둥 4 + 평판 지붕의 **무채색 콘크리트 상자**로 읽힌다. 처마 곡률·서까래·난간 없음, 절병통은 원경에서 1~2 px. |
| 오리배 | `ghat_walk` 좌상단, 수평선에 반쯤 잘림 | **실패** — 400 % 크롭 결과 **백색 무텍스처 박스 + 판때기 목**. "배"로 읽히지 않는다(v5 나룻배 "흑/백 평판" 지적과 동일 실패 모드). |
| 산책 데크 | 전 그리드 프리셋 하단 30~45 % | **부분** — 목재 판재 평면으로는 읽히나 난간·이음 디테일이 없어 "데크"보다 "나무 바닥"에 가깝다. |
| 갈대 | `across_river` 좌우 끝 소수 | 부분 |
| 이끼 밴드 | 수면 아래 2단 | 프레임 미확인 |

- **뷰 4컷이 전부 재해석 요소를 비껴간다.** manifest 역산 : `across_river`(eye 27.96,0,−3.69 → tgt 2,0,−1) 기준 오리배(18.5, 10.5)는 방위 **47.9°** 로 화각 밖, 정자(x −9..−5.8)는 25 m 원경. `ghat_walk`(eye −4,0,1.4)는 시선이 바닥을 향해 **데크 / 석재 포장 / 수면 3색 띠**로 끝난다(v5 "2색띠 미해소"가 3색으로 악화).
- **재질 톤이 재해석을 따라가지 않았다.** 36단 전체가 인도 가트의 **사암 핑크베이지** 그대로여서 `from_river`·`across_river` 는 여전히 "가트"로 읽힌다. 상부에 잔디·수목 밴드가 없어 "공원"의 근거가 프레임에 0.
- 부수 : `preset_h0.9_d5` / `h0.3_d10` 의 **대안(對岸) 전체가 무텍스처 흑색 실루엣**(p5 5.2 / 8.8, dark 18.2 % / 7.4 %)이고 그 위 건물 2동이 검은 상자 — v5 [경미]였으나 데크 신설로 화면 분할이 단순해지면서 흑색 띠의 비중이 커졌다.

### 정상 항목 (재수정 후에도 보존할 것)
- **grazing 은닉 유지 ✓** — `h0.9_d5` / `h0.3_d10` 전 프리셋 계단 단 노출 **0**, 데크 → 포장 → 수면으로 직결.
- 지평 백색 띠(v5 치명 해소분) **재발 없음**, 부유·관통 0건, 클리핑 0.00 %.

### 수정안
1. **미장센 컷 신설·재조준(필수)** — 정자 + 데크 + 수면 + 계단 상단을 한 프레임에 담는 컷.
   예 : eye ≈ (4, 16, 2.0) → tgt ≈ (−7, 5, −0.5) (상부 테라스 북동에서 정자·데크를 내려다보며 수면을 배경으로).
   오리배는 `across_river` 축(y=0) 쪽으로 y 10.5 → 4~5 로 당기면 기존 컷에도 들어온다.
2. **정자 형상 보강** — 처마 돌출·귀솟음(또는 최소한 지붕 4면 경사 + 처마선 2단), 마루 난간(계자난간 근사). 현 평판 지붕으로는 원경에서 정자로 읽히지 않는다.
3. **오리배** — 선체 곡면화(구/캡슐 조합) + 목·부리 비례 조정, 백색 알베도 0.9 → 0.7대.
4. **정체성 재질** — 상부 테라스에 잔디·관목 밴드 추가, 계단 석재 틴트를 회색 화강암 계열로 이동(위험 기하 불변).

---

## scene14_grandstair_illusion — **조건부**

검수 컷 : `beauty_overview` / `side_reveal` / `terrace_read` / `lower_lookup` /
`preset_h0.3_d10` / `preset_h0.3_d2` (+ 400 % 크롭 3 · v5_pt 동일 컷 대조 1)

### 1) 피드백 반영 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **스텝 파라펫(계단층) 제거 → 사선 헌치** | **주요 해소** | `lower_lookup` v5_pt ↔ v6_rt 동일 크롭 대조 : v5 의 **40단 미세 톱니 상면**이 v6 에서 완전히 소멸하고 매끈한 사선 어깨로 대체됐다. `beauty_overview`·`side_reveal` 실루엣에서도 단 톱니 0. |
| **축선 정리(기념비 이설)** | **해소** | `lower_lookup` 에서 기념비 샤프트가 화면 **좌측 1/3** 로 비껴나 중앙 소실점이 열렸다(구 : 샤프트가 마루선 위로 축선을 차단). |
| 볼라드 축선 비움·규정화 | **해소** | `lower_lookup` 전경 볼라드가 **중앙 1.5 m 개구**를 두고 좌우로 분리, 400 % 크롭에서 스테인리스 본체 + 상단 백색 반사띠 확인(h 0.90 급). 점형블록은 의도적 생략(감독 승인). |
| 점자블록 부재(v5.2 §7) | **확인** | `terrace_read` 전경(계단 상단 x −0.4..0 대응 구간) 황색 0. |

### 2) 착시·은닉 (필수 유지 항목)
- **★ 착시 완전 보존 ✓** — `terrace_read`(eye −4,0,0.9)에서 상부 테라스 포장이 하부 광장·분수·화단·원경 건물로 그대로 연속되고 **6 m 계단이 단 하나도 노출되지 않는다.**
- `h0.3_d10`(평균 198.2 · dark 0.00 %) / `h0.3_d2` / `h0.9_d5` 전부 낙차 노출 0. **grazing 은닉 세트 최강 유지.**

### 3) 결함 — 조건부 사유 2건
1. **[중] 헌치 이음부 쐐기 슬릿 + 박공 분절 실루엣.**
   `lower_lookup` 좌·우 400 % 크롭에서 헌치가 **연속 사선 1줄이 아니라 박공 프리즘 3~4채**로 읽히고, 각 정점 직후에 **폭 3~6 px 의 어두운 쐐기 슬릿**이 좌우 대칭으로 관측된다(좌 x≈310 / 740 / 935, 우 대칭). `side_reveal` 크롭에서도 동일 위치에 삼각 노치 2곳.
   fixlog 의 "이음부 Δz = 0.0" 검산은 **높이 방향만** 본 것이고, 확폭(3.0→5.0)으로 참마다 헌치가 ±Y 로 조그(jog)하면서 **평면 방향 겹침이 없어** 슬릿이 남았다.
   **수정안** : ① 참 수평 헌치 박스를 사선 방향으로 0.25~0.35 m 연장해 인접 사선 슬래브와 겹치게(평면 오버랩) ② 조그부에 폭 = 확폭량인 삼각 필렛(또는 수직 마구리 판) 추가 ③ 참에서 정점을 만들지 않도록 헌치 상면을 노징선 오프셋 그대로 단조 하강 폴리라인으로 재보간.
2. **[중] `beauty_overview` 깃대·흑색 깃발 전경 침입 — 3라운드째 미해소.**
   깃대가 화면 우중앙을 세로로 관통하고 **흑색 무지 깃발 판이 계단면을 약 8 % 가린다.** v5_rt·v5_pt 에서 연속 권고("깃대 x +2 이동 또는 −Y 2본 제거")됐으나 v6 에서도 그대로. 씬의 대표 컷이라 데이터셋 시트 노출 시 문제.
   **수정안** : 깃대 x +2.0 이설 또는 `beauty_overview` eye 를 +Y 로 1.5 m 이동.

### 4) 잔여 (경미)
- `terrace_read` 좌·우 하단에 헌치 끝단이 **백색 삼각 매스**로 노출 — 착시는 깨지 않으나 정체 불명 형상.
- 지평 미폐쇄(y ±20 밖 잔디–하늘 직선 접합) — 기존.

---

## scene16_canopy_shadow — **합격**

검수 컷 : `beauty_overview` / `approach` / `under_canopy` / `shadow_band` / `preset_h0.3_d10` / `preset_h0.9_d2`

### 1) 피드백 반영 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **"청색 패널이 공중" → 인방 교체** | **완전 해소** | `beauty_overview`·`approach` 에서 게이트가 **기둥 2본을 잇는 감청 인방으로 폐합된 문형(portal)** 으로 읽힌다. 기둥과 닿지 않고 떠 있던 X 방향 판때기 **소멸**. 인방 상면이 캐노피 지붕 저면과 플러시라 부유감 0. |
| **볼라드 규정화** | **해소** | 구 트렌치 양옆 장식열 6본 소멸. `shadow_band`·`h0.3_d10` 에서 **동측 출구 계단 밖 보도 진입부 1열**만 확인(등간격 1.5 m, 중앙 개구). `beauty_overview` 우상단에도 동일 열. |
| 사인 1매 통합 | **해소** | `approach` 좌측 지주식 `sign_exit`(출구 →) 1매뿐. 개구 상공 별도 패널 0. |
| 점자블록 부재 | **확인** | `h0.3_d10` 에서 v5 잔존분("황색 점자띠 1줄")까지 **소멸**. |

### 2) 은닉·회귀
- **grazing 은닉 유지 ✓** — `h0.3_d10`(평균 165.1 · dark 0.57 %) 개구 전면 은닉, 포장만 노출.
  `h0.9_d2`(dark 30.5 %)는 개구 바닥까지 노출되나 v5 판정대로 **기하적 필연 = "가시" 샘플**.
- `shadow_band` 정중앙 동측 출구 계단 + 상단 랜딩 순광 판독 유지, `under_canopy` 하강 20단이 황색 노징으로 분절(RT 평균 43.6 · dark 54.2 % — 캐노피 그늘의 물리적 결과, 노징·난간·벽면 판독 성립).
- 부유·관통 **0건**, 난간 접지 전 구간 정상.

### 3) 잔여 (경미, 전부 기존)
- `approach` 캐노피 지붕이 여전히 프레임 상단 밖 — v5 권고(eye z 1.6 → 1.3) 미반영. 캐노피가 씬 이름의 주인공이라 다음 라운드 반영 권장.
- 볼라드 본체가 백색 계열 — §4 순백 한계선. 톤 0.6~0.7 권고(비차단).

---

## scene19_fan_winder — **재수정**

검수 컷 : `roof_skyline` / `radial_nosing` / `winder_mid` / `roof_context` / `lower_lookup` /
`preset_h0.3_d3.5` / `preset_h0.9_d3.5`

### 판정 사유 — **지시된 재조준 2컷 모두 실패**
| 지시(v5 판정) | v6 결과 | 근거 |
|---|---|---|
| ① `roof_skyline` 에서 E동 지붕 시인 = **스케일 앵커** | **미해소(2라운드 연속)** | 신 카메라 eye (16.0, 2.5, 2.0) → tgt (−21, 15, −1.5). 실제 프레임은 **백색 파라펫 + 옥탑 흑색 벽 + 하늘**이고, 파라펫 너머로 보이는 것은 **원경 잔디 띠 한 줄뿐**. 건물 지붕 **0채**. eye z 를 2.2 → 2.0 으로 오히려 **낮춘** 것이라 v5 지적(“eye z 3.2 이상”)과 반대 방향. |
| ② `radial_nosing` 재조준(차폐 해소) | **미해소** | 신 카메라 eye (5.2, 0.7, 1.5) → tgt (1.6, 1.6, −0.5). 프레임 **우측 약 40 %를 백색 매스(파라펫/L벽)가 세로로 점유**하고 나머지는 암부 곡면벽 + 바닥 타일. **부채꼴 단 0단**. 차폐 주체가 다크 그레이 슬래브에서 백색 파라펫으로 바뀌었을 뿐 컷은 여전히 무효. |
| ③ d3.5 그리드 | **유지·정상** | `h0.3/0.9_d3.5` 평균 193.6 / 181.0 · dark 1.5~1.7 %. 옥상 데크·L벽·옥탑 문·파라펫 전부 프레임 안, 허공/암벽 컷 0. |
| ④ `winder_mid` 부채꼴 시인 | **유지** | 쐐기형 디딤면이 내측 뉴얼로 수렴하는 방사 배열 판독 성립(평균 45.7 로 어둡지만 디딤/챌면 대비 확보). **씬 정체성은 이 컷 단독으로만 버티고 있다.** |

### 은닉 (정상)
- **grazing 은닉 유지 ✓** — `h0.3/0.9_d3.5` 에서 winder 하강 완전 은닉, 옥상 보도가 평탄하게 이어진다. 암흑 컷 0, 부유·관통 0건.

### 추가 관측 (재수정 시 함께 처리 권장)
- `lower_lookup` : 백색 파라펫 매스가 프레임의 **약 55 %** 를 점유(v5 "좌 절반"보다 악화), 그 면에 **미세 수직 스트리크**(UV 스트레치) 잔존.
- `roof_context` : ① 수관이 여전히 프레임 상단 밖으로 잘림(v5 잔여 그대로) ② 식재 트리에 **지지대 3본**이 붙어 있다(`sc.build_tree` 기본값 — 01/05 에서 제거한 v4-B3 항목의 잔존 경로) ③ 옥탑 설비가 **무텍스처 백색 큐브 3개 + 백색 원통**으로 v5.2 §6(조형물 상한·판독 기여) 통과가 의심된다.
- `winder_mid` 우상단 파라펫 상면이 **계단식 톱니**로 노출 — scene14 에서 제거 지시가 내려간 것과 같은 형태.

### 수정안
1. **`roof_skyline`** — E동 지붕(z −1.5)이 옥상 파라펫 상단보다 낮으므로 **파라펫 상단선 위로 시선을 올리는 것이 유일한 해**다. eye z 를 **3.2~4.0** 으로 올리거나(옥상 위 물리 가능 높이를 넘으면 컷 정의를 "부감 컨텍스트"로 재정의), 그것이 불가하면 **지상 원경 컷으로 대체**해 옥탑 + E동을 한 프레임에 담을 것. 재조준 후 **파라펫 4변 + winder 호 링을 모두 포함한 폐색 검산** 필수(v5 교훈 재발).
2. **`radial_nosing`** — 성공한 `winder_mid`(eye 3.4, 0.9, 1.3) 근방에서 tgt 만 뉴얼 하단으로 낮춰 잡는 편이 안전. 현 eye (5.2, 0.7)는 L벽·파라펫 안쪽이라 어떤 tgt 로도 백색 매스를 피하기 어렵다.
3. `roof_context` eye z 상향(수관 포함) + 트리 `stakes=False` + 옥탑 큐브 축소.

---

## scene20_diagonal_oblique — **합격**

검수 컷 : `oblique_overview` / `along_diagonal` / `low_grazing` / `walk_axis_front` /
`preset_h0.9_d5` / `preset_h0.3_d10` (+ 400 % 크롭 1)

### 1) 피드백 반영 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **사선 에지를 따라가던 볼라드 열(8본) 폐기** | **해소** | `along_diagonal`·`low_grazing`·`walk_axis_front` 전부에서 **사선 낙차 에지 전면 볼라드 0본**. 에지가 깨끗하게 노출된다. |
| **규정 배치(진입부 1열 + 1.5 m)** | **해소** | `along_diagonal` 400 % 크롭 : 하부 광장 사선 복도 입구에 **4본, 간격 균일 1.5 m**, 스테인리스 본체 + **상단 백색 반사띠** 확인. §2 치수 사양 부합. 메사 램프 상단 열은 전 프리셋 후방으로 화각 밖. |
| 경고 팻말 제거(VD) | **확인** | `sign_caution_step` 소멸, 씬 내 사인 0매(`cue_sign=False`). |
| 점자블록 부재 | **확인** | `along_diagonal` 에서 v5 의 "황색 점자띠 → 낙차 지평" 구도가 사라지고 포장 → 낙차로 직결. |

### 2) 은닉·회귀
- **grazing 은닉 유지 ✓** — `low_grazing`(dark 0.03 %) · `h0.3_d10`(dark 0.00 %) · `h0.9_d5` 모두 2.15 m 낙차 완전 은닉, **난간만 잔디 위에 떠 보이는 목표 구도** 그대로.
- 쐐기 트렌치 수평 틈 0, 윗단 매몰 0, 난간·벤치·건물 접지 정상. **신규 결함 0건.**

### 3) 잔여 (경미)
- 볼라드 배후에 **차량 동선(서비스 도로·차도)이 프레임에 없어** §2 근거가 렌더로는 읽히지 않는다. 잔디밭 한복판 진입부처럼 보임 — 원경에 서비스 도로 띠 1줄만 있어도 근거가 성립한다.
- 하부 광장 벤치 2기가 **앵커 없이 좌우 대칭** 배치(§3 취지 저촉, 경미).
- 원경 두 건물 사이 **무텍스처 회색 경계벽**(scene01 과 동일 계열) · 우측 지평 미폐쇄.

---

## scene21_monumental_selfocclude — **합격**

검수 컷 : `oblique` / `facade_front` / `crown_graze` / `railing_line` /
`preset_h0.9_d5` / `preset_h0.3_d10` (+ 400 % 크롭 2)

### 1) 피드백 반영 검증 — "위엄 : 중앙축 위 물체 제거"
| 항목 | 결과 | 근거 |
|---|---|---|
| **축선 조형물·중앙 깃대·볼라드 제거** | **해소** | `preset_h0.9_d5` : 계단 상단 → 하부 광장 → 원경 벽돌 건물까지 **중앙 소실점이 완전히 비었다**(구 : 기념비 샤프트 + 깃대 2본 + 볼라드가 겹쳐 차단). `crown_graze`·`facade_front` 도 동일하게 축선 클리어. |
| **기념비 축선 밖 이설** | **확인** | `crown_graze` 우측 원경에 사암 기단 + 축소 샤프트로 확인(축선에서 12 m 이격). |
| **깃대 측면 대칭 2열** | **확인** | `oblique` 상단·`h0.9_d5` 좌우 가장자리에만 분포. 축선 횡단 1열 소멸. |
| **볼라드 이설(계단 발치 횡단열 폐기)** | **확인** | `oblique` 좌상단 하부 광장 북측 1열로만 존재, 계단 발치 0본. 테라스 볼라드 4본도 소멸. |
| 점자블록 부재 | **확인** | `cue_tactile=False` 코드와 렌더 정합. 황색은 전부 단코 띠. |

### 2) 은닉·회귀
- **grazing 은닉 유지 ✓** — `h0.9_d5` · `h0.3_d10`(dark 0.57~2.13 %) 상부 테라스가 하부 광장으로 연속, 단 노출 0.
- v5 해소분 **회귀 없음** : 파라펫–계단 측면 코플래너 빗살 **0**(`oblique` 400 % 크롭에서 파라펫 외면 균질 백색 솔리드), d10/d5 그림자 크러시 **재발 없음**(p5 63~93).
- 태양 방위(206.5) 그림자 평행성 정상, 부유·관통·클리핑 0건. `facade_front` 열주 6본 + 인방 + 엔타블러처 온전.

### 3) 잔여 (경미·권고, 전부 기존 — 합격 저해 아님)
- **단코 띠 과채도.** `railing_line` 400 % 크롭에서 계단이 **황색 줄무늬 계단**으로 읽힌다. 실측 원인 : `sc.build_nosing` 기본 width 0.05 m 는 규격상 정상이나, 그레이징 각에서 디딤면이 압축돼 **투영 폭이 디딤의 절반**을 차지하고 색이 (0.85, 0.72, 0.10) 고채도라 사암 계열과 충돌한다. **채도 하향(0.75, 0.68, 0.22 수준) 권고** — 기하 무변경.
- `oblique` / `railing_line` 은 **측면 깃대·기둥이 프레임 중앙을 세로로 관통**한다. 축선 자체는 비었으므로 **컷 프레이밍 문제**이며, eye 를 ±1.5 m 옆으로 옮기면 해소.
- 원경 건물 측벽이 정오 역광에서 **흑색 무텍스처 면**으로 떨어짐(`crown_graze` 좌측) · 하부 광장 벤치가 넓은 백색 광장에 앵커 없이 산재(§3, 경미).

---

## 종합 및 후속

1. **재수정 2씬 (09 · 19)** — 둘 다 **카메라·드레싱 판독 실패**형이고 위험 기하는 무결하다.
   - 09 : 재해석 요소가 프레임에 안 들어온다 → 컷 신설 + 정자/오리배 형상 보강 + 석재 톤 재해석.
   - 19 : 재조준 2컷 연속 실패 → 재조준 전에 **파라펫·호 링을 포함한 폐색 검산**을 반드시 선행할 것.
2. **조건부 2씬 (01 · 14)** — 지시된 본 항목은 전부 이행됐고, 각각 볼라드 1건 / 헌치 이음부 + 깃발 1건만 남았다. 코드 수정 범위가 작아 다음 라운드 합격 가능.
3. **v5.2 §7·§8 전수 이행 확인** — 8씬 전 컷에서 점자블록·임의 경고 팻말 **0건**. 은닉 단서가 황색 띠에서 재질 대비(차콜 밴드·포장 전환)로 넘어갔고, 이로써 **01·16·20 의 grazing 은닉은 v5 대비 오히려 강화**됐다.
4. **이번 라운드 교훈**
   - **높이 검산만으로 이음부를 통과시키지 말 것**(scene14) — 확폭·조그가 있는 구간은 **평면 방향 겹침**을 따로 검산해야 슬릿이 안 남는다.
   - **재해석은 재질·톤까지 따라가야 한다**(scene09) — 드레싱 오브젝트만 교체하면 원 정체성(사암 가트)이 그대로 읽힌다.
   - **차폐 검산 실패는 재발한다**(scene19) — v5 판정이 지목한 "호 링 접선 차폐"가 그대로 반복됐다. 재조준 시 **파라펫 전 부재를 포함한 광선 투사**를 규약화할 것.
