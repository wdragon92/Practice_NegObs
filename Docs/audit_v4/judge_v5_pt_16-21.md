# v5_pt 최종 합격 판정 — scene16 / 17 / 19 / 20 / 21

작성 2026-07-27 · PT 파이널 판정관 라운드 (파일 수정 없음, 판정 전용)
근거: `look_check/sceneNN/v5_pt/*.png` (PathTracing 512spp, noon) 육안 + 카메라 역산 투영
직전 판정: `Docs/audit_v4/judge_v5_rt_16-21.md`
수정 내역: `Docs/audit_v4/fixlog_FB.md` (17/21) · `scenes/main/scene19_fan_winder.py` `[v5 판정 반영]` (19)

| 씬 | 최종 판정 | 한 줄 |
|---|---|---|
| scene16 | **합격** | 재확인 — 동측 출구·난간 접지·게이트 유지, grazing 은닉 정상, 신규 결함 0 |
| scene17 | **합격** | `ramp_run` 매몰 해소 + 20단 완전 분해 + 램프 동일 프레임 → 대비쌍 정체성 확보 |
| scene19 | **잔여** | winder 시인·d3.5 신설은 성공, 그러나 `roof_skyline` E동 지붕이 파라펫에 완전 차폐 |
| scene20 | **합격** | 재확인 — 트렌치 0·접지 정상·은닉 완전 |
| scene21 | **합격** | 코플래너 빗살 소멸 + d10/d5 크러시 완전 해소, 태양 방위 변경 부작용 없음 |

> 전 씬 공통: manifest 의 `ok:false` 는 캡처 파일 크기 안정화 루프(40 tick) 미수렴 플래그일 뿐
> 렌더 품질 지표가 아니다. 해당 6컷(16 3, 17 1, 19 2, 21 2) 모두 정상 PNG 로 판독 가능 — 결함 아님.

---

## scene16_canopy_shadow — **합격** (재확인)

검수 컷: `beauty_overview` / `under_canopy` / `shadow_band` / `approach` /
`preset_h0.3_d10` / `h0.9_d2`

- **A1 동측 출구 계단**: `shadow_band` 정중앙에 상승 계단 + 상단 랜딩이 순광으로 잡힘.
  `under_canopy` 원경도 동일. 막다른 벽 없음 — RT 판정 그대로 유지.
- **B1 난간 접지 / C 사인 게이트 / 원경 폐쇄**: `beauty_overview` 에서 문형 게이트 패널 + 기둥 2,
  피트 둘레 난간 전 구간 접지, 벽돌 건물·화단·볼라드·벤치·생울타리 동시 판독. 부유·관통 0건.
- **grazing 은닉**: `h0.3_d10` (평균 168.6 / dark 0.1 %) — 개구 전면 은닉, 황색 점자띠 1줄만 잔존.
  `h0.3_d5` · `shadow_band` 도 깊이·바닥 불가시. 은닉 성립.
- **잔여(경미, RT 와 동일)**: ① `approach` 캐노피 지붕 여전히 프레임 상단 밖(eye z 1.6 → 1.3 권고).
  ② `h0.9_d2` / `h1.8_d2`(dark 7.5 / 6.2 %)는 개구 바닥까지 노출 — 눈높이·거리상 기하적 필연이라
  결함이 아니라 데이터셋의 "가시" 샘플로 처리.
- 신규 결함 **0건**.

---

## scene17_ramp_pair_hangang — **합격**

검수 컷: `ramp_run` / `pair_compare` / `across_river` / `levee_walk` /
`preset_h0.9_d5` / `h0.3_d2` (+ 400 % 크롭 2, 카메라 역산 투영 2)

### 조건부 해소 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| ① `ramp_run` 성토 매몰(퇴행) | **해소** | 신 eye (30, 11.5, 7.8). 성토 내부 관통 소멸, 프레임에 둑길 자갈로 + 계단 20단 + 레인 A + 분리 파라펫 + 볼라드·벤치가 동시에 들어옴. |
| ② 대비쌍 정체성(계단 20단 + 램프 동일 프레임) | **해소 — 단 `ramp_run` 이 담당** | `ramp_run` 400 % 크롭에서 **20단 전부가 디딤/챌면으로 분해**되고 같은 프레임 우측에 램프 노면·파라펫이 병렬. 씬 정체성 렌더 검증 성립. |
| ③ `pair_compare` 부감 | **부분** | 투영 역산상 계단 4모서리가 전부 프레임 안(px 750–1605, py 705–904)이나, **상방(−X)에서 내려다보는 각이라 챌면이 원리적으로 보이지 않아** 계단이 평평한 콘크리트 슬래브로 읽힌다. 단은 좌우 에지의 톱니 실루엣으로만 판독 가능. |

> ③ 보충: fixlog 의 "부각 35.2° > 계단 경사 26.6° → 20단 분해" 는 성립하지 않는다. 하강 계단을
> 상류측 위에서 보면 부각과 무관하게 디딤면만 보이고 챌면은 항상 윗단에 가린다. 챌면 분해에는
> **하류측(+X)에서 되올려보는 각**이 필요하며, 이번 `ramp_run` 이 정확히 그 각이라 조건은 충족됐다.

### grazing 은닉
`h0.9_d5` · `h0.3_d2` · `h1.8_d2` 전부 3.2 m 낙차 완전 은닉 — 둑길 잔디가 둔치 잔디로 이음매
없이 이어지고 계단·램프 개구가 프레임 아래로 사라진다. dark % 1.2–1.7 로 암부 크러시 없음. **유지.**

### 신규 결함 / 잔여
1. (경미·신규 가시) **레인 A 성토 사면 상단 텍스처 수직 스트리크.** `pair_compare` 400 % 및
   `across_river` 좌측에서, 파라펫 바로 아래 폭 좁은 사면 띠에 콘크리트 텍스처가 세로로
   늘어난 줄무늬가 보인다. Z파이팅(2재질 교대) 아님 — 단일 재질 UV 스트레치. 재질 scale 조정 권고.
2. (경미) `pair_compare` 계단 미분해 — 위 ③. 재렌더 필수 아님(정체성은 `ramp_run` 으로 확보).
3. (경미, RT 유지) 억새가 `build_hedge` 근사라 `ramp_run` 좌·우에서 갈색 직육면체로 읽힘.
4. 부유·관통·클리핑 **0건**. 파라펫 알베도 0.90 / `divider_h` 0.9 는 감독 보류 항목이라 미판정.

---

## scene19_fan_winder — **잔여**

검수 컷: `winder_mid` / `lower_lookup` / `roof_skyline` / `roof_context` /
`radial_nosing` / `entry_gate` / `upper_approach` / `preset_h0.3_d2` / `h0.3_d3.5` / `h0.9_d3.5`

### 조건부 해소 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| **그리드 d10 → d3.5 교체** | **해소** | 신설 3컷 평균 197.7 / 184.9 / 171.3, dark % ≤ 0.4. 옥상 밖 허공·파사드 암벽 소멸, 9컷 전부 유효. 옥상 데크·L벽·옥탑 문·파라펫이 모든 컷에 들어온다. |
| **`winder_mid` 부채꼴 단 시인** | **해소(우수)** | 카이트 랜딩 어깨(eye 3.4, 0.9, 1.3)에서 **쐐기형 디딤면이 내측 뉴얼로 수렴하는 방사 배열이 전면 판독**. 15컷 중 0컷이던 winder 실체가 확보됐다. 음영부(평균 58.7)지만 디딤/챌면 대비는 충분. |
| **`lower_lookup` 부채꼴 단 시인** | **부분** | 출구 코너에서 챌면 계단열은 우측에 보이나, 프레임 좌 절반을 백색 파라펫 매스가 점유해 **방사(부채꼴) 성격까지는 읽히지 않는다.** "계단이 있다"는 성립, "부채꼴"은 `winder_mid` 전담. |
| **`roof_skyline` E동 지붕(스케일 앵커)** | **미해소** | 아래 참조. |

### `roof_skyline` 미해소 — 검산
- E동(x −28..−14, y 8..22, 지붕 z **−1.5**) 4모서리를 신 카메라(eye 16.6, −4.2, 2.2 /
  tgt −21, 16, −1.5)로 투영: px **(579, 532) ~ (1322, 552)**, py 515–583 — **전부 프레임 안**.
- 그러나 실제 픽셀은 전부 **근경 백색 파라펫(옥상 U/L 파라펫 + winder 호 링)** 이다.
  시선이 t≈16.7 m 지점에서 winder 파라펫 링(r_in 3.82)에 **접선으로 스쳐** 화면 중앙 수평
  대역을 통째로 가린다. 코드 주석의 차폐 검산(L벽 west 0.25 여유 / L_W 파라펫 상회)은
  맞지만, **호 링 파라펫이 검산 대상에서 빠졌다.**
- 결과: 파라펫 너머는 하늘 + 원경 잔디(z −6) 두께 4~8 px 올리브 실선뿐. **지붕 0채.**
  `roof_context` 도 동일하게 파라펫 너머가 전부 하늘 — 두 컷 모두 스케일 앵커 미충족.

### 신규 결함 / 잔여
1. **[중] `roof_skyline` 스케일 앵커 미달성** — 위 검산. 해소안: eye z 2.2 → **3.2 이상**으로
   올리거나 카메라를 호 링 접선에서 벗어난 방위(예: tgt y 16 → 24)로 틀 것.
2. **[중] `radial_nosing` 무효.** eye (4.6, −1.5, 1.6) → tgt (2.0, 2.2, −1.0) 의 시선축 2 m 앞을
   다크 그레이 슬래브 측면이 세로로 관통해 프레임의 55 % 를 덮는다. 부채꼴 단 0. 재조준 필요.
3. (경미, RT 유지) `roof_context` 수목 수관이 프레임 상단 밖으로 잘림.
4. (경미) `lower_lookup` 백색 파라펫 면에도 scene17 과 같은 계열의 미세 수직 스트리크.
5. **grazing 은닉 유지 ✓** — `h0.3_d2`(dark 0.3 %) · `h0.3/0.9_d3.5` · `d5` 전부 winder 하강이
   완전 은닉되고 상부 보도가 평탄하게 이어진다. 암흑 컷 0. 부유·관통 0건.

### 판정 사유
3개 조건 중 2개(d3.5 그리드, winder 시인)는 명확히 해소됐고 은닉·조도도 정상이나,
지시된 **스케일 앵커(E동 지붕) 조건이 렌더로 전혀 확인되지 않아** 합격 처리 불가.
잔여 2건(`roof_skyline` 재조준, `radial_nosing` 재조준)만 반영하면 다음 라운드 합격 가능.

---

## scene20_diagonal_oblique — **합격** (재확인)

검수 컷: `oblique_overview` / `along_diagonal` / `low_grazing` / `walk_axis_front` /
`preset_h0.9_d5` / `h0.3_d10`

- **A1/A2 쐐기 트렌치·에지 후퇴**: `along_diagonal` 에서 황색 점자띠 바로 위가 곧장 낙차 지평.
  사선 에지 전 구간 수평 틈 0, 윗단 매몰 0.
- **B1/B2/B3 접지**: 점자띠·난간 시작부·원경 벽돌 건물 밑 검은 슬래브 전부 소멸(`oblique_overview`).
- **볼라드 열 vs 30° 사선 에지** 대비 유지, 하부 광장 벤치 2 + 볼라드 4 정상 접지.
- **grazing 은닉**: `low_grazing`(h0.35, dark 0.0 %) · `h0.3_d10` · `h0.9_d5` 모두 2.15 m 낙차
  완전 은닉. 난간만 잔디 위에 떠 보이는 구도 = 목표 맥락 단서. **유지.**
- 신규 결함 **0건**. 잔여는 RT 와 동일하게 B6 계단 실체 확인 컷 부재(선택 사항)뿐.

---

## scene21_monumental_selfocclude — **합격**

검수 컷: `oblique` / `facade_front` / `crown_graze` / `railing_line` /
`preset_h0.3_d5` / `h0.3_d10` / `h0.9_d10` / `h1.8_d10` / `h0.9_d2` (+ 400 % 크롭 2)

### 조건부 해소 검증
| 항목 | 결과 | 근거 |
|---|---|---|
| ① 파라펫–계단 측면 코플래너 빗살 | **완전 해소** | `oblique` 200~400 % 크롭에서 파라펫 외면이 **균질한 백색 솔리드 1면**. 단 피치로 교대하던 백색/석재 빗살 무늬 흔적 0. 2 cm 돌출(y 4.02)이 계단 측면을 완전히 덮어 잔여 슬리버도 없다. |
| ② d10 열 그림자 크러시 | **완전 해소** | d10 3컷 p5 = **88.0 / 79.3 / 73.3**, dark(<20) % = 0.6 / 1.0 / 1.4. 구 RT 의 전경 그림자부 RGB (23, 26, 28) → 전경 전체가 균일 조광 사암 포장. `h0.3_d5` 도 p5 74.7 / dark 2.0 % 로 "하단 40 % 그림자" 동반 해소. |

### 태양 방위 변경(171.5 → 206.5) 전반 점검
- **그림자 방향 일관성 ✓** — `crown_graze` · `h1.8_d10` · `oblique` 에서 깃대 6본·가로등·벤치
  그림자가 전부 평행(수평 성분 +X/+Y 계, 이론 방위 60° 와 일치). 교차·역방향 그림자 0건.
- **그림자 길이 ✓** — elev 49.79° 기준 0.845 h. 깃대·가로등 실측 비율 정합.
- **재질 이상 0건** — 사암 파사드/계단(따뜻한 크림), 하부 광장(백색 포장), 벽돌 원경, 잔디
  전부 정상. `h0.3_d10` 좌측 암부는 벽돌 건물 내측 코너의 정상 음영(텍스처 잔존 확인, 흑색 재질 아님).
- **파사드 서측 3.2 m 이설 부작용 0건** — `facade_front` 에서 열주 6본 + 인방 + 엔타블러처가
  프레임에 온전히 들어오고 하단 대계단과 연속. 부유·틈 없음. (확장 테라스 서단 x −15.2 는
  전 컷이 +X 향이라 화면 미확인 → 좌표 검산으로 대체.)
- **부유·관통·클리핑 0건.**

### 잔여 (전부 fixlog 가 "범위 밖"으로 명시한 기존 항목 — 합격 저해 아님)
1. **단코 띠 과채도·과폭** — `railing_line` · `oblique` 400 % 에서 각 단의 절반가량이 순황색.
   태양 방위 변경으로 해당 면이 더 밝아져 **RT 때보다 눈에 띈다.** 석조 기념 계단의 격에는
   여전히 부적합(폭 축소 + 채도 하향 권고). 다만 부감 컷에서 단 수 계수를 돕는 부수 효과는 있음.
2. `oblique` 중앙 깃대 세로 관통 / `railing_line` 볼라드·깃대 중앙 차단 — 미해소.
3. 파라펫 순백(알베도 0.90) vs 사암 계열 불일치 — 미해소(0.72~0.78 미색 권고).

---

## 종합

- **합격 4** (16 · 17 · 20 · 21) / **잔여 1** (19).
- scene19 잔여 2건은 **카메라 재조준만**으로 해소되며 기하·재질 수정은 불요:
  ① `roof_skyline` eye z 2.2 → 3.2↑ 또는 호 링 접선 이탈 방위, ② `radial_nosing` 재조준.
- 이번 라운드 교훈: **하강 계단은 상류측 부감에서 원리적으로 단이 분해되지 않는다**(scene17 ③).
  "단 실체 확인 컷"은 앞으로 반드시 **하류측 되올려보기** 각으로 규약화할 것.
- 두 번째 교훈: 폐색 검산 시 **호(arc)형 파라펫에 대한 접선 차폐**를 빠뜨리기 쉽다(scene19).
  직선 벽 목록만 검산하면 통과하지만 실제로는 화면 중앙 대역이 통째로 막힌다.
