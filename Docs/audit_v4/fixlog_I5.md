# fixlog I-5 — scene16 / 17 / 20 / 21 (감사 v4 수정 구현)

작성 2026-07-27 · 팀 I-5(Opus 5) · 단독 소유 파일 4종
근거: `audit_scene16-21.md` A/B/D 섹션 + `consolidated_fix_plan.md` 공통 규약
검증: `python3 -m py_compile` 4/4 통과 · scene17/21 SMOKE 자기검증 통과 ·
좌표 산술 스크립트(메사 에지 0.02 m 샘플링, 미러 매핑, 소품 접지) 전 항목 OK.
**scene_common.py 미수정**(읽기 전용 준수). scene19 미접촉.

---

## scene20_diagonal_oblique — 30° 대각 사교 계단

### 수정 결함 (좌표 근거)

| # | 결함 | 조치 | 검산 |
|---|---|---|---|
| A1 | 남측 쐐기 트렌치: 광장 에지 x=0 과 계단 상단선 사이 수평 틈 0.30→**1.25 m**, 그 아래 잔디 −2.15 (낙차 2.15 m) | **S1 UpperWedge 1박스**를 `rot_group` 내부에 추가: 로컬 중심 (−4.0, 0.0, −1.1), 크기 (8.0, 18.0, 2.2) → 로컬 x −8..0, y ±9, 상면 0 / 저면 −2.2. 회전군 안이라 동측 면이 계단 상단선(로컬 x=0)과 정확히 일치 | 메사 동측 에지를 y −9..9 를 0.02 m 로 샘플 → 첫 단차 값 분포가 **{0.15: 216샘플, 2.15: 635샘플}** 뿐. 0.15 구간 폭 = 216×0.02 = 4.32 m = 계단 폭 5.0×cos30 ✔, 나머지는 계단 폭 밖 순수 절벽(의도된 낙차) |
| A2 | 북측 상단 매몰: 광장 솔리드(x ≤ 0)가 계단 윗단을 덮어 첫 단차 0.30~**0.75 m** | 감사 S1만으로는 **미해소**(쐐기도 광장도 같은 x≤0 을 채움). 축정렬 광장 `upper.x1` **0.0 → −4.5** 로 후퇴시켜 광장 에지가 사선(x = −0.5774·y)을 넘지 않게 함 | ① 한계값 근거: 메사점은 lx ≤ 0 ⇒ y ≤ −1.732x ⇒ ly ≤ −2x ≤ 9 ⇔ x ≥ −4.5 → x1=−4.5 가 쐐기(|ly| ≤ 9)로 빈틈없이 덮이는 정확한 경계. ② 내부 구멍 샘플(0.01 m 격자, 4근방 판정) **0개** |
| B1 | 점자띠 남측 2.2 m×0.3 m 캔틸레버 부유 | S1로 자동 해소 | 로컬(0,−2.5)→월드(1.250,−2.165), 로컬(−0.3,−2.5)→월드(0.990,−2.315) 두 점 모두 상면 z=0(쐐기) |
| B2 | 남측 난간 시작부(로컬 x −0.4) 부유·좌우 비대칭 | S1로 자동 해소 (`stair_rail.x_start=−0.4` 유지, ground_fn 재검증) | 로컬(−0.4,∓2.4)→월드(0.854,−2.278)/(−1.546,1.878) 모두 z=0 |
| B3 | 원경 건물 C·D 1.15 m 부유 | `bd["base_z"] = −2.15`(계곡 잔디 상면) 지정 | 셸 저면 = base−1.0 = −3.15 = 잔디 슬래브 저면 ✔ |
| A5 | 상부 광장 진입로 부재(고립 메사) | 서측 진입 램프 1프림: `build_slope(x0=−18, z0=−2.15, run=4, drop=−2.15, y ±2, thick=2.6, margin=0)` (drop 음수 = +X 상승) | 상단 모서리 (−14, 0.00) = 광장 서면과 flush, 하단 (−18, −2.15) = 잔디와 flush. 두께 2.6 → 밑면이 −2.29(상단)로 잔디 상면 아래 → 부유 없음 |
| — | 밴드 5줄이 후퇴한 광장 밖으로 부유할 위험 | `band.x1 −0.6 → −5.0`, `spacing 2.7 → 2.0`(줄 수 5 유지) | 최동단 밴드 x −5.225..−4.775 ⊂ 광장(x ≤ −4.5) ✔ |

> 계단(`Stairs`)·하부광장(`LowerPlaza`)·회전군 트랜스폼은 **일절 불변**.
> 겹침부(광장 ∩ 쐐기)는 두 박스의 상면(0)·저면(−2.2)·재질·월드투영 텍스처가 동일 → Z파이팅이 발생해도 화면 차이 없음.

### 드레싱 요소 (`cue_scene_dressing`)
- **축정렬 볼라드 8** (1순위, 사선 에지와의 대비 = 씬 주제): x=−1.2 열 5개(y −7/−5/−3/−1/1) + (−6.0, ±7.5) + (−10.5, 0)
- 화단 3 (−10, ±5.5)·(−6.5, 6.2) size 3.0 + 수목 / 벤치 4 (−7.5, ±6.6)·(−11.5, ±3.0) / 가로등 3 (−7, 6.8)·(−9, −6.8)·(−12.5, 4.0) (scene18 폴+암+헤드 패턴) / 생울타리 2 (x −14..−9, y ±7.4..8.0) / 하부 광장(회전군 로컬) 볼라드 4 + 벤치 2 / 서측 지평 폐쇄 건물 E(x −40..−32)
- 전 소품 접지 검산: 각 풋프린트 모서리에서 메사 판정 → **이상 0**

### 보류·제안
- S4 에지 가드(사선 파라펫)는 **미적용** — "낙차 은닉" 특색을 약화시키므로 감독 판단 대기.
- S5 하부 광장 확폭(±2.5→±6, x1 18→22)은 **미적용**(하부 광장 트랜스폼 불변 지시). 착지 후 보행축 복귀 여지 부족은 잔존.
- B6 계단 실체 확인 컷(`stair_face`)은 카메라 변경이라 미적용 — 감독 프리셋 결정 후 반영 권장.
- 광장 후퇴로 북동 메사 면적이 줄어 프리셋 h/d 컷의 근경 구성이 바뀐다 → 재렌더 시 확인 필요.

---

## scene21_monumental_selfocclude — 기념비적 대계단

### 수정 결함 (좌표 근거)

| # | 결함 | 조치 | 검산 |
|---|---|---|---|
| B1/A3/A4/B4 | **지반 프림 부재** → 테라스 무한낙하·하부 광장 자유단 공동·건물 부유 | `Ground` 1박스 (14, 0, −3.35), 크기 (110, 90, 1.2) → 상면 **−2.75**, 재질 grass(`ASSET_ROLES`에 grass 추가) | 상면 −2.75 는 하부 광장 −2.70 바로 아래 → 광장 둘레 단차 **0.05 m**(감사안 −3.2 는 0.5 m 턱이 생김). 지반 x −41..69 / y ±45 가 건물 B·C·D 전 풋프린트 포함 |
| B4/T2 | 테라스가 두께 0.5 판 → 옆·아래에서 공중 판 | `terrace.thick 0.5 → 3.7` (저면 −3.7) | 지반 상면 −2.75 아래로 0.95 m 매입, 지반 저면 −3.95 위 ✔ 상면 z=0(위험 기하) 불변 |
| B2 | 양측 석재 파라펫이 계단 폭(y ±4) **밖** 허공의 경사 대들보 | 파라펫 y 대역을 계단 폭 **안쪽**(±3.5..±4.0)으로 이동 + `thick 0.5 → 1.6` | 밑면 x=0 에서 **−0.60**(1단 디딤면 −0.15 아래 0.45 m 매입), x=run 에서 **−3.30**(계단 저면 −3.20 아래) → 전 구간 접지. 유효 폭 8→7 m, 낙차·단 치수 불변 (SMOKE 자동 판정 추가) |
| B3 | 원경 건물 2동 완전 부유 | B·C 에 `base_z=−2.75`, +D(x 44..52, y ±20, h18) 추가로 프리셋(+X) 지평 폐쇄 | 셸 저면 −3.75 ⊂ 지반(−2.75..−3.95) ✔ |

### 드레싱 요소
- **축선 기념 조형물**: 대좌 (16, 0) 3.0×3.0×0.9 (상면 −1.80) + 첨탑 0.9×0.9×9.0 (−1.80..7.20) — 프리셋 시선의 소실점을 채움
- **하부 깃대 열 6본** (x=12, y −9..9 등간, r0.08 h9.0, base −2.70) — "기념/관공서" 판독 최단 경로
- 볼라드 9(하부 5 @x7.5 + 테라스 4) / 벤치 6 / 장식 가로등 4 (6.6,±5.2)·(18,±13)
- SMOKE 리포트에 z 위계·파라펫 접지 자동 검산 4줄 추가

### 보류·제안
- 테라스 측면(x=0, |y| 4.5..9)은 지반 확보로 **무한낙하 → 유한 3.2 m 낙차**가 되었으나 여전히 무방호. 난간을 세우면 "낙차 은닉" 특색이 약해져 **미적용** — 감독 결정 필요.
- T4 하부 광장 확장(x1 34→46, y ±26)은 지반이 생겨 불필요해 미적용.
- 파사드 활용 컷(`terrace_back`) 추가는 카메라 사안이라 미적용.

---

## scene17_ramp_pair_hangang — 한강 둔치 계단·램프 대비쌍

### 수정 결함 (좌표 근거)

| # | 결함 | 조치 | 검산 |
|---|---|---|---|
| A3 | 램프 레인 A/B 경계에서 측면 낙차 **3.2 m**(x=0) 무방호 | ① 레인 폭 1.25 → **1.8 m** (`ramp.y0/y1` 2.5..5.0 → **2.4..6.0**, y_mid 4.2) ② **레인 분리 파라펫**: `build_slope(x0=0, z0=+0.9, run 20, drop 1.6, y 4.20..4.40, thick 1.2)` — 레인 A 노면을 따라 상면 +0.9 | 밑면이 노면 아래 **0.30 m** 로 성토에 매입(부유 없음). 남측 면(y=4.2)이 레인A 성토 옹벽면과 동일 평면 → 레인B에서 보면 옹벽→파라펫 연속. 순폭 1.6 m ≥ 1.5 관행 ✔ |
| B1 | 램프가 두께 0.3 판 1장 → 최대 **2.95 m** 무지지 부유 | 레인별 **성토(embankment)** 3프림: `Ramp_FillA`(z0 −0.02, thick 3.3) · `FillB`(회전군 내, thick 1.7) · `Ramp_FillLanding`(x 19.5..21.5, z −1.80..−3.30) | 성토 밑면 A(x=0) **−3.31** / B(world x=20) **−3.31** < 둔치 상면 −3.25 → 전 구간 접지(SMOKE 자동 판정). 성토 상면은 노면 0.02 m 아래 → 두께 0.3 노면 판에 가려 Z파이팅 없음. 참(Landing) 하부 성토가 평행사변형이 못 채우는 동단 삼각 공동까지 덮음 |
| A5 | 둑길 y ±8 밖 지면 부재 → 무한 낙하 | `levee.y0/y1 ±8 → ±20`(둔치와 동일 폭), `thick 0.5 → 3.5` | 저면 −3.5 < 둔치 상면 −3.25 → 측면에서도 공중 판이 아님. 옹벽 세그 A/B/C 가 y −20..20 을 자동 추종(피어B 0.4 m, 북측 벽 14.0 m) |
| — | 옹벽 밑 5 cm 틈 + 둑길과 동일 평면 Z파이팅 | `retwall.z_bot −3.2 → −3.5`, `z_top 0.0 → 0.02`(콘크리트 코핑) | 벽 저면이 둔치 슬래브 내부로 매입, 상면 2 cm 돌출로 동일 평면 해소 |
| B3 | far_bank·water y ±40 정합(감독 선수정) | 재검증만 | SMOKE 플레이트 표: 수면(−3.40)보다 낮은 인접 플레이트 **없음** ✔ |

> 계단(20단·낙차 3.2)·램프 경사 8%·낙차 3.2 = **대비쌍 불변**(SMOKE OK).

### 드레싱 요소
- **건너편 아파트 3동** E/F/G (x 88..96, h 42/48/38, floors 14/16/13, `base_z=−3.2`) — "한강" 판독의 8할
- **교량 실루엣**: 상판 x 40..44 × y ±40, 상면 z 6.0 두께 1.2 + 교각 4본 (x=42, y −28/−9/9/28, r1.2, −3.6..4.8) 알베도 (0.05,0.05,0.055)
- 억새/갈대 띠 3 (build_hedge, tint (0.42,0.44,0.26), base −3.25) / 자전거도로 갓길 가로등 4 (x 6/14/22/28, y −4.3) / 벤치 4 / 볼라드 4(둔치 2 + 둑길 램프 진입 2) / km 안내표지 1
- 소품 배치 검산: 램프 성토 풋프린트(y 2.4..6.0, x < 21.5)·계단 풋프린트(x 0..6.4, y ±2) 침범 **0건**, 전부 둔치 플레이트(x 0..30, y ±20) 안. 감사 D-5 의 (8,4.5)·(16,4.5) 벤치와 (1.0,2.9)·(1.0,3.7) 볼라드는 확폭된 램프 성토 **내부**가 되어 (8,8)·(16,8)·둑길 램프 진입부로 **재배치**함.
- `build_skyline` 은 `hazard_stairs=False` 대조군에서도 유지(지평 동일).

### 보류·제안
- 레인 A 북측 에지(y=6.0)·레인 B 남측 에지(y=2.4)의 **바깥쪽 측면 낙차**(최대 3.25 → 1.65 m)는 성토 사면 없이 수직면으로 남아 있다. 실제 둔치 램프는 성토 사면 또는 난간이 있음 → 외측 파라펫 2줄 추가 여부는 감독 판단(특색 훼손 우려는 낮음).
- 램프 성토 박스는 평행사변형이라 −3.75(둔치 저면) 아래로 최대 1.2 m 삐져나온다. 모든 프리셋·미장센 시점(eye z ≥ −0.8)에서 비가시.
- `cue_railing=True` 경로(계단 우측 레일)는 미검증(기본 False 유지).

---

## scene16_canopy_shadow — 캐노피 그림자 트렌치

### 수정 결함 (좌표 근거)

| # | 결함 | 조치 | 검산 |
|---|---|---|---|
| A1 | 하부 통로가 **막다른 골목**(x=14 에 전폭 Wall_E) — 2.1 m 내려가 9.5 m 걷고 탈출구 없음 | `Wall_E` 삭제 + **동측 출구 계단**(상승) 신설: `build_rot_group(pivot=(16.24, 0), 180°)` 안에 하강 14단(로컬 x0=14.0, riser 0.15, tread 0.32)을 지어 미러링. 벽·트렌치 동단 `wall.x1 14.0 → 18.48`, 보도 `walk.x_e 20 → 22`, 잔디 컷 `gx1 14.5 → 19.0` | 로컬 x14(z=0) → **월드 18.48**(보도 상면, 첫 단차 0.15) / 로컬 x18.48(z=−2.10) → **월드 14.00**(통로 상면 −2.10 과 flush, 단차 0.00). 점자띠 로컬 13.7..14.0 → 월드 18.48..18.78(보도 위) ✔ 노징·난간도 같은 그룹에서 미러 |
| B1 | 피트 둘레 난간 **0.15 m 부유**(포스트 하단 0.15, y=1.9 지점 지면은 보도 0) | `perim_rail.y 1.9 → 1.65`(파라펫 벽 y 1.5..1.8 의 중심선) → 포스트가 파라펫 상면(0.15)에 안착. `x1 4.48 → 18.48` 로 트렌치 전 구간 방호 | 공극 0.00 m. 캐노피 기둥 내면 y=1.740 vs 난간 외면 y=1.680 → 이격 0.060 m(간섭 없음) |
| C | 원경이 +X 한쪽만 폐쇄 | 건물 C를 x 24→26 으로 이동(확장된 보도와 이격), 서측 건물 D(x −30..−24, h15) 추가 | 지면 잔디 −0.03 위 기본 base_z(셸 −1.0까지) → 매입 ✔ |

### 드레싱 요소 (`cue_scene_dressing`) — 목표 판독어 "지하보도 입구가 있는 도심 광장"
- **출입구 사인 게이트**: 기둥 2 (x=−1.2, y=±2.0, 0.12×0.12×2.6) + 인방 (2.4×0.15×0.45, z_c 2.55), 다크 상수색 (0.03,0.05,0.09) — 캐노피 지붕(x ≥ −1.0)과 0.125 m 이격
- 볼라드 6 (−1.0/1.5/4.0, y ±3.2) / 벤치 3 / 가로등 2 (−3, 6.5)·(9, −6.5) / 화단 2 증설(총 4: +(12,5)·(2,6.5)) / 생울타리 2 / 보도 밴드 2줄 (y ±6.0, 폭 0.45, 알베도 (0.05,0.05,0.055))
- 전 소품 접지 검산: 보도(x −12..22, y ±8) 안 ∧ 트렌치 개구(x 0..18.48, |y|<1.8) 밖 → **이상 0**

### 보류·제안
- B5 `approach` 컷에서 캐노피 지붕(z 2.6)이 프레임 밖 — 카메라 사안이라 미적용(eye z 또는 pitch 조정 권장).
- 동측 계단이 생기며 씬 프림이 늘고(≈+90) 트렌치 길이가 18.48 m 로 확장 → 그림자 밴드 특색(B4, SUN_AZ_OFFSET=146.5)은 서측 계단 기준이므로 불변이나, 재렌더 시 동측 계단이 정오광에 노출되는지 확인 요망.
- A4 광장 외곽 잔디 0.03 m 단차는 무시(경미).

---

## 공통 메모
- `SCENE_CONFIG` 7키·`NEGOBS_*` env 규약·한국어 주석 스타일 유지, 신규 기하는 전부 기존 토글(`hazard_stairs` / `cue_*`) 소속.
- 신규 재질은 전부 상수색(어두운 것은 알베도 0.02~0.06) 또는 기존 텍스처 역할 재사용. 신규 텍스처 역할 추가는 scene21 `grass` 1건뿐(TEX 기존 항목).
- 스캐터 없음. 추가 프림 개략: scene16 +약 90, scene17 +약 90, scene20 +약 80, scene21 +약 70.
