# ACCOUNTING — v3 창의 분모·모집단 단일 정본

- **작성**: Claude Code · 2026-08-23 · **과업**: v3 창 P-1
- **지위**: v3 창의 **모든 분모·모집단·집계 단위의 단일 참조**. 다른 문서가 분모를 말하면 본
  문서를 가리키고, 본 문서와 충돌하면 **본 문서가 이긴다**. 근거: `Docs/experiment/V3_DESIGN_0823.md:354`
  ("분모는 ACCOUNTING 단일 참조") · 동 `:374` (§12 전 항목 본 문서 등재 의무).
- **갱신 규칙**: **append-only**. 라운드가 착지할 때마다 §3에 행을 덧붙인다. 기존 행은 고치지 않고,
  정정이 필요하면 정정 행을 아래에 추가하고 구행에 취소선 대신 "→ (정정: N행)" 주석을 단다.
- **원자료**: `experiments/weekend_0823/cue_audit/VERDICT_CENSUS_v2.csv` (84행) ·
  `experiments/dayrun_0820/dataset_manifest_v2_full.json` (2,832행) ·
  `experiments/dayrun_0820/split_v2_full.json`.
- **행 번호 규약**: 본 문서의 `파일:행` 인용은 **08-23 정정 배너 append 이후 상태 기준**이다
  (`MORNING_REPORT_0823.md` +7행 · `CUEOFF_RESULT_v2.md` +8행 이동). 배너 이전 인용과 대조할 때 이 오프셋을 적용한다.

---

# §1 CUE-OFF 판정 census — 정본 회계

## 1.1 결론 요약

**두 문서는 실제로 불일치하지 않는다.** `MORNING_REPORT_0823.md`의 30과
`CUEOFF_RESULT_v2.md`의 24는 **서로 다른 모집단의 수치이며, 둘 다 `CUEOFF_RESULT_v2.md` 안에
동시에 인쇄돼 있다.** 24는 census가 아니라 **v1↔v2 동일-조건 비교용 제한 모집단**이다.

| | 값 | 실제 집계 단위 | 문서 위치 |
|---|---|---|---|
| "30" | 0 / 2 / 6 / 4 / **VOID 18** | primary leg 전 칸 (**정본 census**) | `MORNING_REPORT_0823.md:95-102`, `:135` · `CUEOFF_RESULT_v2.md:63`, `:69` |
| "24" | 0 / 2 / 6 / 4 / **VOID 12** | 위 30칸 **− s20fix 수리 라운드 6칸** = v1이 가졌던 칸만 | `CUEOFF_RESULT_v2.md:28-30` |

→ **정본은 30이다.** 24는 "v1은 8 SHORTCUT/16 UNDECIDED로 읽었는데 v2는 뭐라 읽나"를 **같은 칸
위에서** 비교하기 위한 수치로, 수리 라운드(v1 시점에 존재하지 않았음)를 뺀 것이다. 두 수는
경쟁 관계가 아니라 포함 관계다.

**정본 선정의 사전등록 근거 — 명시**: 두 집계는 각자 내부적으로 정합하지만 **단위가 다르다.**
이럴 때 정본은 **`PREREG_CUEOFF.md`가 등록한 모집단과 일치하는 쪽**이다. 등록 모집단은
§4.2의 paired strict-H 프레임 위에서 정의된 **등록 칸 전수**이고, 수리 라운드
`260823_cueoff_s20fix`는 **렌더되기 전에** A2-10으로 사전 등록됐다(`PREREG_CUEOFF.md:596-627`,
당시 PNG 0장). 따라서 **등록 모집단은 s20fix를 포함하는 30칸**이며, 그것을 뺀 24칸은
등록 모집단이 아니라 **v1 판독 시점의 스냅샷**이다. 여기에 A2-7이 접은 대응치 21칸을
**함께** 인쇄할 의무를 더한다(`PREREG_CUEOFF.md:548-554`).

## 1.2 칸(cell)의 정의 — 등록된 집계 단위

> **1 cell = (label set × band × scene × model)** — `CUEOFF_RESULT_v2.md:57`

- label set = {`lineage`, `twin`} (2) · band/stem = {`260823_cueoff`(=e), `260823_cueoff2`(=e2),
  `260823_cueoff_s20fix`(=수리)} · model = {`rgb`, `depth`, `b2`} (3)
- **leg는 칸의 차원이 아니다.** `primary` / `primary-twinconditional` / `secondary`는 **같은 칸을
  세 번 판정한 결과**이며, census는 **leg별로 따로 낸다**(§1.4 표의 5행이 그 구조다).
- 판정 모집단은 프레임 수준에서 **양 팔 모두 strict-H인 paired 프레임**뿐이다 —
  `PREREG_CUEOFF.md:175-177` (§4.2). 칸의 `n_paired`가 그 값이다.

**primary leg 30칸의 구성** (`VERDICT_CENSUS_v2.csv`, `leg=primary` 30행):

| label set | band/stem | scene | model | 칸 |
|---|---|---|---|---|
| lineage | `260823_cueoff` | scene12 / scene17 / scene20 | ×3 | 9 |
| lineage | `260823_cueoff2` | scene12 | ×3 | 3 |
| lineage | `260823_cueoff_s20fix` | scene20 | ×3 | 3 |
| twin | (동일 3 stem) | (동일) | ×3 | 15 |
| | | | **합** | **30** |

## 1.3 행 수준 재구성 — 왜 v1이 24였나

v1의 모집단은 `CUEOFF_RESULT_v2.md:84-85`에 **칸 이름으로 전수 열거돼 있다**:

> v1 primary verdicts: 8 SHORTCUT (lineage: s12·e rgb, s17 depth, s17 b2, s20 depth,
> s12·e2 rgb; twin: s17 depth, s17 b2, s20 depth) and 16 UNDECIDED.

8 + 16 = **24칸**. `MORNING_REPORT_0823.md:102`도 같은 값을 독립적으로 인쇄한다 —
`| 판정 가능한 칸 | 24 | **12 / 30** |` (v1 열 = 24).

산술만으로는 n=24·VOID 12를 만드는 슬라이스가 세 가지다. **위 열거가 두 개를 배제한다**:

| 후보 슬라이스 | n | VOID | 판정 |
|---|---|---|---|
| (a) `s20fix` stem 제외 | 24 | 12 | **채택** — v1 열거와 유일하게 정합 |
| (b) `n_paired=6` 행(원본 scene20) 제외 | 24 | 12 | **기각** — v1 열거에 `s20 depth`가 lineage·twin 양쪽에 있다 |
| (c) `n_paired=0` 행(twin scene12) 제외 | 24 | 12 | **기각** — v1의 16 UNDECIDED에 `s12 twin, 양 밴드, 전 모델` 6칸이 포함(`CUEOFF_RESULT_v2.md:92`) |

**결정적 근거**: `260823_cueoff_s20fix`는 v1 판독(07:20) 시점에 **PNG 0장**이었다 —
`PREREG_CUEOFF.md:598-599` ("`dataset/260823_cueoff_s20fix_*` contains **0 PNG files**
… 07:41:57"). v1이 셀 수 없었던 칸이므로 v1 모집단 = 30 − 6 = 24다.

**추가된 6칸은 전부 VOID다** (`VERDICT_CENSUS_v2.csv` lineage/twin × s20fix × 3모델,
`n_paired=27`). 따라서 `VOID 12 → 18`, `n 24 → 30`이고 **다른 네 판정 값은 한 칸도 움직이지
않는다**(0/2/6/4 불변). 이것이 두 수치가 같은 실험의 두 시점 뷰라는 산술적 증거다.

## 1.4 정본 census 표 (primary rule, A2 집계)

`VERDICT_CENSUS_v2.csv` 재집계로 5행 전부 검산 완료 — `CUEOFF_RESULT_v2.md:61-67`과 **일치**.

| 모집단 | CUE EVIDENCE | SHORTCUT | NO-EFFECT | UNDECIDED | VOID | n | 지위 |
|---|---|---|---|---|---|---|---|
| **primary, 전 블록** | **0** | 2 | 6 | 4 | **18** | **30** | **정본 헤드라인** |
| primary, 중복 블록 접음 (A2-7) | 0 | 2 | 4 | 3 | 12 | 21 | **정본 — 독립 반복 주장 시 이 수를 쓴다** |
| primary, 판정된 칸만 (VOID 제외) | 0 | 2 | 6 | 4 | — | 12 | 파생(분자) |
| twin-conditional leg | 0 | 5 | 7 | 0 | 18 | 30 | 별도 leg |
| secondary B1 leg | 4 | 1 | 0 | 1 | 18 | 24 | **승격 금지** (`PREREG_CUEOFF.md:195`) |
| *(참고)* v1 비교용 제한 모집단 | 0 | 2 | 6 | 4 | 12 | 24 | census 아님 — §1.3 |

- **판정 가능 비율 = 12/30 = 40.0 %** (접은 기준으로는 9/21 = 42.9 %).
- **사전등록 1차 규칙의 답은 CUE EVIDENCE 0** — v1·v2 동일. CUE EVIDENCE 4건은 전부
  secondary B1 leg이고 사전등록이 승격을 금지한다(`PREREG_CUEOFF.md:195`).
- **두 수(30/21)를 함께 인쇄할 의무**가 A2-7에 있다 — `PREREG_CUEOFF.md:548-554`
  ("the census reports both the folded and unfolded counts"). 헤드라인만 인쇄하는 것은 위반이다.

## 1.5 VOID 18칸의 출처 — 전수

세 사유 × 6칸. `VERDICT_CENSUS_v2.csv` `leg=primary` 행에서 직접 확인.

| # | 칸 (label set × stem × scene × 3모델) | n_paired | 무효 사유 | 근거 |
|---|---|---|---|---|
| 1 | lineage + twin × `260823_cueoff` × scene20 | **6** | paired-H 6 < 10 | `PREREG_CUEOFF.md:215` (§4.5-3) · A2-2가 `verdict()` 내부로 강제 (`:461-465`) |
| 2 | lineage + twin × `260823_cueoff_s20fix` × scene20 | 27 | `polar_gt` 바이트 동일성 실패 (판정 프레임 6/27) | `PREREG_CUEOFF.md:214` (§4.5-2) · A2-13 **눈가림 사전 룰링** (`:692-720`) |
| 3 | twin × `260823_cueoff`+`cueoff2` × scene12 | **0** | paired-H 0 — A팔 프레임이 교정 발자국에서 tier V | `CUEOFF_RESULT_v2.md:72`, `:46-50` |

- #2는 **모델을 돌리기 전에** 판정됐다 — `PREREG_CUEOFF.md:697` ("No model has been run on this
  round"). 결과를 보고 만든 무효가 아니다.
- #2의 기하 근거: `G2_ADJUDICATION.csv:13-14` — scene20 B1/B2 vs A에서 `dz_max` 3.16 m,
  `cells_in_drop_footprint` 1,624 / 14, `lineage_gt_frames_differ` **6/48**(전 프레임 기준 —
  무효 판정에 쓰인 **판정 프레임 기준은 6/27**, `PREREG_CUEOFF.md:705-706` · `CUEOFF_RESULT_v2.md:71`),
  발자국 `cells_raw` 60,659 → 61,961. `cue_railing`이 scene20에서는 **구조물**이라 위험 기하
  자체를 바꾼다.
- 최대 후보 s20fix `b2` **+0.506**(3/3 시드)은 위 규칙으로 **VOID·비승격**이며, 숫자는
  인쇄하되 세지 않는다 — `CUEOFF_RESULT_v2.md:528`, `MORNING_REPORT_0823.md:573`.

## 1.6 A2-7 접기(fold) — 무엇이 접히나

scene17·scene20은 두 label set에서 tier·점수가 동일해 `twin` 블록이 `lineage` 블록의
**바이트 단위 사본**이다(`PREREG_CUEOFF.md:548-551`). 8개 scene×labelset 블록으로 보이지만
**실제로는 4개**다. `VERDICT_CENSUS_v2.csv`의 `folded` 열이 정확히 이 플래그다.

접히는 9칸 = twin×scene17 (3) + twin×scene20 원본 (3) + twin×scene20 수리 (3) → 30 − 9 = **21**.
검산: NO-EFFECT 6−2=4, UNDECIDED 4−1=3, VOID 18−6=12 → **0/2/4/3/12, n=21**. 표 2행과 일치.

## 1.7 인용 사고 방지 — "24"와 "VOID 12"는 각각 두 곳에 나온다

숫자만 보고 인용하면 반드시 섞인다. 인용 시 **모집단 이름을 함께 적는다.**

| 숫자 | 출현 ① | 출현 ② |
|---|---|---|
| n = 24 | v1 비교용 제한 모집단 (`CUEOFF_RESULT_v2.md:28-30`) | secondary B1 leg, VOID 18 (`:67`) |
| VOID = 12 | 위 v1 비교용 (n=24) | A2-7 접은 census (n=21) (`:64`) |

**규약**: 이후 모든 문서는 census를 인용할 때 `primary/전블록 30칸` 또는 `primary/접음 21칸`처럼
**leg + 접기여부 + n**을 붙인다. 맨숫자 인용 금지.

---

# §2 등록된 정밀 정의 9선 — 규칙 · 운영 귀결 · 준수 대상

출처: `Docs/experiment/V3_DESIGN_0823.md:374-410` (§12). 본 절이 DZ §4~§8과 충돌하면 **본 절 우선**.

## §2-1 G7 재라벨과 A/B 공정성

- **규칙**: "test-core 프레임 불변"은 **이미지 불변이지 정답지 불변이 아니다.** 재융합·재라벨은
  boost 영향 씬의 GT를 바꾼다. 재라벨 완료 즉시 **v2 체크포인트 전부를 교정 GT로 재채점**하고,
  모든 A/B는 **양측 공히 교정 GT 기준으로만** 비교한다. v3만 교정 GT로 채점하면 A/B는 **무효**다.
- **운영 귀결**: test-core 816프레임 중 **480프레임(5개 씬 × 96)이 boost 라운드**이며 이것이 G7
  노출면이다 `[measured]` — scene05·07·14·15·18은 각각 main 48 + boost 96, sceneC2·N3는 main 48뿐.
  boost 라운드는 `fuse_heightmap.py`를 통과한 적이 없다(`PREREG_CUEOFF.md:352`). 공표된 v2 표(구 GT)
  와의 델타는 **별도 리포트**로 내고 A/B 표에 섞지 않는다.
- **준수 대상**: 헤드라인 A/B 표 · FA-정합 표 · 계기판 ①②③ 전부 · v2 재채점 산출물.

## §2-2 반사실 쌍 명명 — (A,C)/(A,B)/(A,D)

- **규칙**: 2×2는 반사실을 둘 내장한다. **(A,C) = 위험만 제거·단서 보존** (신off 세대) ·
  **(A,B) = 단서만 제거·위험 보존** (CUE-OFF 세대) · **(A,D) = 구off 세대** (v2 트윈의 역사적 대응,
  비교 서술 전용). **"트윈"을 쌍 명시 없이 쓰지 않는다.**
- **세대 통일 규칙**: 계기판①의 A/B는 **양측 모두 (A,C)** — test-ext 계기판 씬의 C팔을 제작하고
  v2 체크포인트도 (A,C)로 재채점한다.
- **어느 계기판이 어느 쌍을 쓰나**:

  | 계기판 | 재는 층 | 쌍 |
  |---|---|---|
  | ① 트윈-조건부 recall | ③ 검출 | **(A,C)** |
  | ② CUE-OFF 용량-반응 | ①↔③ | **(A,B)** |
  | ③ N-cue 함정 FA | ④-a | (팔 C·D 단독 FA — §2-3) |

- **운영 귀결**: v2 공표치 **44.7 % / 0 %는 (A,D) 기반**이므로 **참고치로 강등**한다. (A,C)
  기준점은 재채점으로 **새로 만든다**. 세대가 다른 수치를 A/B 표 같은 칸에 넣지 않는다.
- **준수 대상**: 계기판① 전 산출물 · v2 재채점 리포트 · 논문 §결과의 트윈 서술.

## §2-3 FA의 분리 집계 — FA_C / FA_D

- **규칙**: **FA_D**(무위험 − 단서 = 청정 오경보)와 **FA_C**(무위험 + 단서 = 코퍼스 내 N-cue성
  오경보)를 **항상 분리 보고**한다.
- **해석 규약** (표준 보고에 이 분해를 포함):
  - **FA_D = 순수 씬-연합의 직접 게이지** (단서·위험 모두 부재)
  - **FA_C − FA_D = 단서-유발 오경보 몫**
- **v2 `frame_fa_off`와의 대응**: v2 off팔은 **위험 제거·장식 보존**이므로 `frame_fa_off`는
  **FA_C의 근사**이지 FA_D가 아니다. **v2에는 FA_D 표본이 없다** → v2 위에서는 위 분해가
  계산 불가이며, D팔은 v3에서 처음 그 분해를 가능하게 한다. 근사 기준점 **.681**(장식 보존 off팔)은
  §2-8에 의해 **보조 참고치로 격하**된다.
- **주의 (분모 구멍)**: v2의 `none_in_fov`(온팔·GT 전셀 음성)는 recall에도 `frame_fa_off`에도
  들어가지 않는다 — 어느 공표 분모에도 없다. `experiments/mainrun_0819/METRICS.md:1105`,
  test 81/408 = 19.9 %. v3의 FA 정의는 이 구멍을 명시적으로 처리한다(§3.4).
- **준수 대상**: 계기판③ · FA 이중 트랙(DZ §8.1) · FA-정합 곡선 · 모든 FA 인용.

## §2-4 무시 마스크의 사거리

- **규칙**: 무시 마스크는 **훈련 손실에만** 적용된다. **평가 분모·recall·FA 정의는 v2와 동일하게
  유지**한다(본 문서 단일 정의).
- **운영 귀결**: **분모를 줄여 얻은 recall은 개선이 아니라 아티팩트다.** 평가 코드에서 마스크가
  분모에 닿는 경로가 있으면 그것은 버그이며 게이트로 잡는다.
- **준수 대상**: 훈련 손실 코드 · 평가 코드 · §3의 모든 분모 행.

## §2-5 단서 존재 판정의 단위 — 프레임 단위

- **규칙**: 단서-조건부 지도(DZ §5.1)의 판정은 **프레임 단위 가시성**이다. 렌더 산출물로 판정:
  **세그 마스크의 cue 클래스 픽셀 ≥ k**, **k는 게이트로 사전 고정**. 세그 부재 시 **기하 투영 검사**로
  대체(fallback).
- **씬 설정 기반 판정**(주말 `H_CUE_AUDIT` 방식)은 **상한 참고치로만** 쓴다.
- **운영 귀결**: 씬 단위로 지도하면 **화면 밖 단서 프레임에 양성을 주게 되어 억지 지도가 부분
  재발한다.** k는 **사용 전에** 고정하고 해시로 봉인해야 하며, 결과를 본 뒤 k를 움직이면 무효다.
- **미결**: **k 미확정** — §3.4 미결 항목 1. k 고정 전에는 프레임 단위 지도를 착수하지 않는다.
- **준수 대상**: 단서-조건부 지도 라벨 생성기 · 계기판② · cue 커버리지 감사.

## §2-6 cue_* 배선 커버리지 = 보장이 아니라 감사 대상

- **규칙**: 씬에 `cue_*` 토글이 있다고 가정하지 않는다. **로컬 커버리지 감사가 렌더 계획(P-5)의
  선행 입력**이다. 씬 설정은 git 미포함이므로 문서가 아니라 **로컬 파일을 실측**한다.
- **주말 실증**: **s14 가드는 구조물이라 토글 불가**, CUE-OFF 가용 씬은 **3개뿐**이었다.
  scene20의 `cue_railing`은 구조물이라 제거 시 위험 기하가 바뀌어 §4.5-2에 걸렸다(§1.5 #2).
- **운영 귀결**: 신규·개편 씬은 **`cue_*` 토글 + 가드 구조물/장식 사전 분류**를 표준 장비로 제작한다.
  분류는 렌더 전에 끝나야 하며, 렌더 후 분류는 사후 선택이 된다.
- **준수 대상**: P-3(가드 분류) · P-5(렌더 계획) · v3 씬 제작 표준.

## §2-7 P-4 "v2 재현 검증"의 정확한 의미

- **규칙**: **같은 숫자의 재출현이 아니라 "지표 교체가 결과를 만들지 않았음"의 증명**이다.
  신지표를 **v2 기존 런에 적용**하고 **구지표 선택 대비 성적 차이를 보고**한다.
- **절차**: 에폭 체크포인트가 로컬 보존 → **재선택**. 미보존 → **resnet34 3런 한정 재훈련**.
- **합격 기준**: **차이가 시드 σ 이내면 통과.** (σ는 시드 산포=런 재현성의 잣대이며 씬-모집단
  신뢰구간 주장이 아니다 — DZ §7.2.)
- **운영 귀결**: 실패해도 그것이 결과다. "재현 안 됨"을 지표 문제로 덮지 않는다.
- **준수 대상**: P-4 리포트 · 지표 교체를 인용하는 모든 표.

## §2-8 계기판 ③의 v2 기준점 생성

- **규칙**: **"v2에 N-cue 표본 없음"으로 끝내지 말 것.** **v2 체크포인트를 test-ext
  (N-cue·측방·paired-H)에 zero-shot 재채점**하여 **진짜 before**를 만든다. test-ext는 v2·v3
  양측 모두 미학습이므로 공정 비교가 성립한다.
- **격하**: 장식 보존 off팔 FA **.681**은 **보조 참고치**로 내린다.
- **부칙 3건**:
  1. `sceneC2` 재설계판은 **진단·대조 전용**으로 유지 (훈련 미편입).
  2. **레시피 v2의 정본은 문서 서술이 아니라 `runs/*/config.json` + 코드다.**
  3. 구판 `Docs/experiment/Status/PROJECT_STATE_0823.md` 상단에 대체 배너 1줄 append —
     **본 P-1에서 이행 완료** (해당 파일 2행).
- **준수 대상**: 계기판③ · v2 재채점 파이프라인 · 레시피를 인용하는 모든 서술.

## §2-9 계기판의 무대 = test-ext 4팔

- **규칙**: 계기판 ①②③의 v2/v3 비교는 **양 모델이 공히 학습하지 않은 test-ext 씬의 4팔
  (평가 전용 렌더)에서만** 수행한다. **test-core는 헤드라인 표·FA-정합 전용이다.**
- **운영 귀결**: **훈련 코퍼스의 B·C팔로 계기판을 재면 v3만 분포-내가 되어 비교가 구조적으로
  기운다.** 계기판 수치를 train/val 팔에서 뽑은 순간 그 계기판은 무효다.
- **준수 대상**: 계기판 ①②③ 전 산출물 · test-ext 렌더 계획(4팔 전부 제작 의무, DZ §4.3 `:243`).

---

# §3 상시 모집단 표

## 3.1 test-core — 7씬 · 프레임 불변

- **정의**: v2/v3 A/B의 공통 잣대. **이미지 프레임 불변** (정답지는 불변 아님 — §2-1).
- **출처**: `experiments/dayrun_0820/split_v2_full.json` (`test` 키) +
  `experiments/dayrun_0820/dataset_manifest_v2_full.json` (`scene_id`/`tier` 필드). 재집계 검산 완료.

| 항목 | 값 |
|---|---|
| 씬 (7) | `scene05` `scene07` `scene14` `scene15` `scene18` `sceneC2` `sceneN3` |
| 총 프레임 | **816** |
| on팔 / off팔 | **408 / 408** (트윈 짝 정확히 일치) |
| tier (on팔 408) | V **180** · E **45** · H(strict) **96** · H_weak **6** · none_in_fov **81** |
| 씬별 프레임 | scene05/07/14/15/18 각 **144** (main 48 + boost 96) · sceneC2/sceneN3 각 **48** |
| boost 노출 프레임 | **480** (G7 재융합 영향면 — §2-1) |
| 헤드라인 recall 분모 | on팔 중 **그리드 내 양성 칸 ≥ 1**인 프레임 = 408 − 81 = **327** |
| 조건화 공개 의무 | 탈락 **81/408 = 19.9 %**를 같은 표에 인쇄 (`experiments/weekend_0823/redteam/R2_method.md:226`) |

## 3.2 v2 코퍼스 — 2,832 프레임

- **출처 원장**: `experiments/dayrun_0820/dataset_manifest_v2_full.json` (`frames[].tier` 집계).
- **독립 재검증**: `experiments/weekend_0823/redteam/R3_repro.md:26-38` (§1.1, 6범주 전항 ✓) ·
  재현 코드 `R3_repro.md:313-320` · `experiments/mainrun_0819/METRICS.md:439` ·
  `experiments/mainrun_0819/DECISIONS.md:195` (sha `9bc44d4efbe6e83e`).

| tier | n |
|---|---|
| V | **693** |
| E | **72** |
| H (strict) | **243** |
| H_weak | **30** |
| none_in_fov | **378** |
| off | **1416** |
| **합** | **2832** |

- **트윈 항등식**: on팔 = 693+72+243+30+378 = **1416** = off팔 **1416** ✓
- split별 분해 `[measured]`: train **1536** (V438·E21·H141·Hw21·none147·off768) ·
  val **288** (V75·E6·H6·Hw3·none54·off144) · test **816** (§3.1) · hold **192** (none96·off96).
- **이중계상 0**: `merge_corpus.py`의 `::<round>` 접미사 감사 통과 — `R3_repro.md:42-46` (§1.2).
- v1 코퍼스(참고): V438·E36·H45·H_weak12·none261·off792 = **1584** (33씬 × 48컷).

## 3.3 CUE-OFF 판정 모집단 (요약 — 상세 §1)

| 모집단 | n | 정본 여부 |
|---|---|---|
| primary leg 전 블록 | **30칸** | **정본 헤드라인** |
| primary leg 접음 (A2-7) | **21칸** | **정본 — 반복 주장용** |
| primary 판정 가능 | 12칸 (40.0 %) | 파생 |
| secondary B1 leg | 24칸 | 승격 금지 |
| 프레임 수준 판정 단위 | 칸별 `n_paired` (0 / 6 / 24 / 27) | §4.5-3 하한 **10** |

## 3.4 v3 모집단 — 미기입 (라운드 착지 시 append)

| # | 모집단 | 상태 | 채우는 시점 |
|---|---|---|---|
| 1 | **단서 존재 판정 임계 k** (세그 cue 픽셀 하한, §2-5) | **미확정 — 게이트로 사전 고정 필요** | 단서-조건부 지도 착수 **전** |
| 2 | test-ext 씬 목록·프레임 수·4팔 분해 (A/B/C/D) | 미기입 | P-5 렌더 계획 확정 시 |
| 3 | test-ext paired-H 수 (씬별) | 미기입 | test-ext 렌더 완료 시 |
| 4 | FA_C / FA_D 분모 (무위험 프레임 수, 팔별) | 미기입 | 위와 동시 |
| 5 | 코퍼스 v3 train/val 프레임 회계 (G7 재융합 후 tier 재집계) | 미기입 | P-2 재융합·재라벨 완료 시 |
| 6 | 교정 GT 기준 v2 재채점 분모 (test-core 816 위) | 미기입 | P-2 재채점 완료 시 |
| 7 | 무시 마스크 적용 셀 수 (**훈련 손실 전용** — 평가 분모 불변, §2-4) | 미기입 | v3-A 훈련 착수 시 |
| 8 | `none_in_fov` 상당 tier의 v3 처리 규약 (§2-3 분모 구멍) | 미기입 | test-ext 정의 확정 시 |

**append 규약**: 위 행을 채울 때 ① 원장 파일 경로 ② 재집계 명령 또는 스크립트 ③ 측정일자를
반드시 함께 적는다. 세 가지가 없는 숫자는 본 문서에 들어오지 못한다.

---

# §4 정정·추가 로그 (append-only)

## 4.1 【정정 · 08-23】 §2-3의 "v2 `frame_fa_off` = FA_C 근사"는 과단순화 — off팔은 단일 세대가 아니다

P-3 커버리지 감사 실측(`experiments/v3_0823/CUE_COVERAGE.md` · `code/hazgate.py`): v2 off팔에서
단서 빌더가 hazard 게이트에 묶이지 않은 씬 **8개**는 단서가 그대로 서 있고(≈**C팔** 성격),
**25개**는 위험과 함께 소멸한다(≈**D팔** 성격). 따라서 `frame_fa_off`는 FA_C 근사가 아니라
**씬별 혼합**이며, v2 위에서 FA_C/FA_D 대응은 **씬 단위 분류표를 거쳐서만** 가능하다
(분류표 = CUE_COVERAGE). §2-2의 "(A,D) = 구off 세대" 서술도 같은 정정을 받는다 —
구off는 씬에 따라 (A,C)형과 (A,D)형이 섞인 **비균질 세대**다. v2 재채점·계기판 기준점
산출 시 이 8/25 분류를 분모에 반영할 것.

## 4.2 【추가 · 08-23】 교정 GT(정본 B) 기준 v2 코퍼스 tier 재집계

원장 `experiments/v3_0823/dataset_manifest_v2corr.json` · 재집계 `experiments/v3_0823/code/g7_*.py`
· 측정일 08-23. §3.2의 구 GT 행은 "구 GT 회계"로 유지한다(공표 표와의 대조용).

| tier | 구 GT | 교정 GT(B) | Δ |
|---|---|---|---|
| V | 693 | **801** | +108 |
| E | 72 | 72 | 0 |
| H (strict) | 243 | **195** | −48 (전량 scene12) |
| H_weak | 30 | **33** | +3 |
| none_in_fov | 378 | **315** | −63 |
| off | 1416 | 1416 | 0 |

test-core 영향: V 180→**219** · H_weak 6→**9** · none 81→**36** → 헤드라인 recall 분모
327→**372**. **test H 96 불변**(s14+s15). val V 75→**96**. (G7_RELABEL.md §3)

## 4.3 【추가 · 08-23】 CPU-4 신규 결함 4건의 처분 (D57)

| # | 결함 | 처분 | 담당/시점 |
|---|---|---|---|
| 1 | 트윈 tol 3자 불일치 (문서 0.15 / `run_queue_v2.sh:126` `--tol 0.15` / 출하물 헤더 `1e-06`) | **재현 1회로 생성 설정 확정** — 체크포인트 1개에 두 tol로 twin_analysis 재실행, 출하 md와 diff. D27(H/E 허용복원 쌍 0)로 H/E 주장은 tol 무관임을 함께 명시 | v2 재채점 완료 직후 (점수 덤프 재사용, CPU) |
| 2 | `none_in_fov`가 게이트 후 GT로 판정 (`labeler.py:526`) — 스텝게이트 배제분이 "그리드 밖" 이름을 뒤집어씀 | v3 라벨러 요구사항: **pre-gate/post-gate 사유 분리 기록** (기존 라벨 소급 수정 없음 — 구 GT 회계에 각주만) | P-5 라벨링 사양에 등재 |
| 3 | 선택지표 분모 이중 정의 (`n_val_strict_h` vs `h_frame_recall`) | **P-4 필수 입력** — 새 선택식은 단일 분모 정의를 코드 한 곳에서 참조 | P-4 착수 시 |
| 4 | 게이트 번호 충돌 (라벨러 G1–G5 vs CUE-OFF G0–G7) | **접두어 분리 채택**: 라벨러 게이트 = `LG*`, CUE-OFF 게이트 = `CG*`, v3 신설 게이트 = `VG*`. 소급 개명 없음 — 신규 문서부터 적용, 구 문서 인용 시 "(구 G2=LG2)" 병기 | 즉시 (본 행이 규약) |

## 4.4 【정정 · 08-23】 §4.2의 test-core 행 산술 오류 — none 81→39, 분모 327→369

§4.2에 적힌 "none 81→36 · 분모 327→372"는 **오기**다(검산: 219+45+96+9+39=408 ✓, 36이면 405≠408).
정답: **none_in_fov 81→39 · 헤드라인 recall 분모 327→369**. 원장 실측(`V2_RESCORE.md` §0 무결성
게이트)과 `G7_RELABEL.md` 집계 일치 확인. §4.2의 코퍼스 표(2832행)는 정확(재검증: hazard
1038→1101). D55의 "분모 327→372" 기재도 같은 오류 — D59에서 정정. (원인: 오케스트레이터
수기 계산, 이후 분모 숫자는 스크립트 산출만 등재.)

## 4.5 【추가 · 08-23】 FA 센서스 모집단 등재 (CPU-2, FA_CENSUS.md §8 제안 승인 — **전부 구 GT 측정**)

FA 사건 = (checkpoint, frame, cell), score≥τ_op ∧ GT==0. 두 층(OFF/ON_NEG) 합산 금지.
아래는 **구 GT(공표 eval_test) 기준** — 교정 GT 재생성분은 착지 시 별도 행으로 append.

| # | 모집단/수치 | 값 | 원장 | 재집계 |
|---|---|---|---|---|
| A | OFF층 노출 분모 | 8,160칸 (408×20) | `runs/v2/*/eval_test/per_frame_off.csv` | `code/fa_census_extract.py` |
| B | ON_NEG층 노출 분모 (none_in_fov 포함) | 5,469칸 | 동상 per_frame_on.csv | 동상 |
| C | none_in_fov ON_NEG 노출 기여 | 1,620/5,469 = 29.6% | 동상 | 동상 |
| D | none_in_fov ON_NEG 사건 기여 | 2,673/4,713 = 56.7% (lift 1.92×) | `fa_events.csv` | `code/fa_census_analyze.py` |
| E | 정본 9런 총 FA 사건 | 6,925 (OFF 2,212/ON_NEG 4,713) | 동상 | 동상 |
| F | 공표치 대조 | 27/27 일치·불일치 0 | `logs/fa_recount.json` | `code/fa_census_extract.py` |
| G | OFF층 FA 거리 법칙 | ∝ R^1.98 | `logs/fa_census_tables.json` | `code/fa_census_analyze.py` |
| H | OFF 시드-다수결 3모델 동시 FA | 0/362 (rgb∩depth Jaccard 0.000) | `fa_events.csv` | 동상 |
| I | 그리드-고정 검정 기울기 (7씬 OFF) | −0.058~+0.387 (내용추종≈±1) | `logs/fa_dose_response.json` | `code/fa_dose_response.py` |
| J | 장식형 lift (국소화 2씬 OFF) | 1.01/0.98 (효과 없음) | `fa_events.csv`+`code/scene_knowledge.json` | `code/fa_census_analyze.py` |

측정일 전부 2026-08-23. §3.4 미결 8번(none_in_fov 처리)은 행 A~D가 실측 기반을 공급 —
규약 확정은 test-ext 정의 시.

## 4.6 【해소 · 08-23】 §4.3 1번(트윈 tol 3자 불일치) 처분 완료 — **불일치는 실재하지 않음, 공표치는 `--tol 0.15` 산물**

전문: `experiments/v3_0823/TWIN_TOL_RESOLUTION.md`. 재현 1회(CPU, 동결 덤프 재사용) 결과:

| 항목 | 판정 |
|---|---|
| 공표 v2 트윈 생성 설정 | **`--tol 0.15`(D20)로 확정.** 원본 매니페스트 `dayrun_0820/dataset_manifest_v2_full.json` + 원본 per_frame 덤프로 재실행 시 `twin_pairs.csv` **md5 바이트 동일** (rgb_s42 `5e1a51ae…`, depth_s42 `788a1ad3…`). `--tol 1e-6` 재현본은 불일치 |
| 출하물 헤더 | **`tol 0.15`로 정확히 각인**(`runs/v2/*/twin/twin_analysis.md:5`, 10개 파일 전수). `twin_analysis.py:123`이 `a.tol`(인자)을 찍고 `:179→:56`에서 같은 값이 술어로 쓰인다 — **헤더 버그 없음** |
| 원장 결함 ① 다리 ②의 정체 | **오인용.** `tol 1e-06` 각인 파일은 (a) `mainrun_0819/runs/{rgb,depth}_s42/twin/` = **D20(08-20 16:00) 이전 v1 산출물**(manifest v1·168쌍·mtime 08-20 02:15·`grid:` 줄 없음 ⇒ tol 줄이 **4행**, v2 출하물은 **5행**), (b) `METRICS.md:966` = **구멍 프로브**(`eval_probe.py:240`이 설계상 `--tol 1e-6` 하드코딩). 둘 다 v2 공표 산출물이 아니다 ⇒ **3자 불일치 소멸** |
| 층화 독립 재검증 (408쌍 전수, 매니페스트 `cam` 직접 대조) | EXACT(≤1e-6) 312 (V132·E45·**H96**·H_weak/none 39) · **TOL(1e-6,0.15] 54 (V33·H_weak/none 21· E0·H0)** · EXCL(>0.15) 42 (전량 scene07, \|Δz\| 3.57/4.02 m). **METRICS §N.1 표와 완전 일치, D27 항등 재확인** |
| per-key 허용오차 부재(다리 ①)의 실측 영향 | **0.** 408쌍에서 `d`·`h_rel`·`yaw`·`pitch` 최대 \|Δ\| = **정확히 0.0**(회복될 쌍이 구조적으로 없음), `ground_z`만 96쌍에서 상이. **잠재 결함**으로 등재하되 공표치 무관 |

**주장 영향: 움직이는 공표 숫자 0건.** H·E 티어 트윈 Δ와 H 밴드 분해(§N.2), RT.2/RT.6은 전량
EXACT층이라 **tol 무관 항등**(s42 실측 4소수점 동일: rgb H 0.1701 · depth H 0.4131, 두 tol 공통).
tol 의존 항목은 **all-tier Δ와 씬별 표뿐**이며, 그 크기는 METRICS §N.1이 이미 공표(rgb +0.041 ·
depth +0.001 · b2 +0.037, 3시드 평균; s42 단독 rgb +0.032 · depth +0.006).
**논문 숫자 수정 불필요**, 각주 1건만 권고(`runs/v2/SEED_TABLE.md` §2가 kept의 tol 정의를
명시하지 않는 유일한 공표 표 — 각주 초안은 해소 문서 §5.1/§5.2, 본 등재에서 출하 파일은 무편집).

**후속(코드 트랙)**: ① v3 `twin_analysis.py`에 **per-key 허용오차**(`ground_z=0.15`, 나머지 1e-6)
② 산출물 헤더에 **코드 버전·실행 시각** 각인(v1/v2 산출물이 경로로만 구분되는 것이 이번 오인용의
직접 원인) ③ `ASSUMPTION_LEDGER.md` 결함 ①/`LAB-13`/N-1의 "생성 설정 확정 불가" 서술 철회.

## 4.7 【추가 · 08-23】 FA 센서스 **교정 GT(정본 B)** 재생성 — §4.5(구 GT) 대응행

CPU-2 후속. §4.5는 **구 GT 측정으로 동결**하고(공표 `eval_test` 대조용), 본 행이 **교정 GT 정본**이다.
원장 `experiments/v3_0823/eval_v2corr/<run>/per_frame_{on,off}.csv` (9개 정본 런) ·
재집계 `experiments/v3_0823/code/fa_census_{extract,analyze}.py --corr` ·
`code/fa_dose_response.py --corr` · 상세 `FA_CENSUS.md §9` · 측정일 08-23 · GPU 0.

**입력 검증 4건 (실측)**: ① `per_frame` 헤더 md5가 `eval_test`와 동일 ② on팔 tier
`V 219 · E 45 · H 96 · H_weak 9 · none_in_fov 39`(합 408)로 `G7_RELABEL.md:239-241` **AFTER B**행과 일치
— §4.4의 정정치(none 81→**39**)와도 일치 ③ **점수 불변**: 교정 덤프의 `p_*`가 `eval_test`와
전 칸 1e-9 이내 일치(GT 차이만 165칸/런) ④ **부록 resnet50·convnext_tiny는 교정 덤프 없음 → 본 행에서 제외**
(§4.5의 부록 표는 구 GT 전용으로 유지; 스크립트가 `--corr`+`--appendix`를 상호 배타로 강제).

| # | 모집단/수치 | 구 GT (§4.5) | **교정 GT(B)** | Δ |
|---|---|---|---|---|
| A′ | OFF층 노출 분모 | 8,160칸 | **8,160칸** | 0 |
| B′ | ON_NEG층 노출 분모 | 5,469칸 | **5,304칸** | −165 |
| C′ | `none_in_fov` ON_NEG 노출 기여 | 1,620/5,469 = 29.6 % | **780/5,304 = 14.7 %** | −14.9 pp |
| D′ | `none_in_fov` ON_NEG 사건 기여 | 2,673/4,713 = 56.7 % (lift 1.91×) | **683/3,894 = 17.5 %** (lift **1.19×**) | −39.2 pp |
| E′ | 총 FA 사건 | 6,925 (OFF 2,212/ON_NEG 4,713) | **6,106 (OFF 2,212/ON_NEG 3,894)** | −819 (전량 ON_NEG) |
| F′ | 공표치 대조 | 27/27 | **27/27** (교정 `metrics.json` 대조) | 0 |

`none_in_fov` 프레임 **81 → 39** · 노출칸 **1,620 → 780**(=39×20, 실측).

**OFF층 불변은 주장이 아니라 실측이다**: off팔 GT 전셀 0(양 GT·9런 전부 `off_gt_nonzero_cells=0`) +
점수 불변 ⇒ `fa_events.csv`와 `fa_events_v2corr.csv`의 **OFF 2,212행이 가족 플래그·`method` 열까지
포함해 전 행 동일**(차이 0행). 따라서 §4.5의 **G(R^1.98) · H(0/362, rgb∩depth J=0.000) ·
I(그리드-고정 −0.058~+0.387) · J(장식형 lift 1.01/0.98)** 는 **교정 GT에서도 같은 값으로 유효**하다.

**이동은 전량 scene07에 국한**된다 — ON_NEG 씬별 사건 `scene07 2,636→1,817(−819)`, 나머지 6씬 불변.
tier 재분포 `none_in_fov 2,673→683 · V 1,626→2,766 · H_weak 79→110`.
`depth` 3런 중 2런은 `none_in_fov` FA가 **0건**이 됐다(396/327/357 → 15/0/0).

**부호가 뒤집힌 결론은 없다**: 경계칸형 반경 lift 3.27→3.05, 방위 0.72→0.74, 조명형 2.03→2.06,
지형통계형 1.25→1.30, 3모델 동시 FA 5.8 %→5.2 %, depth 시드지속 .623→.578.
**대리선형만 2.18→1.60으로 유의미 약화** — bleed 가드 배제가 88→184로 늘어난 결과이며,
"ON_NEG에서만 보이던 대리선형 농축"의 상당 부분이 GT 결함이었음을 뜻한다(강화 방향의 정정).

**§3.4 미결 8번(`none_in_fov` 처리 규약)에 주는 갱신**: 재라벨은 이 구멍을 **줄인 게 아니라 대부분 메웠다**
(56.7 %→17.5 %, lift 1.91×→1.19×). 다만 잔여 39프레임은 여전히 **어느 공표 분모에도 없고**(§2-3),
§4.3-2가 등재한 결함(`none_in_fov`가 게이트 후 GT로 판정 — `labeler.py:526`)은 **미해소**다.
따라서 미결 8번은 **닫지 않고**, test-ext 정의 시 pre-gate/post-gate 사유 분리와 함께 처리한다.

**자체 결함 1건 정정**: `fa_census_analyze.py`가 `none_in_fov_frames`를 상수 81로 하드코딩하고 있었다
(구 GT에서는 우연히 정답, 교정 GT에서는 조용한 오보). 원장 실측으로 교체 —
교체 후 구 GT 재실행은 81/1,620, 교정 GT는 39/780. `--corr` 없는 §4.5 재생성 명령은
리팩터 후에도 5개 산출물 md5 **전부 동일**(구 GT 회계 무훼손, 실측 확인).

## 4.8 【추가 · 08-23】 RT-A 차단급 4건 등재 + §3.1 교정 분모 병기 (구 §4.7 — 동시 append 번호 충돌로 개번, D63)

**§3.1 병기 행**(교정 GT·정본 B): 헤드라인 recall 분모 **369** = 408 − none_in_fov **39**
(V219·E45·H96·Hw9). 원장 `dataset_manifest_v2corr.json` · 재집계 `V2_RESCORE.md` §0 · 08-23.

RT-A(`redteam/RT_LEDGER_A.md`) 차단급 판정 4건과 처분:

| # | 행 | 결함 | 처분 |
|---|---|---|---|
| 1 | EVL-12 | FA-정합 축이 프레임 any-fire — Depth 칸당 경보 부담(4.29 vs rgb 2.61) 은폐 | **이중 축 규약**: FA-정합은 frame축+cell축(`cell_fpr_off`) **병기**를 표준으로, PREREG_V3에 양축 등록. 기존 덤프 재작도(무료) 즉시 실행 |
| 2 | LAB-19 | void(미측정 높이맵 칸) = GT 음성 인쇄 — on팔 42.4% 프레임에 그리드내 void 존재 | **v3 라벨 사양 필수**: void 칸을 `gt_void` 마스크로 분리 기록(음성 아님), 분모 처리 규약은 test-ext 정의와 동시 확정. P-5 게이트 목록 주입 |
| 3 | LAB-26 | 라벨러 `check_twins` = 트윈 쌍 정의 4번째 자리, `ground_z` 미검사 | v3 라벨러 수리 시 per-key tol과 동시 수정 + 4종 정의 대조표(LG 게이트) — TWIN_TOL_RESOLUTION 후속 |
| 4 | SCOPE-08 | 추론 기본 hfov 69° vs 훈련 62.2° — 파일럿 AUC 판독선 좌표계 불일치 | **촬영 전 필수**: 훈련 hfov를 config 기록 + infer 경고, 파일럿 안내서에 화각 명시. pilot_package 후속 |

## 4.9 【추가 · 08-23】 RT-B 등재 — 규약 3건 채택 + 결재 대기 2건 + 정정 병기 (D64)

**채택 (Claude 예비, PREREG_V3에 반영 — 최종 결재란)**:
1. **σ 추정자 교체**: range/2는 n=3에서 σ를 ~15% 과소추정(E[range]=1.693σ) → **표본표준편차
   ddof=1**로 통일. 사전등록 판정식의 σ는 전부 이 정의. (RT_LEDGER_B §σ)
2. **계기판①은 FA-정합 병기**: "모든 recall 주장은 FA-정합 병기가 법"(DZ :189)을 계기판
   자신에게 적용 — 계기판① 트윈-조건부 recall은 τ_op 단일점 + FA-정합 지점(양축, §4.8
   EVL-12 규약)에서 함께 산출. PREREG_V3 생성 전인 지금이 유일한 반영 창.
3. **|r| 보고 규약**: 팔 수준 φ는 설계상 자동 0(퇴화 미구별) → ① 주변분포 병기 ② 토글
   가능 키 한정 ③ **키별 r** 산출을 표준으로. 13키 중 토글 무 5키는 "상수 키"로 별도 표기.

**결재 대기 (권고 첨부, 무응답 기본값=권고)**:
4. **aux 헤드 on/off (N-5)**: 실측 — 본표 9런 전부 aux OFF(run_queue_v2.sh에 aux 0회),
   유일 aux 런 rgb_s42_aux는 타깃 57.8%가 거짓 공백. DZ §0.1 "학습돼 있음" 서술은 본표
   모델에 대해 거짓 → DZ 정정은 Claude AI/승용 몫으로 이관. **권고**: v3-A는 aux OFF
   유지(동결=v2 본표 그대로), GPU-5는 기존 aux 런 1개 + v3 격리 aux 런 1개(본 판정 비혼입)
   로 재편성 — 귀속 보존과 "aux 최대 활용" 룰링의 양립안.
5. **(A,C) 데이텀 게이트 VG-datum 신설**: v2에서 ground_z 이동으로 183/792쌍 탈락 전례 —
   C팔 제작 게이트에 |Δground_z| 검사 추가(P-5 게이트 목록). 권고: 채택.

**정정 병기**: §2-3의 "none_in_fov 81/408=19.9%"는 구 GT — 교정 GT(정본 B)에서는
**39/408 = 9.6%**(§4.4·§4.7). ASSUMPTION_LEDGER LAB-12의 "372" 인용도 369로 읽을 것
(대장 통합 갱신 시 일괄 반영). 결함 처분 재판(RT-B): ①기각(전제 소멸)·②이름 분리로는
분모 안 닫힘(v3 라벨러 gt_pre 한 줄 수리로 이관)·③진짜 위험은 v3판 재발(무시 마스크
프레임의 발화로 선택되는 경로 — P-4 주입)·④LG/CG/VG 대조표 미산출(P-5 게이트 착지 시 등재).

## 4.10 【추가 · 08-24】 test-ext 모집단 (W3 실측 — §3.4 미결 2·3·4호 충당, PREREG P-17 경유)

원장 `dataset_manifest_v3_textext.json`+`w3_gates_*.json` · 재집계 `code/w3_manifest.py` · 측정일 08-24.

| 항목 | 값 |
|---|---|
| test-ext 씬 (as-built 6) | sceneH1·H2·H3(paired-H) / sceneL1(측방) / sceneN9·N11(N-cue) |
| 총 프레임 | **1,152** (A/B/C/D 각 288) — 계획 1,728의 잔여 576은 B안 3씬(H4·L2·N12) |
| paired-H (씬별) | H1 **33** · H2 **18** · H3 **21** = **72** (하한: 계획 12/사전등록 10) |
| 계기판③ 분모 | FA_C = FA_D = **288프레임 = 5,760칸**, void 버킷 **공집합**(VG-void 1.0 전면) |
| 완전 은닉 층 (D87 ①) | sceneH3 (A,C) 24/24 무정보 — 계기판① 별도 행, Δp 분포 보고 |
| C/D 팔 GT | 전 씬 all-negative (AC-INSTR-1: C GT = 사양 상수 — 매니페스트 라벨러 행 사용 금지) |

## 4.11 【동결 · 08-24】 PREREG_V3 동결 + k 봉인 + 최종 |r| (D91)

- **PREREG_V3.md 동결**: sha256 `05f41322e52b8e085b4d678c57dba4ee984deff3bfcea960123ae586e9fcf56a` · 원본 PREREG_V3_FINAL_DRAFT.md(§9 붙임 포함)
  의 사본 · 사후 수정 = 새 버전+delta note만 허용.
- **k = 7,000 px 봉인**(px_canonical6) — 이후 변경은 동결 위반.
- **훈련 정본 코퍼스** = `dataset_manifest_v3_seg3.json`(3,654프레임, T2-seg 2유닛
  인정 — 자격 3조건·근거는 PREREG §9.1) · 보수판 3,606프레임은 민감도 대조 보존.
- **최종 as-built 존재 |r|**: R 0.1728/Ta 0.0000/N 0.0151/T 0.0788/Sg 0.0117/V 0.1390 —
  전 키 ≤0.2 (원장 VG09_CLEARING.md · corpus_v3_census_seg3.json · 08-24).
- 면적 축 r_pb 0.1150(q99 0.0693) 잔존 — OPEN-9, 판정 비차단, 한계 인쇄 의무.

## 4.12 【추가 · 08-24】 라운드명 날짜 규칙 (D96)

**규칙**: `dataset/` 라운드 접두사(YYMMDD)는 **실제 렌더 개시일**이다. 렌더 계획서의 예약명
(미래 날짜)을 실행에 그대로 쓰는 것을 금지한다 — 계획 식별은 라운드명의 웨이브 토큰
(`v3w1` 등)이 담당한다. **경위·전수 대응표**: `ROUND_LEDGER.md` — 260825/26/27 접두사
라운드는 전부 08-23~24 실렌더(기능 무손상·개명 안 함, 원장이 날짜 의미 복원).
