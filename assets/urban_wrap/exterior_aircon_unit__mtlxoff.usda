#usda 1.0
(
    """Auto-authored by `urban_kit.asset_wrapper` (W3 T4b, MD-F2 / MD-F3).

    Reference wrapper for `exterior_aircon_unit` under treatment `mtlxoff`.
    MD-F3 repair only — block the MaterialX terminal so the asset's own UsdPreviewSurface (diffuse + roughness + normal) shades it.

    The opinion below composes INSIDE the prototype, which is the only place
    it survives `SetInstanceable(True)` — a scene-side binding stops at the
    instance boundary. See section [8b] of `urban_kit.py`.
    Do not hand-edit: `asset_wrapper()` rewrites this file when the stamp
    below no longer matches the registry.
    """
    defaultPrim = "Root"
    metersPerUnit = 1.0
    upAxis = "Z"
    customLayerData = {
        string negobs_wrap_stamp = "374881b32c45329e"
        string negobs_wrap_asset = "exterior_aircon_unit"
        string negobs_wrap_treatment = "mtlxoff"
    }
)

def Xform "Root"
{
    def Xform "Geom" (
        prepend references = @../urban_cc0/exterior_aircon_unit/exterior_aircon_unit_1k.usdc@
    )
    {
        over "_materials"
        {
            over "exterior_aircon_unit_01"
            {
                token outputs:mtlx:surface.connect = None
            }
            over "exterior_aircon_unit_02"
            {
                token outputs:mtlx:surface.connect = None
            }
            over "exterior_aircon_unit_rusted_01"
            {
                token outputs:mtlx:surface.connect = None
            }
            over "exterior_aircon_unit_rusted_02"
            {
                token outputs:mtlx:surface.connect = None
            }
        }
    }
}
