# 비판-의도 지도 v1 — 08-06 검수 지적 요소의 원설계 의도 발굴 (2026-08-06)

> 계기: scene03 "교량" 지적이 알고 보니 v4-D1 강 정체성 앵커(하천 도로교)였던 사례.
> 17개 지적 씬 전부에 대해 "그 요소가 왜 그렇게 만들어졌는가"를 코드·원장·브리핑에서 발굴,
> 지적사항과 기존 설계 의도/조항의 충돌을 표기했다. 읽기 전용 조사(Opus 6기) — 코드 무수정.
> 성격 분류: **결함**(고치면 됨) / **잔재**(은퇴한 독트린이 남긴 것 — 고치되 이력 기록) /
> **의도**(사용자 재확인 필요 — 알고 나면 존치가 맞을 수 있음).

## scene01 — 광장이 아니라 벌판 — 양측 건물 부재 ("buildings again on both sides … Is that a field or a square?")

- **요소**: HEAD `scenes/main/scene01_campus_stairs.py:305-324` `PARAMS["backdrop"]` 5동이 전부다 — E1(x108…126, y −60…−22, h12.5, kind="office", lod=62) · E2(x108…126, y18…56, h11.0) · N1(x −30…26, **y 62…78**, h9.0, kind="backdrop") · S1(x −26…30, **y −80…−64**, h8.0) · W1(x −120…−96, h11.0), 그리고 `corridor=5.5`(line 323). 즉 **측방 최근접 동이 |y| 62~80 m**이고 근접 매스는 0동. 지지 기하는 `ground_plate=dict(size=400.0, z_top=-0.63)`(line 330), 조립은 `build_backdrop`(1343-1422, `plan_building` 이 블록별 `z_ceil` 재유도·출력). 렌더 증거: `look_check/scene01/260806_w3_allview4/pt_noon_beauty_overview.png`(백색 포장 + 잔디 평면 + 원경 벽돌 1동), `pt_noon_preset_h1.8_d2.png`(정면 40 m 개구 사이가 전부 잔디).
- **원설계 의도**: ① **원설계에는 양측 건물이 있었다** — `Docs/briefs/scene01_design_brief.md` §1 표: "건물 R | y +9.5→+14, x −18→+12, h 14" · "건물 L | y −10.5→−15, x −20→+4, h 10". ② 삭제 근거는 사용자 2차 검수 "Scene1 계단은 잘 표현했는데, **주변이 좀 미흡해서** … 환경을 좀 **트인 느낌**으로" → `Docs/surveys/w3_intake_v2_images.md` §2 scene01 (c) "Buildings box the plaza on both long sides (`:210-213`, R `y0=9.5` / L `y1=-10.5`) … the closed, unresolved edge the user calls *미흡*", (e) **BS-4** "backdrop kind for the collegiate buildings **pushed back and lowered** so sky shows above the roofline". 코드에 그대로 남은 실행 주석(scene01:244-249): "From an h0.3 eye that is a 14 m wall subtending 55 deg of the lateral field … They are **deleted, not re-clad**". ③ 08-05 재검수에서 과도 비움 판정 → `Docs/briefs/gallery_fix_plan_v1.md` §1 scene01 **1-4** "대학 광장 정체성 복원 … 건물을 다시 넣되 … §0-2(길 정면 건물 금지) 준수" → 원장 **`Docs/audit_v4/gt_changes_w3.md` §23 GT-67**. 코드 §GT-67 1-4 (274-300): "(a) The head masses move out and up … h 12.5 (ridge 14.77) needs d > 103 m", "(b) **The travel axis stays open (§0-2)** … a **40 m gap** (y −22…18)", "**The lateral wings keep the honest distance and stay 3-prim silhouettes — they are out of ±30° of every judged eye and earn no facade (BS-4)**"(299-300). GT-67 착지기록: "§0-2 길 정면 개방 — 차폐 동: 없음" · "정면 매스 사이 축선 개구 ≥ 24 m — **40.0 m**". §0-2 원문은 gallery_fix_plan §0-2 "통행 가능성(passage): 길이 있으면 씬 끝까지 이어지게 한다. **길 정면을 건물로 막지 않는다**", §0-3 "scene04가 모범 사례".
- **충돌**: **§0-2 와는 무충돌** — §0-2 는 *정면축*만 구속하고 측방(±Y) 근접 매스를 금지하지 않는다(GT-67 이 이미 40 m 축선 개구로 이행). 진짜 충돌은 **BS-4 수용조건**: 판정 눈 최악(h0.3_d2)에서 `z_ceil = h_eye + 0.1405·d` 아래에 모든 ridge 가 있어야 한다는 조건(scene01:255-272, `plan_building` 이 조립 시 검증)과, 사용자가 "미흡"이라 불렀던 **55° 측방 차폐**(scene01:244-249)를 근접 4층 매스가 그대로 되살린다는 점 — 즉 07-31 BS-4 룰링과 08-06 지시가 정면 충돌하므로 "낮고(2~3층) 가깝게"의 절충값을 결재해야 한다. process_spec §4 do-not-touch·§5 parked 에는 해당 행 없음(GT-4 는 intake §7-6 으로 이미 RETIRED — 잔디대 복원 요구가 아님). 추가로 `Docs/reports/continuity_audit_v1.md` scene01 행 "광장 포장이 사방에서 잔디로 끝나고 씬 경계로 나가는 보행로가 하나도 없음. 벽돌 건물 정면으로 접근하는 포장도 없어 **건물이 도달 불가**" — 건물을 넣을 때 접근 포장을 함께 깔지 않으면 §0-2 passage 조항을 새로 위반한다.
- **성격**: 결함도 순수 의도도 아닌 **은퇴한 독트린(BS-4 "환경을 비워라")의 잔재** — GT-67 이 정면축만 절반 되돌리고 측방을 62~80 m 에 둔 탓에 벌판이 됐으며, 사용자가 원하는 것은 원 브리프의 R/L 근접 매스이므로 z_ceil 규칙과 재절충해 복원하면 된다.

## scene01 — 중간 레일 2본 겹침

- **요소**: 공용 kit `scene_common.py:1849` `build_railing_line` 안에서 수평재가 **3본** 만들어진다 — `_seg("RailTop", rail_r, 0.0)`(1911) → z = ground+1.10, `_seg("RailMid", rail_mid_r, rail_mid_drop)`(1912) → z = ground+1.10−**0.52** = 0.58, 그리고 merge 경로의 그립 레일 `sk.build_handrail(..., wall_y=y, wall_gap=0.060, ext_top=max(0.300, x_top−x_start))`(2043-2067, hr_kw 2052-2058) → z = nosing+**0.850**(`stair_kit.py:108` `HANDRAIL_H = 0.850`), y 오프셋은 가드면에서 wall_gap 0.060 + dia/2 0.017 = **0.077 m** 뿐. 호출부 `scene01_campus_stairs.py:1892-1907` 이 세 라인(y 0 / ±5.45)에 대해 `rail_mid_drop=0.52`(line 1903)를 **호출부에서** 넘기는데, `PARAMS["railing"]["rail_mid_drop"]=0.45`(line 533)는 아무도 읽지 않는 죽은 값이다. 결과 단면 1.10 / 0.85 / 0.58. 렌더 증거: `look_check/scene01/260806_w3_allview4/pt_noon_lower_lookback.png` 상단 수평 연장부 — 굵은 튜브 2본이 나란히, 그 아래 가는 mid rail 1본.
- **원설계 의도**: 중간 레일은 원래 가드 구성의 일부 — scene01:532-533 주석 "R2-5: top rail 0.025->0.03 … R2-5: mid rail". 그립 레일 병합은 **GT-67 1-2**: gallery_fix_plan §1 1-2 "handrail 정리 — 지금은 railing 패널이 **따로 노는 문 짝**처럼 보임(검수 이미지 확인). 연장부·경사 연결부를 **한 덩어리로** 정리", 원장 §23 GT-67 "handrail 한 덩어리 정리(패널 따로 노는 문짝 인상 해소)". 코드 계약(scene_common:1876-1880): "`merge_handrail` (False / +1 / −1) **folds the grip rail into the guard instead of standing it beside it**: rail knuckles at the slope transition, end returns … and the handrail carried on **brackets** off the guard plane"; scene01:548-551 "Grip rail bracketed onto the guard plane, offset to the **approach** side". 즉 GT-67 1-2 는 그립 레일의 **지주열**만 없앴고 가드 자신의 중간 레일은 손대지 않았다 — 그래서 중간대에 2본이 남았다. 호출부의 `rail_mid_drop` 0.45→0.52 상향(간격 0.20→0.27 m)은 두 본을 억지로 떼어놓으려 한 미봉의 흔적이 코드에 남은 것이다.
- **충돌**: (b) **process_spec §2.3 공유 kit 규율** + 원장 §23 GT-67 Scope 명문: "`scene_common.py build_railing_line`(단수/낙차 기반 살대 spacing 파라미터 — **기본값 시 기존 13개 호출 씬과 비트동일 필수**, 사용은 scene01만)" → 기본값 경로를 바꾸면 25씬 비트동일 증명(GT-67 착지기록)이 깨지므로, 수정은 scene01 호출부 또는 `merge_handrail` 분기 안으로 한정해야 한다. 법정 제약: 그립 레일 자체는 피난방화규칙 §15④(h 850 · φ32~38 · 수평연장 ≥300, `stair_kit.py:374-385`)라 **삭제 불가** — 지울 수 있는 쪽은 가드의 mid rail 이다. `BALUSTER_CLEAR_MAX`(scene_common:1846, C0-7 gate)는 건드리지 말 것. §4 do-not-touch·§5 parked 에는 해당 행 없음(§4-2 scene17 무난간은 다른 씬).
- **성격**: 의도된 설계가 아니라 **GT-67 1-2 가 그립 레일을 가드 평면에 얹으면서 가드의 기존 중간 레일을 정리하지 않아 남은 미완성 잔재**로, 법정 그립(0.85)을 남기고 mid rail 을 제거하거나 두 본을 한 부재로 통합하는 것이 정답이다.

## scene01 — 기둥 허공 부유 ("pillar … still hanging in midair")

- **요소**: 공용 kit `scene_common.py:1954-1970` `_bal()` 의 밑면 산식 — **line 1963 `zbot = max(gz + 0.04, top0 - drop * t - rail_h + 0.04)`**. `gz`는 계단 디딤면(계단식, 호출부 `scene01_campus_stairs.py:1886-1890` `rail_ground`), 둘째 항은 **경사 레일선** 기준이라 계단 구간 살대 3본(x 0.33 / 0.71 / 1.09)에서 둘째 항이 이겨 밑면이 각 디딤판보다 **일괄 +0.060 m 부유**한다 `[computed: zbot = 0.04 − 0.6·(x/1.52), tread = −0.15·(⌊x/0.38⌋+1)]`. 살대가 GT-67 1-1 로 22→6본까지 성겨져(`cliff_adjacent=False, use_nsteps=True`, pitch = run/nsteps = 1.52/4 = 0.380 m — scene01:535-547, scene_common:1938-1952) 뜬 살대가 개별로 읽힌다. 부수 결함: `post_xs`(scene_common:2001-2007)가 x=1.4 와 x_end=1.52 에 **0.12 m 간격 이중 기둥**을 세운다. 렌더 증거: `look_check/scene01/260806_w3_allview4/pt_noon_amphi_view.png` — 측벽(`flank`, scene01:211) 면 앞에서 살대들이 공중에서 끊긴다.
- **원설계 의도**: 둘째 항의 원 목적은 살대 하부 개구 폐합 — scene_common:1959-1962 "The balusters must come down **close to the ground**. Previously they stopped at the mid rail and filled only 43 % of the guardrail height, and the actual opening in the lower 0.45-0.60 m **violated the cited statutory 100 mm by 11 to 28 times**. (Geometry justified by a statutory requirement was violating that requirement.)" — `props_audit_w1/C1_scene_props_01-05.md` **C0-7**(난간 계열 치명) 대응으로 들어온 코드다. 성긴 살대는 gallery_fix_plan §1 **1-1** "살대(세로 봉) 밀도를 계단 규모에 연동 … 이 씬은 낙차 0.6 m·옆에 절벽 없음 → 성기게" → 원장 §23 GT-67 착지기록 "살대 피치 = 계단 규모 유도(1-1) · 법정 피치보다 성기다 — **380 mm ← run/nsteps · 법정 116 mm**". scene01 자가검증(SMOKE 19행)에는 **살대 접지 검사 항목이 없다** — "부유 0" 검사는 lawn_trees·fountain 전용이라 이 결함을 잡을 수 없었다.
- **충돌**: **무충돌.** 살대는 GT 레지스트리 밖(보행면·낙차 에지·hazard box 아님)이고 §4 do-not-touch·§5 parked 어디에도 없다. 다만 §2.3 + GT-67 Scope 의 "기본값 시 기존 13개 호출 씬과 비트동일" 제약이 그대로 적용되므로, 수정은 **계단식 ground_fn(stepped) 케이스로 한정**해야 평지·균일경사 호출 씬의 결과가 흔들리지 않는다. 이중 끝기둥(1.4/1.52) 정리도 같은 제약을 받는다.
- **성격**: 명백한 **의도치 않은 결함** — 계단 위에서 경사 레일선 datum 이 계단 datum 을 이기는 `max()` 때문에 살대가 일괄 60 mm 떠 있고, GT-67 1-1 로 살대가 성겨진 뒤에야 눈에 보이게 된 잠복 버그다.

## scene05 — side_arc 돌출 오브젝트 (popped-out object)

- **요소**: HEAD `scenes/main/scene05_amphitheater.py:666-671` `PARAMS["backdrop_shrub"]` — `rows=((5.85,0.44,0.36,0.19,1.02,6),(6.25,0.52,0.40,0.20,1.24,7))`, **`arcs=((12.0,78.0),(282.0,348.0))`**, `jit_step=0.35, jit_pos=0.075, jit_scale=0.15`; 생성기 `backdrop_instances()`(841-875), 검산 `backdrop_selfcheck()`(877-945). 충돌 상대는 `cut_wall`(437-439) — r 5.0…7.85, top_z 1.00 + 60 mm 목재 캡, **`arcs=((78.8,82.0),(278.0,281.2))`**. 산술: 관목 열 시작 282.0° 와 cut_wall 종단 281.2° 사이는 0.8° = r 6.0 에서 **0.084 m** 뿐인데 로브 평면반경 rx 는 0.36~0.40 m(+지터) → 각 아크의 첫 로브가 cut_wall 판을 관통해 그 −θ 면 한복판에서 튀어나온다. `side_arc` 카메라(1610, eye (6,−10,1.2) → tgt (6,−2,0.5))는 보울 중심 (6,0) 에서 방위 270°, 즉 **cut_wall 278…281.2° 를 정면으로 본다**. 렌더 증거: `look_check/scene05/260806_w3_allview4/pt_noon_side_arc.png` — 목재 캡 옹벽 면 중앙에 녹색 호박돌 1개가 박힌 채 돌출, 캡 위로는 호박돌 열.
- **원설계 의도**: ① 관목 열은 **v6 판정 (i)** 로 낮춘 배경벽의 방호를 승계하는 장치 — scene05:618-628 "lower the backdrop arc wall 1.40 -> 0.70 (openness / v5.2 §6) … **this wall is the retaining wall carrying the 1.15 m drop from the yard (−0.06) to the stage (−1.207)** … Instead the height lost is replaced by backdrop_shrub"; 631-638 "drop guarding (yard −0.06 -> stage −1.207, 1.15 m): arc wall 0.76 m + a 0.80 m wide, h1.18 shrub buffer = **access deterrence**". ② 호박돌로 읽히는 문제는 **이미 한 번 고쳤고 실패한 상태** — scene05:640-665 "[v7 judgment §4 remaining 1] the new shrubs render as **'an even row of mossy boulders'** … (i) drop the grass texture -> 3 constant-colour tufts … (ii) per lobe rz ≤ 0.25 … (iii) one even row -> 3 rows". ③ 원장 §19 **GT-63** 착지기록의 명시적 제외: "**검토-제외**: scene05 `backdrop_shrub`(무대 가드 완충)는 v7 §4-1 판정으로 이미 무텍스처·플랫(rough 1.0·spec 0) 처리 — '광택 블롭' 실패군 아님. `backdrop_instances()` 단일원천이 자체 검산(lobe 중첩 check 7)과 결합·**가드 기능 겸직** → 본 행 범위 외. 실자산 전환을 원하면 별도 행(아크 배치 place_shrubs) 후보." ④ 원장 §25 GT-69 Scope 도 "backdrop_shrub(v7 가드 겸직)·무대 재설계(P-16)는 범위 외". ⑤ 08-06 감사가 독립 적발: `Docs/reports/continuity_audit_v1.md` scene05 행 "③ **옹벽 우측 끝 너머에 자연석(호박돌) 1개가 벽에서 떨어진 채 공중에 떠 있음.** 옹벽 상부를 따라 늘어선 호박돌 열 자체도 맥락 없는 장식으로 읽힘 — asset 배치 개선 항목에 포함 권장."
- **충돌**: (a) **씬 정체성/방호와 충돌** — 이 열은 yard −0.06 → stage −1.207 **1.15 m 낙차의 접근 억제 기능을 겸직**(scene05:635-638)하므로 단순 삭제·축소는 v6 (i) 룰링(개방성 우선 + 낮춘 벽 + 식재 완충)을 무효화한다. 되돌리려면 `shell.top_z` 를 0.70→1.40 으로 복원해야 하는데 그건 v6 "cistern/bunker" 판정으로 되돌아가는 것. (b) §4 do-not-touch 직접 조항 없음(인접 주의: **§4-16 "scene05 `seat.arcs` 값은 yaw가 아니라 호 스팬(도)"**). (c) §5 parked 해당 없음. (d) **과거 룰링 충돌은 이미 해소됨** — GT-63 §19 와 GT-69 §25 가 두 번 "범위 외"로 봉인했던 것을 원장 §31 **GT-75** Authority("side_arc 컷의 돌출(popped-out) 오브젝트 수리")가 처음 여는 것이므로, 봉인 해제 근거를 행에 남겨야 한다.
- **성격**: 돌출 그 자체는 **검산 구멍이 만든 결함**(`backdrop_selfcheck` 는 ①반경 ②야드 ③부유 ⑤크기 ⑥카메라 ⑦로브 연속만 보고 **cut_wall·entry_cheek 과의 방위각 간섭을 아예 검사하지 않는다**)이지만, "호박돌 열"이라는 인상은 v6 가드 승계라는 의도된 설계의 표현 실패라서 설명 후 존치할지 실관목(place_shrubs)으로 재표현할지 결재가 필요하다.

## scene05 — 무대 계단 과존재감 ("stand out too much")

- **요소**: HEAD `scenes/main/scene05_amphitheater.py:364-370` `PARAMS["podium"]` — `r=3.0, top_z=-0.853, base_z=-1.607`, `steps=dict(radii=(3.75,3.375,3.0), tops=(-1.028,-0.853), seg=12, base_z=-1.6, under=0.10, z_cascade=0.003, arcs=((130.0,160.0),(200.0,230.0)), cheek_deg=3.2, cheek_overlap=0.2)`. 빌더 `scene05:1965-1998` — `"/World/Scene05/Stage/Podium"` 실린더(1967-1971) + `Stage/PodiumStep_{j}_{i}`(1982-1985, 2조×2단, `**ARC` mesh) + **`Stage/PodiumCheek_{j}{A,B}`**(1990-1998, 조당 2개, r 2.90…3.75, top −0.856, 전부 `M["stage"]`). 렌더 증거: `look_check/scene05/260806_w3_allview4/pt_noon_side_arc.png` — 밝은 무대 바닥 위에 어두운 치크 판이 자립 슬래브처럼 서 있고 치크/포디움 접합부에 검은 쐐기 공동이 그대로 보인다.
- **원설계 의도**: ① 포디움 자체가 **사용자 피드백 산물** — scene05:309-314 "[v5.1 realism] circular stage podium - **'the stage must rise more'** … effective stage height 0.693 m … **Meets the feedback request h 0.6~0.9**", 이어 **scene05:324-325 "[v5.2 user] podium height 0.70 -> 0.35 - '**low enough to hop up lightly**'. … the access stair shrinks to one intermediate step (0.175x2)"** — 즉 이 계단은 사용자가 낮추라고 해서 남은 2단 접근 계단이다. ② 치크(마구리)는 v7 판정 대응 — scene05:339-342 "a new **end cap (cheek) at each end** - radius 3.0..3.75 · top face = podium top face (−0.853) · thickness 3.2 deg … Same finish as a real stair cheek wall, so the end reads as a **squared-off cap**, not a 'pointed sliver'". ③ W3 L05 K4(d) 로 쐐기가 구조적으로 0 이 된 뒤에도 **존치가 명시 선언**: scene05:352-353 "**The cheeks stay** - they are no longer a patch for a sliver, they are the squared stair cheek **G8 shows on its curved flights**, and they still cap the run." ④ `under=0.10` / `z_cascade=0.003` 는 포디움 실린더 테셀레이션 함몰(14.4 mm @ n=32)에 대한 랩 (354-363). ⑤ 상태 선언: "the access stair and podium are **not hazard geometry** (tiers · entry arc stair), and their radius / angular range / top-face z all stay as they were"(343-344).
- **충돌**: (c) **§5 parked P-16 "scene05 무대 검은 쐐기 틈 — 해제 gate: 별도 무대 재설계 패키지"** 가 정면에 있다. 원장 §31 **GT-75** Scope 가 이를 재확인: "**P-16(무대 재설계)은 계속 범위 외 — 계단 접속·마감 한정**". 따라서 치크·포디움의 *형태 재설계*는 금지, 허용 범위는 접속·마감(재질 대비·치크 상단 처리·랩 정리)뿐. (d) 과거 사용자 룰링 충돌: 계단을 없애면 v5.1 "무대는 더 올라야"와 v5.2 "가볍게 올라설 수 있게(0.35 m)"가 동시에 무효가 되고, G8 근거의 "cheeks stay"(352-353)도 뒤집힌다. (a) 씬 정체성/hazard 와는 무충돌 — 계단·포디움은 hazard 기하가 아니라고 코드가 선언했고 GT-75 도 R-3 전용이다. (b) §4 do-not-touch 직접 조항 없음(인접 주의: §4-16 seat.arcs).
- **성격**: **설명하면 사용자가 유지를 택할 수 있는 의도된 설계**(v5.2 사용자 지시로 낮춘 포디움 + v7/G8 근거의 치크)이며, 지금 튀는 원인은 형태가 아니라 치크가 무대와 같은 `M["stage"]` 로 어둡게 대비되고 P-16 검은 쐐기 틈이 남아 있는 **마감** 문제라 GT-75 가 허용한 "접속·마감 한정"으로 잡힌다.

## scene06 — 나선 조잡 ("start over and rebuild the spiral … It's sloppy")

- **요소**: HEAD `scenes/main/scene06_overpass_spiral.py:357-359` `railing=dict(outer_r=3.24, inner_r=1.56, rail_h=1.10, steps_per_bay=2, landing_bays=3, **arc_seg=1**, post_r=0.030, post_embed=0.05, joint=0.020)`. 조립 경로: `_guard_bays()`(639-648, 26/2 = **13 bay**) → `_guard_section()`(651-674, shoe/pane/cap 을 데크 PARAMS 에서 읽음) → `_raked_sector_mesh()`(694-750) → `_guard_run()`(2001-2032, 베이마다 Shoe·Glass·Cap 3부재) + `_guard_posts()`(2034-2057). `arc_seg=1` 이므로 캡·유리·슈가 **베이당 평면 1 facet** 이고, 13개 평면 조각이 포스트마다 각을 꺾는 폴리라인이 된다(데크 쪽 `build_deck_rail`(2059-2104)는 직선 BOX 라 매끈해 대비가 커진다). 렌더 증거: `look_check/scene06/260806_w3_allview4/pt_noon_overview.png`(나선 bronze cap 의 꺾임·패널 사이 검은 슬롯·마지막 패널의 불투명 암색), `pt_noon_spiral_up.png`(화면 대부분이 무투과 암색 패널).
- **원설계 의도**: 원장 §24 **GT-68** Authority: "08-05 검수 — 본교량 deck(DeckGlass 유리판+bronze cap, G6)과 나선 tube 2단 불일치 → 나선 guard 를 main 계열로 통일, 곡면 분할은 deck 패널 분할 규칙. **Q2(가로대 본수) 본 행으로 대체**". GT-68 이전의 나선은 **bronze/brown 수평 tube 4단**이었고 그것이 06↔11 정체성 분리축이었다 — 모듈 docstring 71-78: "the railing is **bronze/brown horizontal tube, 4 rails** — against scene11's painted-steel vertical bar. **This pair IS the identity separation the user asked for** … **[GT-68 · 08-05 supersedes the section, not the identity]** the deck run had already gone to glass under a bronze cap (`DeckGlass_*`), so the tube survived only on the spiral and **the one structure carried two guard languages**". 각진 폴리라인은 **의도된 표현**이다 — scene06:354-356 "`arc_seg` 1 = each pane is ONE flat facet with its ends on the guard circle. **Real curved balustrades are built from flat panes; a subdivided pane would be a procedural tell.**" 13 bay 근거는 345-349 "the deck's own `rail_bay.n_bay` … Bay chord 1.296 m at the outer run … is set by the **facet limit, not by taste**: a 2.00 m bay on r 3.24 bows 158 mm off the arc against 65 mm here". GT-68 착지기록: "육안 확정: overview 에서 본교량-나선이 단일 제품군, 곡면은 데크 패널 규칙대로 분할; spiral_up 의 암색 패널은 **역광의 무투과 유리(카메라 매몰 아님 — 구도 정합)**".
- **충돌**: (a) **정체성 충돌** — 06↔11 분리는 이제 **bronze cap** 이 진다: scene06:77-78 "the bronze is what still separates 06 from scene11's painted steel", 2040-2041 "after GT-68 the bronze is carried by the cap, **which is where the 06/11 identity separation lives**". scene11 식 도장강 수직봉으로 '정리'하면 정체성이 무너진다. (a) **hazard 축 충돌** — 원장 §32 GT-76 Scope "5.005 m 웰·보행면·낙차 불변"; 모듈 docstring 의 살아있는 판정축은 **축 2**(20-22행 "from ground grazing (robot h0.3) the spiral reads as a **cylindrical silhouette** … so the spiral descent (the 5 m interior void + the entry passage) is concealed")이므로 가드를 투명·저프로파일로 만들어 실루엣을 깨면 이 축이 죽는다. 축 1(180~270° 난간 결손)·축 3(내측 킥플레이트 부재)은 이미 은퇴 선언(25-29행 "no longer the build")이고 GT-68 착지기록이 "**판정축 협소화 기록** … W4 GT 재판정 후보로 이월"로 등재했다. (b)(c) §4·§5 해당 조항 없음. **중복 주의**: 암색 유리는 원장 §29 **GT-73**("Can the glass be made slightly transparent?", Scope 는 `scene_common.make_glass` + **scene13 우선**, 타 씬 확산은 검수 후 별도 행)가 이미 열어둔 축이라 재질 동결 예외를 이 씬에 임의 확대하면 안 된다.
- **성격**: 조잡함은 은퇴 독트린의 잔재가 아니라 **GT-68 통일이 남긴 미완성 마감**(평면 facet 폴리라인, 포스트마다의 꺾임, 베이 조인트 슬롯, 역광 무투과 패널)이므로 arc_seg·조인트·캡 미터 처리로 다듬되 bronze cap 정체성과 h0.3 실루엣 은닉축은 건드리지 말아야 한다.

## scene06 — 에지 마감 (edge finish)

- **요소**: HEAD `scenes/main/scene06_overpass_spiral.py:205` `fascia=dict(r_in=3.20, r_out=3.30, drop_below=0.002, thick=0.62, arc_seg=6)` — 스텝당 애뉼러 섹터 1장, 상면을 해당 스텝 디딤면보다 2 mm 아래에 고정. `scene06:211-212` `soffit=dict(r_in=1.46, r_out=3.34, thick=0.34, drop=0.42, seg_per_deg=0.5, **top_face=True**)`. 나선 본체 `scene06:191-193` `spiral=dict(..., r_out=3.3, n=26, riser=0.192, mesh=True, arc_seg=6)`. 랜딩-데크 접합은 `landing=dict(..., clip=True)`(233-234) + `_landing_guard_arcs()`(677-691). 렌더 증거: `pt_noon_overview.png` 나선 외곽에서 fascia 가 톱니(스캘럽)로 끝나고 그 아래 soffit·guard shoe 와 만나는 선이 계단상으로 끊긴다.
- **원설계 의도**: **GT-6 "fascia stepped"** — scene06:194-204: "The v6 fascia was a **continuous helical ribbon** whose top line was linearly interpolated while the treads step, so it rode +0.154 m above the tread at each step start and −0.038 m below it at each step end — a **±154/−38 mm sawtooth, 26x round the helix**, and (measured on HEAD, `gt_probe.py`) the ribbon **won the top-face sample** at r 3.15…3.25 over most of the sweep, i.e. it was standing proud of the walking surface it was supposed to trim. **G6 shows the opposite: a smooth continuous helicoid soffit with the scalloped tread ends exposed on the outer rim.** So the ribbon becomes **one annular sector per step** … the scallop of G6, and a sawtooth of exactly 0". 즉 **스캘럽(톱니) 자체가 참조 이미지 G6 에서 온 의도된 표현**이고 sawtooth 를 0 으로 만든 대가다. soffit 의 `top_face=True` 는 NF-1 수정(206-210, `build_helix_ramp` 의 중심-후-회전 버그 — 실측 63.0 mm 접선 이동 · +13.03 mm 리프트). 랜딩 클립은 GT-6 "landing azimuth clipped at the deck edge"(223-232): 9.196 m² 관통을 잔여 **4.166 m², −54.7 %** 로 줄이고 나머지는 "**declared, not hidden**" 으로 남겼다.
- **충돌**: **무충돌**(§4·§5·과거 사용자 룰링 어디에도 해당 행 없음). 단 두 가지 주의: (a) **스캘럽 트레드 엔드는 G6 참조 근거의 정체성 요소**라 매끈한 스트링거로 바꾸는 것은 GT-6 의 G6 논거를 뒤집는 일이므로 '마감'과 '형태 변경'을 구분해야 한다. (a) hazard 측면은 안전 — fascia 상면은 디딤면보다 2 mm 아래라 보행면이 아니고, 원장 §32 GT-76 의 "보행면·낙차 불변" 과 충돌하지 않는다. 또한 랜딩-데크 잔여 겹침 4.166 m² 는 GT-6 이 이미 선언한 기지 부채이니 새 결함으로 오인해 별도 수리하지 말 것(유지 또는 별도 행).
- **성격**: 스캘럽은 **설명하면 유지할 만한 의도된 G6 표현**이고, 지금 "마감이 안 됐다"고 읽히는 실체는 스캘럽 끝단·soffit·guard shoe 가 서로 미터링 없이 끊기고 랜딩-데크 잔여 겹침이 남은 **트림 결함**이다.

## scene06 — 반대측 계단과 단일 제품군 불성립

- **요소**: HEAD `scenes/main/scene06_overpass_spiral.py:287-290` `north=dict(pivot=(3.5,13.0), rot=90.0, x0=3.5, y0=11.5, y1=14.5, riser=0.192, tread=0.30, n=13, z_top=5.0, base_z=-0.30, land_len=1.4, **cheek_t=0.22, cheek_h=0.95, cap_h=0.08, cap_over=0.03**)`. 빌더 `build_north()`(1755-1820): `FlightA`/`FlightB`(1762-1778) + `MidLanding`(1769-1773) + 방호는 **`Cheek_{fi}_{i}`(1793-1795, `sc.build_slope`, `M["parapet"]`, `collider=True`) · `CheekCap_{fi}_{i}`(1798-1801, `M["curb"]`) · `CheekMid_{i}` / `CheekMidCap_{i}`(1806-1812)** — 즉 반대측(북측) 계단은 **불투명 콘크리트 치크 파라펫 + 코핑**이고 유리·bronze cap 이 아니다. 대비군: 나선/랜딩은 `_guard_run()`(2001-2032)의 Shoe·Glass·Cap, 데크는 `DeckKick_*`/`DeckGlass_*`/`DeckPanelCap_*`(2093-2104). 렌더 증거: `pt_noon_overview.png` 좌상단의 계단상 콘크리트 쐐기 매스; `Docs/reports/continuity_audit_v1.md` scene06 행도 같은 지점을 "① 서측(좌측) 교량 접속부가 **콘크리트 쐐기 교대 위에서 끝나고** 하행 계단/램프가 프레임 밖이라 확인 불가" 로 지적.
- **원설계 의도**: 북측 계단 치크는 **scene21 파라펫 관례 + 기하 근거**로 들어왔다 — scene06:1779-1785 "cheek parapet - sloped box parallel to the stair line (scene21 parapet convention). **a single horizontal box would bury the upper steps and float at the bottom**, so `build_slope` is used … the cheek_t width lies **inside** the stair width - no placement in mid-air outside it (audit B2)". 코핑은 **[v6 verdict (1)]** — 1796-1797 "coping - the reason the plain white mass read as **'white teeth'**. A 3 cm overhanging cap gives the conventional parapet silhouette." 결정적으로, **북측은 어느 통일 행의 Scope 에도 들어간 적이 없다**: G6 재건(docstring 63-83)은 나선·데크·기둥만 다뤘고, 원장 §24 GT-68 Scope 도 "나선 guard 를 main 계열로 통일"에 한정됐다. 이를 처음 여는 것이 원장 §32 **GT-76** Authority: "'start over and rebuild the spiral … or pay attention again. It's sloppy' · 에지 마감 · **반대측 상행 계단 동일 처리 — 단일 육교 제품군 읽힘**".
- **충돌**: (a) **정체성 무충돌 — 오히려 강화** : 06↔11 분리는 bronze cap 이 지므로(scene06:2040-2041) 북측을 유리+bronze cap 으로 승계하면 분리축이 더 선명해진다. (a) **hazard 주의** : GT-76 Scope 는 "5.005 m 웰·보행면·낙차 불변"이고 R-3 전용으로 선언돼 있는데, 북측 치크는 `collider=True`(1795) 이므로 **collision box 형상이 바뀌면 §2.4 선신고 규정상 행 갱신이 필요**하다(현 행은 collider 변동을 선언하지 않았다). (b) §4 do-not-touch·(c) §5 parked 해당 조항 없음. (d) 과거 룰링 승계 의무: v6 verdict (1) 의 "코핑으로 'white teeth' 해소" 취지와 검수 체크리스트 §6-③ "대면적 순백/암흑 없음"을 유리+cap 으로 바꿀 때 그대로 승계해야 하며, 암색 유리는 GT-73(§29) 이 별도 축으로 열려 있음을 감안해야 한다.
- **성격**: 결함도 의도도 아닌 **GT-68 이 Scope 를 나선에만 그은 탓에 남은 미적용 구간**으로, 북측 치크를 데크·나선과 같은 shoe·pane·bronze cap 3부재로 승계하면 '단일 제품군'이 성립하고 v6 코핑 룰링도 캡으로 대체 승계된다.

## scene07 — 안내판 제거하라 (사찰 입구 sign_info)

- **요소**: `scenes/main/scene07_temple_stone_path.py` (전부 `git show HEAD` 기준 행번호)
- L152 `"cue_sign": True,  # [v5 shared layer] sign_info (temple notice) at the entrance`
- L546-548 `# --- temple notice board (sign_info) : at the entrance (before the gate), facing the approach (yaw 180) ---` / `#   [v7] Moved −2.6 m with the gate (keeps its 0.65 m relative position behind it).` / `sign=dict(cx=-4.95, cy=-1.15, yaw=180.0, w=0.78, h=0.62, pole_h=1.95),`
- L2593-2599 `def build_sign(M):` → `sc.build_sign(stage, f"{ROOT}/SignInfo", sg["cx"], sg["cy"], 0.0, sg["yaw"], M["sign_info"], w=…, h=…, pole_h=…, pole_mtl=M["wood"], back_mtl=M["sign_back"])`
- L2666-2667 호출부 `if cfg["cue_sign"] and cfg["hazard_stairs"]: build_sign(M)`
- 재질 L1930-1937 `M["sign_info"]`(`sc.tex_path("sign_info","diff")`, `uv_mode=True`, rough 0.55) · `M["sign_back"]`(L631 `sign_back=(0.055, 0.050, 0.045)`)
- 부속 참조 4곳: L729 `ASSET_ROLES` 의 `"sign_info"` · L711-712 PLACEMENT `anchors={… "notice_face": dict(face_bearing_deg=180.0, props=["SignInfo.*"])}` · L1214+L1231-1233 `_grid_obstacles()` 의 `("SignPost", cx±0.06, cy±w/2, 0.0, pole_h)` AABB · L1632-1634 v7 그림자 캐스터 표의 `("안내판", …)` 행
실물 치수(외부 실측 기록 `Docs/surveys/props_audit_w1/C2_scene_props_06-10.md` 7-3행): 지주 CYL φ0.08×1.95 + 쿼드 0.78×0.62, 패널 중심 z **1.59 m**, 텍스처 = 청색 "안내 INFORMATION".
- **원설계 의도**: **v5 공통 레이어의 지정 드레싱이다 — 임의 경고 팻말이 아니다.**
1) 도입 근거: `Docs/briefs/multi_scene_brief_v5.md` §공통 레이어(L9-13) — "`sc.build_sign(stage, prefix, cx, cy, base_z, yaw, tex_key, w, h, pole_h)` — 한글 사인 패널(텍스처 = assets/signs/*.png, TEX 등록: … `sign_info`("안내") …)" + §R2 행(L36) — "드레싱: 일주문(기둥 2+맞배지붕), 석등 2, 돌담(낮은 석축) 양측, 소나무 4~6, 상부에 법당 실루엣(기단+기둥열+지붕), **sign_info(사찰 안내)**. 태양 0(정면 관람)."
2) 구현 기록: `Docs/audit_v4/fixlog_N2.md` L26 "cue | tactile/nosing/railing **False**(산사 비관행, 코드 경로만) · `cue_sign=True`(sign_info)", L72 "`sign_info` 1(일주문 앞, yaw 180 = 접근 시점 대향)". 코드 docstring(L2594)도 동일 — "[v5 shared layer] Temple notice board — at the entrance (before the gate), facing the approach."
3) **한 번 살아남은 물건**: `Docs/audit_v4/fixlog_VD.md`(v5.2 사인 정리, 07-27, 사용자 지시 "도로교통법에 존재하는 팻말 아니면 가능한 경고 팻말 없애줘") — 방침 "**제거**: sign_warn_fall·sign_caution_step / **유지**: sign_no_entry·sign_exit·**`sign_info`(사찰/공원 안내, 주차 요금판, 육교 안내 등 실존 관행물)**", 그리고 「무수정」 절에 명시 "scene07 : `sign_info`(사찰 안내판, 일주문 앞) — **유지**."
4) 이번 제거는 이미 선신고됨: `Docs/audit_v4/gt_changes_w3.md` §40 **GT-84** — "Authority: 08-06 사용자 — 'Remove the guide sign'(사찰 입구 sign_info) / Scope: cue_sign 기본 OFF(어블레이션 경로 보존, v5.2 전례) / GT 판정: **R-3 전용** / Round `260806_w3_fixqueue2` / Status: OPEN(선신고)".
- **충돌**: **무충돌** — 단 처리 방식에 조건이 붙는다.
- `process_spec_v1.md` §4 do-not-touch 19항에 사인 관련 조항 없음. §5 parked 에도 이 팻말을 잠근 행 없음(P-1 은 scene07 **archetype(계단) 재건** gate, P-7 은 prop-엣지 offset 규칙, P-17 은 `sign_kr101` 치수 — 전부 별건).
- **cue-어블레이션 설계와의 관계**: `Docs/surveys/cue_arrangement_survey.md` §4.4-1 "`SCENE_CONFIG` 토글 인프라가 §2.5의 ablation 실험을 그대로 지원한다 … '위험 기하 불변' 규약 덕분에 동일 기하 × 단서 유무 페어를 자동 생성" + `cue_expansion_survey_v1.md` 표준법 **법7** "`cue_*`는 **기하 불변**(기하를 움직이는 토글은 `hazard_*` 전속)". → **키와 코드 경로를 삭제하지 말고 기본값만 False** 로 두면 저촉 없음. 전례가 정확히 이것: fixlog_VD 에서 scene06/10/12/20 이 "`cue_sign` → False(키만 예약)" 처리됐고, GT-84 스코프도 같은 문구를 쓴다.
- 단서 상관 통계와도 무충돌: `cue_arrangement_survey.md` §4.1 집계 대상은 난간(P(낙차|난간)=0.944)·점형블록·논슬립·볼라드뿐이고 표지판은 상관표에 없다. §4.4-5 는 "실사용은 `sign_exit` 14 · `sign_info` 44뿐"을 **규약 준수** 근거로 적었을 뿐 유지 의무가 아니다.
- 유의 2건(충돌 아님, 정리 대상): ① `cue_expansion_survey_v1.md` §D12(죽은 키 `cue_sign` 22씬)·§0-5(죽은 키 처리 방침 미결)에 07이 새로 합류하므로 씬 헤더에 사유 기재 필요. ② 제거 시 L711 `anchors["notice_face"]`(props=["SignInfo.*"])·L729 ASSET_ROLES `sign_info`·L1231 `SignPost` AABB·L1632 그림자 캐스터 "안내판" 행이 참조 없는 잔재가 된다 — 같은 커밋에서 함께 정리해야 LINT/스모크 표가 거짓말을 하지 않는다.
- 덤: `props_audit_w1/C2_scene_props_06-10.md` 7-3행의 **P1 "형태재작"**(산사에 도로표지형 청색판 → 목·석 프레임 안내판으로 재제작) 백로그는 제거로 사멸 — 미착수 항목이라 충돌 아님.
- **성격**: 결함은 아니고 v5 브리프가 §R2 드레싱으로 **명시 지정한 의도된 요소**지만(scene03 교량처럼 '설명하면 지킬 것'은 아님), 형태가 이미 자체 감사에서 "산사에 도로 지시표지 청색 판"으로 P1 형태재작 지적을 받은 창작물이라 사용자 제거 지시가 정합적 — 기하·GT 불변의 순수 드레싱 삭제(R-3)로 `cue_sign` 기본 OFF + 부속 참조 4곳 동반 정리가 정답이다.

## scene08 — 허공에 뜬 핸드레일 정리

- **요소**: **캐스케이드 편측 튜브 난간(`FlankGrp_S/N`)이며, 근본 원인은 킷의 수평-세그먼트 가정이다.**
호출부 — `scenes/main/scene08_sunken_plaza.py`(HEAD):
- L244-246 `flank_rail=dict(rails=4, rail_h=1.10, tube_r=0.024, post_r=0.030, post_pitch=1.80, inset=0.25)`
- L1584-1612 `def build_flank_rails(M)` → `r_hi = b["r_rim"] - fr["inset"]`, `r_lo = b["r_arena"] + fr["inset"]`, **2점 폴리라인** `[(mxx - L/2.0, myy), (mxx + L/2.0, myy)]` 을 `pk.build_tube_railing(...)` 에 전달
- L1786-1788 호출 `if cfg["cue_railing"]: build_guard(M); build_flank_rails(M)` (L134-135 `"cue_railing": True`)
빌더 — `props_kit.py:632 build_tube_railing`(`scenes/main/props_kit.py` 는 `../../props_kit.py` 심볼릭 링크):
- 포스트(L672-684): pitch 마다 `g = float(gz_fn(px, py))` 를 **개별 샘플**해 `(px, py, g + rail_h/2.0)` 에 높이 `rail_h` 실린더 → 노면을 따라간다.
- 가로대(L686-696): **세그먼트 1개당 실린더 1개**를, 세그먼트 **중점 1점**의 지반으로만 `g = float(gz_fn(mx, my))` 잡아 `(mx, my, g + z_off)` 에 `rotY=90.0` 으로 눕힌다 → 경사 런을 2점으로 주면 **수평 튜브 1개**가 된다.
**HEAD PARAMS에서 재유도한 수치**: `_R_ARENA = 4.10`(L163) → r_hi 13.75, r_lo 4.35, **L = 9.40 m**. 중점 r = 9.05 = 중간참(`cascade_courses()` L495-511 의 'L' 코스, top **−2.250**). z_off = {0.12, 0.4467, 0.7733, 1.10} → 4단 가로대 z = **−2.130 / −1.803 / −1.477 / −1.150 고정**. 상단(r 13.75, 노면 −0.150)에서는 가로대가 노면 **아래 1.00~1.98 m(매몰)**, 하단(r 4.35, 노면 −4.350)에서는 **2.22~3.20 m 공중**. 포스트는 `round(9.40/1.80)+1 = 6본`, 상단 z = +0.95 / −0.10 / −1.00 / −1.30 / −2.20 / −3.25 → **6본 중 4본이 가로대 대역(−2.13…−1.15)에 닿지 못한다**.
감사 원문 일치 — `Docs/reports/continuity_audit_v1.md` scene08 행(컷 `look_check/scene08/260731_w3_full/pt_noon_open_gap.png`): "③ 침상광장 림 가드레일이 붕 뜬 수평 튜브로 렌더됨. 좌측·중앙 구간에서 가로대 밑에 기둥이 아예 없고, 반대로 **짧은 기둥 4~5본은 가로대에 닿지 못한 채 포장 위에 홀로 서 있음**(기둥/가로대 높이 불일치)".
대조군: 유리 발러스트레이드(`build_guard` L1539-1583)는 `gz_fn = lambda _x,_y: pr["h"]`(=0.30, 파라펫 코핑 상면 상수)이라 같은 결함이 없다 — 즉 결함은 **경사 gz를 준 쪽에만** 발생한다.
- **원설계 의도**: **계단 편측 난간 법규 충족물로 v7 재건 때 신설된 것이며, '의도적 파손'이 아니다.**
1) 코드 주석/독스트링: 모듈 docstring L72 "**R2 편측 난간**: both flanks of the cascade carry a `props_kit.build_tube_railing` run." · 함수 docstring L1585 "R2 — one `props_kit.build_tube_railing` run on each cascade flank (radial lines)."
2) 도입 행: `Docs/audit_v4/gt_changes_w3.md` **GT-37/GT-38**(원장 §182-183·§356-357, 커밋 `0af6f24`, 2026-07-31) = v7 곡선 전면 재건. 근거는 `Docs/reports/stair_compliance_v1.md` L56 「scene08 선큰광장 남·북 계단 … **P0 L1** 계단참 1개 부족 · **P0 R1** 중간난간 1열 부족 · **P1 R2 편측 난간**」 + `Docs/reports/w3_s08_v1.md` L114 「**R2 편측 난간** | one flank only → **both flanks (`props_kit.build_tube_railing`)**」.
3) **파손 계열은 이 씬에서 이미 소거됐다**(=부유 난간은 파손 독트린의 잔재가 아니다): 모듈 docstring L38-41 "The v6 temporary marking (2 banded safety posts + a sagging tape) is **DELETED** — ruling `w3_execution_spec_v1.md` §1.6, ledger row **GT-7**"; GT-38 착지기록 "**v6의 *demolished* 1.8 m parapet gap 은 함께 사라졌다** — the opening is now a designed stair head". 그 뒤 08-05 독트린 **GT-57**(원장 §13, "파손·결손 난간 소거, 전 씬 연속 가드화", Authority: "위험 단서는 난간 자체(낭떠러지 표지)이지 난간의 훼손이 아니다")이 라이브러리 전체에 확정했고, `process_spec_v1.md` §5 **P-3** 에 "~~B-3 파손 난간 옵션~~ **08-05 독트린으로 소멸**"로 못박혔다.
4) 참고 — 씬 자체 self-check `h03_probes()` L616-627 은 이 난간을 0.25 m 간격으로 `_surface_z(px, py) + fr["rail_h"]` 로 **노면 추종**한다고 모델링한다. 스모크 모델과 실제 빌더 출력이 불일치 = 설계 의도는 노면 추종이었다는 직접 증거.
5) 이번 수리도 선신고됨: 원장 §41 **GT-85** — "Authority: 08-06 사용자 'tidy up the handrails that are hanging in midair'(08-06 감사: 림 가로대 밑 기둥 부재 + 기둥-가로대 높이 불일치) / Scope: 기둥·가로대 결속 체계 수리. **P-3(파라펫 개구 폐합)은 별도 gate 유지** / R-3 전용 / Status: OPEN".
- **충돌**: **수리 자체는 무충돌 — 오히려 08-05 독트린(가드=낙차 표지, 연속 가드화) 방향과 정합.** 단 세 개의 선을 넘으면 충돌한다:
① **§5 P-3** 「S08-B 파라펫 개구부 처리(B-1 폐합 권고) … 해제 gate = 한국 선큰광장 마감 사진 n≥5」 + GT-38 의 B-1 정체성 — 캐스케이드 머리 **150…210° 개구는 닫으면 안 된다**. 모듈 docstring L42-46: "the opening must sit on the grid axis (+X, y = 0), because sealing the approach edge with parapet would fill 6 of the 9 h0.3/h0.9 grid shots with parapet wall and make judgment impossible (the **scene19 d5 blackout precedent**)". GT-85 스코프도 P-3 유지를 명시.
② **계단 기하 불변** — riser **0.150** / tread **0.300** 은 피난방화규칙 §15①3 단서(중간난간 면제)의 근거값이고 docstring L64-71 이 "a *judgment* requirement, not a style choice" 라고 못박았다. 난간을 고치려고 계단·참을 건드리면 R1 P0 판정이 되살아난다.
③ **킷은 공유물** — `build_tube_railing` 은 scene06 나선 가드 등도 쓴다. GT-38 이 남긴 전례 "**Finding S08-F1, reported not fixed (kits frozen)**: `build_glass_balustrade` and `build_tube_railing` are X-axis-only … the kit owes a `bearing=` kwarg"(뒤에 `bearing=` 로 킷이 수정됨, `props_kit.py:614-628`)와 같은 성격의 **수직축 판(版)** 결함이다. 킷을 고치면 R-3 대상 씬이 늘고, 씬 로컬로 폴리라인을 세분해 우회하면 scene08 만 R-3.
- `process_spec_v1.md` §4-2(scene17 무난간 유지)·§4-18(의도적 함정 씬) 은 이 씬에 해당 없음. §4-6(tactile 기본 OFF)도 별건(`cue_tactile=False` 유지).
- **성격**: **은퇴한 파손 독트린의 잔재가 아니라 순수 버그** — 킷이 가로대를 '세그먼트 중점 지반 1점 위의 수평 실린더'로만 만드는데(props_kit.py:686-696) 호출부가 4.5 m 하강 런을 2점 폴리라인으로 넘겨(scene08:1604-1612) 가로대 하나가 통째로 수평이 됐고, 정작 씬 self-check(`h03_probes`)는 노면 추종을 전제하고 있어 '고치면 회귀'가 아니라 '고쳐야 모델과 코드가 일치'하는 사례다.

## scene09 — 호수 건너 구름 같은 것 (What's that cloud-like thing across the lake?)

- **요소**: **대안(對岸) 가을 산능선 벨트 `far_hills` + 그 밑을 받쳐야 할 `far_bank` 판 하나.**
`scenes/main/scene09_ghat_riverfront.py`(HEAD):
- L320-332 `far_hills=[...]` — 능선 **13개** dict `(cx, cy, sx, sy, h, tone)`; cx **78·76·79·77·80 / 98·100·99·101·98 / 126·128·125**, cy −96…+96, sx 15…26, h **11.0…19.5**
- L333 `hill=dict(blobs=11, blob_r=0.58, spread=0.92, body_frac=0.30, seed=91)`
- L2675-2709 `def build_far_hills(M)` — 능선마다 `add_box(.../Body, (cx, cy, base + body_h/2), (sx, sy, body_h))` (`body_h = h * 0.30`) + 그 위에 편평 타원체 11개 `add_sphere(.../Crown_k, (bx, by, base + body_h*0.60 + rz*0.30), (rx, ry, rz))`, `prof = 0.55 + 0.45*sin(πt)`, 인접 겹침 `ry = sy/nb * 1.55` (주석: "crowns must **overlap**, or a ridge reads as a row of separate balls (the first pilot's 'caterpillar')")
- 호출 L2153 (`build_river` 안, L2120-2153)
- **부유의 원인 한 줄**: L2686 `base = water_z + fb["above_water"]` = **−5.190 + 1.6 = −3.590** — 즉 모든 능선의 밑면을 far_bank 상면 z 로 잡는다. 그런데 실제 지반은 `FarBank` 박스 **한 장뿐**이다: L289-290 `far_bank=dict(x0=44.0, x1=74.0, y0=-40.0, y1=40.0, above_water=1.6, thick=2.4)`, 빌드 L2127-2131. 수면판도 L281 `water=dict(x_far=44.0, y0=-40.0, y1=40.0)` 로 x=44 에서 끝난다.
  → 능선 13개 중 **cx 98…128 대역 8개(x 88…141)는 밑에 지반이 전혀 없고**, 나머지 5개(x 68.5…88)도 x=74 밖으로 절반 이상 돌출하며 cy −84·+60 짜리는 y ±40 밖이라 통째로 허공이다. 밑면은 `add_box` 의 **수평 절단면 z −3.590**, 그 아래는 하늘/수면.
- 재질 L706-715 `hill_a=(0.268,0.196,0.078)` 단풍 · `hill_b=(0.288,0.252,0.086)` 은행 · `hill_c=(0.230,0.246,0.288)` "far ridge, washed toward the sky"; 알베도 검산표 L1185-1187
감사 원문 — `Docs/reports/continuity_audit_v1.md` scene09 행(컷 `pt_noon_g9_oblique.png` + `pt_noon_park_vista.png`): "③ 대안(對岸) 수목 매스가 **밑면이 직선으로 잘린 판상 덩어리로 물가 위에 떠 있음(하단 절단면과 그 아래 하늘·수면이 보임)**. 캐노피도 **풍선형 블롭**이라 배경이 판때기로 읽힘."
- **원설계 의도**: **폐기된 원경 건물 2동을 대신하는 '지평 폐합 장치'다 — 장식이 아니라 게이트가 걸린 기능물.**
1) 무엇을 대체했나(코드 주석 L296-307): "[W3 S09 · row (6)] **The 2 far-side building silhouettes are DELETED.** They were the v5/B-09-2 horizon-closure device (**'the top half of the frame was uniform teal, an infinity pool'**). G9 answers the same question differently and the image is the law here: there is **not one building** in the target … Replacement = `far_hills`, which closes the horizon **higher** (12–19 m vs 7 m) and over a **wider** span, so B-09-2's failure mode cannot return; `horizon_selfcheck()` asserts the elevation subtended at the two water cameras against the old boxes." 파일 헤더 L136-138 도 동일.
2) 원장 행: `Docs/audit_v4/gt_changes_w3.md` **GT-35**(09, "G9 renovation", 7행 1커밋) — "**(6)** the 2 far-side **building** silhouettes (`x0 66`, h 7.0, collider) are **DELETED** and an autumn hillside belt (6 ridges, 12–19 m) takes over horizon closure"; R-1 자기검증 목록에 **`horizon_selfcheck`** 포함. 상위 근거 = `w3_intake_v2_images.md` §2 scene09 (b)/(c)/(e) + §7 ruling 8 → **R09-1: scene09 = autumn**, 타깃 이미지 **G9**.
3) **이미 한 번 '판때기' 지적을 받고 부분 수정된 이력**(주석 L313-319): "[revised after the first pilot] the first belt was **9 ridges over y −40…+40 only**, which from `g9_oblique` filled one corner and left the rest of the horizon open, and its ridge **body box stood out as a hard slab** under the crowns (`body 0.55 h`). Two measured corrections: the belt is widened to **y −108…+108** … and the body drops to **0.30 h** with the crowns overlapping enough to hide it."
4) **재표현이 반드시 지켜야 할 제약 2건**:
   · `horizon_selfcheck()` L1606-1640 — `from_river`/`across_river` 두 수면 카메라에서 신 능선의 앙각이 **폐기 건물(h 7.0 @ x0 66)** 이 만들던 앙각 **이상**이어야 OK, 미만이면 `FAIL(지평이 낮아졌다)`. SMOKE 게이트에 물려 있다(L1889-1892 `ok5, _ = horizon_selfcheck()`).
   · **밑면 절단이 하늘을 새게 한다는 판정이 이 파일에 이미 있다** — far_bank 두께 v5 판정(L283-288): "thick 0.6 → 2.4: the bottom face was … **floating 1.0 m above the water (−5.19) → sky leaked under the slab and put a hard-edged white band (RGB 183–197) on the horizon of every water cut**. At 2.4 the bottom face sits at water_z − 0.8, safely submerged." far_hills 에는 이 교훈이 적용되지 않았다.
5) 이번 건도 선신고됨: 원장 §42 **GT-86** — "Authority: 08-06 사용자 'What's that cloud-like thing across the lake?' (= far_hills/far_hedge 실루엣 매스 — **밑면 절단 부유·풍선형 판독**, 08-06 감사 일치). GT-63 의 원경 매스 존치 판정을 본 씬에 한해 사용자 지시로 개정. Scope: 대안 매스 재표현(접지·실루엣·질감). R-3 전용. Status: OPEN".
- **충돌**: **정면 충돌은 없지만, 재표현이 반드시 통과해야 하는 게이트·조항이 넷.**
(a) **씬 정체성 = 지평 폐합**: 매스를 낮추거나 지우면 `horizon_selfcheck()` FAIL = B-09-2 '인피니티 풀'(프레임 상반부 균일 청록) 회귀. 가을(R09-1)·무건물(G9)도 고정 — 능선이 계절을 운반하는 유일한 장치다(헤더 L136-138 "the hills close the horizon **and** carry the season").
(b) **`process_spec_v1.md` §4-8** 「원경 흐림(대기 원근) 금지 — 서울 실측 소광은 90 m에서 2.6~7 % 대비 손실뿐」 — 현행 `hill_c`(원경 능선을 하늘 쪽으로 **탈색**, L713-714 "above `hill_a`/`hill_b` in value with a blue bias, which is also what G9 shows")는 채도만 건드려 통과 상태지만, **부유를 안개/블러로 감추는 해법은 §4-8 위반**이다.
(c) **이전 룰링 — 이미 명시 개정됨**: 원장 §19 **GT-63** 「**제외**(관용구 상이 — 존치): FarHedge/RidgeCrest/BackHedge/BgHedge/Overhang/**TreeLine** 계열(scene04·07·09·10·12·C2·N4·D3) = 원경 숲/실루엣 매스, **v6 C-5 blob 이 의도된 저비용 관용구**」 — 08-05 시점의 '원경 매스 존치' 판정. GT-86 이 "**본 씬에 한해** 사용자 지시로 개정"이라 못박았으므로 **scene09만 열리고 나머지 7씬의 같은 blob 관용구는 그대로다(횡전개 금지)**.
(d) **`fov_selfcheck()`** — GT-35 가 건 게이트("every element this row adds is asserted **outside the ±30° FOV of all nine `sc.grid_views` presets**, smallest margin **+3.2°**"). 대체 매스가 커지거나 앞으로 나오면 이 게이트를 다시 통과시켜야 하고, h0.3 판정컷의 낙차 은닉 전제(§6 체크리스트 ④)를 건드리면 R-3 을 넘어선다.
(e) §5 parked 중 이 요소를 잠근 행 없음(P-14 는 0.5~1.1 m 단일 바위 조달 공백, 별건). §4-7(계절 요소 prop 금지)은 낙엽 asset 배치 규칙이라 R09-1 로 고정된 씬 계절과 저촉 아님.
- **성격**: **의도된 설계이되 접지를 빠뜨린 미완성 구현** — 요소 자체는 살려야 하고(지우면 폐기 건물이 막던 B-09-2 인피니티 풀이 곧장 회귀, `horizon_selfcheck` FAIL) 고쳐야 할 것은 far_bank(x 44…74 · y ±40) 밖으로 나간 8개 능선의 z −3.590 수평 절단 밑면과 노출된 body 박스다. 같은 파일이 이미 far_bank 두께 0.6→2.4 판정에서 "밑면 절단 → 지평에 흰 띠"를 고쳤으므로, 그 판정을 능선에 확장 적용하는 것이 사용자 지시와 기존 룰링을 동시에 만족시키는 경로다.

## scene10 — 데크 접속 경로 자연 흐름 부족(무맥락 매트)

- **요소**: 지형 전체가 축정렬 BOX 8매 + rotX 슬래브 4매 + build_slope 세그먼트뿐이다. `git show HEAD:scenes/main/scene10_park_deck_switchback.py`
· `plates=[...]` :416-432 — `("UpperTrail", -40.0, -1.5, -1.60, 8.00, 0.00, 0.45, "grass")` :417 · `("TrailPath", -40.0, -1.5, -0.85, 0.85, 0.002, 0.06, "dirt")` :425 (폭 1.70 m 직사각 흙 띠) · `("LowerParkMain", -1.5, 44.0, -13.00, 8.00, -6.62, 1.50, "grass")` :426 · `("LowerPath", ... -4.40, -2.60, -6.618 ...)` :428
· `ybanks=[...]` :435-448 (rotX 4매) · `corridor=dict(y0=-2.60, y1=8.00, thick=7.00)` :479 — 폭 10.6 m 단일 띠
· **비판 대상인 '접속 경로' 자체** = `exit_paths=[("DeckExit", 24.10, 44.00, -0.90, 0.90, -6.618, 0.06), ("DeckExitLink", 26.00, 27.80, -2.60, -0.90, -6.618, 0.06)]` :494-495 → 직각 T자로 만나는 2 mm 흙 베니어 2매, `build_terrain` 안에서 `col=False`로 생성 :2280-2283. 지형 생성 루프 전체는 :2261-2299.
· 색: `grass_tint=(3.40, 1.55, 3.30)` :697 → straw #7F734E. `LowerParkMain`·`corridor`·`CorridorSlope_*` 가 전부 이 재질이라 감사 소견의 "평탄 황토색 매트"가 된다.
· 식재 공백: `trees=[...]` :570-575 12주가 전부 y≥4.5(북) 또는 y≤−5(남·하부), `shrubs=[...]` :580-589 13군이 전부 x≤12 — 데크 회랑(x 0…24.14, y −2.60…3.10) 남측을 따라 식재 0.
· 낙차 레지스트리: `TOTAL_DROP = -SEQ[-1]["z_bot"]  # 6.600 = 44 x 0.150, frozen (§9 P-2)` :838, `deck_module_selfcheck` :1414-1419 가 `n_tot == 44 and abs(drop_id - 6.600) < 1e-9 and abs(TOTAL_DROP - 6.600) < 1e-9` 를 매 스모크마다 단언("OK (6.600 정확, §9 P-2 동결)").
- **원설계 의도**: **① 매트의 정체는 '석축 갱도를 지운 자리'다.** docstring [W3 S3] :174-179 — "S3-10 — de-stacking, Option A. Flights tile the X axis ... the masonry shaft (BankCut + two-tier east wall + copings + berm) is deleted and replaced by a real 25.8 % corridor slope with level benches under the landings". PARAMS 주석 :404-415 이 같은 이유를 다시 진술한다 — "they existed only because a pure switchback makes no horizontal progress, so the ground beside it had to be effectively vertical ... A traversing deck descends with the hill, so the hill is modelled instead of walled — §2.B-2 C15/C16". 권한 문서: `Docs/briefs/s3_scene07_10_rebuild_spec_v1.md` §8.R OQ-6 (S3-10 Option A), 그리고 지면 프로파일의 법적 근거는 `ground_line()` docstring :854-871 의 [law] KFS-TRAIL 특별시방서 12-3 마 (계단하단부 300 mm 이내).
**② 황토색은 선택이 아니라 유도값이다.** :179-186 "S3-11 — season, 만추 leaf-off", PARAMS 주석 :690-697 — "§4.2-5 proposes the literal triple (0.62,0.60,0.42) `[assumed]` ... it darkens the lawn without making it dormant ... Derived instead from a straw target: **#7F734E**, linear (0.211,0.173,0.077), Y 0.174 → L* 48.8, R/G **1.22**, inside the project's ≤0.30 ground albedo clamp". 근거 이미지는 G10(`Docs/reference_photos/Generated Image - Scene10.jpg`), "G10 has **zero** green vegetation objects and wall-to-wall litter" :507-508.
**③ 접속 경로 2매는 '걷는 선 잇기'까지만 의도됐다.** docstring [GT-65] :191-217, 특히 (2) :204-213 — "the stair arrived at x 24.14 and stopped **on grass** ... the arrival landing railed off its own exit onto grass 1.0 m north of a `LowerPath` it never touched". PARAMS 주석 :483-493 — "Both are **2 mm veneers over ground that already exists** ... built **without colliders**: they add a trail reading, not a walking surface, so no collider, no drop edge and no GT-read AABB moves — the row stays R-3". 원장 행: `Docs/audit_v4/gt_changes_w3.md` §21 **GT-65** (:1205-1224) — Authority "08-05 갤러리 검수('이 씬 먼저')", Scope "낙차 6.60 유지 · P-2 파크 존중(archetype 재건 아님, 접합부·연속성 정리만)". 지시 원문은 `Docs/briefs/gallery_fix_plan_v1.md` §1 scene10 (:32-35) "데크 느낌이 끝까지 이어지게".
**④ 이번 비판의 원장 행은 이미 선신고돼 있다.** `gt_changes_w3.md` §33 **GT-77** (:1412-1418) — Authority "08-06 사용자 — 'the path the deck connects to flows naturally'", Scope "낙차 6.60·P-2 보존 유지", "GT 판정: R-3 전용. Round `260806_w3_fixqueue2`". 감사 소견 원문: `Docs/reports/continuity_audit_v1.md` :74-75 "① 데크가 무맥락 평탄 황토색 매트 위에 얹혀 있고 주변 지형·식재가 전무."
**⑤ 6.60 레지스트리의 출처**: `Docs/briefs/w3_execution_spec_v1.md` §9 **P-2** (:704) verbatim "total drop **6.60 m unchanged**. **Explicitly keep**: plank gaps (`flights.gap = 0.02`), the stone pit / log fence, and **`rail.broken_landing = 0`**".
- **충돌**: **(c) §5 parked register P-2 — 부분 충돌(경계 명확).** `Docs/briefs/process_spec_v1.md`:142 "P-2 | scene10 archetype 재건(D1 후보; 낙차 6.60 유지) — ~~broken_landing 보존~~ **08-05 독트린으로 파손 베이 조항은 무효**, 나머지(널 틈·돌구덩이·통나무 펜스 보존)는 유효 | 해제 gate: **사용자 레퍼런스 이미지 + KDS 34 00 00 원문**". gate의 앞 절반(G10 이미지)은 발화했으나(`s3_scene07_10_rebuild_spec_v1.md` 머리말 5행 "Their release gate ... **has fired**") 뒤 절반은 미발화 — `w3_execution_spec_v1.md`:704 가 KDS 34 00 00 을 "clause located, **text never fetched**" 로 기록. 그래서 최신 원장 두 행(GT-65 §21, GT-77 §33)이 모두 "P-2 파크 존중 / P-2 보존 유지"를 재확인한다.
  ⇒ **P-2가 막는 것**: 계단 archetype 재건(D1 후보 — 2련×14, riser 0.150/tread 0.360/폭 1.80, 휴게참 1개)과, 낙차 **6.600**(:838, :1414-1419) · 널 틈(`flights.gap`) · 돌구덩이(`outcrop` :551-554) · 통나무 펜스 4항의 이동.
  ⇒ **P-2가 막지 않는 것**: 지형·접속로·식재·드레싱. GT-77이 바로 그 스코프로 이미 선신고돼 있으므로 이번 작업은 P-2와 무충돌.
**(b) §4 do-not-touch — 2개 조항이 해법을 제한한다.** §4-7(계절·이벤트 요소 금지, 낙엽 asset은 낙엽 씬만): scene10은 승인된 낙엽 씬이라 낙엽은 합법이나, S3-11 핀이 :504-506 에서 "EXCLUDED by the pin: green leaf flush on any tree · green ground shoots · any flowering shrub (`Forsythia`/`Rhododendron` stay out of the pools)" 라고 못박았다 → **초록 식재로 매트를 덮는 해법은 금지**. §4-1(각진 헤지 유지) + 원장 §19 GT-63: 회랑변 식재는 `sc.place_shrubs` 경로여야 하고 `build_hedge`는 원경 TreeLine 계열 전용(:212-217 이 이미 그렇게 확인했다).
**(d) 과거 룰링/절차 — 1건 주의.** GT-77이 "GT 판정: **R-3 전용**" 으로 선언돼 있는데, 현행 접속로는 `col=False`(:2280-2283)다. 자연 흐름을 준다며 이 경로를 실제 보행면(collider)으로 승격하면 GT가 읽는 AABB가 새로 생겨 R-3 전용 선언을 벗어난다(§2.4 선신고 규율, R-1 필요) → 행 갱신이 선행돼야 한다.
**§5 P-7**(prop-엣지 규칙 M1~M6, LINT-9는 그때까지 WARN, :146): 새 경로변에 벤치·등·안내주를 붙이면 offset 규칙이 아직 parked 상태임을 인지할 것.
**(a) 씬 정체성/GT hazard: 무충돌** — hazard 4항(연속 가드·오픈 라이저·낙엽 은닉·남측 30° 무방호 사면 :13-27)은 전부 데크와 남측 사면에 있고, 접속로·주변 지형은 그 어느 것도 구현하지 않는다.
- **성격**: 결함이 아니라 **의도된 대수술의 미완성 잔여물**이다 — 평탄 매트는 S3-10이 7.40 m 석축 갱도를 지우고 그 자리에 넣은 단일 축정렬 벤치 슬래브이고 황토색은 S3-11 만추 핀에서 유도된 값이므로, 되돌릴 대상이 아니라 '그 다음에 놓였어야 할 지형·식재가 아직 안 놓인 상태'다(GT-65가 의도적으로 '걷는 선 잇기'까지만 범위를 잘랐다).

## scene11 — H형 육교인데 계단만 뜯어내고 급조 — 자연 접속으로 수리

- **요소**: `git show HEAD:scenes/main/scene11_footbridge_stairs.py`
· 상판 `deck=dict(x0=-13.20, x1=13.20, y0=-1.20, y1=1.20, z_top=5.50, thick=0.40, ...)` :178-180
· 타워 2기 `tower=dict(width=1.80, head=2.40, mid=1.80, lane_gap=0.10, z_top=5.50, pad_t=0.40, ...)` :161-163 · `east=dict(pivot=(14.10, -1.20), rot=-90.0, kind="switchback", kickplate=False)` :169-170 · `west=dict(pivot=(-14.10, 1.20), rot=90.0, kind="straight", kickplate=True)` :175-176
· **진행축(+X)을 막는 물건** = `build_balustrade(M, f"{prefix}/HeadGuardOuter", (px + A_HEAD, py + BA1), (px + 0.0, py + BA1), Z_TOP, height=ra["rail_h"])` :1904-1906. 동측 로컬 (a,b)→월드 (px+b, py−a) 매핑(:595-597)으로 풀면 **x = 15.00 선, y +1.20 → −1.20 의 횡단 난간** — 즉 상판을 걸어온 로봇이 정면에서 만나는 벽. `HeadGuardBack` :1907-1909 는 y=+1.20 측면.
· **"기둥 4본 위에서 끊긴다"의 실체** = `_pad()` :1824-1838 이 상부참 0.40 m 패드마다 `col_inset` 0.34로 CYL 4본을 세운다.
· 하행 계단이 프레임 밖인 이유 = 동측 다리 y −1.20…−10.04 / 서측 다리 y +1.20…+17.08 (docstring :29-35, 보행연속표 :57-72) — 두 다리 모두 **±Y**, 상판축과 직교.
· 감사 소견: `Docs/reports/continuity_audit_v1.md` :16-17 "육교 데크가 막다른 길 ... overview에서도 반대편 데크 끝이 **기둥 4본 위에서 그냥 끊기고 하행 계단/램프가 없음** — 육교가 한쪽에서만 내려갈 수 있는 구조로 읽힘."
- **원설계 의도**: **① S11-H 법령의 출처(사용자 육성).** 파일 헤더 `Law : Docs/surveys/w3_intake_06_10.md **§S · S11-H** ("한국 육교는 H형이야")` :9-11. 원문 `Docs/surveys/w3_intake_06_10.md` **§S — Supervisor addendum (2026-07-31, user feedback round 2)** :737-747 — "**S11-H (user, verbatim intent): Korean footbridges are H-plan.** *'육교가 너무 양쪽으로 뻗어있어. 한국 육교는 H형이야'* — stair towers run PARALLEL to the carriageway on each sidewalk (often switchback flights hugging the road edge), the deck crosses between them: an H in plan. Our scene11 ... extends stairs INLINE with the deck axis (I-plan), sprawling along the bridge direction — **wrong archetype**." 같은 절이 차별화도 못박는다 — "06 = spiral identity, 11 = H-form + grating see-through identity".
**② R11-1 / R11-2.** `Docs/surveys/w3_intake_v2_images.md` §2 scene11 (g) :472-475 — "**R11-1**: adopt G11's **asymmetric H** (one straight leg + one switchback leg) rather than a symmetric double-switchback, and record it as a refinement of the §S ruling. **R11-2**: G11's tactile is a **band across the walk at the stair foot**". 같은 §2 (b) :453-457 이 G11 판독 근거를 준다("left tower as a single straight flight ... right tower as a switchback hugging the far sidewalk").
**③ '계단을 뜯어냈다'는 픽스의 정체 — 문자 그대로 계단을 제거한 커밋은 없다.** `git log --follow scenes/main/scene11_footbridge_stairs.py` 상 해당 작업은 **`f05efb5`** "GT-36 · S11 — 육교를 H형으로 재구축: 계단 타워 2기를 차도축(±Y)으로 90° 회전, 상판이 그 사이를 건넌다" 하나뿐이다(그 뒤 `e9d7a50` 메시 재배치, `afc28f2` 태양 판정, `6ef387d`+`15b63e9` GT-42 보도 축소). 원장 표행 **GT-36** (`Docs/audit_v4/gt_changes_w3.md` :111)이 pre-state를 명시 — "Pre-state `[repro]`: both stair sets ran **inline with the deck axis** — east head landing x 13.20…15.00, flight A x 15.00…22.04 ... and the west set its 180° mirror ... out to x −30.88". 같은 행의 rider **(b)**: "the two `GratingBand_*` carriageway slabs (`x 15.0…31.5` and its mirror ...) are **deleted** (user rectangle ban; **the H-plan removes their pretext**)", rider **(c)**: 상판 보수패치 2매 삭제. 이어 **GT-42**(원장 :186, 커밋 `6ef387d`)가 보도를 29.50 → **11.50 m/측**으로 줄였고, 코드 주석 :214-221 이 사유를 적는다 — "29.5 m ... **its own justification died with the I-plan**: it was sized to hold a stair foot at x 30.88, and the H-plan put that foot at 16.90". ⇒ 상판축(+X)에서 보면 계단 → 그레이팅 → 보도가 차례로 사라지고 횡단 난간 하나만 남았다. 사용자의 "뜯어내고 급조"는 이 세 삭제의 누적 결과이지, 미완성 커밋이 아니다.
**④ 남은 막다른 끝은 hazard ① 그 자체다.** docstring :42-47 — "① **Deck-end / tower-head drop.** A robot crossing the deck (+X) meets the east head landing's outer edge at x = 15.00: beyond it is a **5.505 m** fall to the sidewalk (−0.005). At h0.3 the balustrade's lower band is open, so the far sidewalk and roadway show through and **the floor reads as continuous**. This is the drop the h0.3 preset grid frames head-on (**grid origin unchanged at 15.00/0/5.50**)." Camera axis note :92-101 — "under the H-plan that point is the east head landing's outer edge, i.e. the drop a deck-travelling robot meets head-on ... **no judge preset had to be edited** to make the H-plan legible (the scene17 R17-1 principle: legibility comes from geometry)". 원장 GT-36 착지기록(:158)도 "**Grid origin unmoved**: the 9 preset `eye`/`tgt` triples are byte-identical between the baseline and the pilot `manifest.json`".
**⑤ 이번 비판의 원장 행.** `gt_changes_w3.md` §36 **GT-80** :1437-1445 — Authority "08-06 사용자 — 'I asked to fix it into an H-shaped overpass, but it looks like they just removed the stairs and rushed the fix. Fix it neatly so it connects naturally.' (S11-H 법령: '한국 육교는 H형이야')", Scope "**H-plan 4지 계단 접속 완결(막다른 데크 금지)**, 접합 마감. **낙차·그레이팅 축 불변**", "R-3 + FRAME 선언. Round `260806_w3_fixqueue2`, baseline `260731_w3_full`".
- **충돌**: **(a) 씬 정체성 / GT hazard — 정면 충돌 위험(가장 중요).** '막다른 데크를 없앤다'를 **x = ±15.00 축 연장(정면 하행 계단·램프)**으로 풀면 hazard ①(5.505 m 단일 낙하 + h0.3 개방 밴드 은닉)이 소멸하고 h0.3 그리드 원점 (15.00, 0, 5.50)이 이동한다. GT-36 착지기록이 "9 preset eye/tgt byte-identical"을 증거로 삼았고 docstring Camera axis note(:92-101)가 이를 원칙으로 선언했다. GT-80 스코프의 "낙차·그레이팅 축 불변"도 같은 결론 → **접속 보강은 ±Y(4지) 방향으로만** 가능하다.
  추가로 hazard ②(동 중간참 킥플레이트 부재 vs 서측 존재)의 대조쌍은 R11-1 비대칭 때문에 이미 후퇴해 있다 — docstring :51-56 "**Honest limitation of R11-1**: the asymmetric H means the two mid landings are no longer *identical* boxes (east 1.80 × 3.70, west 1.80 × 1.80). The control pair is therefore held on the **matched edge class**", 상세는 `Docs/reports/w3_s11_v1.md` §3.1 :104-115("동측 측면 연단 (+16.90, −9.14) 킥플레이트 無 / 서측 (−15.00, +9.14) 有"). 다리를 2기 더 놓으면 이 '정합 연단 등급' 정의를 다시 써야 한다.
**(b) §4 do-not-touch — 2건.** §4-6 "**tactile(점자블록) 기본 OFF를 켜지 말 것**": R11-2가 계단 발치 횡단 tactile을 지정했지만 코드는 `"cue_tactile": False` :135 ("[v5.2 user] tactile paving is rare in reality - default OFF")이고 PARAMS 주석 :279 도 "`cue_tactile` stays **OFF by default** (the standing v5.2 user directive)" — 새 다리 발치에 tactile 밴드를 켜면 위반. §4-10 "사람·차량 금지".
**(c) §5 parked register — 무충돌.** scene11 해당 행 없음(P-13 무연석은 03·12·17).
**(d) 과거 사용자 룰링 — 2건 주의.** ① **R11-1(비대칭 H)**: G11에서 유도된 '직선 1련 + 스위치백 1련'이 현행 형태의 근거다. GT-80이 요구하는 '4지'로 늘리면 §S(H형)와는 정합하지만 R11-1의 *비대칭* 근거를 다시 진술해야 한다 — 원장에 사실을 기록할 것. ② **GT-42의 supervisor 채택 좌표**: 보도 11.50 m/측은 `w3_s11_v1.md` §10-1 의 filed number이고 코드 주석 :220-221 이 "this lane does **not** re-open a supervisor-adopted coordinate"라고 동결했다 → 신설 다리는 **보도 11.50 m 안에** 들어와야 하고, 구 I형 발치 자리(x 30.88)에는 이제 잔디밖에 없다.
**절차 충돌 1건**: GT-80이 "R-3 + FRAME 선언"만 했는데 계단 2련을 신설하면 낙차 연단·collider가 신설된다 → §2.4 상 R-1(hazard 레지스트리 재유도)까지 필요, 행 보강이 선행돼야 한다.
- **성격**: 결함도 폐기 독트린의 잔재도 아닌 **의도된 설계가 판독에서 실패한 사례**다 — 막다른 상판 끝은 GT-36이 일부러 남긴 hazard ①이고 그 지점에 h0.3 판정 그리드 원점이 박혀 있으므로, 고칠 것은 '내려갈 길이 없어 보인다'는 인상이지 그 낙차 연단이 아니며, 수리는 ±Y 방향 4지 계단 추가와 접합 마감으로 한정해야 한다.

## scene12 — 통과 판정 + 데크 밑 깨진 벽돌 의도 질문(= 호안 사석으로 답변됨)

- **요소**: `git show HEAD:scenes/main/scene12_riverside_deck.py`
· 호안 사석 단 7매: `rip_bot=-3.2` :275, `riprap=[dict(tag="A", ... z=-0.55), B −1.45, C −1.45, D(y 1.25..2.30) **z=-1.75**, E(y 2.30..3.90) −1.78, F(y 3.90..5.60) −1.95, G(y 5.60..7.40) −2.40]` :276-282 → `build_riprap()` :1553-1565 가 각 단을 `BOX(f"{ROOT}/Riprap_{tag}", ..., M["rock"], col=True)` 로 **축정렬 평판**으로 세운다.
· "벽돌"의 정체 = 재질: `M["rock"] = PBR(f"{ROOT}/Looks/Rock", sc.tex_path("rock_wall", "diff"), ... sca["rock_wall"])` :1419-1422 + `scale=dict(wood_dark=0.6, **rock_wall=0.7**, ...)` :382 — **석벽(masonry) 텍스처를 수평 평판에 0.7 m 타일로 반복**.
· 흩어진 판석 96개: `rocks=dict(n=96, seed=712, x0=-38.0, x1=44.0, y0=0.2, y1=4.8, s_lo=0.30, s_hi=1.10)` :287-288 → `sc._oriented_box(stage, f"{ROOT}/Rock_{i}", (rx, ry, top - s * 0.15), (s, s * 0.85, s * 0.7), M["rock"], rotz=yaw)` :1599-1602. **yaw만 U(0°, 90°), 사면 tilt 없음**, 침하는 크기의 15 %뿐.
· 부유 판독의 기전: `band_top(x, y)` :1579-1592 는 **돌 중심이 속한 단**의 top만 반환하고 단은 계단식(D −1.75 / E −1.78 / F −1.95 / G −2.40)이다 → 단 경계에 앉은 최대 1.10 m 돌은 아래 단 위로 최대 0.55 m 내밀며 0.17~0.45 m 떠 보인다. 스모크 검산 :1073-1089 는 "부유 0"을 출력하지만 그 검사도 **중심점이 어느 단 위인지만** 본다(같은 판정 로직 재사용).
· 물때 밴드 `waterline=dict(y0=2.90, y1=3.90, z=-1.78, proud=0.012)` :284, 빌드 :1567-1575.
· edge_void: `views["edge_void"] = dict(eye=[-4.80, 0.90, 0.35], tgt=[-3.05, 3.45, -1.62])` :1302, 근거 주석 :1289-1301.
- **원설계 의도**: **① '호안 사석'이라는 답은 코드 안에 문자 그대로 있다.** docstring Goal (4) :33-34 "revetment riprap tiers (−0.55/−1.45/−1.75/−1.78/−1.95/−2.40) + water −1.8 + reeds and silver grass", 그리고 자체 알베도 표의 라벨 `("호안 사석", "rock_wall", None, True, True)` / `("물때 사석", "rock_wall", "wet_tint", True, True)` :613-614.
**② 왜 이렇게 넓게 노출됐나 = W3 가시성 조치(사용자 요청 코드 :46-53).** "The real cause of 'the water looks glued to the deck top face' was not z but **visibility**: with exposed riprap covering only y 1.25..2.35 (1.10 m), the high-angle sight line was cut by the deck edge, riprap and shoreline were hidden wholesale ... **measure (1) widen the exposed revetment band 2.4×** (shoreline y 2.35 → 3.90, water y0 1.90 → 3.70) + film band + **96 rubble stones (was 42)** → the riprap revetment stays in frame." PARAMS 주석 :269-274 가 같은 결정을 반복하며 불변량을 못박는다 — "D top face −1.75 is unchanged, so the **deck edge -> riprap fall height 1.75 m also stays**". 판정 근거는 `Docs/audit_v4/judge_v6_rt_new7.md` §6 + director decision 4("GT drop 1.80 unchanged", :40-41).
**③ 잡석 96개의 두 번째 이유**: v6 ruling (4) — 주석 :285-286 "Count and range raised with the wider exposed band (answers ruling (4) '**smooth cut-stone paving -> not a rubble revetment**')" + 재질 주석 :380-381 "a 1.4 m tile turns the revetment riprap into 'smooth cut-stone paving'. **Halving it to 0.7** makes each stone block read half the size."
**④ 계획은 실암석이었고, 미집행 상태다.** `Docs/briefs/w3_execution_spec_v1.md` **N-A7** (:353) — "riprap gap fill | `rock_02` (463 mm) fills the lower half of **scene12's 0.30–1.10 m band**; `rock_03_broken` (1714 mm) is a boulder above the band | 12 | **P2**". `Docs/surveys/w3_intake_v2_images.md` §2 scene12 (a) :483 도 캐리 항목으로 "B3 reeds, **N-A7 riprap**, G-2 species" 를 명시. 즉 박스 96개는 실암석 USD로 교체될 예정이었으나 P2 우선순위로 남았다.
**⑤ edge_void hazard 설계.** docstring Hazard :14-26 — "the negative obstacle is the concealed drop, not a damaged guard: **the near water is not visible** — the sight line grazing the deck outer edge (y 1.25, z 0) reaches the water only at y = 8.75 m, so the **7.50 m stretch of riprap and water over y 1.25..8.75 disappears from frame (missing ground band)**." v7 §8 (1) :85-91 이 컷을 재조준했다 — "water_rough 0.08 → **0.14** ... and `edge_void` tgt pulled in to just short of the shoreline so the **exposed riprap band (y 3.03..3.90) sits at frame centre** (eye, slope and the **1.78 m hidden band** all unchanged)". 카메라 주석 :1289-1301 이 같은 수치를 반복("the through-edge slope −1.000/m is unchanged too, so the 1.78 m hidden band backing the ruling holds").
**⑥ '판석이 흩어져 떠 보인다'의 감사 원문**: `Docs/reports/continuity_audit_v1.md` :76-77 — "① **통행 연속성은 양호** — 산책로·자전거도로·잔디대가 프레임 양 끝까지 이어짐(**수정 불요**). 다만 **호안 위 판석들이 제각각 각도로 흩어져 일부는 사면에 떠 보임**. **edge_void(의도된 hazard)는 손대지 말 것.**"
**⑦ 직전 사이클의 씬 상태**: 원장 §22 **GT-66** (:1226-1246) — "scene12 픽켓 롤백: GT-43 원 단면 복원 ... Authority: 08-05 검수 — '원래 가드 느낌(기둥 Ø120 + 상·중 통나무 2단, 인필 없음)'", 픽켓 collider 129본 제거, "**Q3(edge_void 재조준)은 본 롤백으로 대체**".
- **충돌**: **(a) 씬 정체성/GT hazard — 사석 수치 3개가 고정 변수다.** 데크 상면 0.000 − 수면 −1.800 = **1.800**(스모크 :1055-1063), 데크 에지 → 사석 D 상면 **1.75**(:1068-1069), 은닉 밴드 **1.78 m**(:88-91). 사석 단 D의 top −1.75와 노출대 y 1.25..3.90 은 v6 measure (1)의 산출물이므로 재표현 시 **좌표는 유지하고 표현만 바꿔야** 한다.
**(c) §5 parked register — 2행 접촉.** **P-13**(`process_spec_v1.md`:152) "03·12·17 무연석(kerbless) 확인 | 한강 제방·자전거도로 실사진 확인" — 호안·자전거도로 단면을 함께 손대면 걸린다(사석만 손대면 무관). **P-14**(:153) "0.5~1.1 m 단일 바위 대역 공백 | 조달 — **블록하지 말 것**" — `rocks` 의 `s_lo=0.30, s_hi=1.10` 이 정확히 그 대역이라, 실암석 조달 공백을 이유로 이 행을 기각하지 말라는 뜻이다.
**(b) §4 do-not-touch — 직접 조항 없음(사석 관련 조항 부재) → 재질·자산 교체 자체는 무충돌.** 오히려 §4-14 "예산은 triangle 수가 아니라 instance 공유다 — **instance 수를 이유로 row를 기각하지 말 것**"(:126-127)이 N-A7 실암석 전환을 지지한다.
**(d) 과거 사용자 룰링 — 1건 명시적 금지 대역.** `process_spec_v1.md` §7 **Q3** (:181) "scene12 edge_void 재조준 — 보류 — 사용자 검수 후 결정" → `gallery_fix_plan_v1.md` §3 (:75-76) "Q2·**Q3(scene12 edge_void 재조준)**는 ... scene12 롤백으로 **대체됨 — 별도 답 불요**", 원장 GT-66도 동일. 감사 소견까지 "edge_void(의도된 hazard)는 손대지 말 것" → **edge_void의 eye/slope/1.78 m 은닉 밴드·수면은 재론 금지**.
**절차**: 08-06 사용자 판정이 '통과'였고 신규 행 GT-77~88(원장 §33~§44)에 **scene12 행이 없다** → 지금 이 씬에는 열려 있는 지시가 없다. 사석을 손대려면 §2.4 선신고로 새 행을 먼저 써야 한다.
- **성격**: 반은 설명하면 유지될 의도된 설계이고 반은 미집행 결함이다 — 사석 단·2.4배 노출 폭·96개 잡석은 v6 가시성 조치가 GT 1.80 m를 눈에 보이게 만들려고 **일부러 넓히고 늘린 것**(scene03 교량과 같은 유형)이지만, 그 표현 수단이 축정렬 박스 + 석벽 텍스처 0.7 m 타일이라 '깨진 벽돌'로 읽히고, `band_top`이 중심점만 보는 탓에 큰 돌이 단 경계에서 뜨는 것은 **P2로 남아 미집행된 N-A7(실암석 교체)의 잔여 결함**이다.

## scene14 — 측벽 handrail 왜 달았나 — 제거

- **요소**: `scenes/main/scene14_grandstair_illusion.py` (git show HEAD)
· 빌더: `def build_wall_handrail(M)` **:1618-1693** — 측면당 바 8 + 지주 13 = **42프림/측** (도크스트링 :1640 "Census: 8 bars + 13 posts per side = 42 prims"). 실제 프림: `CYL(f"{ROOT}/WallRail_{tag}/Bar_{j}", …)` **:1680-1683**, `CYL(f"{ROOT}/WallRail_{tag}/Post_{i}", …)` **:1688-1690**, 로그 :1692-1696.
· 파라미터: `parapet=dict(width=0.5, z0=0.35, thick=1.6, cap_t=1.4, rail=dict(r=0.019, lift=0.100, post_r=0.015, pitch=1.80, y_in=0.15, margin=0.03, embed=0.02))` **:272-274** (근거 주석 :242-271 — Ø38 mm·리프트 0.100·|y| 5.200·12베이 1.790 m).
· 호출: **:1939** `build_wall_handrail(M)` — `if cfg["hazard_stairs"]:` 블록 안이지만 **`cue_railing` 토글 밖에서 무조건 호출**. 대비: 진짜 난간 경로는 `if cfg["cue_railing"]:` **:1886-1909**(현재 False).
· 자기검증: `_wallrail_selfcheck()` **:817-836**(⑱ 벽 0.85~0.95 · ⑲ 마루 ≤1.10·순간격 ≥0.05 · ⑳ 보행면 비돌출), 호출 **:932**, 배너 :921-922.
· 재질: `M["rail"] = PBR(f"{ROOT}/Looks/Rail", …)` **:1076** — 단 이 재질은 FlagPole **:1787** 및 cue_railing 경로 **:1908** 이 공유.
· 측벽 본체(파라펫 몸통·상부 haunch·헤드 뉴얼·토 엔드캡)는 `build_parapets` **:1495-1614** 이며 GT-70 은 여기를 **한 줄도 건드리지 않았다**(:1626-1631).
- **원설계 의도**: **왜 달렸나 — 08-05 사용자 답변을 GT-70 이 실행한 것.**
· 코드 헤더 **:61-78** `═══ [GT-70] 08-05 gallery answer ═══`: 사용자 룰링을 그대로 인용 — *"측벽은 적당한 벽 겸 handrail 로 읽히게"*. 이어 :74-78 "**The side wall gains a handrail** (`build_wall_handrail`). The wall itself is unchanged — its top face stays on the nosing line + 0.950, the top of the 08-05 doctrine band (wall-rail 0.85~0.95) — and a Ø38 mm rail on Ø30 stanchions now rides 0.100 above it, so the guard line reads at 1.069 m. **The rail is the drop cue; the wall alone was reading as a raking slab.**"
· 빌더 도크스트링 **:1619-1627**: "**Why.** The 08-05 doctrine is that the guard itself is the drop cue. This scene had the guard — a 0.950 m wall over the nosing line … — but nothing on it that reads as a *rail*, so at `beauty_overview` / `side_reveal` scale the two flanks were raking slabs and the 6.0 m drop between them had no line drawn along it."
· **지시 원문(= 이번 롤백이 되돌리는 조항)**: `Docs/briefs/gallery_fix_plan_v1.md` **:42-44** — "### scene14 — 착시 대계단 / · 화강석 재질 집착 중단 … / · 측벽은 **적당한 벽 겸 handrail**로 읽히게: 재질 단순화 + **측벽 위 handrail 추가 검토**." 원문은 *"검토"*(consider)였고 GT-70 이 이를 실행으로 확정했다.
· 원장: `Docs/audit_v4/gt_changes_w3.md` **:1321-1341** "## 26. GT-70 — scene14 재질 단순화 · 측벽 handrail (선신고)", §4 착지기록 :1336-1339 "본 씬 귀속: WARN 3(FAIL 0) — … + **측벽 상부 handrail 신설**(벽 겸 handrail 읽힘, h 0.85~0.95 독트린)". 롤백 행은 이미 선신고됨 — **:1419-1425 "## 34. GT-78 — scene14 측벽 handrail 제거 (GT-70 부분 롤백, 선신고)"**, Authority: 08-06 사용자 "Why did you add a handrail on the wall? Remove it".

**착시(illusion) 정체성이 측벽에 요구하는 것 — 도크스트링 근거.**
· 헤더 **:9-14**: "Walking forward from the small upper viewing plaza, **only the landings of the 40-step stair are visible and it is misread as a flat terrace.** In reality a **6.0 m drop hides between the landings.** The perspective illusion of the width opening from upper y±3 to lower y±5 strengthens the concealment." — 씬의 연구 정체성은 **낙차 은닉**이다. :35-38 "The **illusion is the research identity and it is frozen** … `_stair_selfcheck` asserts the drop to 1e-9".
· `build_parapets` 도크스트링 **:1496-1502**: 측벽 형태의 근거는 v5.1 사용자 피드백 인용 — *"stairs are good. **Remove the stepping (stepped parapet) of the white railings on both sides.**"* → 톱니(계단형) 실루엣을 없애고 "a polyline of **straight rakes per section, level only at the landings**" 로 재건.
· :1557-1558(마지막 줄) "Both are at |y| >= 5.05, **outside the stair width (w_bot 5.0) -> hazard geometry and illusion unchanged**."
⇒ 측벽에 걸린 요구는 ① 계단형 실루엣 금지 ② |y| ≥ 5.05 밖 유지 **두 가지뿐**이고, "낙차선을 그어라"는 요구는 어디에도 없다. GT-70 이 얹은 근거(가드=낙차 단서)는 08-05 독트린에서 온 것이지 씬 정체성에서 온 것이 아니다.
- **충돌**: **무충돌** (제거는 깨끗한 롤백). 4축 점검:
(a) **씬 정체성/GT hazard 설계 — 무충돌, 오히려 순방향.** 레일 계열은 `col=False` 드레싱이고 벽·파라펫·노징·낙차 좌표 무변경(:1626-1631 "no parapet body, haunch, newel or end-cap coordinate moves, and the bar family carries **no collider**"), 착시 기하 자기검증(:1e-9 드롭 어서션)은 레일과 무관. 게다가 GT-70 자신이 "**The rail is the drop cue**"(:78)라고 적었는데, 이 씬의 정체성은 낙차 **은닉**이므로 레일 제거는 정체성을 훼손하는 게 아니라 복원한다.
(b) **process_spec_v1.md §4 do-not-touch — 해당 조항 없음.** §4-2 는 scene17 전용, §4-6(tactile)·§4-9(전주)·§4-15 모두 무관.
(c) **§5 parked — 무충돌.** P-15(`process_spec_v1.md:154` "scene14 D11 파라펫 하단 결손(실기하 버그, 판정컷 비가시) | 구조 수리는 예산 밖 — 유예")는 파라펫 **하단**이고, 코드도 :81 / :1631 에서 "P-15 … stays deferred and is NOT touched here" 로 분리해 두었다. 레일만 걷어내면 P-15 유예 상태는 그대로 유지된다.
(d) **구 룰링 — 표면 충돌 1건, 이미 해소되어 있음.** 08-05 독트린(`gt_changes_w3.md:861-864` GT-57 Authority: *"위험 단서는 난간 자체(낭떠러지 표지)이지 난간의 훼손이 아니다"*)과 부딪히는 듯 보이나, `process_spec_v1.md` **§4-2 :110-111** 의 단서가 정확히 이 경우를 면제한다 — "(08-05 독트린과 공존: **난간이 '있으면' 낙차 표지라는 것이지, 실관행상 없는 곳에 세우라는 뜻이 아님**.)" 이 씬의 실관행 선언은 SCENE_CONFIG **:108** "cue_railing: False — **A monumental stair is open-sided**". 같은 08-06 세션의 GT-87(scene20, `gt_changes_w3.md:1499-1505`)에서 사용자가 독트린을 근거로 난간을 **추가**시킨 것과도 모순 아님(그쪽은 사선 계단, 여기는 개방형 기념계단).

**실행 시 주의 2건(충돌은 아니나 놓치면 깨짐):** ① 프림만 지우면 `_wallrail_selfcheck` ⑱⑲⑳(:817-836·호출 :932)이 죽은 검증으로 남고 배너 :921-922·census 로그 :1692 도 거짓말이 된다 — 함께 회수해야 함. ② `M["rail"]` / `Looks/Rail`(:1076)은 FlagPole(:1787)과 cue_railing 경로(:1908)가 공유하므로 **재질은 삭제 금지**. ③ 원장 GT-78 행(:1419-1425)에 **baseline 미기재** — scene14 의 현행 기준선은 GT-70 이 착지한 `260806_w3_fixqueue`(:1332-1335)이지 형제 행들이 쓰는 `260731_w3_full` 이 아니다.
- **성격**: 결함도 퇴역 독트린의 잔재도 아닌 **하루짜리 과잉집행의 청산** — fix_plan 의 "handrail 추가 **검토**"(gallery_fix_plan_v1.md:44)를 GT-70 이 실행으로 확정했다가 사용자가 즉시 되돌린 것이므로 설명해서 지킬 것이 없는 깨끗한 롤백이고, 정작 남는 진짜 결함은 "A monumental stair is open-sided"(cue_railing=False, :108)를 선언해 놓고 그 토글 **밖에서** 무조건 난간을 세운 호출 구조(:1939) 쪽이다.

## scene16 — 정체성 불명 — 도로 밑 지하보도처럼 읽히게

- **요소**: `scenes/main/scene16_canopy_shadow.py` (git show HEAD)
**(1) 정체성을 만드는 것 = 캐노피와 그 그림자**: `canopy=dict(x0=-1.0, x1=4.6, y0=-2.0, y1=2.0, z_roof=2.6, post_r=0.13, roof_t=0.14, base_z=-0.05)` **:136-137**, 빌더 `build_canopy` **:852-858** (`sc.build_canopy` 솔리드 루프 + 기둥 4).
**(2) 하부 통로 계열**: `pit=dict(x0=0.0, x1=4.48, y0=-1.5, y1=1.5)` **:117** · `stairs`(14단 riser 0.15·tread 0.32, base_z −2.6) **:119-120** · `passage=dict(x0=4.48, x1=14.0, z_top=-2.1, base_z=-2.6)` **:122** · `east_stairs=dict(x0=14.0, …)` **:128-130** · `wall=dict(thick=0.3, y_in=1.5, parapet_top=0.15, base_z=-2.6, x1=18.48)` **:133** · `perim_rail=dict(y=1.65, x0=0.0, x1=18.48, …)` **:144-146**. 빌더 `build_walls` :768-, `build_stairs`(하부 통로 슬래브 포함) **:784-797**, `build_east_exit` **:799-**.
**(3) 상부 도로는 존재하지 않는다**: 씬 전체에 **`road=dict` 자체가 없다**. 지상은 `walk=dict(x_w=-12.0, x_e=22.0, y_s=-8.0, y_n=8.0, z_top=0.0, thick=0.5)` **:115** 한 장 + BS-4 배경 street wall(:277-325)뿐이고, 연석은 색만 있고 프림이 없다(K5 유예).
**(4) 이름만 '지하보도'인 자리들**: 헤더 :7 "scene02_underpass.py (urban skeleton)" · **:127** "This completes the scene as an **'underpass'**" · :106 cue_sign "one sign_exit (**underpass** exit)" · **:328** "context dressing … 'a downtown plaza with an **underpass entrance**'" · :360 Exit(−1.6, 2.6) "the underpass entrance exit sign".
- **원설계 의도**: **T20 정체성 — 상부 암밴드가 위험 신호(T3의 반전).**
· 코드 헤더 **:5** "Type : **T20 canopy stair (the inverse of T3 — the upper shadow band is the danger signal)**", :9-13 "in noon light the whole stair is sunk in the canopy shadow and reads as a dark band — **the inverse of T3 (dark below): here the upper shadow band that floats above is what signals the drop.** The cue_nosing yellow strip survives inside the shadow at low contrast."
· 유형표 원문 `Docs/surveys/stair_typology_survey_v2.md` **:28**: "**T20. 캐노피/지붕형 계단** … 지붕이 **상부에 짙은 그림자 띠**를 드리워 상단 단코가 암부로 소실(**T3 지하 암부를 지상 캐노피로 재현**) … **T3는 하부 암부, T20은 상부**". T3 = 지하도 = scene02(같은 문서 :41 "기구현 5종 = scene01 T1 광장 / **T3 지하도** / …").
· 착지 확인 `Docs/legacy/scene_library_v3_status.md` **:21**: "07-27: **개구+캐노피 음영이 단일 암밴드로 융합, 낙차 경계 흡수**" — 즉 개구 그림자와 캐노피 그림자가 **하나의 암밴드로 합쳐지는 것**이 이 씬이 낸 결과물이다.

**하부 통로는 무엇에 이어지도록 되어 있었나 — 원 스펙에는 통로가 없다.**
· 원 스펙 `Docs/briefs/multi_scene_brief_v3.md` **:59**: "**scene16_canopy_shadow (T20)** — 캐노피 계단: **지하상가형 입구지만 지상형** — 14단(폭 3), 위에 반투명 대신 솔리드 루프(옅은 회색)+기둥 4, 정오광에서 계단 전체가 캐노피 그림자 안. 주변 밝은 보도. 특색: T3의 반전 — 하부가 아니라 **상부 그림자 밴드**가 위험 신호." → 원안은 **지하상가 출입구의 지상부**이고, 하부 통로의 목적지는 §D 어디에도 쓰여 있지 않다(통로 자체가 스펙 문장에 없다).
· 통로·동측 계단은 나중에 붙은 **막다른 길 수리**다 — 코드 **:123-127**: "[audit v4 A1] east exit stair — there used to be a full-width blocking wall (Wall_E) at x=14, so **you went down 2.1 m, walked 9.5 m and hit a dead end with no way out**. … This completes the scene as an 'underpass'." 즉 통로가 이어지는 곳은 **동측 지상 복귀(x 18.48)** 뿐이며, 그 이상의 접속처(상가·역사·도로 반대편)는 설계된 적이 없다.
· 시대고증도 지하상가 쪽: `Docs/surveys/era_consistency_survey_v1.md` **:272** "16 | 캐노피 그림자 계단 | 1990s structure + 2010s canopy/remodel | An **지하상가-type entrance set in an open sidewalk.** Underground arcades are 1970s–80s … a solid modern canopy over the head of the stair is a later addition, which is why the scene reads two-layer."

**상부 횡단 도로는 검토된 적이 있는가 — 기록 전무. 08-06 이 최초.**
· W3 인테이크 scene16 행(`Docs/surveys/w3_intake_v2_images.md` **:592-630**)의 지시는 ⑴ 점자블록 위치와 ⑵ 배경(G2 street wall) 두 가지뿐이고 도로 항목이 없다. 도로에 가장 근접한 것은 K5 연석인데, 레드팀이 못을 박았다 — `Docs/archive/reports/redteam_w3_intake.md` **:132**: "scene16 `M[\"curb\"]` material with no 보차도 curb prim ✔ (nuance: scene16 has **no `road=dict` at all**, so 'colour without geometry' is true but **there is also no carriageway to curb**)". K5 는 `Docs/reports/w3_s16_v1.md` **:497** 에서 Lane-1 유예.
· 최초 제안은 08-06 사용자 발화로 이번에 선신고된 행: `Docs/audit_v4/gt_changes_w3.md` **:1427-1434** "## 35. GT-79 — scene16 정체성 재구축: 도로 횡단 지하보도 (선신고) / Authority: 08-06 사용자 — 'I don't know Scene16's identity … it should feel like it crosses the road, like an underpass'. T20(캐노피 그림자 계단) 정체성 위에 **상부 횡단 도로**를 신설해 '도로 밑을 지나는 지하보도'로 재문맥화. / Scope: … 계단·하부 통로·그림자 밴드 단서(씬 정체성 T20)·GT 불변 — 상부 도로·연석·접속 보도가 신설 dressing."
- **충돌**: **충돌 3건 — 전부 '도로를 어디에, 어떻게' 의 형태를 구속한다.**

**(a) 씬 정체성 + 라이브러리 중복 — `w3_intake_01_05.md` §02-A(:756~) 의 명시 룰링.** :818-822: "**Affected scenes** — 02; **scene16 (지하보도 입구, which `tonglam_v2.md` scores PASS and whose canopy is explicitly praised) is the in-library reference for Option B and must not be disturbed.** Compare the two before choosing, **so the library does not end up with two scenes of the same type and the same canopy.**" 16을 실제 도로밑 지하보도로 만들면 T20의 존재 이유("T3 지하 암부를 **지상** 캐노피로 재현", stair_typology_survey_v2.md:28)가 소멸하고 scene02(T3 지하도)와 동종 씬이 된다. → **도로는 지상 맥락으로만 들어와야 하고, 계단·개구는 '지상 노출 트렌치'로 남아야 한다.**

**(b) GT-V(개구 위 요소 금지) — `Docs/briefs/ground_kit_spec_v1.md` §6.3 :734-741.** 규칙 원문: "**규칙 GT-V**: `plan_ground(voids=…)` 로 받은 개구·공동 위에 **어떤 요소도 걸치지 않는다.** … 자기검산이 **전 요소 AABB × 전 void 교차 0** 을 어서션한다." 이어 :740-741 "동일 규칙을 **낙차 에지**에도 적용한다 — … **다리처럼 이어지면 '낙차가 없는 것처럼' 보이는 가짜 연속 단서**가 된다." 씬 코드가 이미 이 규칙 때문에 배경 apron 을 3조각으로 쪼갰다 — **:320-322** "Kept as three strips, not one slab, **so that nothing is laid over the trench void (GT-V)** — the east strip starts 3.52 m past the trench end at x 18.48", 피트의 void 선언은 **:677-679**. ⇒ **통로 위에 도로 슬래브(복개부)를 얹는 방식은 규칙 문언과 정면 충돌**한다. (규칙의 취지는 지면 평면·줄눈·도색의 가짜 연속이므로, '명시 선언된 구조물 데크'가 예외인지 여부는 **은묵히 짓지 말고 별도 룰링으로 받아야 할 항목**이다.)

**(c) 기하 한계 — 낙차 2.1 m 는 GT 불변인데 복개하면 유효고가 그것뿐.** `passage z_top=-2.1 / base_z=-2.6`(:122), 트렌치는 지금 **지붕 없는 개방 트렌치**(지상 둘레는 `perim_rail` x 0…18.48, :144-146). 도로로 덮으면 순 유효고 ≤ 2.1 m 이고, 깊이를 늘려 푸는 길은 GT-79 스코프 자신이 막아 두었다 — "계단·하부 통로·그림자 밴드 단서(씬 정체성 T20)·**GT 불변**"(gt_changes_w3.md:1432-1433).

**(추가 구속, 충돌은 아님)** · §4-11 + GT-59(`process_spec_v1.md:120-122`): 새 차도를 ground_kit 차도류 프로파일(P4/P7/P8)로 깔면 **맨홀·잡초 전면 소거** 조항이 그대로 적용된다. · §4-6(:116): tactile 기본 OFF 유지 — 단 이 씬의 계단머리 점형은 §7-4 BOTH BANDS 룰링으로 **토글 밖 무조건 배치**가 이미 확정(:95-101, :103-105)이므로 도로가 생겨도 그 밴드는 건드리지 않는다. · §4-10: 사람·차량 금지 — 도로는 깔되 **차량은 놓을 수 없다**.
- **성격**: 캐노피=상부 암밴드(T20)는 **설명하면 사용자가 지킬 만한 의도적 설계**(scene03 다리와 같은 부류)지만, '정체성 불명' 자체는 오해가 아니라 실제 결함이다 — 원안의 '**지하상가형 입구지만 지상형**'(brief_v3:59)과 코드 곳곳의 '지하보도' 명명(:127·:328·:360)이 어긋난 채 지상에 도로가 한 줄도 없어(`road=dict` 부재) 어느 정체성으로도 완성된 적이 없기 때문이다. 따라서 도로 신설은 정당하되 **트렌치를 덮지 않는 지상 맥락**으로 놓아야 T20(지상 반전쌍)과 GT-V가 함께 살고, 복개형으로 가려면 02와의 중복 금지 룰링(§02-A)과 GT-V 예외를 사용자 결재로 먼저 열어야 한다.

## scene17 — 씬 목적 불명 · 본선이 사면에 너무 붙음 — 이격 확장

- **요소**: `scenes/main/scene17_ramp_pair_hangang.py` (git show HEAD)
**(1) '본선' = 제방 마루 자전거도로(아스팔트)**: `crown_bike=dict(x0=-4.0, x1=0.0, proud=0.006, embed=0.06)` **:179** — 폭 4.0 m, **강측 가장자리 x1 = 0.0 이 낙차 시단과 정확히 일치**. 빌더 `build_levee` **:1200-1216**(BOX 가 levee y 전장 −30…48 을 그대로 덮음, `levee=dict(x0=-24.0, x1=0.0, y0=-30.0, y1=48.0, z_top=0.0, thick=3.6)` :176). 중앙 파선 `crown_line=dict(x=-2.0, …)` :180.
**(2) 본선과 사면 사이의 완충 = 0.** 보도는 반대편(`crown_walk=dict(x0=-7.5, x1=-4.5, …)` :177), 0.5 m 식재대도 보도–자전거도로 **사이**(`crown_green=dict(x0=-4.5, x1=-4.0, …)` :178). 마루 끝에는 5 cm 코프뿐 — `cope=dict(x0=-0.20, x1=0.05, h=0.05, y_segs=((-30.0,-1.5),(1.5,2.6),(4.4,48.0)))` **:233-235**, 빌더 :1235-1241; 레지스트리에서 이것은 낙차가 아니라 up_step(:593-597, 자기검증 :960-962).
**(3) 사면·낙차**: `slope=dict(segs=((0.7,0.18)…(1.0,0.50)), thick=3.0, …)` **:239-241** — 7세그 폴리라인 평균 1:2, 높이 3.2.
**(4) 대비쌍 본체**: `stairs=dict(x0=0.0, riser=0.16, tread=0.32, nsteps=20, y0=-1.5, y1=1.5, …)` **:242-243** + `ramp=dict(p0=(0.0,4.0), length=25.6, width=2.5, e_up=0.60, deck_t=0.35, …)` **:304-310**, 빌더 `build_ramp` **:1324-1385**.
**(5) '목적 불명'의 물리적 원인 후보**: 램프 진입 apron 이 **`apron=dict(build=False, …)`** **:309-310** 으로 꺼져 있고(빌더 분기 :1363-1370), 그 사유가 :1371-1385 — "the 'crest ↔ ramp start' step … is already covered by the crest slab … what remains is a **0.152 m threshold at most**". 즉 램프 머리는 마루와 **접속 프림 없이** 잔디에서 시작한다.
- **원설계 의도**: **왜 '쌍'인가 — 경사도와 낙차를 분리 학습시키는 대비쌍.**
· 원 스펙 `Docs/briefs/multi_scene_brief_v3.md` **:60**: "**scene17_ramp_pair_hangang (T21)** — 한강 고수부지형: 제방 콘크리트 계단(20단 폭 4) + 바로 옆 자전거/휠체어 램프 … 특색: **같은 낙차의 '주행 가능 경사'가 인접 — 모델이 경사도와 낙차를 분리 학습해야 하는 대비쌍.**"
· 코드 헤더 **:6** "Type : **T21 ramp-stair contrast pair (same 3.2 m drop — stairs vs drivable grade)**", :107-108 "**Both routes drop the same 3.20 — the basis of the contrast pair.** Stairs 50% vs ramp 12.5% (was 8%): the contrast is 4.0x instead of 6.25x, and **it now fits one frame**."

**왜 본선이 마루에 붙어 있나 — 확폭이 육지쪽으로만 이뤄졌고, x=0 은 한 번도 재검토된 적이 없다.**
· 코드 **:166-175** "[W2-D · spec §5.9 17 (2)] Cross section re-cut. **The old crown was walk 3.0 (x −6..−3) + bike 3.0 (x −3..0)** with the two hard surfaces butted together. §5.9 prescribes **bike 3.0 -> 4.0, walk relocated, 0.5 m planting strip between them**" — 구판도 신판도 강측 끝은 **x=0 고정**, 확폭분 1 m 는 전부 육지쪽(−3 → −4)으로 갔다.
· x=0 이 고정인 이유는 좌표 규약 그 자체 — 헤더 **:123** "Coordinates: Z-up, m, travel axis +X … **Drop start edge x=0.**" 이고, R17-1(ii)가 "the flight's `y0/y1/riser/tread/nsteps/x0` are **frozen**"(:37-41)로 재확인, 자기검증도 매 실행 "**계단 낙차 시단 x 0.00 · GT drop edge 불변**"(:965-967)을 찍는다.
· 자전거도로의 존재 자체가 한정 허가 사항 — **:166-168** "the Han-river levee bikeway is **the one place the supervisor left the bike road valid** (§5.7 ruling, 07-29: 'scene03 stays natural, **the bike road is valid only on the scene17 levee**')".

**무난간(§4-2)·무연석(P-13)의 근거는 씬 안에 이미 완결되어 있다.**
· SCENE_CONFIG **:144** "cue_railing: **False** — Han river levee stairs are **customarily unrailed**"; 헤더 :52-54 "**There is no guard** (Han river levee stairs are unrailed by custom). The river-side edge of the diagonal ramp is only a kerb (h0.15) … — **a below-code reality**" (선언된 below-code 2건: :55-70).
· 헤더 **:67-78** "**S06-B kerbless-by-design — VERIFIED, EV-C closed** … 「도로의 구조·시설 기준」 제16조's 연석 separates a **carriageway** from a footway, and what this crest carries is a 자전거도로 … **A 보차도 경계석 would be a fabrication.** `infra_kit.build_curb_line` is therefore **not** called here."

**'목적 불명'은 재발 지적 — 1차 처방이 이미 있었고 실패했다.**
· 2차 리뷰 원문 `Docs/surveys/w3_intake_v2_images.md` **:63 / :634** "**Scene17은 계단 정체성이 좀 사라진 느낌**이긴 한데, 수정 깔끔하게 진행해줬으면 좋겠고". 진단 **:645-647** "the ramp is **13× the stair's plan length**, so **the contrast pair reads as a ramp scene with a stair in it**". 룰링 **:659-662 / :1099-1101** "**7. scene17 — R17-1 = (ii).** Shorten/re-site the 40 m ramp so the stair-ramp pair reads in one frame; **no flight widening** (GT stays A); **judge presets untouched — legibility comes from geometry, never from camera edits.**" 실행 결과는 코드 :244-303(길이 40.0→25.6, 비 4.84→3.59, 재시도 3안 비교 기록).
· 이번 행: `Docs/audit_v4/gt_changes_w3.md` **:1443-1450** "## 37. GT-81 — scene17 본선-램프 이격 확장·정리 (선신고) / **Authority**: 08-06 사용자 — '**The main road is right next to the slope, so please widen the distance and trim it neatly.**' / **Scope**: … 램프 쌍 기하(씬 정체성)·무난간 관행(§4-2) 불변 — **본선 이설·완충 정리**."
· 연속성 감사가 짚은 실제 증상 `Docs/reports/continuity_audit_v1.md` **:22-25**: "① **램프 슬래브가 잔디 속 고립 판.** 상단 끝·하단 끝 모두 잔디에서 각지게 끊기고 **상단 제방도로와도, 하단 강변도로와도 접속하지 않음**" / "① 제방 마루 좌측 보도가 연속체가 아니라 분절된 포장 슬래브들. **차도는 마루 너머까지 이어지는데** 보도·회색 띠는 각각 잔디에서 사각으로 끝나 별개 판으로 읽힘."
- **충돌**: **충돌 3건 + 등급 주의 1건.** (대비쌍 정체성 자체와는 무충돌 — 이격은 본선을 육지쪽으로 물리면 확보되고 계단·램프 기하는 손대지 않아도 된다.)

**(a) §4-2 — 마루에 펜스/난간을 세우는 해법은 금지.** `Docs/briefs/process_spec_v1.md` **:110-111**: "2. **scene17 계단 무난간** — 한강 제방 실관행; **`cue_railing=False` 유지.** (08-05 독트린과 공존: 난간이 '있으면' 낙차 표지라는 것이지, 실관행상 없는 곳에 세우라는 뜻이 아님.)" 더 근본적으로, 마루선에 수직 요소를 세우면 이 씬의 은닉 전제가 즉사한다 — 헤더 :46-51 및 자기검증 **:930-948**("[h0.3/h0.9 grazing 은닉 검산] 마루 어깨(x=0,z=0) 스치는 시선 … ⇒ 은닉 컷에서는 둔치 원측만 지평으로 남아 … **negative obstacle 성립**").

**(b) §5 P-13 — 새 이격대에 보차도 연석을 끼우는 해법은 금지.** `process_spec_v1.md` **:152**: "| **P-13** | **03·12·17 무연석(kerbless) 확인** | 한강 제방·자전거도로 실사진 확인 |" — 해제 gate 는 여전히 live 이고, 씬 도크스트링은 이미 연석을 "**a fabrication**"(:74)으로 판정했다. 완충은 **잔디/식재대**로만 가능하다(기존 `crown_green` :178 의 어법 확장).

**(c) R17-1(ii) 룰링이 '목적 불명' 쪽 레버 2개를 이미 봉인했다.** `w3_intake_v2_images.md` **:1099-1101** — (i) 계단 확폭 **금지**("no flight widening"), (iii) 카메라 재조준 **금지**("judge presets untouched — legibility comes from geometry, never from camera edits"). 코드도 이를 이중으로 못박음(:37-44 "the flight's y0/y1/riser/tread/nsteps/x0 are frozen … `build_views` is **byte-frozen**"). ⇒ 남은 합법 레버는 **계단 아닌 요소의 기하 이동**(본선 이설·램프 접속 복구·마루 마감)뿐.

**(등급 주의) GT-81 의 판정 등급이 이 파일의 자체 선례보다 낮게 적혀 있다.** 본선 강측 가장자리를 물리는 것은 **주행면(walked surface) 자체의 이동**인데, 이 파일은 같은 종류의 변경을 이렇게 판정한 전례가 있다 — **:109-112** "[W3 S17] **Route B is a walked surface, so shortening it is a GT FULL change** (…FULL = walked surface moves -> R-1+R-2+R-3). The intake's (f) guessed A; **the measurement says FULL and the ledger row says FULL**." 반면 GT-81 행(:1447-1448)은 "**R-3 + FRAME 선언**" 뿐이다 — R-1(hazard_registry 재캐시, :544-)·R-2 필요 여부를 다시 판정해야 한다. 다만 마루 어깨(x=0,z=0)와 계단 시단은 건드릴 필요가 없으므로 **낙차 3.2·은닉 전제·drop edge 는 불변**으로 유지 가능.

**(추가 구속)** §4-9(`process_spec_v1.md:113`) "전신주는 15·18만 허용, **17·20 금지**" — "trim it neatly" 를 가로시설물 추가로 푸는 것은 막혀 있다(가로등은 이미 x −7.9 = 보도 밖 0.4 m, :377-380).
- **성격**: 두 지적의 성격이 다르다 — **'본선 밀착'은 명백한 미처리 결함**(구판 walk 3.0 + bike 3.0 시절부터 강측 끝 x=0 을 한 번도 재검토하지 않은 채 W2-D 확폭을 육지쪽으로만 해서, 3.2 m 낙차 마루에 폭 4 m 아스팔트 본선이 5 cm 코프 하나만 두고 접해 있다)이고, **'씬 목적 불명'은 처방이 한 번 실패한 재발 지적**(R17-1(ii) 램프 단축으로 비 4.84→3.59 를 만들었지만 사용자는 여전히 목적을 못 읽는다)이라 램프를 더 줄이는 방향이 아니라 continuity_audit :22-23 이 짚은 "램프가 위·아래 어느 길과도 접속하지 않는 고립 판"(apron `build=False` :309)을 푸는 것이 본질이며, 계단 확폭·카메라 재조준은 R17-1 로 이미 봉인되어 있다.

## scene19 — 옥상 하행 부채꼴 계단만 있으면 되는데 웬 건물을 추가했나 — 제거

- **요소**: 배후 건물 3동은 `PARAMS["buildings"]` C/D/E (git show HEAD:scenes/main/scene19_fan_winder.py:237-245) 이고, 소비처는 `build_dressing` 안의 `sc.build_building(stage, f"/World/Scene19/Building_{key}", bd, M["brick"], M["glass"], M["parapet"])` **단 1곳**(:1470-1472), `cue_scene_dressing` 게이트(:103, :1523-1524)로만 켜진다. `M["brick"]`=brick_red 재질(:1068-1071)의 소비처도 이 호출이 전부여서 3동을 지우면 brick_red가 씬에서 사라진다. 부수 배선: 시선/occlusion 박스 등록 `_obstacle_boxes` 의 `for k, bd in PARAMS["buildings"].items()` (:694-699, Bldg{K} + Bldg{K}_roof 2박스/동), SMOKE 전용 게이트 `_geom_report` 의 `bE = PARAMS["buildings"]["E"]` → "E동 지붕 z … vs 옥상 눈높이 → OK(눈높이 이하)"(:800, :808-810), **E동 전용 판정컷** `out["roof_skyline"] = dict(eye=[10.0,2.0,2.0], tgt=[13.0,29.0,-2.0])`(:989)와 BANNER 체크리스트 8번 "roof_skyline — [옥상 v4] 북측 파라펫 너머 E동 지붕이 **눈높이 아래**로 열리는가(스케일 앵커)"(:1009-1010).
- **원설계 의도**: docstring `[rooftop v3] (2026-07-27, user instruction "widen the approach + rooftop scene context")` (:22-31) 의 (2)항이 원천이다 — "rooftop reframing — the upper sidewalk (z0) = the roof terrace of a low wing, the L wall = the rooftop core … and **roofs of surrounding low-rise buildings (scale anchors at or below eye height, build_building with base_z support)**". PARAMS 주석도 같은 취지로 "horizon-closing buildings (base_z = ground) — C high-rise + [rooftop v3] D/E low-rise (roofs at or below eye height, −2.5/−1.5 = **rooftop scale anchors**)"(:228-236). 이어 `[rooftop v4]` (1)항(:38-42)은 v5·v6 판정이 2라운드 연속 실패한 뒤 "**distant building E is moved north** (old x−28..−14·y8..22 → x6..20·y16..42, height 4.5 unchanged)" 로 재조준하고 `_frame_scan sky 44.6 % · building E 38.9 % (roof top face 12.3 %)`(:987-988)를 기록했다. 원장에도 남아 있다 — `Docs/audit_v4/gt_changes_w3.md` GT-50 §4 착지기록: "`roof_skyline` E동 38.9 %"가 R-1 산출값. 즉 3동은 임의 드레싱이 아니라 **2026-07-27 사용자 지시로 씬을 '가로 코너'에서 '옥상'으로 재문맥화할 때 도입된 스케일 앵커**이며, 그 중 E동은 판정컷 하나를 통째로 점유하는 피사체다. 다만 실행은 실패로 감사됐다 — `Docs/surveys/scene_composition_audit.md`:100 "scene19 `roof_skyline` 컷은 '1층 벽돌 상자 + 잔디 지평선'으로, **정체가 성립하지 않는다**", `Docs/reports/continuity_audit_v1.md`:42 "좌우 건물 매스가 완전 무텍스처 순흑 슬래브로 렌더되어 건물이 아니라 빈 공동으로 보임".
- **충돌**: **등록부는 무충돌** — `process_spec_v1.md` §4 중 scene19 조항은 5번("scene19 물탱크 없음 유지")뿐으로 배후 건물과 무관하고, §5 parked register 에 scene19 행은 없다. 실제 충돌은 세 갈래다: ① **씬 정체성** — 07-27 사용자 지시가 만든 옥상성의 스케일 앵커라, 3동 제거는 `roof_skyline` 컷의 피사체 전멸(=컷 폐기 또는 재조준)을 뜻한다. ② **SMOKE 게이트** — `_geom_report`(:800-810)의 "E동 지붕 눈높이 이하" 불변식과 `_obstacle_boxes`(:694-699)의 6개 박스를 함께 폐기해야 하며, 두 코드는 v6 판정 2연속 실패 후 "occlusion을 좌표로 검사"하려고 신설된 §[C] 블록의 일부다(:38-42, :433-439). ③ GT-82 스코프 자신이 선언한 "부채꼴 계단 기하(T7)·낙차 불변"과는 무충돌.
- **성격**: scene03 교량과 동일 계열 — 사용자 본인의 07-27 "rooftop scene context" 지시가 낳은 **의도된 스케일 앵커**이나 실행(무텍스처 벽돌 상자 + 잔디 지평선)이 실패해 정체가 안 읽히는 것이므로, 처방은 무조건 제거가 아니라 "왜 있는지 설명 + roof_skyline 재조준(계단이 프레임에 들어오게) 또는 원경 스카이라인으로 재표현"이 후보다.

## scene19 — 계단부 과암 — 부채꼴 계단 구간이 너무 어둡다

- **요소**: 원인 기하는 L자 벽 `PARAMS["walls"] = dict(z_bot=-6.0, z_top=3.5, south=dict(x0=-8.0,x1=4.0,y0=-1.0,y1=0.0), west=dict(x0=-1.0,x1=0.0,y0=-8.0,y1=4.0))`(:129-133, 빌드 :1234-1243)와 그 재질 바인딩 `granite_dark` — 코드 주석 "building corner L wall and newel = dark granite (granite_dark) — contrasts with the steps" + `# step material = light plaza_light (contrasts with the granite_dark walls) **[A-19(1)]**"(:1058, :1063). 광원은 `light=dict(noon_sun_elev=49.79, noon_sun_intensity=2450.0 …)` + `SUN_AZ_OFFSET=171.5`(:395-404). 피해 컷은 `winder_mid`(:935), `lower_lookup`(:952), `radial_nosing`(:961), `entry_gate`(:962).
- **원설계 의도**: v4 docstring 말미가 이미 원인과 유예를 명시한다 — "※ Not fixed (structural): **the whole winder corridor lies in the shadow of the L wall (top 3.5)**. Lighting adjustment is out of scope for this instruction — read the PT judgment on the premise that 'the corridor cuts receive sky light only'"(:51-53). W3 L19 레인이 이를 수치화해 상신했다 — `Docs/reports/w3_l19_v1.md` §6 **L19-F1**: `lower_lookup` mean **20.1 / ≤8 LSB 17.2 %**, `radial_nosing` **28.5 / 13.2 %**, `winder_mid` **35.9 / 25.5 %**, 대조군 h0.3 데크컷 **147.6**; 원인은 "a 3.5 m `granite_dark` (linear-Y **0.078**) L wall at a southern noon sun" 의 북동향 리세스. 레버 3개를 순위로 제시하고 **하나도 실행하지 않았다** — ①L벽 재질(권고안, "a real Korean 옥탑 PH is 시멘트 뿜칠 or 도장 at 0.35–0.50"), ②벽고 3.5→2.7, ③SUN_AZ 회전. ①을 안 잡은 사유가 명시돼 있다: "**Not taken** because the dark wall is a **declared scene design constant** — `[A-19(1)]` … and overriding another author's stated design intent on my own initiative … is what the ledger discipline exists to prevent." §7 표에서 **L19-F1 / HIGH / owner: supervisor · lighting round** 로 이월됐고, 같은 라운드가 상황을 −1.14 LSB 악화시킨 것도 자진 신고돼 있다. 원장 GT-50 §4에도 "**GRAZE abstained and is NOT recorded as a pass** — photometry moved on this scene" 로 남아 있다.
- **충돌**: §4·§5 등록부 **무충돌**(scene19 관련 조항은 §4-5 물탱크뿐). 충돌은 ① **`[A-19(1)]` 선언 설계 상수**(scene19_fan_winder.py:1058) — 계단=밝은 plaza_light / 벽=granite_dark 대비가 씬 설계 의도로 코드에 박혀 있어 L벽 재질 변경은 그 상수의 사용자 개정이 필요하다. ② 레버②(벽고 3.5→2.7)는 L19 §6의 기재대로 "the wall is a hazard box and its height is **the datum three judged cuts were aimed against**" — `roof_skyline`(U_N/L_W 시선 예산)·`radial_nosing`·`lower_lookup` 조준이 재계산 대상이 된다. ③ 레버③(SUN_AZ)은 전역 photometry 이동이라 GT-50 분할증명의 판독을 흐린다. ④ 재질 변경은 재질동결 게이트 사안 — 선례로 GT-73(유리 투명화)이 "재질 동결의 명시적 예외(사용자 지시)"로 열렸다.
- **성격**: 의도된 설계가 아니라 **이미 측정되어 HIGH 로 상신된 미결 결함(L19-F1)** 이며, 사용자의 이번 지적이 곧 L19가 감히 못 당긴 레버①(L벽을 시멘트 뿜칠·도장 0.35~0.50으로) 실행 승인에 해당한다.

## scene19 — 출입(입구)이 협소하다 — 확대 필요

- **요소**: 입구 폭을 정하는 값은 `PARAMS["access"] = dict(parapet_first=3, parapet_last=9, threshold=dict(x0=3.70, y0=0.0, **y1=1.53**), guard=dict(x0=3.7, x1=4.0, y0=1.53, y1=4.0, h_top=1.1), post_r=0.05, post_h=1.25)`(:152-161) 와 `winder … kite_n=3`(:124-126) — 개방 섹터 0..2(카이트 랜딩 병합) 22.5° 아크의 외호가 1.53 m(=4·sin22.5°)다. 슬롯을 만드는 두 벽은 `Wall_south` 끝면 x=4·**z_top 3.5**(:131-133, 빌드 :1240-1243)와 `EdgeGuard` x 3.7…4.0 · y 1.53…4.0 · 상단 +1.1(:160, 빌드 :1294-1298). 문턱은 `Threshold`(:1286-1290). 판정컷 `entry_gate` eye(6.5,0.5,1.2)(:962), BANNER 6번 "entry_gate — [접근성 v2] 문턱→카이트 랜딩 연속(**입구 폭 1.53**)"(:1003-1004).
- **원설계 의도**: 두 번의 사용자·감사 주도 확장이 겹쳐 있다. ① `[accessibility v2] (2026-07-27, look_refs/scene19 study)`(:15-20): 구판은 파라펫이 12섹터 전부를 막아 "pedestrian entry was impossible" → 파라펫을 섹터 2..9로 줄이고 게이트 포스트·코너 슬래브·문턱·에지가드·L벽 밀착을 도입. ② `[rooftop v3] (2026-07-27, **user instruction "widen the approach** + rooftop scene context")` (1)항(:22-25): "**approach widened** — entry sectors 0..2 are merged into a kite landing …, parapet sectors 3..9 → the entry opens over a 22.5 deg arc (**outer arc 1.53 m, was 1.05 m**)", PARAMS 주석에도 "threshold x0 3.86→3.70 …, **y1 1.05→1.53**(=4·sin22.5 deg). guard follows at x0 3.7·y0 1.53"(:148-153). 즉 현재의 1.53 m 는 **사용자가 이미 한 번 '넓히라'고 해서 나온 값**이다. 협소감의 나머지 절반은 폭이 아니라 어둠으로 문서화돼 있다 — `Docs/reports/continuity_audit_v1.md`:44 "scene19 `entry_gate` — ① 접근 보도가 **조명 없는 좁은 슬롯**으로 들어가 어둠 속에서 소멸. 이어지는 통로인지 막다른 곳인지 판독 불가".
- **충돌**: §4·§5 **무충돌**. 실질 제약은 ① 개방 섹터를 더 늘리면 파라펫 존치 구간(3..9)이 줄어 GT-51이 옥외선 1.10 m 로 올린 `Parapet_i` 7링 + `Rail_i` 7링(외측 1.8 m 낙차 방호)의 스팬이 줄어든다 — 원장 GT-51 "7 `Parapet_i` + `EdgeGuard` 8개 AABB 100 mm 상승" 이 이 스팬 기준이다. ② `kite_n` 을 건드리면 GT-50 분할증명의 **음성 대조군**이 깨진다 — 원장 GT-50: "Boundaries **1| and 2| contribute 0** — they are the kite landing, where the riser is 0.000 m: the negative control proving the probe measures the hand-over and nothing else"(코드 :827-830 의 `kite` 검사와 동일). ③ GT-82 스코프 선언 "부채꼴 계단 기하(T7)·낙차 불변"과 정면 충돌 가능 — 폭 확장이 `sector_deg`/`kite_n`/`r_out` 을 건드리면 T7 기하 자체가 움직인다.
- **성격**: 의도된 협소가 아니라 **v2·v3 두 차례 완화(1.05→1.53 m)에도 남은 미완의 개선**이며, 실제 체감은 3.5 m L벽이 만든 그림자 슬롯(L19-F1)과 결합돼 있으므로 폭만 늘리는 처방으로는 풀리지 않는다.

## scene20 — 계단 양측이 무방호다 — 위험해 보임, 난간이든 벽이든 뭐라도 있어야

- **요소**: **계단 자체에는 양측 난간이 이미 있다** — `SCENE_CONFIG["cue_railing"] = True  # Sloped rails on both sides of the stair`(git show HEAD:scenes/main/scene20_diagonal_oblique.py:78) → `build_diagonal` 안에서 `for sgn, tag in ((-1.0,"S"), (1.0,"N")): sc.build_railing_line(stage, f"{grp}/StairRail_{tag}", sgn*sr["y"], …)`(:678-696), 사양은 `stair_rail=dict(y=2.4, x_start=-0.4, **rail_h=0.9**, post_r=0.02, rail_r=0.03, rail_mid_r=0.018, rail_mid_drop=0.45, spacing=1.3)`(:134-136); 살대는 `build_railing_line` 기본값 상속(scene_common.py:1849- , `baluster_r=0.009 · baluster_gap=0.098` = 안목 98 mm — `Docs/reports/w3_l20_v1.md` L20-F2(b) 실측). 무방호인 것은 **계단 밖 메사 연단 전장**이다: 낙차 경계는 30° 사선 `x = -0.5774·y` 전 구간(GT-45 R-1 레지스트리 "낙차 경계 = 30° 사선 x = -0.5774·y … 낙차 2.10 m"), 메사는 `upper=dict(x0=-14.0,x1=-4.5,y0=-8.0,y1=8.0,z_top=0.0,base_z=-2.2)`(:101) + `wedge=dict(x0=-8.0,x1=0.0,y_half=9.0,…)`(:103), 계곡 잔디 상면 `valley … z_top=-2.15`(:132) → 사면 전주 2.10~2.15 m 절벽에 가드는 계단 폭 5 m 구간뿐. 렌더로 확인됨: `look_check/scene20/260805_w3_hedgeswap/pt_noon_walk_axis_front.png` 에서 프레임을 가로지르는 사선 연단 전체가 무가드이고, `pt_noon_preset_h0.3_d5.png` 에서 프레임 내 유일한 낙차 단서가 우측 계단 난간 1식이다.
- **원설계 의도**: **명시적 '무난간 룰링'은 없다** — scene17 §4-2 같은 실관행 근거 조항은 scene20에 존재하지 않는다. 대신 감사에서 '경미'로 접힌 뒤 그대로 승계됐다: `Docs/archive/audit_v4/audit_scene16-21.md` §scene20 **A5** "UpperPlaza(x −14..0, y −8..8)는 **사방이 2.15 m 낙차인 고립 메사**이며 진입 램프/계단이 없다 … 프리셋 카메라는 전부 메사 위에 있어 렌더 판정에는 영향 없으나 '이 광장은 어떻게 올라가는가'가 화면에서 답이 안 된다. **경미(맥락)**" (같은 감사 B2는 남측 레일 시작부 부유를 '주의'로 기록). W3 L20(GT-45)도 손대지 않았다 — `w3_l20_v1.md` §4 "**The 30° rotation, the 14 × 0.150 m flight, the 2.10 m drop and the wedge joint are the scene's identity and are untouched**", §7 findings 6건 중 연단 가드 항목 없음. 08-05 독트린(GT-57, 원장 §13)은 "전 씬 연속 가드화"를 선언했으나 실제 Scope 는 `scene_common.build_railing_line` + **01·05·06·10·12·14 6씬**뿐이고 **scene20은 대상이 아니었다**. 동급 선례는 scene21이다 — 원장 GT-48: "the terrace plinth's own unguarded flank … an **undeclared 2.75 m fall** … every candidate remedy moves a walked surface or a hazard box, so it takes its **own row**" → **L21-F1** 로 분리.
- **충돌**: ① **§4 등록부**: scene20 직접 조항은 §4-9("전신주는 15·18만 허용, **17·20 금지**")뿐으로 난간과 무관하며 GT-87도 이를 "유지"로 명시. §4-2(scene17 계단 무난간)는 **17 전용**이라 20에 전이되지 않는다(같은 조항이 "난간이 '있으면' 낙차 표지라는 것이지, 실관행상 없는 곳에 세우라는 뜻이 아님"이라고 스스로 경계를 긋는다). §5 parked 에 scene20 행 없음. ② **씬 정체성/연구 전제와는 실질 충돌** — scene20은 `ground_kit.py` 에 은닉 착시 씬으로 등록돼 있다: `TACTILE_OFF_REASON["scene20"] = "정체성 충돌 — 은닉 착시"`(ground_kit.py:2132), `GROUND_INVARIANTS["scene20"] = [_inv_hidden_illusion]`(:2274) — "14/20/21/N3 concealed-illusion integrity - no permanently high-contrast cue"(:2240-2248). 씬 자체 BANNER 도 "2. h0.3·d5~10 — 사선 경계 위로 낙차 2.1m가 **은닉**되는가"(scene20:413). 사선 연단 전장에 연속 가드를 세우면 `process_spec_v1.md` §6 갤러리 체크리스트 ④ "h0.3 컷에서 **낙차 은닉 전제 유지**"가 정면으로 뒤집힌다. ③ 08-05 독트린(난간=낙차 표지)과는 방향 일치 — 즉 독트린 대 은닉 전제의 충돌이며 사용자 결재가 필요한 지점이다.
- **성격**: 계단 좌우 난간은 이미 존재하므로 지적의 실체는 **계단 밖 30° 사선 메사 연단 전장의 무방호**이고, 이는 audit v4가 '경미'로 접은 뒤 아무도 승계하지 않은 미결 결함이지 의도된 설계가 아니다 — 다만 연단 전장에 세우면 h0.3 은닉 전제(§6-④)를 깨므로 '계단 개구 주변 한정'인지 '연단 전장'인지 사용자에게 되물어야 한다.

## scene13 — 계단박스 문을 외짝(single)으로 바꾸고 열린 상태로 둘 것

- **요소**: `PARAMS["stair_canopy"]["east"] = dict(x=11.30, y0=3.50, y1=6.90, jamb_y=(3.55,5.00), jamb_w=0.10, **door_y0=3.60, door_y1=4.95**, door_z1=2.10, **meet_w=0.006**, leaf_t=0.05, glass_t=0.019, stile=0.06, lap=0.010, handle_r=0.016, …)` (git show HEAD:scenes/main/scene13_apartment_parking_entry.py:492-506). 빌드는 `build_stair_canopy` 의 EAST ENTRANCE FACE 블록 — `for li, (ly0, ly1) in enumerate(((ea["door_y0"], ymid - mg), (ymid + mg, ea["door_y1"])))`(:2847-2870) 로 **2짝을 닫힌 상태로** 세우고(`Leaf{li}_SL/SR/RB/RT/Glass/Handle`), 위에 `DoorHead`(:2828)·`DoorTransom`(:2837), 옆에 `EastKick`(:2877)·`EastGlass_b`(:2883)·`EastMullion_b`(:2890). 자체 출력 "[GT-64 계단 출입문] 강재 프레임 + 강화유리 **양여닫이 2짝(폐쇄 모델)** · 유효 개구 1.35 m … → 문이 유일 통로 · 문 옆 낙차 접근 폐합"(:2916-2920). 스모크 검산은 :1687-1708(동측면 y 구간 무틈 + 유효 개구폭 ≥0.90).
- **원설계 의도**: 원장 §20 **GT-64** Authority ③ — "전 턴 사용자 제안 승인분: 중앙 벽(ShaftWall_Mid) 철거 · **하행 계단 입구 문** · 문 옆 낙차 폐합". 코드 주석이 원문을 그대로 담고 있다(:455-474): "[08-06 user · GT-64] the east face stops being an open mouth. Korean basement-stair standard: steel frame + **closed** tempered-glass double leaf over the descending flight, fixed glass over the remainder, so **the DOOR IS THE ONLY PASSAGE** and nobody can step past its edge into the shaft ('block the area next to it to avoid drop-off')". 리스크는 착지 전에 선언돼 있었다(:475-480): "`[risk, declared]` this library has **no transmissive material** (scene08 §'No transmission is available in this material stack'), so the leaf reads as a dark reflective pane rather than clear glass, and it now stands between the old `stair_head` eye and the flights". 원장 GT-64 §4 착지기록이 그 실현을 확인한다 — "**유리문은 무투과 재질 스택이라 렌더에서 불투명 판으로 읽힘**(라이브러리 전역 제약 — scene08 전례) — '유리 너머 낙차 가시' 취지는 렌더상 성립하지 않음을 기록", 그리고 "stair_head 아이도 에이전트가 2.50 m 내부 진입(**닫힌 유리문이 구 아이를 가림**)".
- **충돌**: §4·§5 등록부 **무충돌**. 걸리는 것은 ① GT-64가 세운 안전 논리 "문이 유일 통로 → 문 옆 낙차 접근 폐합" — 외짝·개방으로 바꾸면 개구가 1.35 m 그대로여도 '항상 열린 개구'가 되어 GT-72 스코프의 "낙차·레지스트리 불변" 아래에서 낙차 접근 차단 근거를 재작성해야 하고, 스모크 :1691-1701 의 세그먼트 정의(잼S|문|잼N|고정유리)도 재구성 대상이다. ② **GT-73(유리 투명화 파일럿)과 직결** — 원장 §29가 scene13 유리 프림(캐노피 커튼월·계단박스·**문**)을 우선 적용 대상으로 이미 선신고했으므로 본 건과 같은 라운드에서 판정해야 한다.
- **성격**: 08-06 GT-64가 신설한 요소가 **라이브러리에 투과 재질이 없다는 기지의 제약**(착지 전 선언·착지 후 확인) 때문에 검은 판으로 읽힌 사례로, '닫힌 양짝' 선택 자체의 실패이므로 외짝+개방은 그 실패의 정공법 수정이다.

## scene13 — 중앙 레일 중간대 끝이 날카롭게 돌출 — 종단마다 포스트 추가·결속

- **요소**: `PARAMS["stair_handrail"] = dict(r=0.02, h=0.85, **mid_h=0.45**, off=0.05, post_r=0.022, post_below=0.30, post_xs=(8.3,9.4,10.5), bracket_r=0.013, bracket_len=0.14, u_off=0.08, newel_x=11.16, jamb_x=11.30, wall_embed=0.06)`(:568-572). 빌드(:2416-2500)는 플라이트 A/B 각각 **Top·Mid 2본**을 깐다 — `for lab, dz, rr in (("Top", hr["h"], hr["r"]), ("Mid", hr["mid_h"], hr["r"]*0.75))`(:2441-2448). 그러나 종단 처리는 사실상 Top 레벨 전용이다: `HeadReturn`(:2476-2478)은 z = `hr["h"]` **1본만**, `UTurn`(:2464-2466)·`UStub_{A,B}`(:2467-2470)는 `z_low = st["mid_z"] + hr["h"]` **1레벨만**, `FootStub_{Top,Mid}`(:2489-2494)만 2본. 결과 자유단: **플라이트 A의 Mid 튜브 동단(x=11.20)** 1개 + 참(landing) 측 A·B Mid 튜브 단부 — U-리턴이 Mid 레벨에 없다. 코드는 그럼에도 "종단 = 문틀 잼 + 머리 뉴얼 + 복도 남벽 매입(**공중 종단 0**)"을 출력(:2496-2500)하고, 스모크(:1735-1741)는 PARAMS 값(jamb_x·newel_x·wall_embed·u_off)만 읽어 같은 문구를 재확인하므로 이 결함을 잡지 못한다. 렌더 확인: `look_check/scene13/260806_w3_s13fix6/pt_noon_stair_head.png` 에서 U-리턴 옆 가는 튜브 1본이 허공에서 끊긴다.
- **원설계 의도**: 2단 계보다. ① 원장 §18 **GT-62** — 08-05 사용자 5차 답변 "보행 입구는 유리로 감싸고, **내려가는 난간 하나만** 추가하라 했는데 난간으로 대체됐다" → "신규 **하행 핸드레일**: 중앙벽(ShaftWall_Mid) 벽부 STS 단관 r0.02 · 답면 위 0.85 — 플라이트 A 하행 + 중간참 U-리턴(x 7.52) + 플라이트 B 하행, 브래킷 7개". ② 원장 §20 **GT-64** ④ — 08-06 사용자가 핸드레일을 "loose"라고 지적 → 중앙벽 철거에 맞춰 자립 양면 가드로 재작성. 코드 주석이 그 수정 의도를 명시한다(:562-567): "**TERMINATIONS — the 08-06 review called the railing 'loose', and it was**: `SlopeA` ended at (11.20, ·, 0.85) in mid air. Now flight A runs on past the rim as a level return into the door's north jamb post at x 11.30 …; flight B's lower end is buried 60 mm into the corridor's south wall face at x 11.20. **No free tube end.**" `mid_h` 0.45 의 근거는 :559-561 — "the well between the flights is only 0.30 m wide, so a bare top tube would leave a 0.85 m gap. Two-tube section, the same one the trench guard uses (`rail.mid_h` 0.46)".
- **충돌**: **무충돌**. 오히려 GT-64가 같은 지적('loose')을 고치려던 행이므로 본 건은 그 수정의 미완이며, GT-72 스코프("보행면·낙차·레지스트리 불변") 안에서 처리 가능하다(가드 부재 추가는 R-3).
- **성격**: 명백한 미완 결함 — GT-64의 종단 결속이 **Top 레일에만** 적용되고 GT-62가 도입한 Mid 레일에는 승계되지 않았으며, 코드·스모크가 출력하는 "공중 종단 0" 문구가 그 사실을 가려 왔다.

## scene13 — 계단 측부 빈 공간을 채우게 (가드를) 연장 + 난간 기둥은 계단 위에 세울 것

- **요소**: GT-64가 `ShaftWall_Mid` 를 삭제해 두 플라이트 사이가 **오픈 웰**이 됐다 — 주석 :2362-2368: "[08-06 user · GT-64] **`ShaftWall_Mid` is DELETED.** The user approved opening the switchback: **the 0.30 m strip between the flight bands is now an open well**, guarded by the free-standing double-sided rail below. The collider removal is declared in ledger GT-64 — it is the round's ONLY collider deletion." 기하: 플라이트 A `y_a0=3.55, y_a1=4.95` / B `y_b0=5.25, y_b1=6.65`, `base_z=-4.4`(:184-188) → 웰은 y 4.95…5.25 · 폭 0.30 m 가 −4.4까지 관통, 동측 0.25 m만 `shaft_east.bands` 의 (4.97, 5.25, None) 피어로 남는다(:215-217). 핸드레일 포스트는 그 웰 **안**에 선다 — 레일선 `off=0.05` → y 5.00(A)/5.20(B)(:556-558), 포스트 `CYL(… (bx, yc, (zt+zb)/2), hr["post_r"], zt-zb …)` 에서 `zb = nose(bx) - hr["post_below"]`(:2450-2456), 즉 답면 아래 0.30 m 에서 **끊기고 바닥에 닿지 않는다**(웰 바닥은 −4.4). 렌더 `pt_noon_stair_head.png` 에서 포스트 하부의 어두운 관통 슬롯으로 확인된다.
- **원설계 의도**: 원장 §20 **GT-64** Authority ③ 의 사용자 승인분 "중앙 벽(ShaftWall_Mid) 철거", Scope "ShaftWall_Mid 철거(**collider 제거 본 행 귀속**) → 플라이트 사이 자립 양면 핸드레일(U-리턴·h 0.85)". 코드 주석 :552-558 이 근거를 적는다: "`ShaftWall_Mid` is DELETED, so the rail can no longer be wall-mounted. It becomes a **free-standing double-sided guard** … `off` 0.05 → rail lines y 5.00 (flight A, edge 4.95) and y 5.20 (flight B, edge 5.25). Both are OUTSIDE the flight bands, so **the 1.40 m clear width of each flight is restored in full — the wall rail cost 80 mm per flight**." 즉 포스트를 웰 안(플라이트 밖)에 둔 것은 **유효폭 1.40 m 회복이 목적**이었고, 웰 바닥 처리는 설계되지 않았다.
- **충돌**: ① 원장 GT-64 §4 착지기록이 "ShaftWall_Mid collider 제거 1(본 행 선언)"을 **그 라운드의 유일한 collider 삭제**로 신고했으므로, 웰을 슬래브·판으로 되메우면 그 선언을 되돌리는 것이라 별도 GT 귀속·A/B 가 필요하다. ② GT-72 스코프가 "보행면·낙차·레지스트리 불변"을 선언했는데 웰 바닥/측판을 채우면 `hazard_registry()`(:1217-)의 샤프트 관련 행이 움직일 소지가 있다. ③ "기둥을 계단 위에" 는 GT-64가 명시적으로 확보한 **각 플라이트 유효폭 1.40 m**(:556-558)를 다시 잠식할 수 있어, 폭을 깎지 않는 배치(답면 가장자리 매입·측면 브래킷 강화)로만 가능하다.
- **성격**: 사용자 승인으로 벽을 뜯은 자리(0.30 m 오픈 웰)를 가드가 메우지 못하고 포스트마저 허공에서 끊긴 **GT-64의 미완 마감**이며, '기둥을 계단 위에'는 곧 포스트 기저를 웰이 아니라 답면으로 옮기라는 지시로 읽힌다.

## scene13 — 수정이 계속되다 보니 각 형상의 에지 마감이 안 끝났다 — 정밀 마감 패스 필요

- **요소**: 한 구조물 위에 라운드마다 쌓인 **서브-25 mm 오프셋 규약 스택**이 실체다: `fascia_proud=0.02`(20 mm 드립엣지, :413 · 빌드 :2628-2635) · `embed=0.02`(램프 캐노피 :415, 계단 캐노피 :484) · N/S 파시아 밴드를 W/E 대비 1 mm 낮추고 2 mm 안으로 물린 턱 `fz - 0.001` + `2.0*(y_full - 0.002)`(:2636-2644) · 엔드월 10 mm 이격 `ew_x1 = cp["x1"] - 0.01`(:2699) · `end_wall=dict(x0=23.90, t_in=0.012)`(:421) · `shaft_east … cope_drop=0.001`(:215) · `ShaftFace_Entry` 의 `face=dict(… drop=0.22, gap=0.002)`(:218 · 빌드 :2411-2415) · 문틀 `lap=0.010`/`meet_w=0.006`(:503) · 유리 `joint=0.012`(:419) · 밴드 랩 20 mm(:198-211). 각 값의 사유는 예외 없이 "no coplanar face / Z-fighting 회피"로 코드에 적혀 있다.
- **원설계 의도**: 이 규약은 6라운드 연속 적층의 산물이다. 원장 §14 **GT-58** §4: "검증 워크플로(3렌즈, 08-05) 반영: **파시아 림 공면 → 드립엣지 20 mm 돌출 재설계** · 보 자유피치 → 기둥 스테이션 정착". 원장 §16 **GT-60** verify r2 ③: "**파시아 모서리 2 mm 공면 → N/S 밴드 1 mm 하강**". 코드가 그 이력을 그대로 보존한다 — :389-396 "fascia_proud 0.02: the band ring projects 20 mm past the deck rim on all 4 sides (RC drip-edge detail) — **verify round r1 found the flush ring bit-exact coplanar with the deck rim planes** … Corners: W/E bands run the full width; N/S bands tuck 2 mm INTO the W/E solids", :2636-2637 "N/S bands sit 1 mm lower than W/E (**verify r2**: identical fz left the 2 mm corner tuck strips with bit-exact coplanar soffit faces)", :499-503 "`lap` 0.010: every infill pane laps 10 mm into its frame member instead of sitting flush, **the same no-coplanar rule the canopy fascia uses**". 사용자 자신이 GT-72 Authority 에서 원인을 정확히 지목했다 — "**수정 누적으로 각 형상 에지 마감 불량** → 정밀 마감 패스".
- **충돌**: §4·§5 **무충돌**이나 방향 제약이 있다 — 오프셋을 **없애는** 쪽 수정은 verify r1/r2 가 실측으로 잡아낸 Z-fighting/공면 결함군을 되살린다(GT-58·GT-60 §4 기재). 따라서 GT-72 의 '정밀 마감 패스'는 오프셋 제거가 아니라 코너 솔리드화·부재 폭 정합·모따기 쪽으로 가야 하며, 프림 증감은 GT-72 스코프("보행면·낙차·레지스트리 불변") 안에서 R-3 로 귀속된다.
- **성격**: 개별 결함이 아니라 **GT-58→64 6라운드 누적의 구조적 부산물** — 각 오프셋은 개별적으로 정당하지만 한 구조물에 8종 이상이 겹쳐 '마감 미완'으로 읽히므로, 되돌리기가 아니라 코너·부재 정합 재설계가 처방이다.

## scene13 — 문 하부에 잔디가 있다 — 정리할 것

- **요소**: 세 값의 어긋남이 원인이다: 문 평면 `stair_canopy.east.x = 11.30`(:492), 샤프트 연단 `shaft=dict(x0=5.0, **x1=11.2**, …)`(:189), 보도 `walk_spur=dict(**x0=11.4**, x1=14.4, y0=3.3, y1=7.2, proud=0.150)`(:323). 즉 **x 11.20…11.40 의 0.20 m 띠에는 포장이 없고**, 그 자리는 `build_ground` 가 깐 `Ground_N2`(세그 `("N2", sh["x1"], x_p, 3.3, gr["y1"])`)의 상면이며 재질은 `M["grass"]` 다(:2031-2041). 문(x=11.30)은 정확히 그 잔디 띠 한가운데 선다. GT-64가 넣은 동측 마감(`shaft_east.bands` / `cope_bands` / `face`, :215-218 · 빌드 :2394-2415)은 **연직 절단면과 코핑만** 다루고 상면 포장에는 관여하지 않는다.
- **원설계 의도**: 원장 §20 **GT-64** ③ "문 옆 낙차 폐합" + (d) 동측 연단 마감. 코드 주석 :190-196 이 도입 사유를 적는다: "[08-06 user · GT-64 (d)] shaft EAST rim: wall + coping, was bare ground … the EAST rim (x 11.20, y 3.30…6.90) had neither, so what stood at the drop edge was **Ground_N2's cut face in the grass material — the lip the 08-06 review saw**." 문 평면을 x 11.30 에 둔 근거는 :460-464: "The plane sits 0.10 m EAST of the shaft rim x 11.20 — **on solid Ground_N2** — which is what lets the jambs be full posts instead of the r2 half-over-the-void post." 즉 "solid Ground_N2"는 **구조적 지지** 근거였을 뿐 포장 근거가 아니었고, 같은 라운드가 `walk_spur.x0` 을 11.40 에서 당기지 않아 0.20 m 잔디 띠가 남았다. 스모크의 잔디립 검사(:1709-1719)는 `shaft_east` 밴드의 **y 방향 무틈**만 보므로 x 방향 이 띠를 검사하지 않는다.
- **충돌**: §4·§5 **무충돌**. `walk_spur.x0` 을 11.40→11.20 으로 당기면 `spur_ramp=dict(x0=11.4, run=2.0)`(계단머리 방향 7.5 % 턴다운, :327-330)과 docstring 보행연속성 표 2b/3행(:101-102)의 검산이 재실행 대상이 되지만, 판 상면 z(=proud 0.150) 자체는 불변이라 GT-72 의 "보행면·낙차·레지스트리 불변" 선언과 충돌하지 않는다.
- **성격**: GT-64가 연단 **절단면**은 가렸으나 문이 실제로 서는 **상면 0.20 m 띠**를 포장하지 않아 남은 순수 마감 누락 — 의도 없는 명백한 결함이다.

## scene13 — 차량 진입구·유리 돌출부의 에지 마감

- **요소**: 마우스(서단 x=0) 종단에서 6개 부재가 정합 없이 만난다: 데크·유리를 마우스까지 당긴 `canopy=dict(**x0=0.0**, x1=24.0, …)`(:411)와 `glass=dict(t=0.019, joint=0.012, kick_h=0.12, kick_t=0.05, **x0=0.0**)`(:419-420); 20 mm 돌출 파시아 `Fascia_W` at `cp["x0"] - p + cp["fascia_t"]/2`(:2631-2635); 강재 킥밴드 `Kick_{N,S}` 가 gx0 에서 **직절**로 끝남(:2670-2673); 첫 유리 베이가 `ga_ = a + gl["joint"]` 로 시작(:2679-2691); 마우스 기둥쌍·보 `col_mouth_x=0.40`(:415, :2648-2661)과 거기 매달린 높이제한바 `height_bar=dict(x=0.40, z=2.30, r=0.09, y0=-3.05, y1=3.05, hang_z0=2.35, hang_z1=2.54)`(:365-366); 그 아래 트렌치 코핑(`wall=dict(thick=0.3, cope_over=0.06, cope_h=0.12)`, :180-181)이 기둥 기저 `rail.base_z=0.12`(:537-538)와 만나는 지점. 동단은 `end_wall=dict(x0=23.90, t_in=0.012)`(:421) → `EndWall` x0…23.99(:2698-2703). 렌더 `look_check/scene13/260806_w3_s13fix6/pt_noon_entry_approach.png` 에서 유리 양 기부의 흰 판이 마우스 밖으로 삐져나온 채 종단된다.
- **원설계 의도**: 마우스 연결은 원장 §18 **GT-62** ① — 08-05 사용자 5차 답변 "**유리벽을 끝까지 설치하려면 캐노피를 끝까지 연결하고 높이 차단 기둥을 캐노피에 바로 붙이면 된다**" → "`canopy.x0` 2.75 → **0.0**(데크가 마우스까지 — 무롭 구간 소멸). 구 '갠트리 채광 개구' 근거 폐기. 유리 상부 채널 캡(GlassCap) 삭제 — 전 구간 데크 매입. 마우스 기둥쌍 + 보 1본 신설 x=0.40 … **갠트리 프레임 삭제** … 높이제한바는 **마우스 보에 행어로 직결**". 코드가 그대로 옮겨 적는다(:370-378): "x0 0.0 [08-05 user, 5th answer]: the deck connects to the mouth — the old x0 2.75 'gantry daylight' rationale is retired with the gantry frame itself … col_mouth_x 0.40: one extra column pair + beam at the height-bar station". 유리 커튼월 자체는 원장 §16 GT-60 3차 답변("난간 대신 측면 유리 월 패널로 완전히 감싸라"), 유리 전장 연장은 §17 GT-61 4차 답변("유리를 트렌치 끝(x=0)까지 연장"), 엔드월은 GT-60 (3)항("the east end x 23.90..24.0 closes with a solid end wall … also masks Ground_E's exposed west face", :408-410).
- **충돌**: §4·§5 **무충돌**. 되돌릴 수 없는 제약만 있다: ① `col_mouth_x=0.40` 기둥·보는 높이제한바의 **유일한 지지**(GT-62가 자립 갠트리를 삭제했으므로, :349-366)이므로 마우스 스테이션을 옮기거나 없앨 수 없다. ② `canopy.x0` 을 다시 동쪽으로 물리는 것은 GT-62 사용자 지시의 롤백이라 별도 결재가 필요하고, 판정 아이(모든 h/d 프리셋 x ≤ −2)가 데크 밖이어야 한다는 규율(:373-374)과도 얽힌다. ③ 파시아 20 mm 돌출·킥밴드·조인트 값을 줄이는 방향은 앞 항목(에지 마감 총론)과 같은 Z-fighting 회귀 위험.
- **성격**: 사용자 지시(GT-62)로 마우스까지 당긴 데크·유리의 **종단 상세가 아직 작성되지 않은 상태** — 의도 자체는 유효하고 결손은 킥밴드 직절·코핑/기둥 기저 접합·파시아 돌출의 마우스 코너 처리라는 마감 범위에 국한된다.

## sceneC2 — scene04 가을 분위기와 겹쳐 위치 애매 — 다른 아이디어?

- **요소**: 낙엽 정체성을 구현하는 프림·파라미터 전부 (`git show HEAD:scenes/batch1/sceneC2_leaf_stairs.py`): ①`:79-80` 정체성 토글 `"leaf_cover": True` ("identity toggle: False -> remove every leaf mound·scatter = same-geometry twin") ②`:135-156` 매몰 마운드 3매 `mound=[A/B/C]` — A 접근로 카펫(x −3.20..−0.10, top +0.06→+0.13), **B 매몰 램프**(x0 −0.25, z0 0.20, run 1.27, drop 0.5897 → 경사 0.4643 < 노징선 0.4706 이라 1~3단 상면을 항상 +0.086~0.090 덮음), C 점진노출 램프(y_half 0.90, 중앙만) ③`:157-163` 측면 드리프트 `drift`/`drift_geo`(rotX 5.0) ④`:164-192` `leaf_scatter=dict(count=900, seed=2702, …)` + W2 G2 전역화 5-rect(`core` cover 0.55·max 1150 / `up` x0 −10.60 / `sideN`·`sideS` / `down`) ⑤`:277` `scale=dict(…, leaf_ground=2.2)`, `:280` `leaf_tex_tint=(0.95,0.72,0.48)`, `:281-284` 4색 낙엽 틴트, `:293-294` autumn canopy 틴트 ⑥`:205-210` 가을 수목 6주, `:306-309` `noon_sun_enable=True, noon_sun_elev=42.0` ⑦`:369` `ASSET_ROLES`에 `leaf_ground` ⑧헤더 `:12-22` Hazard/Goal/GT 블록. — 겹침 상대편(scene04, HEAD): `:48` 계절 고정, `:254-256` 3-D leaf cards, `:340-357` `leaf_piles` 4개, `:365-371` `leaf_ground` 1.20 배율, `:489-491` `grass` 역할 폐지→`leaf_ground` 승계.
- **원설계 의도**: **원 스펙(§B)** — `Docs/legacy/nanobanana_batch1_geometry_map.md:48-51` sceneC2_leaf_stairs: *"이미지 판독: 공원 석계단(석재 단+양측 석재 경계석), 낙엽이 하단·중단을 두껍게 매몰… 구현은 프롬프트 의도대로: 상부 접근로에서 볼 때 첫 3단이 낙엽 마운드에 완전 매몰(계단 시작 에지 소실), 이후 점진 노출. **난간 1선이 유일 단서로 하강 지속**"* / *"핵심 기하: … 낙엽 레이어 2단 구성 ①매몰 마운드 ②근경 산포"* / *"GT: 양성(기하 불변)"*. **look_ref 근거** — `look_refs/c2_leaf_stairs.jpg`, 매칭표 `look_refs/nanobanana_batch1_prompts.md:15` *"OK — 상단 3단 완전 매몰 조건은 약함(엣지 일부 보임)"*, 프롬프트 원문 `:50-51` *"Dense fallen autumn leaves completely bury the first three steps so the top edge of the staircase is invisible; lower steps gradually emerge… A metal handrail continues down as the only clear cue."* **C-시리즈 슬롯 요건** — 같은 문서 `:39` 표제 *"B. 조건 변주 (기존 기하 + 환경 레이어, GT = 기하 그대로 양성)"*, `:7` 씬 ID 체계(07-27 사용자 지시, 레퍼런스 이미지와 1:1 대응), 프롬프트 `:3` *"배경은 낮(주간)으로 고정 — **C3 야간은 연구 스코프 제외**"* → C축은 C1·C2·C4 3슬롯뿐이며, C4 docstring `:16-17` *"with C1 (snow) this forms the 'material variation axis'"* 가 말하듯 C1·C4는 **재질·광학** 변주이고 **C2만 '부피 매몰(occlusion by volume)' 변주** = 축의 유일 축. **C2 전용 룰링** — `Docs/archive/reports/leaf_globalization_budget_v2.md:17` 표 5행 *"C2 외 씬으로 확산? **하지 않는다. C2 전용 유지가 맞다.** | 실사 표본 n=54 중 보행면 낙엽 가시 5건(9.3%), 연속 카펫 **0건(0%)** [통계]"*, 같은 문서 `:343·:371` *"sceneC2 는 정확히 그 유형(공원 석계단)이므로 C2 전용 유지가 맞다"*, `:391` *"씬 세계관 = 늦가을(태양고도 42.0°, '낙엽 절정 10월 말~11월 중순')"*. **겹침이 생긴 시점 — C2가 아니라 scene04가 나중에 넘어왔다**: git 상 C2 = `22c011f`(2026-07-28) → C2 낙엽 전역화 `2a97e56`·`dd94501`(07-29) → **scene04 늦가을 고정 `024a985`(07-30) "scene04 W3 S04-1 — … 늦가을 고정(R04-2)"**. 근거 룰링은 `Docs/surveys/w3_intake_v2_images.md:1102-1106` **§7 ruling 8 SEASON POLICY** *"Every scene pins the season of its own target image… The global summer lock is retired. → **R04-2: scene04 = late autumn / leaf-off**. R09-1: scene09 = autumn."* 및 `:273-276` R04-2 원문, 착지는 `Docs/audit_v4/gt_changes_w3.md:94` **GT-22**(verge 143 프림 삭제, VEG_DEBRIS 320 leaf cards, leaf-off 22 trunks, `grass`→`leaf_ground` 재바인딩, prims 835→1335, LANDED). 결정적 자백은 scene04 코드 자신에 있다 — `scene04_parktrail.py:255-256` *"Season-legal here **because 04 is now pinned late autumn** (R04-2); **C2/07/10/D3 were the earlier scope**."*
- **충돌**: **(a) §4-7 (do-not-touch) 정면 관련** — *"계절·이벤트성 요소를 prop에 넣지 말 것; **낙엽류 asset은 낙엽 씬에만**"*. C2가 바로 그 '낙엽 씬'(유일 수납처)이다. C2를 낙엽에서 떼면 이 조항의 지시대상이 사라지고, `leaf_ground`/`VEG_DEBRIS`를 쓰는 scene04·07·10·D3(및 C4 `shrub_color`)의 근거가 통째로 재검토 대상이 된다. **(b) leaf_globalization_budget_v2 §5 룰링 충돌** — "C2 전용 유지가 맞다"(실사 n=54, 연속 카펫 0%)는 *C2가 존재한다*를 전제로 한 판정. C2 폐기·전환은 이 룰링의 전제를 파기하므로 §5 재판정이 선행돼야 한다. **(c) 역방향 해법(scene04를 여름으로 되돌리기)은 사용자 룰링 위반** — R04-2(§7 ruling 8)는 사용자 결재 룰링이고 GT-22는 LANDED 행이다. 되감으면 143 프림 삭제·G4 지면층·R04-1 로프 핸드라인까지 함께 풀린다. 게다가 gallery_fix_plan_v1.md `§0-3`은 *"씬 맥락이 한눈에 읽혀야 한다 — **scene04가 모범 사례**"*로 scene04를 라이브러리 기준점으로 못박았다. **(d) §7 Q5 / §1 씬 번호 규약** — *"씬 번호 재부여는 하지 않는다(Q5, 08-05)"*. C2를 다른 아이디어로 갈아끼워도 파일명·ID는 `sceneC2`로 고정이고, geometry map `:7`의 "레퍼런스 이미지와 1:1 대응" 체계상 `look_refs/c2_leaf_stairs.jpg` 대응이 끊긴다(새 look_ref 조달 필요). **(e) 원장 GT-8·GT-9** — C2는 **GATE-1 파일럿 4씬(03·07·**C2**·01)** 이자 CB-2 파일럿(`gt_changes_w3.md:80·81`). 씬을 재설계하면 두 행의 파일럿 baseline 근거가 이동한다. **(f) §5 parked** — P-1~P-19 중 C2를 지목한 행 **없음 = 무충돌**. **(g) §4 나머지 18항** — 무충돌(C2는 헤지·볼라드·전주·맨홀·tactile 어느 조항의 대상도 아님).
- **성격**: scene03 교량과 같은 유형 — C2 자체는 의도대로 작동하는 설계이고 결함은 '가을이 2씬'이라는 중복인데, 그 중복은 C2보다 2일 늦게 들어온 scene04의 R04-2 계절 고정이 만든 것이므로, C2의 고유 축이 계절이 아니라 **부피 매몰(단 시작 에지 소실)**임을 설명한 뒤 매몰 매질만 비계절성으로 교체(예: 예초 더미·모래/토사 퇴적·공사 방수포·자재 적치)하면 C축 3슬롯과 §4-7을 모두 지키면서 겹침만 해소된다.

## sceneC4 — 물이 이상함 · 물은 맑아야 · 패드 가릴 필요 없음 (원장 GT-88: "The water looks strange … water must be clear, so there's no need to occlude t

- **요소**: `git show HEAD:scenes/batch1/sceneC4_wet_stairs.py` — **물 재질(핵심)**: `:277` `water_color=(0.03, 0.05, 0.06), water_rough=0.02, water_spec=1.0` + `:523-526` `M["water"] = PBR(f"{ROOT}/Looks/Water", diffuse_color=mp["water_color"], roughness_const=mp["water_rough"], metallic=0.0, specular_level=mp["water_spec"])` — **opacity/transmission 인자가 없다**. 하부 빌더도 마찬가지: `scene_common.py:2074-2084` `def build_water(...)` docstring *"Water slab. Constant colour (0.05,0.10,0.11), roughness 0.03, metallic 0. A thin box whose top sits at z"* → `add_box` 1장짜리 **완전 불투명 박스**. 즉 '물'은 알베도 0.03~0.06 의 검은 판이다. **수막 프림**: `:167-172` `water_films=[step 3/6/9/12]` → `:680-724` `build_water()` 가 각 필름을 3 lobe 로 쪼개 총 12 lobe + `:710-717` 암화 링 12매(`M["tide"]`, `ring_grow_x 0.022`, `ring_grow_y 0.075`, `ring_proud 0.0045`); `:173-178` `water_sheets` 2매 → `:725-745` 회전 lobe 3+ring 3 각각. **패드(=물이 가리는 대상)**: `:640-679` `build_tread_films()` — 14단 × `patch_min 3`~`patch_max 5` 매 = **트레드 위 42~70매의 패치 플레이트**(`film_proud 0.003`, `film_t 0.012`, `patch_seed 2804`), 재질은 `:150-155` `patch_tiers` 3단(Strong rough 0.10/spec 0.95/tint 0.352·0.362·0.398 · Mid 0.20 · Damp 0.35). **미러 파라미터**: `:132` `wet_rough=0.14, wet_spec=0.9,   # r1: 0.08 was a full mirror -> specular eased`. **트윈 팔**: `:70` `"wet_surface": True,   # [feature] False -> dry materials + water films removed = twin`, `:56-59` 주석, `:641-655` docstring. **판정 계기**: `:427-428` BANNER *"2. grazing_mirror·PT — 트레드 경면이 하늘을 반사해 단 에지가 병합되는가(특색) / 3. film_closeup·PT — **수막 4곳이 수평 거울면으로 읽히는가** (RT는 과소평가)"*. **모호한 두 번째 '패드' 후보**: `:880-883` `bc.build_bollard_v51(stage, …, M["tactile"], front_dir=…)` — `tactile=` 인자를 넘기지 않아 볼라드 전면 점형 소판이 항상 ON(`Docs/surveys/cue_expansion_survey_v1.md` §0.3 **D2 CONFIRMED**, `batch1_common.py:221`).
- **원설계 의도**: **거울성은 의도된 단서였다** — docstring `:11-17`: *"the rain-soaked tread tops **mirror the overcast sky**. The bright sky reflection washes the riser shading off the adjacent step so the step edges **merge**, and treads holding a water film turn into **outright horizontal mirrors**."* `:23-29` **Feature precondition [IMPORTANT]**: *"An **overcast sky dome** is what makes this scene work… Under clear noon light the sun prints a point highlight and hard shadows revive the edges, and **the feature collapses**. -> light profile = sc.OVERCAST_HDRI + lookfix=False + no sun… **judge with PT**."* **원 스펙** — `Docs/legacy/nanobanana_batch1_geometry_map.md:56-60` sceneC4_wet_stairs: *"이미지 판독: … 젖은 트레드가 흐린 하늘을 **경면 반사**, 단 에지 시각 병합. 일부 트레드에 수막"* / *"**젖음 = 재질 레이어**: 트레드 상면 rough 0.06~0.12·spec 강화… **수막 = build_water 박판(두께 0.005, 트레드 3~4곳 부분 폭)**"* / *"GT: 양성. C1과 함께 '재질 변주 축' 확립"*. 프롬프트 원문 `look_refs/nanobanana_batch1_prompts.md:56`: *"The wet stone treads mirror the bright overcast sky, so the reflective step surfaces visually merge together… **Small puddles** on some treads."* — **원 스펙의 물은 '작은 웅덩이'이지 검은 판이 아니다.** **지금의 어두운 패드·패치워크는 07-27 사용자 지적의 산물이다** — `:138-149` *"[just-after-rain package v2 · 07-27] **User note: 'the rain does not read / it is just glare on bright stone'.** (1) Patchwork: tread wetness goes from one material to partial-width plates within the tread across 3 tiers (kills the per-tread boundary). (2) **Darkening: strong wet 0.36 vs the dry reference (damp_tint 0.74) = 51% down** (the brief asked for 30~40%+)… (3) Roughness per tier 0.10/0.20/0.35 — strong wet stays specular so the 'edge merge' feature does not collapse. (4) **Geometry unchanged**"*. 미러 완화 이력도 이미 1회 있었다 — `:132` r1 주석 및 geometry map `:102` *"C4 wet_rough 0.14(r2)"*. **트윈 팔 설계** — `:56-59` *"wet_surface is a material toggle (geometry unchanged); the water-film plates are removed with it so we never get 'water on a dry surface'"*, `:641-655` *"In the dry twin (wet_surface=False) all 3 tier materials collapse to the same dry stone, so **twin consistency holds in both geometry and look**"* — 즉 두 팔은 **기하가 비트 동일**해야 한다.
- **충돌**: **(a) 07-27 사용자 지시와 08-06 사용자 지시가 정반대** — 51 % 암화(`:142-144`)는 사용자가 "비가 안 읽힌다"고 해서 넣은 값이다. 단순히 밝게 되돌리면 그때 지적이 부활한다 → '투명한 물' 과 '젖은 기재의 암화' 를 **분리**해야만 두 지시를 동시에 만족한다(무충돌이지만 되감기 금지 조건). **(b) 씬 정체성** — 경면 반사에 의한 단 에지 병합이 GT 단서 자체다(`:11-17`, `:23-29`, BANNER 2·3). '미러 과장 완화'는 되되 **경면 자체를 죽이면 씬이 소멸**. 원장 GT-88 scope도 "젖음 단서(씬 정체성) 유지"로 명기. **(c) 트윈 기하 불변식** — `:56-59`·`:641-655`. 투명화는 **재질 경로**로 풀어야 하고, 수막 프림 추가·삭제·형상 변경으로 풀면 wet ON/OFF 두 팔의 기하 동일성(BANNER 4 "기하 불변인가")이 깨진다. **(d) 재질 동결 조항** — `Docs/briefs/gallery_fix_plan_v1.md §0-1` *"**배치 우선**: 재질 사실화보다 asset 배치·활용 품질이 먼저다. **재질 작업은 당분간 동결.**"* GT-88은 순수 재질 작업이다. 전례는 원장 §29 **GT-73** *"'Can the glass be made slightly transparent?' **재질 동결의 명시적 예외(사용자 지시)**"* — GT-88도 같은 예외 선언을 행에 남겨야 §0-1과 정합. **(e) '패드' 지시대상 모호 → §4-6·P-10·P-12 접촉 위험** — 후보 ①트레드 wet-film 패치 플레이트(`:640-679`, 42~70매) ②볼라드 전면 점형 tactile 소판(`:880-883`, 항상 ON). ②로 읽어 소판을 지우면 **§4-6** *"tactile(점자블록) 기본 OFF를 켜지 말 것, **볼라드 아래 tactile 패드 금지 (P-10·P-12)**"* 및 **§5 P-10** *"RF-4 부착식 tactile 패드 스펙 — 기록만, **구현 금지**"*, **§5 P-12** *"볼라드 전면 tactile 12/42 ON — **사용자 소유, 손대지 않음**"* 의 소유권 행을 건드린다. 원장 GT-88 scope는 ①로 확정 기재("수막 플레이트 투명화·미러 과장 완화·**트레드 가시성 회복**")되어 있으므로 ②는 별건으로 남겨야 한다. **(f) §4-6 앞단·§4-13** — C4의 `cue_tactile`은 W2 §12.4 룰링으로 이미 기본 True(`:64`, `:92-95` 비준수 setback 1.00 재현)이고 이번 작업은 켜는 방향이 아니므로 **무충돌**; D4 tactile 띠 조항도 비해당. **(g) §5 나머지(P-19 ambientCG decal 등)** — 무충돌.
- **성격**: 거울성은 존치해야 할 의도된 단서지만 '검은 불투명 판'은 `scene_common.build_water`가 opacity를 지원하지 않아 생긴 구현 한계의 부산물 = 결함이며, 07-27의 암화 지시와 08-06의 투명 지시를 동시에 만족시키려면 물(투명·경면·굴절)과 젖은 기재(암화 51 %)를 서로 다른 레이어로 분리하는 것이 유일한 해다.

## CROSS — sceneC2 — scene04 가을과 겹침 (원장 행 없음, 미신고)

- **요소**: `sceneC2_leaf_stairs.py` 전체 낙엽 레이어(위 상세 항목 참조) · 상대편 `scene04_parktrail.py:48·:254-256·:365-371`
- **원설계 의도**: geometry map §B + `look_refs/c2_leaf_stairs.jpg` + leaf_globalization_budget_v2 §5 "C2 전용 유지" 룰링
- **충돌**: **§4-7** (낙엽류 asset은 낙엽 씬에만) = C2가 그 씬 → **충돌(전제 소멸)**. **§7 Q5/§1** 씬 번호 재부여 금지 → 아이디어를 바꿔도 ID는 `sceneC2` 고정, look_ref 1:1 대응 재조달 필요 → **조건부**. **원장 GT-22 + §7 ruling 8 R04-2** → scene04를 되돌리는 역방향 해법은 **충돌(사용자 룰링 위반)**. **원장 GT-8·GT-9** GATE-1 파일럿 4씬(03·07·C2·01) baseline 이동 → **조건부**. **§5 P-1~P-19** → **무충돌**(C2 지목 행 없음). **§4 나머지 18항** → **무충돌**. **미신고 상태 자체가 §2.4 선신고 규율의 대상** — C2만 GT-72~88 중 행이 없으므로, 방향 결정 후 착지 전에 행을 먼저 써야 한다.
- **성격**: 17건 중 유일하게 원장 행이 없는 항목이므로 오케스트레이터는 '존치+매질 교체 / 전면 재설계 / 현상 유지' 중 어느 쪽이든 결정 즉시 GT 행 선신고부터 처리해야 한다.

## CROSS — sceneC4 — 수막 투명·표현 개선 (GT-88)

- **요소**: `sceneC4_wet_stairs.py:277`(water_color/rough/spec) · `:523-526`(M["water"]) · `scene_common.py:2074-2084`(build_water 불투명 박스) · `:640-679`(패치 패드 42~70매) · `:880-883`(볼라드 전면 tactile 소판, 인자 누락으로 항상 ON)
- **원설계 의도**: docstring `:11-17`(거울 = 의도된 단서) · `:138-149`(07-27 사용자 지시로 51 % 암화·패치워크 도입) · geometry map §B "수막 = build_water 박판 0.005" · 원장 §44 GT-88
- **충돌**: **gallery_fix_plan §0-1 재질 동결** ↔ GT-88은 순수 재질 작업 → **충돌, 단 GT-73(원장 §29) 전례대로 '사용자 지시에 의한 명시적 예외' 선언으로 해소 가능**. **§4-6 (볼라드 아래 tactile 패드 금지, P-10·P-12 참조)** + **§5 P-10 (부착식 tactile 패드 — 기록만, 구현 금지)** + **§5 P-12 (볼라드 전면 tactile 12/42 ON — 사용자 소유, 손대지 않음)** → '패드'를 볼라드 점형 소판으로 오독하면 **3중 충돌**; GT-88 scope가 트레드 가시성으로 확정 기재했으므로 **명시적으로 ①로 못박아 해소**. **§4-13 (D4 tactile 띠 청소 금지)** → 비해당·**무충돌**. **씬 정체성(경면 병합)·트윈 기하 불변식** → 재질 경로 한정이면 **무충돌**, 프림 변경이면 충돌. **§5 나머지** → **무충돌**.
- **성격**: '패드'의 지시대상을 트레드 패치 플레이트로 문서상 확정하지 않으면 P-10·P-12(사용자 소유 행)를 실수로 건드릴 수 있으므로, 해석 확정이 이 행의 첫 번째 게이트다.

## CROSS — scene01 — 양측 건물 복원 · 이중 중간레일 버그 · 부유 기둥 (GT-74)

- **요소**: `scenes/main/scene01_campus_stairs.py:55-59` 삭제 기록(*"Buildings **R** and **L** — the 14 m and 10 m masses that boxed the plaza on both long sides, **68 prims** — are **deleted**, not re-clad: they are what the user called '미흡'"*, BS-4) · `:103-108` GT-67 1-4 backdrop 복원(4층 매스 2동 + **주행축 40 m 개구** + 측면 wing 2 + 서측 매스 1, 55~78 m) · `:96-100` GT-67 1-1 picket 밀도 · `:101-102`+`scene_common.py:1876-1880` GT-67 1-2 `merge_handrail`(브래킷 그립레일·rail knuckle·end return) · `:156` upper_plaza(x −16…0, y ±8)
- **원설계 의도**: 진자 3회차의 기록이 코드에 그대로 남아 있다: 원설계는 양측 건물 → 사용자 "미흡" → **BS-4로 삭제**(`:55-58`, sky-above-roofline 규칙 `:57-59`) → 08-05 검수 "**과거 코멘트 오해로 과도하게 비워짐**"(`Docs/briefs/gallery_fix_plan_v1.md` 1-4) → **GT-67 1-4**(원장 §23)로 backdrop 복원, `:87-89` *"BS-4 (item 3 above) read the user's '미흡' as 'delete the environment', and what came back was **a white paving field with one distant brick slab**"* → 08-06 "buildings again on both sides … field or square?"
- **충돌**: **gallery_fix_plan §0-2** *"길 정면을 건물로 막지 않는다"* → 양측 복원 시 **GT-67 1-4의 주행축 40 m 개구 유지 필수** → **조건부**. **scene01:57-59 BS-4 자기규칙** *"sky shows above every roofline in every judged cut (`z_ceil` printed per block)"* → 근접 고층 복원은 이 검사와 **충돌**(높이 재배분 필요). **§4-17** *"`rivermark_plaza_bldg_*` 사용 거부는 유지되며 guard로 강제됨 — **guard를 고치지 말 것**"*(`assets/download_urban.py:127`, `assets/verify_urban.py:552`) → 신규 건물 자산 선정 시 **금지 목록 준수**. **§4-10 사람·차량 금지**, **§4-19 CC0/KOGL** → 준수 조건. **§5 P-8** *"빈 archetype 패널 5 + top-up 2 | gate=이미지 수확"* → 건물 archetype 확충으로 번지면 **parked gate 발동**. **§4-4 가로등 등간격은 옳다** → 1-5 배치 재조정에서 램프 열을 불규칙화하지 말 것. **§2.3 공유 kit 규율** → 이중 중간레일 원인이 `scene_common.build_railing_line`(GT-67 1-2 `merge_handrail`)이면 **공유 kit 변경이므로 씬 1개 렌더 선검증 의무**.
- **성격**: 양측 건물은 원래 있다가 사용자 코멘트 오독으로 지워진 것이므로 복원은 정당한 되돌림이고, 진짜 결함은 거리·높이 배분과 GT-67 1-2가 새로 만든 레일 중복·부유 기둥이다.

## CROSS — scene03 — 교량 더 높이·측면으로 이설 (GT-83)

- **요소**: `scenes/main/scene03_riverbank.py:443-451` `bridge=dict(x0=12.0, x1=64.0, y0=-22.5, y1=-13.5, deck_top=-0.2, …)` + 교각 7기 @8.0 m bay(14·22·30·38·46·54·62) · `:582-589` 재질(v5 룰링으로 albedo 0.055 → `concrete_dark`(0.28), `concrete_parapet`(0.22)) · `:862-881` v7 occluder 재구축(회전 프레임 + deck/pier 분리) · `:753-759` `levee_walk` 뷰 설계
- **원설계 의도**: `:443` *"**D1 [top priority] distant bridge — fixes 'river' in one cut.** The river axis is Y, so the bridge crosses along X."* / `:446-448` *"Piers go 5 -> 7 at the same 8.0 m bay — **a bridge is the one place a constant pitch is correct**, and **G3 shows exactly that: a long low girder deck**"*. 즉 육교도 고가차도도 아닌 **v4-D1 '강 정체성' 백드롭 거더교**이며, 판정컷 `:759` *"bridge crossing in one frame"* 이 그 목적.
- **충돌**: **§4-4** *"가로등 등간격은 옳다(광학 설계) — '등간격 금지' 규약은 장식 배치용"* → 교각 8.0 m 등간격도 같은 논리로 보호되며 코드가 명시(`:446-447`); 이설 시 **pitch 유지**. **§4-8 원경 흐림(대기 원근) 금지** → "더 높이·측면으로"를 '멀리 밀어 흐리게'로 풀면 **충돌**. **§5 P-13** *"03·12·17 무연석(kerbless) 확인 | gate=한강 제방·자전거도로 실사진"* → 교량만 옮기면 무충돌, 제방 단면·연석을 건드리면 **gate 발동**. **원장 GT-83 scope** "제방·수면·낙차 불변" → 자기구속. **§4 나머지·§5 나머지** → **무충돌**.
- **성격**: 육교로 오독됐을 뿐 결함이 아니라 강 정체성을 한 컷에 확정시키는 v4-D1 백드롭 거더교이므로, 높이·측면 위치만 옮기고 등간격 7피어·저형 거더 어휘는 반드시 보존해야 한다.

## CROSS — scene05 — side_arc 돌출 오브젝트 · 무대 계단 정리 (GT-75)

- **요소**: `scenes/main/scene05_amphitheater.py:12-16` 진입 호형 계단(θ ±9°)·무대·`cut_wall`(파라펫 상면 z=+1.0) · `:268-275` tiers 3단 · `:295-313` v4-A1 무대-tier3 관통 틈 수정 + v5.1 원형 무대 podium(유효 높이 0.693 m) · `:101-102` *"the regressions this scene has actually had before (**v5.1 planter × `side_arc`**, W3 census C02-P1). The drop edge, the lip kerb, the tiers and the stage are untouched"*
- **원설계 의도**: v5.1 사용자 지시 *"무대는 더 솟아야 한다"*(`:309-313`)로 podium 신설; 08-05 gallery_fix_plan §1 scene05 *"유형 식별은 합격. **asset 배치·활용도 개선에 집중**"* → 원장 §25 **GT-69** 착지. side_arc는 이 씬이 과거에 실제로 회귀를 낸 바로 그 자리(`:101-102`).
- **충돌**: **§5 P-16** *"scene05 무대 검은 쐐기 틈 | 해제 gate = **별도 무대 재설계 패키지**"*(문서: `Docs/briefs/scene05_stage_redesign_v1.md`) → 무대 계단 '정리'가 무대 본체·쐐기 틈에 닿으면 **충돌**. 원장 §31 GT-75가 *"P-16(무대 재설계)은 계속 범위 외 — 계단 접속·마감 한정"* 으로 선제 회피. **§4-16** *"scene05 `seat.arcs` 값은 yaw가 아니라 **호 스팬(도)** (C-30)"* → side_arc 돌출 수리 중 arcs를 yaw로 오독하면 **즉시 위반**. **§4-1 각진 헤지** → 관목 재검토 시 형태 지적 금지. **§5 P-6(관목 군식, gate=산림청 도판)** → bush 교체가 군식으로 번지면 gate. **§4 나머지** → **무충돌**.
- **성격**: 돌출 오브젝트는 명백한 결함이지만 '무대 계단 정리'는 P-16 재설계 패키지와 한 뼘 차이라, 계단 접속·마감으로 범위를 못 박은 GT-75 scope를 문자 그대로 지키는 것이 유일한 안전선이다.

## CROSS — scene06 — 나선 재정밀·에지 마감·양측 통일 (GT-76)

- **요소**: `scenes/main/scene06_overpass_spiral.py:15-24` 원 정체성 3축(축1 상부 90° 외측 난간 결손 / 축2 원통 실루엣 자기폐색 / 축3 환형 보이드 r 0.5~1.5·깊이 5 m) · `:25-29` GT-68 노트 · `:63-76` W3 S06 G6 재건(연속 helicoid soffit·scalloped tread ends·`DeckGlass_*` 유리+bronze cap) · `:31-33` GT drop invariance
- **원설계 의도**: `:63` 사용자 원문 *"정체성 그렇게 안 겹치도록 이미지 참조해서 나선 구조 고쳐줘"* → G6 재건. 이후 gallery_fix_plan §1 scene06 *"본교량 deck 난간 = 유리판 + bronze cap(`DeckGlass_*`, G6). 나선만 tube 2단으로 바뀌어 불일치 → **나선 guard를 main(유리+cap) 계열로 통일**"* → 원장 §24 **GT-68** 착지.
- **충돌**: **scene06:25-29 (GT-68 자기선언)** *"Axes 1 and 3 above… are **no longer the build**: axis 1 went with the 08-05 continuous-guard doctrine, and axis 3 goes with the glazed guard landed here"* → "start over and rebuild the spiral"를 문자로 받아 **결손 난간(축1)·개방 환형 보이드(축3)를 되살리면 08-05 독트린(원장 §13 GT-57) 위반**. **process_spec §1 W4** *"W4 GT 재판정(연대: **계단참 06·08**, …)"* + `STATUS.md` 다음 5 *"W4 GT 재판정(**scene06 로봇높이 개구 폐합 이월분** 포함)"* → **재판정 대기 씬을 지금 재건하면 W4 입력이 이동** → **충돌(순서 문제)**. **§7 Q2** "scene06 rail 본수 — 보류" → gallery_fix_plan §3이 *"scene06 통일·scene12 롤백으로 **대체됨** — 별도 답 불요"* 로 이미 종결 → **무충돌**. **원장 GT-76 scope** "5.005 m 웰·보행면·낙차 불변" → 자기구속. **§4·§5 나머지** → **무충돌**.
- **성격**: sloppy는 실제 마감 결함이 맞지만 '처음부터 다시'를 문자로 받으면 08-05 독트린이 지운 결손 난간과 개방 보이드가 되살아나므로, 재건은 마감·에지·양측 통일에 한정하고 W4 재판정 순서와의 간섭을 먼저 정리해야 한다.

## CROSS — scene07 — 안내판 제거 (GT-84)

- **요소**: `scenes/main/scene07_temple_stone_path.py:152` `"cue_sign": True,    # [v5 shared layer] sign_info (temple notice) at the entrance` · `:546-548` `sign=dict(cx=-4.95, cy=-1.15, yaw=180.0, w=0.78, h=0.62, pole_h=1.95)` · `:729` `ASSET_ROLES` 에 `"sign_info"` · `:1931-1933` `M["sign_info"]` · `:2597` 배치 · `:2666` `if cfg["cue_sign"] and cfg["hazard_stairs"]:` · `:1632` 자기검사 "안내판" 행
- **원설계 의도**: v5 공용 큐 레이어로 도입된 **사찰 입구 게시판**(진입 방향 대면, yaw 180). 자산 역할은 `sign_info` — **`sign_kr101`이 아니다**.
- **충돌**: **§5 P-17** *"`sign_kr101` 0.844 m vs 900 mm 표준 | gate=연대 3요소 | **일괄 rescale 금지**"* → 본 씬 자산은 `sign_info`이므로 **비해당·무충돌**(오귀속 주의 — 흔한 혼동 지점). **§5 P-7** *"prop-엣지 규칙 M1~M6(벤치·등·휴지통·플랜터·**표지판** offset) — LINT-9는 그때까지 WARN | gate=Commons 프레임 픽셀 실측 각 ≥5장"* → 제거로 이 씬의 M-룰 적용 사이트가 소멸하나 P-7 gate 자체는 유지 → **무충돌(사이트 감소만 기록)**. **§5 P-1** *"scene07 archetype 재건 | gate=사용자 레퍼런스 이미지"* → 표지판 제거는 archetype 재건이 아니므로 **미발동**; 다만 '사찰 입구' 정체성을 표지판이 지탱하고 있었다면 정체성 보강은 P-1로 넘겨야 한다 → **조건부**. **원장 §40 GT-84 scope** "**cue_sign 기본 OFF**(어블레이션 경로 보존, v5.2 전례)" → 키·코드 경로를 삭제하지 말 것(`cue_expansion_survey_v1.md` D12: `cue_sign` 죽은 키 22건을 더 늘리면 안 됨) → **조건부**. **§4 19항 전부** → **무충돌**.
- **성격**: 제거 자체는 무충돌이나 P-17을 이 씬에 잘못 걸지 말 것 — 자산이 `sign_kr101`이 아니라 `sign_info`이고, 삭제가 아니라 `cue_sign` 기본 OFF여야 어블레이션 팔이 살아남는다.

## CROSS — scene08 — 공중에 뜬 핸드레일 정리 (GT-85)

- **요소**: `scenes/main/scene08_sunken_plaza.py` 림 가로대·기둥 결속 계통(08-06 감사: 림 가로대 밑 기둥 부재 + 기둥-가로대 높이 불일치) · `"cue_railing": True`(cue 매트릭스 `cue_expansion_survey_v1.md` §0.2: `08 sunken_plaza  T  F  F  T`)
- **원설계 의도**: 원장 §13 **GT-57 — 08-05 독트린: 파손·결손 난간 소거, 전 씬 연속 가드화(사후 신고)**. 선큰 광장 림에 연속 가드를 두르는 일괄 적용의 결과물이며, 파라펫 개구 처리는 그때 **의도적으로 분리 보류**됐다.
- **충돌**: **§5 P-3** *"S08-B 파라펫 개구부 처리(B-1 폐합 권고) — ~~B-3 파손 난간 옵션~~ 08-05 독트린으로 소멸 | gate=**한국 선큰광장 마감 사진 n≥5**"* → 핸드레일 결속 수리가 파라펫 개구 폐합으로 번지면 **사진 gate 무단 해제 = 충돌**. 원장 §41 GT-85가 *"P-3(파라펫 개구 폐합)은 별도 gate 유지"* 로 선제 분리. **process_spec §1 W4** *"GT 재판정(연대: 계단참 06·**08**)"* → **재판정 대기 씬**이므로 가드 기하 변경 시 W4 입력 이동 → **조건부**. **원장 GT-57** → 부유 핸드레일은 그 일괄 적용이 남긴 미결속 잔재일 가능성이 높아 수리는 독트린 **완성**이지 위반이 아님 → **무충돌**. **§4 19항** → **무충돌**.
- **성격**: 08-05 독트린 일괄 적용이 남긴 미결속 잔재로 보이므로 결함 수리가 맞고, 다만 같은 손으로 P-3 파라펫 개구를 닫으면 사진 n≥5 gate를 무단 해제하는 셈이 된다.

## CROSS — scene09 — 호수 건너 '구름 같은 것' (원경 매스) (GT-86)

- **요소**: `scenes/main/scene09_ghat_riverfront.py:291-292` `far_hedge=dict(cx=52.0, sx=1.4, length=24.0, h=3.6)` + `far_hedges` 3열 · `:308-333` **가을 능선 벨트** `far_hills=[…]` 3능선 + `hill=dict(blobs=11, blob_r=0.58, spread=0.92, body_frac=0.30, seed=91)`(각 능선 = 저형 능선체 위 겹친 타원체 11개) · `:136-138`·`:301-312` 설계 주석
- **원설계 의도**: 원장 `gt_changes_w3.md:110` **GT-35 (6)**: *"the 2 far-side **building** silhouettes (`x0 66`, h 7.0, collider) are **DELETED** and an **autumn hillside belt** (6 ridges, 12–19 m) takes over horizon closure"*, 코드 `:136-138` *"G9 has no buildings: the horizon is rolling wooded hills in ginkgo yellow / maple orange. The 2 dark boxes were the v5 horizon-closure device; **the hills close the horizon and carry the season**"*, `:304` *"Replacement = `far_hills`, which closes the horizon **higher** (12–19 m vs 7 m)"*. 계절은 §7 ruling 8 → **R09-1 (scene09 = autumn)**.
- **충돌**: **§4-8 (do-not-touch)** *"**원경 흐림(대기 원근) 금지** — 서울 실측 소광은 90 m에서 2.6~7 % 대비 손실뿐"* ↔ 코드 `:311-312` *"the far ridge is **desaturated toward the sky (aerial perspective)**, which is why `hill_c` is both lighter and greyer than `hill_a`, not darker"* → **현행 구현이 §4-8 위반 후보이며 '구름·풍선' 판독의 유력 원인** → 수리 방향은 흐림 강화가 아니라 **탈색 철회 + 접지·실루엣·질감** → **충돌(현행 코드 쪽)**. **원장 §42 GT-86** *"GT-63의 원경 매스 존치 판정을 **본 씬에 한해** 사용자 지시로 개정"* → GT-63(원장 §19, 전정 밴드 8씬) 판정의 **부분 개정임을 행에 명기 필요** → **조건부**. **브리프 v3 §A-4** *"지평선 폐쇄: 주 카메라 축 정면에 원경 요소(건물/능선/울타리/암벽). 부지 가장자리 허공 금지(대지 y±40+)"* + **process_spec §6 체크리스트 ③ '대면적 순백/암흑 없음'** → 매스를 **지우면** 지평이 열려 두 조항 동시 위반 → **재표현이지 제거가 아님**. **§4-1 각진 헤지** → `far_hedge`는 헤지 계열이므로 형태 지적을 '헤지 각짐'으로 오독해 둥글리면 **충돌**. **§5** → **무충돌**.
- **성격**: GT-35가 건물을 지우고 세운 '가을 능선 지평 폐쇄 장치'이므로 존치가 옳고, 결함은 밑면 절단으로 인한 부유감과 §4-8을 어긴 대기원근 탈색이다.

## CROSS — scene10 — 데크 접속 경로 자연화 (GT-77)

- **요소**: `scenes/main/scene10_park_deck_switchback.py:191-193` GT-65 노트 · `:269`·`:838` `TOTAL_DROP = 6.600 = 44 × 0.150, frozen (§9 P-2)` · `:419` GT-65 흙길 x1 −1.60 → −1.50 · `:481` `[GT-65 · §0-2] deck -> lower park hand-off` · `:1000-1003`·`:1291-1335` GT-65 난간 인벤토리·접합 자기검사
- **원설계 의도**: `:191-193` *"[GT-65 — 08-05 gallery review: '연결부가 아직 지저분' + '데크 느낌이 끝까지'] **P-2 keeps the archetype parked, so this is junction and continuity work only** — the **널 틈**, the **돌구덩이 (rock outcrop)** and the **통나무 펜스** family are untouched"*. 상위 근거는 gallery_fix_plan §0-2 *"통행 가능성(passage): 길이 있으면 씬 끝까지 이어지게 한다"* 및 §2 실행순서 1번(사용자 우선 지정).
- **충돌**: **§5 P-2** *"scene10 archetype 재건(D1 후보; **낙차 6.60 유지**) — 08-05 독트린으로 파손 베이 조항은 무효, 나머지(**널 틈·돌구덩이·통나무 펜스 보존**)는 유효 | gate=사용자 레퍼런스 이미지 + KDS 34 00 00 원문"* → '경로 자연화'가 이 3종 또는 총낙차 6.600을 움직이면 **충돌**. 원장 §33 GT-77이 *"낙차 6.60·P-2 보존 유지"* 로 선제 구속. **§5 P-6(04 관목 군식 + scene13 식재, gate=산림청 도판)** → scene10 bush 재검토(gallery_fix_plan §1)가 군식으로 번지면 **gate 발동**. **§4-14** *"예산은 triangle 수가 아니라 instance 공유다 — instance 수를 이유로 row를 기각하지 말 것"* → 경로 자연화로 프림이 늘어도 기각 사유 아님(방어 조항). **§4 나머지·§5 나머지** → **무충돌**.
- **성격**: GT-65가 접합만 고치고 경로 형태는 P-2 아래 남겨둔 결과의 잔여 어색함이므로, 파킹된 archetype 3종과 6.600 낙차를 건드리지 않고 접속 형상만 다듬을 수 있는지가 이 행의 성패다.

## CROSS — scene11 — H형 육교로 고쳐달랬는데 계단만 떼고 급히 막은 인상 (GT-80)

- **요소**: `scenes/main/scene11_footbridge_stairs.py:6-9` Type/Law(S11-H) · `:24-28` H-plan 근거 · `:42-46` 정체성 낙차(데크 끝/타워 헤드) · `:65-94` 보행 연속성 자기검사 표(서측 → 상행 → 데크 → 하행 → 동측) · `:92-94` 카메라 축 주석
- **원설계 의도**: `:26` 사용자 원문 인용 — *"육교가 너무 양쪽으로 뻗어있어. **한국 육교는 H형이야**"*(법: `Docs/surveys/w3_intake_06_10.md` §S · **S11-H**). `:27-28` *"A Korean 육교 is an **H in plan**: the stair towers run **parallel to the carriageway** on each sidewalk, and the deck crosses between them."* 구 빌드는 계단 2조가 데크 축과 일직선(x 15…30.88)이었고 그것이 기각된 형태.
- **충돌**: **`:92-94` 판정 원점 고정** *"the grid keeps the **deck run axis (+X)** and the origin **(15.00, 0, 5.50)** it already had — under the H-plan that point is the east head landing's outer edge"* + **§7 ruling 7 R17-1의 일반 원칙** *"judge presets untouched — **legibility comes from geometry, never from camera edits**"* → 4지 접속을 고치며 이 원점·판정 프리셋을 옮기면 **충돌**. 원장 §36 GT-80 scope "낙차·그레이팅 축 불변" 이 자기구속. **gallery_fix_plan §0-2** *"길이 있으면 씬 끝까지 이어지게 한다"* → 막다른 데크 금지의 근거(**상위 원칙, 충돌 아님**). **§4-19 라이선스**, **§4-10 사람·차량 금지** → 준수 조건. **§4 나머지·§5 P-1~P-19** → **무충돌**(scene11 지목 행 없음).
- **성격**: H형 지시는 이미 반영됐으나 4지 접속이 미완이라 '계단만 떼고 급히 막은' 인상을 준 미완 결함이며, 고치되 판정 원점 (15.00, 0, 5.50)만은 건드리면 안 된다.

## CROSS — scene13 — 단문 개방·가드 종단 결속·계단 측부 갭·전면 에지 마감 + 유리 투명화 (GT-72 / GT-73)

- **요소**: `scenes/main/scene13_apartment_parking_entry.py` — GT-58~64 누적 산물: 전장 플랫데크 캐노피(GT-58)·보행구 캐노피(GT-59)·건물형 유리 구조물(GT-60)·유리 전장+캡(GT-61)·계단부 유리 랩+하행 핸드레일·헤지 실관목 Privet 31주(GT-62)·A104 신설/계단박스 자립 양면 가드+동측 강화유리문(GT-64) · 신규 `scene_common.make_glass`(GT-73 예정)
- **원설계 의도**: 08-05~08-06 사용자 답변 6차 누적(원장 §14~§20 GT-58·59·60·61·62·64). U-5 문자적 해석(전장 캐노피), §0-2 정면축 개방, 미지시 자산 금지 원칙이 이 씬에서 확립됐다.
- **충돌**: **§4-9** *"전신주는 15·18만 허용, 17·20 금지… *(08-05 GT-59: **13도 금지 목록에 추가** — G13 시그니처였으나 사용자 지시로 제거)*"* → 배경·진입로 정리에서 전주 재도입 **금지**. **§4-11** *"맨홀 prop 재론 금지… *(08-05 GT-59 예외: 차도류 프로파일 P4/P7/P8 의 맨홀·잡초는 사용자 지시로 **전면 소거**)*"* → 차량 진입구·교차로 마감에서 맨홀·잡초 부활 **금지**. **§4-1 각진 헤지 유지** + 원장 §18 GT-62(Privet 31주) + §27 **GT-71 밀도 파일럿** → 헤지를 건드리면 GT-71 A/B와 얽힘 → **조건부**. **§5 P-6** *"04 관목 군식 + **scene13 식재** | gate=산림청 가로수 매뉴얼 도판"* → 수목 21주는 GT-64로 착지했으나 **추가 식재는 gate 발동**. **gallery_fix_plan §0-1 재질 동결** ↔ **GT-73 유리 투명화** → 원장 §29가 *"재질 동결의 **명시적 예외**(사용자 지시)"* 로 이미 선언 → **해소 완료**. **§4-10 사람·차량 금지** → '차량 진입구 에지 마감'은 구조물 마감이지 차량 배치가 아님을 행에 명기 필요 → **조건부**. **원장 §28 GT-72** "배경(단지 외)은 **보류 선언**" → 범위 밖 확산 금지.
- **성격**: 7차까지 온 누적 수정의 마감 결함이 본체이고, 이 씬에서 유일하게 규약을 해제하는 유리 투명화는 GT-73으로 이미 예외 선언이 끝나 있어 추가 해제가 필요 없다.

## CROSS — scene14 — 벽에 붙인 handrail 제거 (GT-78)

- **요소**: `scenes/main/scene14_grandstair_illusion.py:242-249` `[GT-70] rail` dict(`r 0.019` = Ø38 mm, *"the upper bound of the graspable 3.2~3.8 cm handrail (장애인등편의법 시행규칙 별표1)"*, scene13 벽부 핸드레일과 동일 계열 r 0.020) · `:1618-1692` `def build_wall_handrail()` + 착지 로그 *"[GT-70] 측벽 손잡이 바 n · 지주 n (bay …)"* · `:1939` 호출 · `:818` 자기검사 · `:62-74` GT-70 근거 주석
- **원설계 의도**: `:62-63` 08-05 **사용자 룰링 원문** — *"화강석 재질 집착 중단 — '그냥 큰 계단'으로 읽히면 충분"* 및 *"**측벽은 적당한 벽 겸 handrail 로 읽히게**"* (동일 문구가 `Docs/briefs/gallery_fix_plan_v1.md` §1 scene14 에도 기록) → `:74` *"**2. The side wall gains a handrail** (`build_wall_handrail`)"* = 원장 §26 **GT-70** 2항.
- **충돌**: **직전 사용자 룰링(08-05 GT-70)과 08-06 지시의 정면 충돌** → 원장 §34 **GT-78** 이 *"GT-70의 측벽 상부 handrail 항을 사용자 지시로 **롤백**(재질 단순화는 유지)"* 로 **부분 롤백** 처리 = 08-05 룰링을 폐기하는 것이 아니라 '벽 겸 handrail' 해석 중 **handrail 프림 해석만 철회** → **해소 조건: 롤백 범위를 프림에 한정**. **§5 P-15** *"scene14 D11 파라펫 하단 결손(실기하 버그, 판정컷 비가시) | **구조 수리는 예산 밖 — 유예**"* → 측벽을 건드리다 파라펫 하단에 닿으면 **parked 행 발동**. **원장 GT-52** *"**The illusion geometry does NOT move**"* + `_stair_selfcheck`(총낙차 6.000000 m, 43-entry 노징 폴리라인 vs 6 앵커, 오차 0) → 측벽 프림 제거는 착시 기하 불변이므로 **무충돌**(단 자기검사 재실행 필요). **원장 GT-57 08-05 독트린(연속 가드화)** → 측벽 **자체가 방호**이므로 손잡이 제거로 가드가 사라지지 않음 → **무충돌**. **§4 19항** → **무충돌**.
- **성격**: 하루 전 사용자 룰링을 사용자 본인이 뒤집은 케이스라 결함도 잔재도 아니며, 롤백을 handrail 프림에 한정하고 GT-70 1항(재질 단순화)은 남겨야 08-05 룰링이 살아 있다.

## CROSS — scene16 — 정체성 불명, '도로를 건너는 지하보도' 느낌이어야 (도로 신설) (GT-79)

- **요소**: `scenes/main/scene16_canopy_shadow.py:356` `hedges=[(-12.0, 6.5, -9.0, 7.3), (14.0, -7.3, 18.0, -6.5)]` · `:1063-1091` `[GT-63] place_hedge_row(… pitch_frac=0.62, jit_along=0.10, jit_across=0.06)` + 주석 *"User verdict on the GT-63 hedgeswap was '**a bit more natural, but still pretty dense**', so pitch_frac…"* · `:703-704` ground_kit tactile 사이트(`cue_expansion_survey_v1.md` **D5 HIGH** — 계단머리 경고 띠가 `cue_tactile`이 아니라 `hazard_stairs`에 배선된 부분 토글 결함) · 캐노피 그림자 밴드(T20 정체성)
- **원설계 의도**: scene16 = **T20 캐노피 암부(양성)** 이며, `Docs/legacy/nanobanana_batch1_geometry_map.md:` §A sceneN1 이 *"GT: 전 픽셀 '낙차 없음'. **T20(캐노피 암부, 양성)과 대조쌍**"* 이라고 명시 — **sceneN1(hard negative)의 짝**이다. 헤지 밀도는 원장 §27 **GT-71 A/B 파일럿**(scene16 11→9주, pitch 0.62)의 대상이며 `STATUS.md` 상 **검수 대기·미종결**.
- **충돌**: **process_spec §2.3 변경 루프 규율** *"비교는 **같은 prim끼리 A/B**(다른 요소가 섞인 비교 금지)"* + *"측정이 도는 동안 공유 모듈 **동결**"* + `STATUS.md` 다음 2 *"**GT-71 A/B 판정** → 헤지 밀도 확산 여부"* → **판정 대기 중인 바로 그 씬에 도로를 신설하면 A/B 대조가 오염** → **충돌(순서 문제)**. **§4-11** *"차도류 프로파일 P4/P7/P8 의 맨홀·잡초는 사용자 지시로 **전면 소거**(GT-59)"* → 신설 도로가 ground_kit 차도 프로파일을 부르면 소거 상태 **유지 필수**. **§4-9** *"전신주는 **15·18만 허용**"* → 16은 허용 목록 밖이므로 도로가 생겨도 전주 **금지**. **§4-10 사람·차량 금지** → 도로에 차량 배치 **금지**. **씬 정체성/대조쌍** → 상부 도로가 캐노피 그림자 밴드를 가리면 **N1의 대조쌍이 깨진다**; 원장 §35 GT-79가 *"계단·하부 통로·**그림자 밴드 단서(씬 정체성 T20)**·GT 불변"* 으로 선제 구속 → **조건부**. **§4-18 의도적 함정 씬 유지(Q1)** → N1 쪽 조항이나 짝을 흔들면 함께 흔들림 → **간접 조건부**. **§5** → **무충돌**.
- **성격**: 정체성 보강 요구 자체는 정당하나 같은 씬이 GT-71 A/B 판정을 기다리는 중이고 sceneN1의 대조 짝이기도 해서, '도로 신설'은 순서·범위 통제 없이 착수하면 열린 판정 두 개를 동시에 오염시킨다.

## CROSS — scene17 — 본선 도로가 사면에 바짝 붙음, 이격 확대·정리 (GT-81)

- **요소**: `scenes/main/scene17_ramp_pair_hangang.py:144` `"cue_railing": False,   # Han river levee stairs are **customarily unrailed**` · `:161-180` 제방 마루(보도 3 + 식재대 0.5 + 자전거도로 4) + `crown_bike`(x0 −4.0, x1 0.0) + `crown_line`(x −2.0, 파선) · `:166-167` supervisor §5.7 룰링(07-29) *"scene03 stays natural, **the bike road is valid only on the scene17 levee**"* · `:17-18`·`:32` 단면(제방 마루 → 잔디 사면 1:2 h 3.2 → 테라스 → 사석 호안 → 광폭 수면)과 **무난간 직하강 flight**
- **원설계 의도**: 한강 제방 실관행 재현 — 무난간 계단이 씬 정체성이자 do-not-touch 등재 항목.
- **충돌**: **§4-2 (do-not-touch)** *"**scene17 계단 무난간** — 한강 제방 실관행; `cue_railing=False` **유지**. (08-05 독트린과 공존: 난간이 '있으면' 낙차 표지라는 것이지, 실관행상 없는 곳에 세우라는 뜻이 아님)"* → 본선 이설·완충 '정리' 중 난간을 세우면 **즉시 위반**. 원장 §37 GT-81이 *"무난간 관행(§4-2) 불변"* 으로 선제 선언. **§4-9** *"전신주는 15·18만 허용, **17**·20 금지(간선·광장, 지중화율 94.16 %)"* → 본선(간선) 정리에 전주 **금지**. **§5 P-13** *"03·12·**17** 무연석(kerbless) 확인 | gate=한강 제방·자전거도로 실사진 확인"* → 완충 정리가 연석 신설로 가면 **gate 발동**. **§7 ruling 7 R17-1**(`w3_intake_v2_images.md:1098-1100`) *"Shorten/re-site the 40 m ramp so the stair-ramp pair reads in one frame; **no flight widening (GT stays A)**; **judge presets untouched — legibility comes from geometry, never from camera edits**"* → 이격 확대에도 동일 규율(계단 폭 확대 금지, 카메라 편집 금지). **§4-1 각진 헤지** → 완충 녹지를 헤지로 채우면 형태 조항 적용. **§4 나머지** → **무충돌**.
- **성격**: 본선이 사면에 붙은 것은 실제 배치 결함이 맞지만 이 씬은 무난간·무전주·무연석 3중 잠금 + R17-1 카메라 불가침이 걸려 있어, '정리'라는 말이 손쉽게 네 조항을 동시에 건드릴 수 있다.

## CROSS — scene19 — 옥상 하행 계단만 있으면 되는데 건물을 왜 추가? 제거 + 계단부 과암 + 출입 폭 (GT-82)

- **요소**: `scenes/main/scene19_fan_winder.py:228-237` `buildings=dict(C 고층 · D/E 저층)` (*"horizon-closing buildings (base_z = ground) — C high-rise + [rooftop v3] D/E low-rise"*) · `:38-42` *"roof_skyline — building E's roof was being occluded… **distant building E is moved due north** … so building E's roof (−1.5, cap −1.0) opens up 3.0 m below eye height"* · `:694`·`:800` 빌드/참조 · `:978-987` 프레임 스캔 *"[_frame_scan] **sky 44.6 % · building E 38.9 %** (roof top face 12.3 %) · ground …"* · `:103` `cue_scene_dressing`(planter, **buildings**, grass) · `SCENE_SEASON = "summer"`(§7 ruling 8, G8 상속)
- **원설계 의도**: 브리프 v3 §A-4 *"지평선 폐쇄: 주 카메라 축 정면에 원경 요소(건물/능선/울타리/암벽). 부지 가장자리 허공 금지(대지 y±40+)"* 를 만족시키는 지평 폐쇄 장치이자, `roof_skyline` 자기검사가 **스케일 앵커**로 쓰는 대상(인간 시고 2.0 m 유지를 위해 카메라 대신 건물 E를 북쪽으로 이동시킨 설계).
- **충돌**: **브리프 v3 §A-4 지평선 폐쇄** → 건물 일괄 제거 시 지평이 열림 → **충돌**. **process_spec §6 갤러리 체크리스트 ③ '대면적 순백/암흑 없음'** → 현재 sky 44.6 % + building E 38.9 %; E 제거 시 하늘이 80 %대로 → **충돌**. **`:38-42`·`:978-987` `roof_skyline` 자기검사** → building E 제거 시 검사가 근거를 잃어 코드 동반 수정 필요 → **조건부**. **§4-5** *"scene19 **물탱크 없음** 유지"* → 옥상 정리 중 물탱크 재도입 **금지**. **원장 GT-51**(옥상 하드웨어 `exterior_aircon_unit` 2 + `power_box_01` + `utility_box_01`, 플랜터 토심 0.75 m 조경기준 제12조, **난간 1.10 m** 연대 rider §6.2-B, 옥상 파라펫 1.20 m 건축법 시행령 제40조) → '건물 제거'가 옥상 하드웨어·난간고까지 쓸어가면 **GT-51 회귀**. **§4-19 라이선스**, **§4-10 사람·차량 금지** → 준수 조건. **§5** → **무충돌**.
- **성격**: '옥상 계단만 있으면 되는데 왜 건물?'은 근경 잉여 매스에 대한 지적일 가능성이 크고, 원경 3동은 지평 폐쇄·스케일 앵커·`roof_skyline` 자기검사를 동시에 지탱하므로 어느 건물인지 특정하지 않은 일괄 제거는 §A-4와 §6-③을 함께 깬다.

## CROSS — scene20 — 계단 양측에 난간이든 벽이든 뭐라도, 너무 위험해 보임 (GT-87)

- **요소**: **사실관계 정정 근거**: `scenes/main/scene20_diagonal_oblique.py:78` `"cue_railing": True,   # Sloped rails on **both sides** of the stair (inside rot_group - follows the diagonal)` · `:678-696` `for sgn, tag in ((-1.0, "S"), (1.0, "N")): sc.build_railing_line(…)` — **계단 양측 난간은 이미 존재** · `:134-136` `stair_rail=dict(y=2.4, rail_h=0.9, …)` · `:119-120` `stairs=dict(… y0=-2.5, y1=2.5, base_z=-2.6)` · `:103` `wedge=dict(x0=-8.0, x1=0.0, **y_half=9.0**, z_top=0.0, base_z=-2.2)` · `:642-648` v4-S1 주석 *"**the drop boundary becomes the whole 30 deg line** instead of a single point at the origin, so the scene's character is reinforced"* · `:121-128` `rot=dict(pivot=(0.0,0.0), deg=30.0)` + *"★ [W3 R20-1, restated in code] `deg=30.0` is a **scene-wide skew, not per-instance jitter**… It is the whole identity of the T8 case… and it is **FROZEN**"*
- **원설계 의도**: T8 = 보행축에서 벗어난 **사선 낙차 경계**가 정체성. v4-S1이 대각 wedge로 낙차 경계를 '원점 한 점'에서 '30° 선 전체'로 확장한 것이 의도된 강화였다.
- **충돌**: **(0) 사실관계** — 계단 양측 난간은 이미 True로 서 있다(`:678-696`, y ±2.4). 무방호 구간은 계단 폭(±2.5) **밖**의 대각 낙차 에지 **|y| 2.4…9.0**(wedge `y_half 9.0`, 낙차 2.2 m) → 신설 대상은 '계단 난간'이 아니라 **광장 대각 에지 방호**임을 행에 정정 기재해야 한다. **§4-2 괄호절** *"난간이 '있으면' 낙차 표지라는 것이지, **실관행상 없는 곳에 세우라는 뜻이 아님**"* → 광장 대각 에지에 전장 가드를 두르는 것이 한국 광장 실관행인지 **근거 필요**; 원장 §43 GT-87은 08-05 독트린만 인용 → **조건부 충돌**. **§4-9** *"전신주는 15·18만 허용, 17·**20** 금지"* → GT-87이 이미 명기 → **해소**. **`:121-128` W3 R20-1 `deg=30.0` FROZEN** → 방호를 rot_group 안/밖 어디에 두든 30° 스큐를 건드리면 안 되고, 지터 폐지 린터가 이 값을 0으로 만들면 안 됨 → **조건부**. **process_spec §6 체크리스트 ④** *"h0.3 컷에서 **낙차 은닉 전제 유지**"* → 대각 에지 전장에 가드가 서면 은닉 전제가 약해지므로 hazard-off 팔과의 대칭·GT 라벨 영향 확인 필요 → **조건부**. **§5** → **무충돌**.
- **성격**: '너무 위험해 보인다'의 실체는 계단이 아니라 광장 대각 에지 약 6.5 m×2 구간의 무방호 2.2 m 낙차라는 명백한 결함이며, 다만 거기에 가드를 세우면 h0.3 은닉 전제(§6-④)와 §4-2 실관행 조항을 동시에 건드린다.

## CROSS — 전 씬 공통 — 17건 일괄 처리 시 걸리는 절차·상태 조항

- **요소**: `Docs/STATUS.md:16-18`(검수 대기 2건: `look_check/_review/260806_w3_s13fix6/` scene13 15컷 GT-64 · `look_check/_review/260806_w3_fixqueue/` scene01·05·06·10·12·14·16 97컷 GT-65~71) · `Docs/audit_v4/gt_changes_w3.md` §28~§44(GT-72~88, 전부 **OPEN(선신고)**) · `Docs/briefs/process_spec_v1.md` §2.2·§2.3·§2.4·§2.5·§2.7
- **원설계 의도**: §2.4 선신고(declare-before-land) 규율에 따라 08-06 비평 17건 중 16건이 이미 GT-72~88 행으로 선신고돼 있다(C2만 행 없음). §7 R7 gate: 씬 상태를 바꾸는 round는 gallery 사용자 검수 통과 시에만 CLOSED.
- **충돌**: **§7 R4 조건부 승인** *"큐 폐기는 `260805_w3_doctrine` gallery 검수(R7) **통과 후 확정**"* + `STATUS.md` 다음 1 *"검수 통과 시 GT-62~71 CLOSED, GT-57 CLOSED + R4 큐 폐기 확정"* → **GT-62~71이 아직 미종결인 상태에서 GT-72~88을 그 위에 쌓는 중** → baseline 사슬(`260806_w3_fixqueue` → `260806_w3_fixqueue2`)이 미검수 판 위에 얹힌다 → **절차 충돌(귀속 추적 필요)**. **§2.2 커밋 전 검증 floor 4종** + **§2.3 공유 kit 변경은 씬 1개 렌더 선검증** → scene01의 `build_railing_line`·GT-73의 `scene_common.make_glass`·GT-88의 `build_water` 는 모두 **공용 모듈**이므로 확산 전 1씬 선검증 의무. **§2.7 GPU 배타·순차(`flock /tmp/negobs_gpu.lock`)** + **§2.4 ledger `flock /tmp/negobs_ledger.lock`** → 다중 에이전트 병렬 작업 중이므로 필수. **§2.5 round 명명(상태 주장 단어 금지) + `--out` 필수 + `stamp_round.py`** → 갤러리 3건째 생성 시 준수. **§2.6** → Opus 5 + xhigh, 파일 소유권 분리, pathspec commit. **gallery_fix_plan §0-1 재질 동결** → 이번 라운드에서 재질을 건드리는 행은 GT-73·GT-88 둘뿐이며 **둘 다 사용자 지시 예외로 선언 필요**(GT-73은 선언 완료, GT-88은 미선언).
- **성격**: 17건 자체보다 위험한 것은 미검수 상태인 GT-62~71 위에 GT-72~88이 쌓이는 baseline 사슬이며, 재질 동결 예외는 GT-73만 선언돼 있고 GT-88은 아직 비어 있다.

