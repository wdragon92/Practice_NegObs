# fixlog V-B — scene13 교체(R8) / scene17 재구성 (v5.1 현실성 갈아엎기)

작성 2026-07-27 · 팀 V-B(Opus 5) · 단독 소유 파일 2종
근거: `Docs/audit_v4/user_feedback_v5_1.md` §전역 현실성 규약 + 씬별 지시 13·17행,
`Docs/briefs/multi_scene_brief_v5.md`(파일 구조·공통 레이어), 참고 구현 scene16.
검증: `python3 -m py_compile` 2/2 통과 · `NEGOBS_SMOKE=1` 자기검증 2/2 전 항목 OK
(렌더·Isaac 미실행 — 지시대로 감독 일괄 렌더 대기). **scene_common.py 미수정.**

---

## scene13 — 나선 주차램프 폐기 → **아파트 단지 지하주차장 진입부**

| 항목 | 내용 |
|---|---|
| 아카이브 | `scenes/main/scene13_helical_parkingramp.py` → `scenes/archive_v3/`(파일명 유지). git 미추적 파일이라 `git mv` 실패 → `mv` 로 이동(로그 남김) |
| 신규 | `scenes/main/scene13_apartment_parking_entry.py` (약 900행, SCENE_CONFIG 7키 · `_smoke_report` · 보행 연속성 검증표 포함) |
| 태양 | `SUN_AZ_OFFSET = 171.5` (지시값) |

### 교체 논거(현실성)
지상에 홀로 선 나선 주차램프는 실물 대응이 없다. 한국 아파트 단지에서 "차량이
지면 아래로 사라지는 곳"은 사실상 전부 **직선 지하주차장 진입 램프**다 →
범용성(어느 단지에나 있음) + 치수·설비가 법규로 고정(현실성)을 동시에 얻는다.

### 기하 (스모크 자동 검산 값)

| 요소 | 사양 | 검산 |
|---|---|---|
| 램프 | 폭 6.0(2차로 규정) · 완화 3.6 m @8.5% → 본선 19.694 m @17% → 완화 3.6 m @8.5% · 총 run 26.894 · 낙차 **3.960** | 최대 경사 17.0% ≤ 17% OK (주차장법 시행규칙 직선 17% 이하 · 상하단 완화구간 경사 1/2·길이 3.6 m 관행) |
| 병설 계단 | riser 0.165 × 24단(12+12 되돌음) · 폭 1.4 · tread 0.30 · 중간참 2.35 m | 계단 낙차 3.960 = 램프 낙차 → **두 경로가 같은 지하 1층 바닥에 착지** OK |
| 개구 | 램프 트렌치 x[0,24]×y[±3.3] · 계단 샤프트 x[5,11.2]×y[3.3,6.9] | 지반 **6박스** 분할(W/S/N1/N2/N3/E) → 개구 위를 덮는 플레이트 **0** OK |
| 지하 | 상부 슬래브 밑면 −1.2 = 지반 플레이트 저면 · 포털 x=24 노면 −3.714 | 유효고 **2.514** ≥ 표기 높이제한 2.3 OK · 복도 유효고 2.76 ≥ 2.1 OK |
| 지하 동선 | 계단 → 복도(x 11.2→24, y 5.25..6.65) → 주차장(x 24..38, y ±9.3) → 램프 하단 합류 | 보행 연속성 15단계 표 전 구간 단차 ≤ 0.165 |

### 현실성 반영 (v5.1 전역 규약)
- **규정 볼라드**(피드백 §2): h0.9 · 지름 0.16 · 간격 1.5 · 상단 반사띠 밴드 +
  **전면 0.3 m 점형블록**. 배치는 **보도가 차로를 횡단하는 접점 2곳뿐**
  (y=±4.35 × x{−2.9,−1.4}) — 장식적 볼라드 열 없음.
- 진입부 설비: 캐노피 슬래브(x −1.6..4.2, 지붕 밑면 2.85, 기둥 4본 지상 접지) +
  **캐노피 전면 보에 매달린 높이제한바**(z 2.30, 황·흑 8절) + 옹벽 코핑 위
  **차단기**(함체 + 하강 상태 적·백 7절 차단봉) + 요금 안내판(sign_info 지주식).
- 옹벽 + 코핑(폭 0.42 · h0.12) — 난간은 코핑 **위**에 착지(base_z=0.12, 부유 0).
- "규정 미달의 현실": 남측 개구 난간 **x 10.5~13.5(3 m) 철거 결손** — 그 지점
  낙차 1.73 m 무방호. 계단 샤프트는 코핑만(무난간).
- 배치 비정형: 화단 5·수목 8·생울타리 3·벤치 3(yaw 174/−6/3°)·가로등 3 전부
  비등간·비격자, 벤치는 화단/수목 앵커 옆. 아파트 3동은 `build_building`
  (base_z=0, 창 인셋) — +X/−X 지평 동시 폐쇄.
- 재질: 순백 대면적 없음(도색선 0.70, 반사띠 0.72, 파라펫 0.60), 어두운 상수색
  0.035, 인스턴스 틴트 지터 ±5% 변종(grass/grass_b, wall/wall_b, shell/shell_b).
- 지하 약발광: `make_pbr(emission_*)` 천장 패널 7매(PT 8바운스 전제) + 주차구획선.

### 위험 본질 / 카메라
h0.3 grazing 에서 시선 부각(d=5 → 3.43°, d=10 → 1.72°)이 램프 초기 경사
4.86°보다 작아 노면이 평면으로 압축되고, 개구 너머 지반(x ≥ 24, 상부 슬래브 위
조경)이 근측 보도와 **연속 평면**으로 읽힌다(스모크 자동 판정).
컷: grid 9 + `entry_approach` / `ramp_graze` / `bollard_walk` / `stair_head` /
`portal_look` / `beauty_overview`.

### 조립 중 자체 교정(초안 → 최종)
1. 난간 c 값 ±3.48 → **±3.15(코핑 중심선)** + base_z 0.12 — 초안은 지면 위
   0.12 m 부유였다.
2. 횡단 보도와 종주 보도(walk_north/south)가 **동일 상면 z 로 2×2 m 겹침** →
   횡단 보도를 4토막(CrossN1/N2/S1/S2)으로 분할, Z파이팅 제거.
3. 지하 주차장 **서측 마구리 미폐합**(흙 속으로 뚫린 공동) → 램프 개구·복도
   개구만 비운 서측 벽 3토막 추가.
4. 캐노피 북측 기둥이 계단 샤프트 개구 위에 떨어지는 문제 → 샤프트를 x 0..6.2
   에서 **x 5.0..11.2** 로 동진(기둥 4본 전부 지반 위).

---

## scene17 — "정체 불명·억지 지형" → **실제 한강 제방 단면**으로 재구성

파일 유지: `scenes/main/scene17_ramp_pair_hangang.py` (전면 재작성).
태양 `SUN_AZ_OFFSET = 171.5` 유지 · **GT 낙차 3.2 m 불변** · 대비쌍 학습 의도 보존.

### 폐기 / 신설

| 폐기(구 버전) | 신설(v5.1) |
|---|---|
| 지그재그 2절 램프 · 페어레인(A/B) · 분리 파라펫 · 레인별 성토 | **사선 단일로** 1장(길이 40 m, 경사 8%, 폭 2.5) |
| 제방 옹벽 3구간 + 개구(계단·램프가 벽에서 튀어나옴) | **잔디 사면 7세그 폴리라인**(구배 1:2), 계단이 사면을 직선 관통 |
| 둔치 위 X 방향 자전거도로(강과 직교 — 비현실) | **강 평행(Y 방향) 산책로 폭 3** + 둑길 마루 보도3+자전거도로3 |
| 둔치 −3.25 / 수면 −3.40 | 둔치 −3.20(계단·램프 종점과 flush) / 호안 사석 사면 / 수면 −3.42 |

### 기하 (스모크 자동 검산 값)

| 요소 | 사양 | 검산 |
|---|---|---|
| 사면 | 7세그 (25.7 → 42.5 → 55.6 → 58.0 → 60.0 → 50.0 → 50.0 %), 총 6.40 × 3.20 | 평균 구배 **50.0% = 1:2** OK · **전 절점이 계단 현(−0.5x) 위** → 계단이 잔디에 파묻히지 않음 OK · 세그 margin 0.25 겹침(틈 0) |
| 계단 | 20단 × riser 0.16 · tread 0.32 · 폭 3 · 무난간(관행) | run 6.40 = 사면 수평 6.40 OK · 낙차 3.20 OK |
| 사선 램프 | `rot_group(yaw 80.7931°)` + `build_slope` 1장. cos yaw = 6.4/40 = 0.16 (사면 50%를 8%로 늘여 탄다) | 시점 (0,4,0) → 종점 중심 (8.23, 43.19, −3.20) |
| 램프 단면 | 상류 절토 **+0.00~+0.30**(매몰 0) · 강측 석축 **최대 1.53** < 성토 두께 2.35 | 최소 절토 ≥ 0 OK(노면이 잔디에 안 묻힘) · 부유 0 OK |
| 접합 | 마루/사면/둔치/호안/수면 | 수면(−3.42)과 x겹침 중 더 낮은 지면 **없음** OK · 호안 사석 −3.65 < 수면 → 물가 잠김 OK |

램프 기하 유도(파일 주석에도 기재): 노면이 구배 50% 사면을 8% 로 타려면 진행
1 m 당 X 전진 dx = 0.08/0.5 = 0.16 → cos(yaw) = 0.16. 상류 가장자리를 사면 현
접선에서 강측으로 e_up = 0.60 밀어 절토량을 항상 ≥ 0 으로 유지(사면 어깨
라운딩이 현보다 최대 0.23 높기 때문 — 이 보정이 없으면 노면 상류 절반이
잔디에 매몰된다).

### 접합/부유 방지 세부
- 진입 apron: `rot_group` 안 수평 박스(로컬 x −1.4..0, 상류로 1.2 확장) → 마루
  슬래브(x<0) 내부까지 물려 **마루 ↔ 노면 사이 0.15 m 단차 제거**. 상면은 마루보다
  0.015 낮춰 동일평면 Z파이팅 회피.
- 마루 끝 연석(x −0.20..0.05, h0.05)이 마루/사면 이음선을 덮는다. 계단 폭
  (y ±1.5)과 apron 구간(y 2.6..4.4)은 연석을 비워 통행선을 열어둔다.
- 사면 세그 1은 margin 0(마루와 정확히 flush), 2~7은 margin 0.25 로 상호 겹침.
- 사면 세그는 계단 폭만 비운 **2 Y밴드**(S: y −30..−1.5 / N: 1.5..48) — 사선
  램프는 그 위에 얹히므로 밴드 분할 불필요(스트립이 Y축에 거의 평행해 축정렬
  절취로는 근사 불가하다는 계산 결과에 따른 설계 선택).

### 드레싱 (배치 비정형)
억새 4띠(비등간, x 24.6~26.8 + 사면 토우 1) · 둔치 수목 5 · 마루 수목 4 ·
벤치 4(yaw 96/−84/93/−86°, 수목·억새 앵커 옆) · 가로등 둔치 4 + 마루 3 ·
이정표 1 · 건너편 아파트 **4동**(y −36..58 로 확장 — 램프가 +Y 로 40 m 뻗어
북측 지평이 열렸기 때문) · 교량(피어 5본) · 건너편 둔치/관목/수목.

### 카메라
`pair_compare` **재조준**: eye(−20,−13,15) → tgt(7,13,−2.2). 검산 — 오프축
계단 14.6° · 램프 s=20 13.8° · 램프 종점 21.1°(전부 hFOV 30° 이내), 부각
계단 32.0° · 종점 16.3°(카메라 피치 24.6° ± 17.5° 안) → 계단·사선 램프가 한
프레임. 그 외 `levee_walk`(둑길 종주 grazing) · `ramp_run`(노면 종주) ·
`across_river` · **`toe_lookup`(둔치에서 사면·계단 올려봄, 신규)**.
grid 9컷(gy=0)은 은닉 판정용으로 불변 — h0.3/h0.9 전 조합에서 사면·계단 은닉
성립(스모크 자동 판정).

---

## 잔여 / 감독 판단 필요

1. **scene17 램프 길이 40 m** 는 경사 8%·낙차 3.2 의 산술적 귀결(단축 불가).
   Y 방향으로 y 4 → 43.5 까지 뻗으므로 지면·수면·건너편 y 범위를 −30..48 /
   −42..60 으로 확장했다. 렌더 시 북측 프레이밍 확인 필요.
2. **scene13 지하 공간**(복도·주차장)은 PT 전제 약발광이라 RT 캡처에서는 거의
   검게 나온다. `portal_look` 은 PT 로 판정할 것.
3. scene13 계단 샤프트를 무난간(코핑만)으로 둘지, 규정대로 난간을 세울지는
   "은닉 특색 vs 규정" 트레이드오프 — 현재는 **무난간 + 남측 램프 난간 3 m
   결손** 조합으로 두었다. 감독 판정 후 `cue_railing` 경로에 추가 가능.
4. 두 씬 모두 렌더 미실행(지시). 나무 v2 는 `sc.build_tree` 호출만으로 자동 반영.
