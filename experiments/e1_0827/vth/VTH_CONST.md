# VTH_CONST — W1 capture 상수 대장 (V 문턱 실험 · 창고 A트랙)

- 작성 2026-08-28 · 단계 **W1 (렌더 캡처)** · 브리프 `Docs/briefs/edge_relabel_brief_v6.md` §7
- 태그: `[문헌]` 출처 있는 값 · `[실측]` 이 단계에서 잰 값 · `[도구기본값]` 도구 기본값 ·
  `[기존값]` 이전 라운드에서 이월 · `[방법]` 보고·계산 방법으로 정한 값 · `[임시]` 결재 대기
- 원칙 P1: **문턱은 여기서 정하지 않는다.** 아래는 전부 "어떻게 쟀는가"의 상수이지 판정 기준이 아니다.

---

## 1. 원 논문 카메라 (그대로 복사) [문헌]

출처: `Baseline_NegObs/src/negativeobstacleavoidandance/bumperbot_description/urdf/realsense2.urdf.xacro`
(시뮬 RealSense D435, `sensor name="cameradepth" type="depth"`, 104–129행)

| 상수 | 값 | 태그 | 비고 |
|---|---|---|---|
| `HFOV_RAD` | 1.2043 rad (69.001°) | [문헌] | urdf 104행 |
| `RES` | 1280 × 720 | [문헌] | urdf 106–107행 |
| `CLIP_NEAR / CLIP_FAR` | 0.1 m / 100 m | [문헌] | urdf 111–112행 |
| depth 노이즈 | **없음** (`<noise>` 태그 자체가 없음) | [실측] | 원 논문 urdf에도 없다 → 우리 depth는 무노이즈. §6-⑲ 도메인 해석에 필요 |
| 센서 타입 | `type="depth"` 1개 (RGB·depth 동일 센서) | [방법] | 같은 센서라 RGB↔depth 화소 정합이 구조적으로 보장된다 |
| `update_rate` | 2.0 Hz | [방법] | 정지 카메라 30개를 한 번에 띄우므로 낮게. 원 논문은 30 Hz(주행용) |
| `always_on` | 1 | [방법] | 게으른 센서의 첫 프레임 반합성 버퍼 회피 |

## 2. 씬 기하 실측 [실측]

`bumperbot_description/models/large_holed_floor/meshes/large_holed_floor.stl`
(sha256 `ab75bc68feb3adae…`, 이진 STL, 삼각형 21,834개)

| 상수 | 값 | 태그 |
|---|---|---|
| 판(plate) 두께 | **0.12432 m** (윗면 z = 0.12432, 밑면 z = 0.0, 모델 로컬) | [실측] |
| 판 크기 | 41.65 × 62.52 m (로컬 x [−6.990, 34.660], y [−52.048, 10.472]) | [실측] |
| 판 실면적(구멍 제외) | 2557.07 m² | [실측] |
| 래스터 해상도 `RASTER_M` | **0.01 m** (4165 × 6252 셀) | [기존값] `gazebo_wh_0824/tools/project_hole.py`와 동일 |
| 윗면 판정 `TOP_EPS` | 0.002 m (mesh 최대 z에서 2 mm 이내) | [방법] |
| 윗면 판정 `UP_NZ` | \|n_z\| ≥ 0.90 | [방법] |
| 구멍 최소 크기 `MIN_HOLE_CELLS` | 25 셀 = 25 cm² | [방법] 이하는 메시 이음매의 래스터 잡음 |
| 구멍 개수 | **270개** (25 cm² 이상), 그중 254개가 ≥ 0.01 m² | [실측] |
| 구멍 면적 범위 | 0.0049 – 0.7351 m² (최대 0.97 × 0.97 m) | [실측] |
| 모양 분류 `rect_fill` 문턱 | < 0.87 → 원형, ≥ 0.87 → 사각. 단 종횡비 > 1.30 은 `slot` 으로 따로 뺀다 | [방법] 원은 bbox의 π/4=0.785, 사각은 1.00 을 채운다 |
| 구멍 계열 | **원형 135개**(지름 0.136–0.967 m = 반지름 0.068–0.484 m) · **사각 121개**(0.0090–0.6862 m²) · slot 14개 | [실측] |
| 판 배치 pose (**두 월드 동일**) | (−13.56098, 20.2009, 0, 0, 0, 0) | [실측] |
| 판 윗면 세계 z (= 보행면) | **0.12432 m** | [실측] |

> 논문 본문의 "원형 반경 0.05–0.8 m · 사각 ≤0.8 m²" 서술이 메시 실측과 일치한다(우리 실측 반경
> 0.068–0.484 m, 사각 ≤0.686 m²). 이제 서술이 아니라 숫자로 확인된 값이다.

**낙차 아래에 무엇이 있는가** [실측]

| 월드 | 구멍 밑 | 낙차 깊이 |
|---|---|---|
| `eworld2` | **아무것도 없다.** `model://ground_plane` include가 주석 처리돼 있고(11행) 다른 바닥 에셋도 없다 | **무한(뚫림)** |
| `expandedworld` | `aws_robomaker_warehouse_GroundB_01_001` 이 pose z = −0.090092 로 깔려 있다. 윗면 세계 z = **0.0344**. 단 그 판의 범위는 x [−6.990, 7.010], y [−10.453, 10.468] 뿐 | 그 범위 안 = **0.0899 m**, 밖 = **무한(뚫림)** |

→ 이 단계의 표적 구멍 7×2개 중 **`H60`(2.16, −3.78) 하나만** expandedworld에서 바닥이 있고(0.090 m 낙차),
나머지는 두 월드 모두 뚫린 낙차다. (판정에 쓰지 말고 서사·한계로 기록)

## 3. 두 월드의 조명·에셋 차이 [실측]

| 항목 | `eworld2` | `expandedworld` |
|---|---|---|
| 광원 | `model://sun` + 점광원 `Warehouse_CeilingLight_003b` (0, −17, 8.5, diffuse 0.5) 1개 | **광원 없음** (`<light>` 0개, sun include 없음) |
| `<scene>` ambient / shadows | 0.2 / **false** | 0.4 / **true** |
| 기하 있는 모델 수 | 48 (+판) | 53 (+판) |
| 벽 | 없음 (`WallB_01` 주석 처리) | `aws_robomaker_warehouse_WallB_01AB` x [−12.7, 1.3], y [−10.7, 10.2], 높이 9.0 m |
| 바닥 판 pose | 동일 | 동일 |
| SDF 버전 | 1.6 | 1.7 |

## 4. 포즈 격자 [문헌 밴드 + 방법 격자]

| 상수 | 값 | 태그 |
|---|---|---|
| `STANDOFFS` (근처 rim까지, m) | 0.3, 0.5, 0.7, 1.0, 1.5, 2.0, 3.0, 4.0, 6.0, 8.0 | [방법] 브리프 §7 "거리 스윕" 지시를 10점으로 구현 |
| `HEIGHTS` (판 윗면 기준, m) | 0.3, 0.6, 1.0, 1.5, 2.0 | [문헌] 브리프 §7 밴드 0.3–2.0 m |
| `PITCHES_DEG` | −15, −5, +5 | [문헌] 브리프 §7 밴드 −15°…+5° |
| 부호 규약 | `pitch_deg` 음수 = 아래로 봄. SDF pitch = −radians(pitch_deg) | [기존값] 08-24 창고 manifest와 동일 |
| 접근 방향 `N_DIRS` | 구멍당 **1개** (±x, ±y 중 사용 가능한 스테이션이 가장 많은 방향) | [방법] |
| 크기 사다리 `SIZE_BANDS` (m²) | L1 0.70–0.80 · L2 0.55–0.65 · M1 0.30–0.36 · M2 0.19–0.26 · S1 0.13–0.17 · S2 0.055–0.070 · T1 0.0085–0.0105 | [방법] 크기–거리 교락 진단(§6-⑱)용 층화 |

## 5. 포즈 유효성(스킵) 규칙 [방법]

| 상수 | 값 | 뜻 |
|---|---|---|
| `CAM_CLEAR_M` | 0.25 m | 카메라 발밑 반경 0.25 m가 전부 실판이어야 한다(다른 구멍 위·판 밖이면 스킵) |
| `ASSET_PAD_M` | 0.20 m | 에셋 AABB를 이만큼 부풀려 "에셋 안"을 판정 |
| `CORRIDOR_HALF_W` | 0.5 m | 접근 통로의 반폭 — 이 안에 에셋이 있으면 그 방향은 탈락 |
| `MIN_HOLE_PX` | 1 px | 구멍 윗면 사각형의 투영이 화면에 1 px도 안 들어오면 스킵(`hole_outside_fov`) |
| 에셋 형상 모델 | 충돌 메시(COLLADA/STL)의 **로컬 AABB → yaw 회전 → 세계 AABB** | 항상 실제 메시보다 크게 잡히는 보수적 근사 |
| **구멍 발자국 판정** | 원형은 **내접원**, 사각·slot 은 bbox | ⚠ 원형 구멍의 bbox 중 1−π/4 = **21.5 %는 밝은 바닥**이다. bbox 로 '속'을 세면 바닥 화소가 조용히 섞인다(이 단계에서 실제로 발생 → 구멍 내부 포화도가 정확히 0.212 로 나와서 잡음). W2 도 반드시 원은 원으로 잴 것 |

## 6. 캡처 실행 [도구기본값 / 방법]

| 상수 | 값 | 태그 |
|---|---|---|
| 배치 크기 | 월드 로드 1회당 카메라 **30대** | [기존값] 08-24와 동일 |
| `SETTLE` | 8 s (구독 후 버리는 시간) | [방법] 08-24는 3 s. depth 센서가 더 늦게 안정돼 늘렸다 |
| `MINWAIT` | 12 s (gzserver 기동 후 토픽을 보기까지) | [방법] ROS2 데몬이 이전 월드 토픽을 캐시해 준비 판정이 거짓 통과하는 함정 차단(RUN_GUIDE.md 기록) |
| gzserver 플러그인 | `-s libgazebo_ros_init.so` **만** | [방법] `libgazebo_ros_factory.so`를 함께 물리면 이 설치에서 `rcl node's context is invalid`로 abort. 우리는 spawn을 안 하므로 불필요 |
| GUI | gzclient **한 번도 띄우지 않음** | [기존값] 승용 지시(팝업 금지) |
| depth 저장 | `float32` **미터**, 무스케일 (`.depth.npy`) | [방법] Gazebo 32FC1 그대로 |
| GPU 락 | 배치마다 `flock -o /tmp/negobs_gpu.lock` | [기존값] 프로젝트 규약 |

## 6b. YOLO용 파이썬 환경 [실측]

`vth/venv` (python3.10, gitignore 대상). `experiments/dayrun_0820/code/yolo/requirements_yolo.txt`
는 ROS 환경 전체의 `pip freeze` 라서 그대로는 설치되지 않는다(ROS 패키지 다수). 그 파일이 고정한
**YOLO 관련 핀만** 뽑아 설치했고 **전부 그 핀 그대로 들어갔다**:

| 패키지 | 버전 | 태그 |
|---|---|---|
| python | 3.10.12 | [실측] |
| torch | **2.13.0+cu130** (CUDA 13.0, RTX 4090 인식됨) | [문헌] requirements_yolo.txt 의 `torch==2.13.0` |
| torchvision | 0.28.0 | [문헌] 같은 파일 |
| ultralytics | **8.4.123** | [문헌] 같은 파일 |
| numpy / pillow / opencv | 2.2.6 / 12.3.0 / 5.0.0 | [문헌] 같은 파일 |

⚠ 주의: `Baseline_NegObs/neg_env` 는 **torch 2.5.1 고정**이다(논문 저자의 `best.pt` 를 읽기 위해).
우리 venv 는 우리가 직접 학습할 때 쓰는 것이고, 논문 가중치를 로드할 목적이면 baseline venv 를 써야 한다.

## 7. depth 검증 [방법]

| 상수 | 값 | 뜻 |
|---|---|---|
| 평면 기준 | z = 0.12432 (판 윗면) | 화소별 기대 depth를 닫힌 식으로 계산 → 자유 파라미터 0 |
| 샘플 간격 | 8 px | 화면 전체를 8 px 격자로 검사 |
| 이상치 게이트 | \|측정−기대\| < 0.25 m | 시선 앞에 에셋이 선 화소를 빼기 위한 게이트, 그 뒤 중앙값 보고 |
| 통과 기준(이 단계 게이트) | nadir 화소 오차 ≤ **0.02 m** | 지시서 게이트 |

---

## 8. W2 — 라벨(속 화소) 계산 상수 [방법/실측]

작성 2026-08-28 · 단계 **W2 (라벨 + 논문 가중치 곡선)** · 도구 `tools/w2_labels.py`

| 상수 | 값 | 태그 | 뜻 |
|---|---|---|---|
| 창고 '속'의 정의 | 구멍 발자국을 지나는 시선 + 보행면 아래 도달점 (절단면 + 빈 공간) | [방법] | ❓W1-4 의 치환을 **실제로 사용**함. 승인 대기 |
| `INT_Z_MARGIN` | **0.01 m** (`z < plate_top − 0.01`) | [방법] | 지시서가 준 값. 대가: 낮고 먼 포즈에서 far rim 근처 **중앙값 11.6 %** 가 grazing 으로 빠진다 |
| xy 판정 위치 | 시선 ∩ `z = plate_top` 평면 (도달점 아님) | [방법] | 절단면 도달점의 xy는 발자국 **경계 위**라 1 cm 래스터 조회가 동전던지기. 도달점 방식과의 일치도 IoU 중앙값 0.626·p10 0.037 로 측정됨 |
| 발자국 래스터 | `holes_local_grid.npz['grid']` 1 cm, 대상 구멍 bbox 로 크롭 | [기존값] | 270개 전부 측정 셀 수와 일치(불일치 0). 원은 원, bbox 아님 |
| `RIM_RING_PX` | ±2 px | [방법] | 발자국 폴리곤 외곽선 두께 |
| `RIM_TOL_M` | **0.05 m** | [방법] | rim 화소의 측정 depth 와 평면 기대 depth 차 허용치 → `rim_visible` |
| `MIN_OTHER_BOX_PX` | 4 px² | [방법] | 비표적 구멍을 프레임 목록에 넣는 최소 투영 넓이 (프레임당 중앙값 30개) |
| 크기군 `SIZE_GROUPS` | large r_eq ≥ 0.35 · medium 0.18–0.35 · small < 0.18 m | [방법] | 7단 사다리를 3군으로 (L1·L2 / M1·M2 / S1·S2·T1) |
| 면적 로그 구간 | 반십진 1,10,32,100,316,…,5e5 px | [방법] | |
| 길이 로그 구간 | 옥타브 1,2,4,…,512,1281 px | [방법] | |
| 교차 수준 | 0.25 / 0.50 / 0.75 / 0.90 | [방법] | **보고 전용**. 문턱 결정 아님 (P1) |

### 라벨 검증 [실측]

| 검사 | 결과 |
|---|---|
| `labels.csv` 행 == 프레임 | 1231 == 1231 |
| 발자국 래스터 vs 폴리곤 닫힌식 넓이 (전체가 화면 안인 633 프레임) | 중앙값 **−0.47 %**, p5 −2.7 %, p95 +2.7 % |
| `occ_frac_asset` (에셋에 막힌 비율) | 크기축 1135 프레임 전부 **0.0000**, 가림 세트 중앙값 1.000 |
| 근거리 마스크 비어있지 않음 | standoff 0.3 m 최소 `int_area_px` = 1259 px |

## 9. W2 — 추론 상수 [문헌/도구기본값/방법]

| 상수 | 값 | 태그 |
|---|---|---|
| 가중치 | `bot_camera/model/best.pt`, sha256 `f95126cee2b348ac…` (**제자리에서 읽기만**, 복사 금지, AGPL-3.0) | [문헌] |
| 환경 | `Baseline_NegObs/neg_env` — torch **2.5.1+cu124** · ultralytics **8.0.196** · py 3.10.12 · RTX 4090 | [기존값] W1 §5-8 지시대로 vth/venv 아님 |
| `IMGSZ` | 640 | [문헌] 가중치 자체의 학습 설정 |
| ROI 사다리꼴 | TOP 0.20 W · BOTTOM 0.80 W · HEIGHT 0.7 H → (512,216)(768,216)(1152,720)(128,720) | [문헌] `clean_yolo.py` ROIConfig |
| ROI 외곽선 그리기 | **그린다** (노드가 `polylines` 후 마스킹) — arm `roi` | [문헌] 08-24 트랙은 안 그렸다(arm `roi_nopoly`); 박스 수가 2배 차이 |
| `FLOOR` | 0.05 에서 예측 후 읽을 때 임계 적용 | [방법] |
| 운용 임계 | **0.25** (ultralytics predict 기본값 = `self.model(image)` 이 쓰는 값) + 0.10 · 0.50 | [도구기본값 + 2] |
| `MAX_DET` | 300 | [도구기본값] |
| 매칭 | box IoU ≥ **0.50** | [문헌 PASCAL VOC / COCO] |
| 보조 매칭 `centre` | 검출 박스 중심이 GT 박스 안 | [방법] 이 검출기의 박스가 GT 높이의 1.60배로 길어 IoU 0.5 를 못 넘는다(75개 중 29개가 ROI 상단 경계에 붙음) |
| 논문 포즈 부분집합 | height 0.3 m · pitch −15° | [실측] 논문 로봇 카메라 = 보행면 위 **0.125 m** · **14.95°** 아래(bumperbot.urdf.xacro). 기울기는 0.05° 일치, 높이는 2.4배 높다 |


## 10. W3 — 복제 학습 상수 [문헌/방법/실측]

원 논문 레시피를 그대로 따랐다. "더 좋은 검출기"가 아니라 **복제본**이 목적이므로 나머지는 전부 ultralytics 기본값.

| 상수 | 값 | 태그 |
|---|---|---|
| 아키텍처 | YOLOv8n, COCO 사전학습 | [문헌] |
| 사전학습 가중치 | `Desktop_Arrangement/workspace/isaac/isaacsim_test/yolov8n.pt` (nc=80, COCO), sha256 `f59b3d833e2ff32e…` | [실측] 로컬 사본 |
| epochs / batch / imgsz | **100 / 16 / 640** (조기 종료 없음 — 100/100 완주) | [문헌] |
| 증강 | ultralytics 기본값 (mosaic 1.0 · hsv · scale 0.5 · fliplr 0.5) | [문헌 "default augmentation"] |
| 클래스 | 1개 (`hole`) | [문헌] |
| optimizer | `auto` → AdamW lr 0.002 momentum 0.9 | [도구기본값] |
| seed | 0 | [방법] |
| 입력 전처리 | W2 arm `roi` 와 완전 동일: 초록 사다리꼴 외곽선을 **그린 뒤** 마스킹 | [문헌] `clean_yolo.py::_process_image` |
| 학습 월드 | `eworld2` · `int_area_px > 0` · 가림 세트 제외 | [방법] 단계 지시 |
| GT 박스 | `labels.csv` 의 가시 속 bbox 를 사다리꼴 ROI 로 클립 | [방법] |
| `MIN_CLIPPED_PX` | 20 px² (클립 후 조각만 남은 프레임 버림) | [방법] |
| 프레임 수 | 후보 543 → ROI 클립 탈락 51 → **채택 492** (train 443 / val 49) | [실측] |
| 검증 분할 | 프레임 무작위 10 %, seed 0 — **체크포인트 선택 전용** | [방법] |
| 같은 프레임 안의 다른 구멍 | **라벨 안 함** (단계 지시 "GT = interior bbox"). 프레임당 중앙값 30개가 투영되지만 중앙값 18 px² · 높이 0.9 px, 400 px² 넘는 것은 프레임당 약 1.5개 | [방법] + [실측] |
| 학습 시간 | **118 s** (100 epoch, RTX 4090, `flock -o /tmp/negobs_gpu.lock`) | [실측] |
| `best.pt` | `runs/w3_train/replica/weights/best.pt`, sha256 `d747ccd18fba31ca80d3063bee7f10e8ecc90bbafe998ebf44cc86e77e579c32` | [실측] git 무시 대상 |
| `last.pt` sha256 | `425623499c6d720b…` | [실측] |
| 학습 환경 | `vth/venv` — torch **2.13.0+cu130** · ultralytics **8.4.123** · CUDA 13.0 · py 3.10.12 | [실측] 논문 가중치(neg_env, torch 2.5.1)와 다른 환경 |
| 월드 내 검증 성적 | mAP50 **0.915** · mAP50-95 0.823 · P 0.996 · R 0.918 | [실측] 논문 자체 보고 val mAP50 0.935 와 근접 |
| 평가 | `expandedworld` 전체 프레임(가림 제외 574, GT 있는 것 547) — **월드 분할**, 논문 자신의 무작위 분할보다 엄격 | [방법] |
| 추론 상수 | W2 와 완전 동일 (FLOOR 0.05 · τ 0.10/0.25/0.50 · IoU 0.50 · centre 보조 · arm 3종 · MAX_DET 300 · 같은 bin) | [기존값] |
| 추론 시간 | 21 s / 1231 프레임 × 3 arm | [실측] |

### W3 실측 — 두 월드는 같은 구멍을 공유한다 [실측]

| 검사 | 결과 |
|---|---|
| 판 모델 pose | 두 월드 모두 `[-13.56098, 20.2009, 0, 0, -0, 0]` — **동일** (AABB·메시도 동일) |
| 따라서 개구부 | **270개 전부 같은 물리적 구멍** |
| 표적 구멍 공유 | `H00 H30 H42 H60 H98` — 7개 밴드 중 **5개** |
| 결론 | 월드 분할은 **조명과 주변 에셋만** 바꾼다. 위험물 기하는 안 바뀐다 → 암기 한계 |

### W3 실측 — 구멍/바닥 대비의 **부호가 뒤집힌다** [실측]

`int_area_px ≥ 500` 인 크기축 프레임에서 GT 박스 안 평균 밝기 vs 박스를 2배로 키운 고리의 평균 밝기:

| 월드 | n | 구멍 밝기 | 주변 바닥 밝기 | 구멍 − 바닥 | 구멍이 더 어두운 프레임 비율 |
|---|---:|---:|---:|---:|---:|
| `eworld2` (학습) | 412 | 118.2 | 233.1 | **−118.0** | **0.993** |
| `expandedworld` (평가) | 406 | 40.9 | 39.0 | **+4.7** | **0.126** |

학습 월드에서 구멍은 "밝은 바닥 위의 검은 얼룩"이고, 평가 월드에서는 "어두운 바닥 위의 (거의 같거나 살짝) 밝은 얼룩"이다.
월드 격차는 단순한 밝기 차가 아니라 **검출기가 배운 단서의 극성 반전**이다.


---

## 결재란

| 번호 | 항목 | 담당 |
|---|---|---|
| ❓W1-1 | 접근 방향을 구멍당 1개로 제한한 것(프레임 예산) | 승용 |
| ❓W1-2 | `expandedworld`에 광원이 하나도 없다는 사실을 그대로 두고 렌더한 것(원본 불변 원칙 우선) | 승용 |
| ❓W1-3 | 표적 구멍 7개 크기 사다리의 적정성 | 승용 |
| ❓W1-4 | **창고에서 '속'의 정의** — 브리프 §5의 "보행면보다 0.3 m 이상 낮은 면"을 그대로 쓰면 창고 구멍은 판 두께가 0.1243 m 뿐이라 **바닥 없는 구멍조차 속 픽셀 0개**가 된다. W2 는 창고 한정으로 '속 = 구멍 발자국 안의 픽셀(절단면 + 빈 공간)' 로 읽어야 한다 — 이 치환의 승인 | 승용 |
| ❓W1-5 | 두 월드의 **밝기 격차**(eworld2 평균 140–207 · expandedworld 37–80)를 안은 채 월드 분할(§9-❓4)을 진행할지 | 승용 |
| ❓W2-1 | 창고 '속' 치환을 W2 가 **실제로 사용**했다 — 이 파일의 모든 숫자가 여기 얹혀 있다. 승인 또는 교체 | 승용 |
| ❓W2-2 | `INT_Z_MARGIN = 0.01 m` 이 발자국의 중앙값 11.6 %를 grazing 으로 깎는다. 유지할지 | 승용 |
| ❓W2-3 | 경계 축을 **전체 포즈대**(검출률 최대 0.46, ceiling 707 px)로 볼지 **논문 포즈 부분집합**(최대 0.86, n=124, ceiling 2515 px)으로 볼지 | 승용 |
| ❓W2-4 | 논문 로봇 카메라 높이 0.125 m 가 브리프 포즈대(0.3–2.0 m) 밖이다. 후속 단계에서 0.125 m 행을 캡처할지 | 승용 |
| ❓W2-5 | ROI 외곽선을 그리는 노드 정확 재현(`roi`)과 08-24 트랙(`roi_nopoly`) 중 무엇이 논문 비교의 정본인지 | 승용 |
| ❓W2-6 | 월드 격차가 밝기·암기(H00/H30/H60/H98)·벽 3중 교락이다. 월드 분할을 유지할지 | 승용 |
| ❓W3-1 | **월드 분할을 유지할지** — 두 월드는 개구부 270개가 전부 동일하고(암기), 조명은 4배 차이이며, 구멍/바닥 대비의 **부호가 반대**다. 복제본은 eworld2 미학습 프레임에서 0.918, expandedworld 에서 0.126 → 이 격차는 "일반화 실패"가 아니라 "단서 극성 반전"으로 읽어야 한다 (❓W1-5·❓W2-6 의 후속) | 승용 |
| ❓W3-2 | 학습 라벨에서 **같은 프레임의 다른 구멍을 배경으로 취급**한 것(단계 지시대로). 400 px² 넘는 비표적 개구부가 프레임당 약 1.5개 있으므로 복제본의 원거리 재현율이 이만큼 눌려 있다 | 승용 |
| ❓W3-3 | GT 박스가 사다리꼴 ROI 밖이라 학습에서 빠진 eworld2 프레임 51개 — 두 검출기 모두 이 51개에서 검출률 0.000 이다. ROI 자체가 문턱의 일부인지(=축에 넣을지) | 승용 |

