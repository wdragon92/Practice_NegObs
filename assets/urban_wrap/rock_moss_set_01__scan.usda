#usda 1.0
(
    """Auto-authored by `urban_kit.asset_wrapper` (W3 T4b, MD-F2 / MD-F3).

    Reference wrapper for `rock_moss_set_01` under treatment `scan`.
    the scan's own albedo, rebound onto OmniPBR — the MDL arm of the MD-F3 repair, no tint applied.

    The opinion below composes INSIDE the prototype, which is the only place
    it survives `SetInstanceable(True)` — a scene-side binding stops at the
    instance boundary. See section [8b] of `urban_kit.py`.
    Do not hand-edit: `asset_wrapper()` rewrites this file when the stamp
    below no longer matches the registry.
    """
    defaultPrim = "Root"
    metersPerUnit = 1.0
    upAxis = "Z"
    customLayerData = {
        string negobs_wrap_stamp = "9643b929e9d2198f"
        string negobs_wrap_asset = "rock_moss_set_01"
        string negobs_wrap_treatment = "scan"
    }
)

def Xform "Root"
{
    def Scope "WrapLooks"
    {
        def Material "Mtl"
        {
            token outputs:mdl:displacement.connect = </Root/WrapLooks/Mtl/Shader.outputs:out>
            token outputs:mdl:surface.connect = </Root/WrapLooks/Mtl/Shader.outputs:out>
            token outputs:mdl:volume.connect = </Root/WrapLooks/Mtl/Shader.outputs:out>

            def Shader "Shader"
            {
                uniform token info:implementationSource = "sourceAsset"
                uniform asset info:mdl:sourceAsset = @/home/vislab/miniconda3/envs/env_isaaclab/lib/python3.10/site-packages/omni/mdl/core/Base/OmniPBR.mdl@
                uniform token info:mdl:sourceAsset:subIdentifier = "OmniPBR"
                asset inputs:diffuse_texture = @../urban_cc0/rock_moss_set_01/textures/rock_moss_set_01_diff_1k.jpg@ (
                    colorSpace = "auto"
                )
                asset inputs:reflectionroughness_texture = @../urban_cc0/rock_moss_set_01/textures/rock_moss_set_01_rough_1k.jpg@ (
                    colorSpace = "raw"
                )
                float inputs:reflection_roughness_texture_influence = 1
                bool inputs:project_uvw = 0
                color3f inputs:diffuse_tint = (1.0000, 1.0000, 1.0000)
                float inputs:metallic_constant = 0.000
                token outputs:out
            }
        }
    }

    def Xform "Geom" (
        prepend references = @../urban_cc0/rock_moss_set_01/rock_moss_set_01_1k.usdc@
        prepend apiSchemas = ["MaterialBindingAPI"]
    )
    {
        rel material:binding = </Root/WrapLooks/Mtl> (
            bindMaterialAs = "strongerThanDescendants"
        )

        over "_materials"
        {
            over "rock_moss_set_01"
            {
                token outputs:mtlx:surface.connect = None
            }
        }
    }
}
