# SCENE_TEXT_BUILD — `sceneH1_berm_levee` · `sceneH2_landing_campus` 저작 보고 (**test-ext**)

- **작성**: Claude Code · 2026-08-23 · **과업**: RENDER_PLAN_V3 **WP-5 (Wave A)** 중
  §3.1–3.2 「test-ext 사양」의 **paired-H 계열 2씬** — 계획 §8 결재 1 **A안 순서의 첫 두 test-ext 씬**
- **지위**: 이 두 씬은 **계기판 ①②③의 무대**다. test-ext 는 v2·v3 **양 모델 모두 미학습**이므로
  비교가 공정하고(§3.1), 그래서 **4팔(A·B·C·D) 완비가 의무**다(DZ §4.2 `:243`).
  훈련·검증에는 한 프레임도 넣지 않는다.
- **선행**: `SCENE_H67_BUILD.md` (1차 빌더 런). 그 보고의 확정 교훈 3건 —
  ⓐ **림 조건이 실효 구속**(§2.2) ⓑ **ID 마스크는 컷별 갱신이 필요**(§7) ⓒ **AZ 원장 런타임
  패치로 정본 무수정 렌더**(§6.1) — 를 전부 설계 입력으로 받았다.
- **GPU 사용**: 스모크 2컷 + R0 프로브 4팔 64컷 + 근거리 16컷 + 개정 후 확정 프로브
  4팔 **128컷**(8포즈 × 2조명 × 4팔 × 2씬) + 근거리 16컷(개정 후 재렌더) = **렌더 226컷 ·
  보존 210컷** ≈ **0.37 GPU-h** (근거리 R0 16컷은 개정 후 같은 스탬프로 덮어썼다.
  실측 wall 약 54분, 부팅 27회 포함). 전부 `flock -o /tmp/negobs_gpu.lock` 직렬화 —
  웨이브 w1b(기존 22씬 B팔)와 **한 락을 나눠 썼고**, 라운드를 8–16컷으로 잘게 끊어
  w1b 의 24컷 라운드 사이에 끼어드는 방식으로 상대 웨이브를 굶기지 않았다.
- **정본 무수정 원칙**: 기존 씬 파일 **0바이트 수정**. kit(`scene_common`·`ground_kit`·
  `variation_kit`)·드라이버(`run_data_render.py`) **0바이트 수정**. 유일한 정본 문서 수정은
  `placement_rules_v1.yaml` 의 **가산적 레지스트리 2행**이며 라이브러리 전수 린트로
  기존 35씬 판정 불변을 확인했다(§8-3).

---

## 0. 결론 네 줄

1. **두 씬 다 착지했고 H 밴드 실측 strict-H 는 `sceneH1` 14/16 = 0.875 · `sceneH2`
   12/16 = 0.750 이다** — 계획 §3.2 가 test-ext 에 쓰는 가정 **0.50** 대비 1.75·1.50 배,
   개정 발동선(pro-rata 의 60 % = 0.30)의 2.92·2.50 배. **paired-H 는 14 · 12** 로
   §3.2 하한 12 를 둘 다 넘겼다(A팔 48컷 외삽 26.2 · 22.3).
2. **4팔 게이트 전부 통과** — VG-01(발자국 안 z 이동 **0셀**) · VG-02(발자국 안 C↔D 낙차 **0셀**) ·
   VG-datum(3팔 전부 `datum_exact` 16/16 · max \|Δground_z\| **0.000000 m**) · VG-10(포즈 불일치 **0**) ·
   VG-06(격자 안 낙차 구조물 화면기여 **0 px** · 26프레임 전부) · VG-08(8포즈 → 고유 마스크 8 ·
   `fetch=orch`) · VG-void(4팔 전부 **1.0000**) · VG-07(**정보량 0 쌍 0개**).
3. **계열이 하나 늘었다** — `sceneH2` 는 v3 자기가림 3계열 중 **계단참형의 첫 구현**이고,
   둔덕형·굴절형과 **실효 구속이 다르다**(§3.2). 그 차이가 강제하는 **단서 배치 법칙**을
   이 런이 확정했다(§4) — *"계단참형에서는 낙차 위로 솟은 단서 프림이 strict-H 를
   원리적으로 깨뜨린다"*.
4. **계획에 반영돼야 할 게이트 정의 결함 4건**을 실측으로 확정했다(§9). 넷 다 같은 구조 —
   *"게이트를 **어느 영역**에서 세는가"* 를 §6.1 문면이 말하지 않았다: VG-01·02 는 **발자국 안**,
   VG-06 은 **높이맵 격자 안**, VG-08 은 **포즈 단위**로 세야 하며, 그러지 않으면 **정상 씬이
   실패로 읽힌다**(넷 다 이 런에서 실제로 그렇게 읽혔다).

---

## 1. 산출물

| 파일 | 행 | 무엇 |
|---|---|---|
| `scenes/main/sceneH1_berm_levee.py` | 1,605 | **둔덕형** 제방·수변 씬 (test-ext · base+H) |
| `scenes/main/sceneH2_landing_campus.py` | 1,459 | **계단참형** 캠퍼스 씬 (test-ext · base+H) — **계열 신설** |
| `experiments/v3_0823/code/h67_probe.py` | +25행 | 신설 씬 2개를 AZ 원장 런타임 확장에 추가 (가산) |
| `experiments/v3_0823/code/run_h12_probe.sh` | 175 | 4팔 프로브 러너 (flock·conda·사이드카·스탬프 가드) |
| `experiments/v3_0823/code/h12_gates.py` | 545 | **4팔 전용 게이트** — VG-01/02/06/07 + paired-H |
| `experiments/v3_0823/code/run_h12_label.sh` | 69 | 라벨 3회 + 게이트 2종 재현 스크립트 |
| `experiments/v3_0823/code/h67_yield.py` | +9행 | test-ext 수율 모형(0.50) · sceneH1/H2 PLAN 행 (가산) |
| `Docs/briefs/placement_rules_v1.yaml` | +4행 | C-9 볼라드 캐리어 레지스트리에 sceneH1·H2 추가 |
| `experiments/v3_0823/annotations/h12_{r0,rev}_*.json` | — | 라벨 6세트 — R0·확정 각각 (A,C) · (B,D) · 근거리 |
| `experiments/v3_0823/h12_gates_r0.json` · `h12_gates_rev.json` | — | 4팔 게이트 기계 판독 결과 (R0 · 확정) |
| `experiments/v3_0823/logs/h12_probe.log` | — | 렌더 로그 |

데이터 라운드(코퍼스 아님 — **증거로만 보존**):
`dataset/260823_v3p5_h12smoke_A` (2컷) · `…_h12probe_{A,B,C,D}` (R0 · 16컷씩) ·
**`…_h12rev_{A,B,C,D}` (확정 · 32컷씩)** · `…_h12near_{A,C}` (8컷씩).
**확정 수치는 전부 `h12rev_*` 에서 나온다** — `h12probe_*` 는 개정을 발동시킨 R0 증거다.

---

## 2. sceneH1_berm_levee — 둔덕형 (test-ext)

### 2.1 설계 요지

제방 마루 **관리도로**(z 0.00)가 성토 둔덕을 넘어 호안 상단 **점검로**(z 0.62 · 폭 1.83 m)로
내려서고, 점검로 끝은 **호안 파라펫** — 노출면 2.24 m · 저수로 수면까지 **2.57 m 낙차**다.
은닉체는 **둔덕 마루 `BermCrest`** (z 1.14) — 낙차 개구보다 카메라에 **가깝고 높다**
(계획 §2.1 표의 요구 그대로).

종단면 (walk axis +X · 카메라는 x = −d):

| x | z | 무엇 | 프림 |
|---|---|---|---|
| ≤ −9.60 | 0.000 | 제방 마루 관리도로 (콘크리트 타설) | `Ground/Crown` |
| −9.60 → −4.80 | 0.00 → 1.14 | 성토 둔덕 사면 1:4.21 — **중앙 4.6 m 콘크리트 램프 + 잔디 어깨** | `Ground/BermRise{S,C,N}_00..47` |
| −4.80 → −4.05 | 1.140 | **둔덕 마루** | **`BermCrest/{S,C,N}`** |
| −4.05 → −2.45 | 1.14 → 0.62 | 콘크리트 4단 (R 0.13 · T 0.40) | `Ground/ShoulderStep_0..3` |
| −2.45 → −0.62 | 0.620 | 호안 상단 점검로 | `Ground/InspectWalk` |
| −0.62 → −0.20 | 0.620 | 파라펫 갓돌 — 점검로와 **동일 z** | `RetainWall/Coping` |
| −0.20 → 2.10 | −1.62 / −1.90 | 계단식 콘크리트 호안 2단 | `Revet/T0,T1` |
| ≥ 2.10 | −2.05 | 자갈 여울(하상) | `Bed/Gravel` |
| ≥ 3.40 | −1.95 | 저수로 수면 | `Water/Surface` |
| ≥ 26.0 / 34.0 | 0.70 / 5.20 | 대안 둔치 · **강변 고가도로** — 격자 밖 | `FarBank/*` · `Dress/Viaduct*` |

**낙차 = 0.620 − (−1.950) = 2.570 m** (브리프 "2.5–3.0 m" 준수).

### 2.2 H 판정식 — 1차 런의 교훈을 **설계 입력으로** 받았다

`SCENE_H67_BUILD.md §2.2` 는 *"실효 구속은 **림 조건**이다"* 를 실측으로 확정했다(H6 의 예측이
틀렸던 지점). 이 씬은 그 결론을 **설계 시점부터** 반영해 `_h_max(d)` 의 기본 판정을
`crit="rim"` 으로 두었고, CPU 자기검사가 `|s_rim| < |s_int|` 를 **하드 체크**로 건다.

| 조건 | 부등식 | 임계 |
|---|---|---|
| 낙차 **림**(갓돌 상면 z 0.62 · x −0.20) 은닉 | 0.62 ≤ 1.14 + s·3.85 | s ≥ **−0.1351** |
| 낙차 **내부**(수면 z −1.95 · 격자 동단 x 14.0) 은닉 | −1.95 ≤ 1.14 + s·18.05 | s ≥ **−0.1712** |

⇒ **림이 실효 구속**(|−0.1351| < |−0.1712|) — H6 가 실측으로 발견한 관계가 여기서는
설계상 성립한다.

| d | z(−d) | h_max(림) | H밴드[0.25,1.0] 통과율 |
|---|---|---|---|
| 4.5 | 1.140 | 0.061 | 0.00 |
| 6.0 | 0.855 | 0.548 | 0.40 |
| 7.0 | 0.617 | 0.921 | 0.89 |
| 8.0 | 0.380 | 1.294 | 1.00 |
| ≥ 9.6 | 0.000 | ↑ | 1.00 |

밴드 적분: **H 0.918 · base 0.216** — CPU 자기검사가 매 실행 인쇄한다.
**퇴화 아님**: base 밴드의 78 %는 V/E 로 남는다(근거리 d ≲ 4.8 에서 카메라가 마루 위에
올라서면 낙차가 그대로 보인다).

### 2.3 CUE_CLASS 선언 (렌더 **전** · ACCOUNTING §2-6)

```python
CUE_CLASS = {
    "cue_railing":        "decorative",   # 화강석 **각기둥** 3단 횡대 난간 (H6 는 원형 동자기둥 2단)
    "cue_tactile":        "decorative",   # 갓돌 앞 0.30 m + **마루 계단 상단** 2밴드 (proud 4 mm)
    "cue_nosing":         "decorative",   # 콘크리트 4단 논슬립 띠 (proud 12 mm) — 발자국 밖
    "cue_material_break": "decorative",   # 재질 재바인딩 전용 — 프림 집합 불변
    "cue_sign":           "decorative",   # 관계자외 출입금지 법정 표지 1매
    "cue_scene_dressing": "decorative",   # 갈대 군락·벤치·관목·고가도로·대안 둔치
    "cue_shadow_caster":  "decorative",   # 미루나무 3주 + 가로등 3주
    "cue_manhole":        "decorative",   # 관리도로 배수 맨홀 (flush)
    "cue_tree_grate":     "decorative",   # 관리도로 수목보호격자 (flush)
    "cue_slab_joint":     "decorative",   # 콘크리트 수축줄눈 + **램프 횡단 줄눈 6줄**
    "cue_drainage":       "decorative",   # U형 측구(복개) + 빗물받이 2
    "cue_bollard":        "decorative",   # 제방 진입 볼라드 5본 (피치 1.50 법정치)
    "cue_delineator":     "decorative",   # 반사 시선유도봉 (기본 OFF — 법4)
    "_structural":        ["BermCrest", "RetainWall", "Revet", "Bed", "Water",
                           "Ground/ShoulderStep", "Ground/InspectWalk"],
}
```

**이 선언이 왜 기계적으로 방어되는가**: 모든 `cue_*` 프림은 x ≤ −0.62(보행 가능측)에 서고,
낙차 발자국(x ≥ −0.20)을 덮는 단서 프림이 **0개**다 — 씬 자기검사 (4) 가 매 조립마다
스테이지 전수 AABB 로 확인한다(실측 **0건**). VG-01 의 실효 내용이 바로 이것이다.

### 2.4 val 공급원 `sceneH6` 와의 **부지 분리** (§2.1 각주 · §12-9)

계획 §2.1 은 같은 둔덕형 계열의 test-ext(H1)와 val(H6)이 *"부지·드레싱·재질을 공유하지
않는다"* 를 명령한다. 8축에서 구성상 분리했다 — 씬 파일 상단에 표로 박아 두었다:

| 축 | sceneH6 (val) | **sceneH1 (test-ext)** |
|---|---|---|
| 부지 | 한강 둔치 · 진입 광장 → 관리통로 | **소하천 제방 관리도로** → 호안 점검로 |
| 상부 포장 | 인터로킹 200×100 (`levee_paved`) | **콘크리트 타설 + 수축줄눈** (`alley_concrete`) |
| 마루 → 하부 | 화강석 3단 (R 0.1667 · T 0.50) | **콘크리트 4단** (R 0.13 · T 0.40) |
| 낙차 구조물 | 화강석 갓돌 + **사석 호안** + 수면 | **콘크리트 파라펫 + 계단식 호안 2단 + 자갈 여울** |
| 난간 | 화강석 **원형 동자기둥** 2단 · 피치 1.50 | **화강석 각기둥** 3단 · 피치 1.80 |
| 표지 | 하천구역 안내(`sign_info`) | **관계자외 출입금지**(`sign_no_entry`) |
| 드레싱 | 억새 · 양버들 · 아파트 실루엣 | **갈대 군락 · 미루나무 · 강변 고가도로 + 대안 둔치** |
| 계절 톤 | 여름 (0.42/0.55/0.30) | **초가을** (0.46/0.50/0.27) |

---

## 3. sceneH2_landing_campus — 계단참형 (test-ext) · **계열 신설**

### 3.1 설계 요지

폭 22 m 화강석 판석 **광장**을 지나 **계단참**(`LandingSlab`, 폭 3.63 m, 진회색 판석)에
이르고, 그 아래로 **하행 4단**(R 0.18 · T 0.35) → **중간 계단참**(1.50 m) → **하행 4단** →
**선큰 광장**(z −1.44)이 이어진다. 선큰 광장의 동단은 **강의동 서측면**(x = 11.00)이다.

**낙차 = 1.440 m** (8단 × 0.18). 브리프의 *"계단참 아래 하행 4단"* 은 **첫 플라이트**이고,
중간 계단참이 있어야 "계단참형"이라는 계열 이름이 실체를 갖는다.

### 3.2 왜 이 계열의 **실효 구속이 다른가** — 이 런이 확정한 것

| 계열 | 가림체 | 닫히는 문제 | **실효 구속** |
|---|---|---|---|
| 둔덕형 (H6 val · **H1 test-ext**) | 마루가 림보다 **0.52 m 높다** | 종단면 | **림** |
| 복도 굴절형 (H7 val) | 옹벽 수직 모서리 | 평면 | 평면 각도 (수직은 항상 폐합) |
| **계단참형 (H2 test-ext, 신설)** | 계단참 코가 림과 **같은 높이** | 종단면 | **내부** |

계단참형은 가림체가 낙차 림보다 높지 않다 — "림 위의 여유 높이"가 **0**이다. 그래서
림 조건은 s ≥ −0.5143 로 헐겁고(발자국 근단 립이 1단 상면 −0.18 이라 코에서 0.35 m 만에
0.18 m 를 떨어뜨린다), **내부 조건 s ≥ −0.1244** 가 먼저 깨진다.

⇒ H 조건 : **h ≤ 0.1244 · (d − 0.575)** · 밴드 적분 **H 0.880 · base 0.178**.

**계열마다 실패 모드가 다르다는 것이 §3.5 가 "2계열 이상"을 요구한 이유**이고, 본 씬이
그 세 번째 축을 test-ext 에 세운다.

### 3.3 **셀 중심 정렬** — 이 씬이 도입한 설계 장치

높이맵 격자는 `x0 = −2.00 · step = 0.05` 이므로 **x = 11.00 은 정확히 셀 중심**이다.
강의동 서측면을 그 평면에 두면:

- 선큰 광장의 마지막 발자국 셀은 **10.95**, 셀 11.00 의 수직 레이는 강의동 지붕(+9.50)에 맞는다
- H 프레임에서 보이는 **외벽·기단 픽셀은 셀 11.00 으로 반올림**되어 **발자국 밖**이 되므로
  `int_px` 에 들어가지 않는다 — 깊이 양자화(±0.02 m)가 반올림을 뒤집지 못한다

벽면을 셀 **경계**에 두면 잡음이 반올림을 뒤집어 프레임마다 판정이 흔들린다.
**셀 중심 정렬은 이 씬의 H 판정을 깊이 잡음으로부터 격리하는 장치**이며, CPU 자기검사가
`(11.00 − (−2.00))/0.05 = 260.000` 을 하드 체크로 건다. 계단참 코(−0.575)는 반대로 셀
**경계**에 두어 1단/2단의 셀 귀속을 결정론으로 만든다(`= 28.500` 반정수).

### 3.4 CUE_CLASS 선언

```python
CUE_CLASS = {
    "cue_railing":        "decorative",   # 진입 보행로 **양측** 핸드레일 — 계단 위에는 없다(§4)
    "cue_tactile":        "decorative",   # 계단 상단 점형블록 (계단참 위, proud 4 mm)
    "cue_nosing":         "decorative",   # 계단참 코 **매입 인레이**(proud 2 mm) + 테라스 2단 띠
    "cue_material_break": "decorative",   # 재질 재바인딩 전용 — 프림 집합 불변
    "cue_sign":           "decorative",   # 캠퍼스 시설 안내표지 1매
    "cue_scene_dressing": "decorative",   # 테라스 관목·벤치·가로수·원경 건물동
    "cue_shadow_caster":  "decorative",   # 광장 가로수 3주 + 가로등 3주
    "cue_manhole":        "decorative",   # 광장 맨홀 (flush)
    "cue_tree_grate":     "decorative",   # 광장 수목보호격자 (flush)
    "cue_slab_joint":     "decorative",   # 판석 줄눈 (flush)
    "cue_drainage":       "decorative",   # 광장 빗물받이 (flush)
    "cue_bollard":        "decorative",   # 광장 진입 볼라드 5본
    "cue_planter_edge":   "decorative",   # **FA_REALITY L1 ★1** — 지상 화단 둘레 난간
    "_structural":        ["LandingSlab", "Stair", "MidLanding", "LowerPlaza",
                           "Building", "Plaza/Terrace"],
}
```

**`cue_planter_edge` 를 양성 씬에 심은 이유**는 `cue_shadow_caster` 경보(§2.0)와 같다.
L1 은 *"지상 화단에는 추락방지 난간 법정 의무가 없다"* 는 음성 단서인데, 그것을 음성 씬에만
두면 "난간 = 위험" 대신 "화단 난간 = 안전"이라는 **또 다른 지름길**이 생긴다. 이 씬은
**낙차가 있는 계단**과 **낙차가 없는 화단 난간**을 한 프레임 안에 함께 세워 base rate 독립을
씬 내부에서 강제한다.

### 3.5 브리프의 명시 요구 이행 — *"노징 프림은 자립 데칼로"*

계획 §2.1 표는 H2 에 *"노징 프림은 **자립 데칼**로 지어 모서리 주인이 되지 않게 한다"* 를
명시한다. 두 장치로 이행했다:

- **계단참 코의 논슬립 띠는 매입형 인레이**다 — 융기 **2 mm** · 코에서 **45 mm 안쪽**.
  기울기 2/45 = **0.044** 이하의 시선만 코를 가릴 수 있는데 H 밴드의 최대 |s| 는 **0.145**
  이므로 **구성상 불가능**하다. CPU 자기검사가 이 부등식을 하드 체크로 건다.
- **하행 8단의 계단코 논슬립 띠는 `cue_nosing` 이 아니라 `build_hazard` 가 짓는다.**
  계단이 없으면 존재할 수 없는 부재이므로 계획 §4.4.1 이 정의한 *"낙차 자신의 기하"*
  (F·E 부류)이지 토글 가능한 단서가 아니다. `cue_nosing` 이 토글하는 것은 **팔 불변 지면
  위의 띠**(계단참 코 인레이 + 테라스 진입 2단)뿐이다.

---

## 4. **계단참형이 강제하는 단서 배치 법칙** (본 런이 새로 확정)

둔덕형·굴절형에서는 가림체가 낙차와 **그 위에 선 단서까지 함께** 가린다 — H7 의 벽면
손잡이가 슬롯 안에 있어도 strict-H 가 성립한 이유가 그것이다. **계단참형은 다르다**:
가림체가 가리는 것은 **코 평면 아래**뿐이므로 낙차 위로 **솟은** 프림은 무엇이든 보인다.

그리고 라벨러의 `int_px` 는 *"발자국 셀에 재투영되고 z_off 보다 0.15 m 이상 낮은 픽셀"* 을
센다(`labeler.py:498-503`). 계단 위에 선 손잡이·기둥은 **보이면서 동시에 발자국 셀에 속하는**
픽셀을 만들므로 — 그 프림이 아무리 얇아도 — **strict-H 를 원리적으로 깨뜨린다.**

검산 (설계 시): 중간 손잡이를 y = ±2.40 에 세우고 A팔에서 계단 경사를, C팔에서 수평
(z 0.90)을 따르게 하면, x = 0.5 의 손잡이는 z = −0.36 + 0.90 = **0.54** 이고
`z_off − 0.15 = 0.75` 아래다 ⇒ 그 픽셀이 곧 `int_px` 다. 눈(−6, 0.5)에서 그 점으로 가는
시선은 코(x −0.575)에서 z = 0.534 > 0 이라 **가려지지도 않는다**.

⇒ **법칙**: *계단참형 씬의 모든 `cue_*` 프림은 계단참 코보다 뒤(x ≤ 코) 또는 측방 팔 불변
지면에 서야 한다.* 핸드레일도 예외가 아니다 — 이 씬은 진입 보행로 양측을 따라와
점형블록에서 **0.30 m 수평 연장 후 종단**한다. 그것이 편의증진법 시행규칙 별표1 이 요구하는
바로 그 연장이므로 **규정 배치가 곧 strict-H 안전 배치**다. 계단 위의 중간 손잡이는
**의도적으로 두지 않으며**, 그 사실과 이유를 씬 파일 상단에 남겼다.

> **적용 범위**: 이 법칙은 계획 §2.1 의 **H5(train · 계단참형 변형)** 에도 그대로 적용된다.
> H5 저작 시 이 절을 설계 입력으로 삼을 것.

---

## 5. 프로브 게이트 표 — **확정 라운드 `260823_v3p5_h12rev_{A,B,C,D}`**

라운드 사양: **4팔 × 16컷**(L0 4 + L5 4 … cams 8 × conds 2) · 밴드 H
`{d LogU[6,12] · h U[0.25,1.0]}` · 시드 20260823 · `NEGOBS_SEG_SIDECAR=1
NEGOBS_SEG_STRICT=1` · split `test`.

### 5.1 strict-H 수율 — 계획 §3.2 대조

| 씬 | tier 분포 (A팔) | **strict-H** | 계획 §3.2 가정 0.50 대비 | 개정 발동선 0.30 대비 | **paired-H (A∧B)** | §3.2 하한 |
|---|---|---|---|---|---|---|
| **sceneH1** | H 14 · V 2 | **14/16 = 0.875** | **1.75 배** | **2.92 배** | **14** / 16컷 | ≥ 12 ✅ |
| **sceneH2** | H 12 · V 4 | **12/16 = 0.750** | **1.50 배** | **2.50 배** | **12** / 16컷 | ≥ 12 ✅ |

- **유효 표본은 8포즈**다(16컷 = 8포즈 × 2조명). Wilson 95 % CI: H1 `14/16` → [0.64, 0.97] ·
  H2 `12/16` → [0.51, 0.89]. 두 하한 모두 계획 가정 0.50 **위**다.
- non-H 프레임의 정체는 전부 **설계 모형이 예측한 그대로**다 —
  H1: `d 6.31 · h 0.96` (h_max(6.31) = 0.664 → 초과) · `int_px 220 · edge_vis 0` → V.
  H2: `d 6.78 · h 0.93` (h_max 0.772) · `d 6.60 · h 0.84` (h_max 0.750) · `int_px 2511/1409 ·
  edge_vis 0` → V. 즉 **모형이 틀려서 실패한 프레임은 0개**이고, 넷 다 `h` 가 `h_max` 를
  0.09–0.30 m 초과한 정직한 V 다.
- A팔 48컷 외삽(base 24 × CPU 모형 + H 24 × 실측): H1 = 24·0.216 + 24·0.875 = **26.2** ·
  H2 = 24·0.178 + 24·0.750 = **22.3**. 계획 §3.2 하한 12의 **2.2·1.9 배**.
- **계획 §7.4 톱업 규칙 미발동** — 두 씬 다 pro-rata 기대(8컷 × 0.5 = 8)를 넘겼다.

### 5.2 4팔 게이트

| 게이트 | sceneH1 | sceneH2 | 판정 |
|---|---|---|---|
| **VG-01** A/B 위험-GT 동일성 | `hm_tol_offprint` · 다른 셀 4,144 (max \|Δz\| 1.4216 m) · **발자국 안 0 / 91,164** | `hm_tol_offprint` · 다른 셀 2,994 (max 1.121 m) · **발자국 안 0 / 71,904** | ✅ (§9.1 3층 정의) |
| **VG-02** C/D 전 칸 음성 | **발자국 안 C↔D 0셀** (무제한 1,576 = 장식) · C 잔존위험 0셀 | **발자국 안 0셀** (무제한 92) · C 잔존위험 0셀 | ✅ (§9.2) |
| **VG-datum** A↔B/C/D | `datum_exact` **16/16 × 3팔** · max \|Δground_z\| **0.000000 m** · 쌍 생존율 **1.0000** | 동일 (16/16 × 3팔 · 0.000000 m) | ✅ |
| **VG-10** 포즈 6키 | 불일치 **0** (3팔 × 16컷) | 불일치 **0** | ✅ |
| **VG-08** 세그 사이드카 | 4팔 전부 16/16컷 · **8포즈 → 고유 마스크 8** · `fetch=orch` | 동일 | ✅ (§9.3) |
| **VG-void** | 4팔 전부 **1.0000** (103,041/103,041) | 4팔 전부 **1.0000** | ✅ |
| **VG-06** 모서리 소속 | H 14프레임 · 가림체 px>0 **14/14** · **격자 안 낙차 구조물 px>0 = 0/14** (격자 무제한 2 — 격자 밖 수면, §9.4) | H 12프레임 · 가림체 **12/12** · **격자 안 낙차 0/12** (무제한도 0) | ✅ |
| **VG-13** 과노출 | clip 0.000–0.137 % · `reject` 0 | clip 0.000 % · `reject` 0 | ✅ |
| 발자국 | `cells_raw` 91,164 = `cells_kept` · `max_diff` **2.67 m** | 71,904 = `cells_kept` · `max_diff` **1.44 m** | — |

**VG-06 의 단서 픽셀 범위** (같은 프레임에서 잰 값):
sceneH1 **161,360 – 523,751 px** · sceneH2 **48,930 – 93,260 px**.
sceneH1 의 R0 최소값은 **0** 이었다 — 그 0 이 개정 R3 를 발동시킨 관측이다(§6).

### 5.3 **(A,C) 광학차 분포** (VG-07 · D58 영정보 층화의 입력)

계획 §6.1: *"쌍마다 광학 변화량 기록. **≈0 쌍은 '정보량 0' 으로 층화하고 무벌점**"*.
그레이스케일 `|ΔI|`(0–255) 기준, 쌍 = 같은 파일명의 (A팔, C팔):

| 씬 | n | mean\|ΔI\| min / p50 / mean / max | `frac(\|ΔI\|>8)` min / p50 / max | **정보량 0 쌍** (`frac>8` < 0.001) |
|---|---|---|---|---|
| **sceneH1** | 16 | 1.26 / 2.43 / 3.54 / 8.02 | 0.0102 / 0.0340 / 0.1899 | **0** |
| **sceneH2** | 16 | 2.93 / 7.67 / 9.37 / 20.55 | 0.0259 / 0.1186 / 0.3399 | **0** |

쌍별 `mean|ΔI|` 오름차순 (층화용 원자료):

```
sceneH1  1.26 1.44 1.59 1.62 2.03 2.12 2.29 2.36 2.49 3.00 4.33 4.74 5.55 6.49 7.30 8.02
sceneH2  2.93 3.03 3.43 3.97 4.85 4.90 5.87 7.31 8.03 8.14 11.61 13.42 15.92 16.25 19.63 20.55
```

**읽는 법 세 가지.**

1. **정보량 0 쌍이 없다.** 최소 쌍조차 `frac>8` 이 1 %(H1)·2.6 %(H2)라 화면이 실제로 변한다.
   D58 의 층화는 "무벌점 격리" 장치인데, **두 씬은 격리할 쌍이 0개**다.
2. **두 씬의 분포가 뚜렷이 다르고, 그 차이가 계열 차이와 정확히 대응한다.**
   sceneH2 의 중앙값이 H1 의 **3.2 배**인 이유는 계단참형에서 낙차가 **화면 중앙·근거리**를
   차지하기 때문이다(코 바로 너머부터가 발자국). 둔덕형은 낙차가 마루 뒤 **원거리**에 있어
   같은 은닉 상태에서도 픽셀 변화가 작다. ⇒ **계기판 ①(A,C)의 대조 강도는 계열별로 층화해
   읽어야 한다** — 이것이 이 표가 W3 에 남기는 실무 지침이다.
3. 분포가 **연속**이다(H1 1.26→8.02, H2 2.93→20.55). 이분법(정보 있음/없음)이 아니라
   **연속 층화 변수**로 쓰는 것이 옳다.

### 5.4 근거리 비-H 대조 (`260823_v3p5_h12near_{A,C}` · 밴드 `d 1.5–4.5 · h 0.6–1.8`)

**두 씬 다 V 4/4** — 근거리·고시점에서는 낙차가 그대로 보인다.
즉 두 씬은 **전 프레임 H 인 상수 씬이 아니다**(퇴화 방지 장치가 실렌더로 확인됐다).
CPU 모형의 base 밴드 수율(H1 0.216 · H2 0.178)과 부호가 맞는다.

> `h67_yield.py` 는 H 수율 판정기이므로 이 대조 라운드에 대해 *"미달 — 씬 기하 개정 필요"*
> 를 인쇄한다. **그 문장은 이 라운드에서 무의미하다** — 근거리 밴드는 V 가 나오라고 찍은
> 것이다. 판정은 위 문장(V 4/4 = 설계대로)이며, 스크립트의 verdict 는 무시한다.

---

## 6. 개정 이력 — **프로브가 잡은 것 / 육안이 잡은 것**

라운드 계보: `h12smoke_A`(1컷 육안) → **`h12probe_{A,B,C,D}`(R0 · 4팔 8컷)** → 개정 →
**`h12rev_{A,B,C,D}`(확정 · 4팔 16컷)** + `h12near_{A,C}`(근거리 비-H 대조).
확정 라운드에서 컷을 8 → 16 으로 올린 이유는 §9.3 이 밝힌 것과 같다 — **유효 표본은 컷 수가
아니라 포즈 수**이고, 조건 2종이 포즈를 공유하므로 8컷 = **4포즈**였다. 16컷 = **8포즈**.

### sceneH1 — 개정 3건 (기하 개정 **0회**)

| # | 무엇이 잡혔나 | 어떻게 잡혔나 | 조치 | 기하 영향 |
|---|---|---|---|---|
| **R1** | `Looks/GranitePost` 가 look 층에서 **metal**(alb_max 0.50)로 분류돼 화강석 각기둥이 **흰 금속 기둥**으로 렌더 | 1차 스모크 육안 + `sc._look_spec` 대조 | `Looks/GraniteColumn` (**stone**, alb_max 0.34, ground MDL) | 0 |
| **R2** | 원경 상부구조가 x ≈ 17.5(카메라 23 m · **수면 한복판**)에 서서 화면 상단 12 %를 밝은 콘크리트 띠로 채움. 게다가 **하천을 가로지르지 않고 하천과 나란히 선 판**이었다 | 1차 스모크 육안 + 종단면 재검토 | 어휘를 **강변 고가도로**로 바꾸고 대안 뒤(x ≥ 34)로 이설 · 알베도 0.30 → 0.26 | 0 (격자 밖) |
| **R3** | **H 프레임의 정보량이 0** — 카메라가 평지에 서면 눈높이 h ≤ 1.0 < 마루 1.14 이라 마루 능선이 화각 위로 밀리고, 피치 ≤ −15° 에서는 능선조차 밖으로 나간다. strict-H 는 성립하는데 **화면이 잔디뿐**이다 `[R0 실측: VG-06 의 단서 px **최소 0**]` | R0 프로브 A팔 육안 + `h12_gates` 의 `cue_px` 분포 | 둔덕을 **3밴드**로: 잔디 어깨 + **콘크리트 관리 램프**(중앙 4.6 m) + 잔디 어깨. 램프에 **횡단 줄눈 6줄**(`cue_slab_joint`) · 마루에 **계단 상단 점형블록**(`cue_tactile` 2밴드) | **0** — 마루 x·z, 점검로 z, 림 x 불변. CPU 수율 모형 개정 전후 **H 0.918 · base 0.216 동일**, 그리고 A팔 `heightmap.npy` 해시가 **R0 와 바이트 동일**(`807d5bba2a3557ef`) |

> **R3 이 기하 개정이 아닌 이유의 기계 증거**: 램프(x −9.6…−4.8)와 마루 점형블록(x −4.55…−4.15)은
> **전부 높이맵 격자 서단(x = −2.00) 밖**이다. 그래서 A팔 높이맵이 R0 와 **바이트 동일**하고
> 발자국도 `cells_raw 91,164 · max_diff 2.67 m` 로 완전히 같다. 계획 §7.4 의 톱업 규칙
> (수율 미달 시 씬당 상한 2회)은 **미발동** — R0 수율이 이미 게이트를 넘겼다
> (`h12probe_A` 실측 **6/8 = 0.750**, 확정 라운드 8포즈에서 **14/16 = 0.875**).
> **R3 는 수율이 아니라 정보량 때문에 한 개정**이며, 그 구분을 여기 명시한다.

### sceneH2 — 개정 1건 (**기하 개정 1회** · 상한 2회 중 1회 사용)

| # | 무엇이 잡혔나 | 어떻게 잡혔나 | 조치 |
|---|---|---|---|
| **R1** | **strict-H 0/8** — 설계 모형은 전 포즈 H 를 예측했는데 실측은 전부 V(`int_px` 73–7999) | R0 프로브 + ID 마스크 프림별 집계(`Building/Plinth` **168,669 px**) | 아래 진단 참조 |

**진단 (이 씬이 남기는 두 번째 설계 법칙)**: 강의동 **석재 기단**을 x ∈ [11.00, 11.24] ·
상면 **−0.34**(광장 레벨 **아래**)에 두었더니, 셀 11.00 의 수직 레이가 A팔에서는 기단 상면
(−0.34)을 맞고 C팔에서는 채움면(0.00, SEAM 으로 x 11.004 까지)을 맞았다. ⇒ diff 0.34 ≥
hazard_depth ⇒ **셀 11.00 이 발자국으로 뒤집혔고**, 그 셀에 재투영되는 **기단 서측면 픽셀이
곧 `int_px`** 가 되었다. 낙차 자체(`Stair`·`MidLanding`·`LowerPlaza`)는 **전 프레임 0 px** —
즉 은닉은 완벽했는데 **낙차가 아닌 구조물이 낙차 셀을 빌려** strict-H 를 깨뜨린 것이다.

⇒ **조치**: 기단 상면을 **광장 레벨 위(+1.10)** 로 올리고 벽돌 몸체를 11.30 으로 민다. 이제
셀 11.00…11.25 는 **양팔 모두 기단 상면 +1.10** 을 읽으므로 diff = 0 이고 발자국이 아니다.
선큰 광장에서 올라서는 석재 포디엄은 실제 건축의 표준 구성이기도 하다.

> **법칙 2**: *낙차의 종단 평면에 접하는 구조물의 **상면은 반사실 채움면보다 높아야 한다**.*
> 낮으면 그 셀이 발자국으로 뒤집히고, 그 구조물의 수직면이 `int_px` 로 계상된다.
> CPU 자기검사가 이 부등식(`plinth_top > fill.z`)을 하드 체크로 건다.

---

## 7. §2.0 공통 표준 적합성 체크리스트 (예외 없음)

| # | 표준 (계획 §2.0) | sceneH1 | sceneH2 | 근거 |
|---|---|---|---|---|
| 1 | **생활권 유형** 4종 중 하나 | ✅ 제방·수변 | ✅ 캠퍼스 경로 | 파일 상단 선언 |
| 2 | **씬 조립 공식** ①경로 ②위험 ③토글 단서 ④은닉 | ✅ | ✅ | 파일 상단 4요소 표 |
| 3 | *"주변 환경은 장식이 아니라 단서 ③ 그 자체"* | ✅ 갈대·맨홀·측구·격자·볼라드·표지·램프 줄눈 | ✅ 판석 줄눈·맨홀·핸드레일·화단 난간·그림자·안내표지 | `[cue]` 조립 로그 |
| 4 | **법1** 손상·노후 표현 금지 | ✅ `surface=None` 로 잡초·균열·오염 데칼 OFF | ✅ 동상 | `plan_ground(overrides=surface=None)` |
| 5 | **법2** 임의 경고판 금지 | ✅ 법정 제식 1매(`sign_no_entry`) | ✅ 법정 제식 1매(`sign_info`) | `build_sign` 호출 1회 |
| 6 | **법4** 기능 필수성 · 비움이 기본값 | ✅ 조형물 0 · `cue_delineator` 기본 OFF | ✅ 조형물 0 · **계단 위 중간 손잡이 의도적 미설치**(§4) | SCENE_CONFIG |
| 7 | **법5** 차량·계절소품 금지 | ✅ 0 | ✅ 0 | 프림 인벤토리 |
| 8 | **법6** 재질 동결 (신규 역할 0) | ✅ 기존 TEX 역할만 | ✅ 동상 | `ASSET_ROLES` |
| 9 | **법7** `cue_*` 기하 불변 | ✅ 최대 융기 12 mm(나이징) · **전부 발자국 밖** | ✅ 최대 융기 12 mm(테라스 띠) · **전부 발자국 밖** | 자기검사 (4) |
| 10 | **법8** 근거 태그 | ✅ `[규격]`/`[계획]`/`[computed]`/`[측정]` | ✅ | 파일 전역 |
| 11 | **조명/HDRI** L0–L7 · 과노출 금지 | ✅ clip 0.000–0.137 % · reject 0 | ✅ clip 0.000 % · reject 0 | `img` 레코드 |
| 12 | **재질 3단** (지면·구조물 / 식생·목재 / 금속·도색·수면) | ✅ | ✅ | `setup_materials` |
| 13 | **props 규율** 볼라드 h 0.8–1.0 · φ0.1–0.2 · 피치 1.5 · 반사띠 | ✅ h 0.90 · φ0.15 · 피치 1.50 | ✅ h 0.92 · φ0.156 · 피치 1.50 | LINT-6 통과(ERROR 0) |
| 14 | **props** 격자·등간격 금지 | ⚠️ **의도적 정렬** — §8-4 | ⚠️ 동상 | LINT-10 |
| 15 | **props** 순백(>0.8) 대면적 금지 | ✅ 개정 R2 후 원경 재배치 | ⚠️ 판석 광장 sunlit — §10 R7 항목 | 픽셀 측정 |
| 16 | **표준 장비 16키** · 사문 0 | ✅ 16키 전부 프림/재질 생성 | ✅ 16키 | 자기검사 |
| 17 | **v3 신설 3키** 배선 | ✅ shadow_caster·manhole·tree_grate 전부 ON | ✅ 전부 ON | SCENE_CONFIG |
| 18 | **프림 위생** — 단서는 자립 프림, `hazard_*` 분기 밖 | ✅ 조립부 hazard 분기 2줄, 단서 0줄 | ✅ 동상 | `grep 'cfg\["hazard_stairs"\]'` |
| 19 | **카메라 데이텀** — 스트립 위 토글 프림 금지 | ✅ `datum_fail` 0 (선언 6 · AABB 전수 재확인) | ✅ `datum_fail` 0 | VG-datum |
| 20 | **void 커버리지 1.0** (개방 바닥 금지) | ✅ 103,041/103,041 | ✅ 103,041/103,041 | `heightmap_meta` |
| 21 | **커밋 전 검증 4종** | ✅ 4/4 green | ✅ 4/4 green | §8-1 |
| 22 | **룩체크 R7 사용자 게이트** | ⬜ **미실시 — 사용자 검수 대기** | ⬜ 동상 | §10 |

---

## 8. 커밋 전 검증 · 린트 · 레지스트리

### 8-1. 커밋 전 검증 4종 (`process_spec_v1.md:32-40`)

| 게이트 | 결과 |
|---|---|
| `py_compile` | **PASS** (2/2) |
| `NEGOBS_SMOKE=1` 씬 실행 (CPU 자기검사) | **PASS** — 두 씬 전항목 통과 (수율·낙차·데이텀·16키·CUE_CLASS 완전성) |
| `geom_invariance_check.py` | **PASS** — R-5 잔존참조 0 · R-4/R-6 해시 3자 일치 (sceneH1 370프림 · sceneH2 204프림) |
| `placement_lint.py` | **PASS** (exit 0) — **ERROR 0** · WARN 2 · BLOCKED 2 |

### 8-2. `placement_lint` 가 실제로 잡은 것 (설계 중 수정 2건)

| # | 규칙 | 무엇 | 조치 |
|---|---|---|---|
| L1 | **LINT-1** (PE-1 · 산림청 고시 2-3(6)(가)1) | sceneH2 가로수(y −4.60)가 연석선(y −5.20)에서 **0.60 m** — 요구 1.00 m | 볼라드 열(=연석선)을 y **−6.10** 으로 이설 → 이격 **1.50 m** |
| L2 | **LINT-5** (PE-8 · 도로의 구조·시설 기준 §16) | sceneH2 안내표지가 핸드레일 선에서 free **−0.733 m** | §8-5 참조 — `walk_edges` 미선언으로 전환 |

### 8-3. `placement_rules_v1.yaml` C-9 레지스트리 추가 (**정본 문서 수정 1건**)

LINT-6 의 `carrier_set` 은 볼라드를 가진 씬의 **레지스트리**다 — 목록에 없는 씬이 볼라드를
조립하면 ERROR, 목록에 있는데 조립하지 않아도 ERROR. 신설 2씬을 추가했다(+4행, 주석 포함).
**가산적 변경**이며 D73 ② 가 sceneH6·H7 에 쓴 것과 같은 형식이다.

**라이브러리 전수 회귀 확인** — `placement_lint.py --scenes all` (**37씬**) 실측.
개정 **전후 두 번** 돌렸고 두 번 다 같은 값이다:

| | 이 런 (37씬) | SCENE_H67_BUILD §5-4 기록 (35씬) | 델타 |
|---|---|---|---|
| ERROR | **16** | 16 | **0** |
| WARN | 348 | 346 | +2 (신설 2씬의 LINT-5 `nodata`) |
| BLOCKED | 33 | 31 | +2 (신설 2씬의 LINT-9 todo 게이트) |

ERROR 16건의 씬별 분해도 **한 칸도 움직이지 않았다** — `scene02` 4 · `scene06` 6 ·
`scene12` 1 · `sceneC2` 4 · `sceneN5` 1. **sceneH1·sceneH2 의 ERROR 기여는 0건**이다.

### 8-4. LINT-10 「배치 지터」 판정 — 정렬을 택했다 (H6/H7 판단 승계)

`user_feedback_v5_1 §2` 는 *"격자·등간격 금지, yaw ±3–8° 지터"* 를 요구하고, 07-30 룰링
**J-3/J-5(배치 지터 폐지)** 는 그 반대를 요구하며 `placement_lint` LINT-7/LINT-10 이 후자를
강제한다. 두 씬은 **후자를 따랐다** — 볼라드 피치 1.50 m 는 교통약자법 시행규칙 별표2
제7호의 **법정 수치**이므로 규칙성이 결함이 아니라 요건이고, 벤치·표지 yaw 는 **축값만**
쓴다. 불규칙성은 대신 **재질·그림자·지면 문양**이 담당한다.

### 8-5. LINT-5 `walk_edges` 미선언 (WARN 2건 — 의도)

LINT-5 는 「도로의 구조·시설 기준에 관한 규칙」 제16조의 **보도 유효폭**을 재는 규칙이다.

- **sceneH1** 의 상부 경로는 보도가 아니라 제방 **관리도로**(차량 통행 가능·보도 구분 없음).
- **sceneH2** 의 접근부는 보도가 아니라 폭 22 m **광장**이고, 핸드레일 두 줄은 유효폭 경계가
  아니라 **보행 동선 유도**다. 설계 시 핸드레일 선을 `walk_edges` 로 선언해 보았더니 LINT-5 가
  안내표지를 ERROR 로 잡았고, 규칙이 요구하는 이격(1.5 + 표2.2 점유 0.90 = **2.40 m**)을
  만족하는 |y| ≥ 4.8 은 H 밴드(d 6–12 · hfov 62°) **화각 밖**이라 단서가 화면에 잡히지 않는다
  `[측정]`. 즉 그 데이텀을 선언하면 규칙이 **단서를 화면 밖으로 밀어내는** 결과를 낳는다.

없는 데이텀을 지어내면 그 규칙은 검사가 아니라 허구가 되므로 **선언하지 않고 `nodata` 로
남긴다** — 규칙 파일 자신의 원칙(*"A rule whose datum is undeclared does not silently pass"*)이
그렇게 하라고 적혀 있고, SCENE_H67_BUILD §5-5 가 sceneH6 에 내린 것과 같은 판단이다.
`kerb_lines`·`anchors`·`routes` 는 실재하므로 선언했고 LINT-1/2/3/7 은 **live 로 통과**한다.

---

### 8-6. **4팔 준비 판정** (W3 test-ext 본렌더 착수 가부)

| 항목 | 요구 (계획) | sceneH1 | sceneH2 |
|---|---|---|---|
| 4팔 완비 (§3.1) | A·B·C·D 전부 렌더 가능 | ✅ 16컷 × 4팔 실렌더 | ✅ 16컷 × 4팔 |
| C팔 = `keep_dressing` 계열 (§1.2) | 위험 OFF · 단서·장식 유지 | ✅ 신규 씬이라 이식이 아니라 **씬 내부 배선**(모듈 상수 + 모순 FATAL 2종) | ✅ 동일 |
| D팔 = 전 단서 OFF | `hazard=False` + 13키 OFF | ✅ | ✅ |
| B팔 레버 = 최대 제거 (§1.2) | VG-CLS 가 허락한 전 키 | ✅ 13키 전부 OFF 실렌더 · VG-01 통과 | ✅ 13키 |
| paired-H ≥ 12 (§3.2) | A·B 둘 다 strict-H | **14** | **12** |
| VG 게이트 전항 (§6.1) | — | ✅ | ✅ |
| 룩체크 R7 | 사용자 게이트 | ⬜ 대기 | ⬜ 대기 |

⇒ **판정: 두 씬 모두 W3(test-ext) 본렌더 웨이브에 투입 가능**하다 — 단 **R7 사용자 룩체크가
선행 조건**이며(§10 항목 1·3), 본 보고는 그 게이트 앞에서 멈춘다.
W3 이 이 두 씬에 요구하는 것은 계획 §3.2 표 그대로다: **밴드 base + H · 팔당 48프레임 ·
4팔 192프레임/씬**. 본 프로브는 그중 H 밴드만 8포즈로 찍었으므로 W3 에서
**base 밴드 24컷 × 4팔 × 2씬**이 추가로 필요하다.

---

## 9. 부수 발견 — **VG 게이트 정의 결함 4건** (계획 반영 필요)

1차 런이 `NEGOBS_SEG_STRICT` 결함 1건을 올렸다면(SCENE_H67_BUILD §7), 이번 런은 **4팔을
처음 갖춘 라운드**여서 A/B·C/D 축의 게이트 정의를 처음으로 실측에 걸었다. 셋 다
**게이트가 틀렸지 씬이 틀린 것이 아니다** — 어느 것도 씬 개정으로 이어지지 않았고,
셋 다 `h12_gates.py` 안에 수정된 판정으로 구현되어 있다.

### 9.1 VG-01 「A/B heightmap **바이트 동일**」은 실행 불가능한 상한이다

계획 §6.1 은 VG-01 을 *"팔 A·B 의 `heightmap.npy` + `heightmap_meta.json` **바이트 동일**"*
로 적었다. 그러나 높이맵은 **수직 레이의 최초 히트**이므로, 격자(x ∈ [−2,14] · |y| ≤ 8) 안에
서는 단서 프림이 하나라도 있으면 B팔에서 그 셀의 z 가 지면으로 되돌아간다.

**실측** `[260823_v3p5_h12probe · sceneH1]`: A↔B 는 **4,144셀 · max |Δz| 1.421583 m** 로 다르다.
범인은 mm 급 지면 문양이 아니라 **갈대 군락**(`Dress/Reed_*`, 높이 1.35 m, x −2.42…−1.70)이다.
그러나 그 셀은 **전부 낙차 발자국 밖**이고, 발자국(91,164셀) 안에서 A↔B 가 다른 셀은 **0개**다.

⇒ **제안**: VG-01 을 VG-datum 과 같은 **3층**으로 바꾼다.
`hm_exact`(바이트 동일) / `hm_tol_offprint`(다른 셀이 전부 발자국 밖 → `polar_gt` 불변) /
`hm_fail`(발자국 **안**에서 z 가 움직임 → 그 키는 구조물). 결정층은 **발자국 안**이며,
발자국은 A↔C 트윈이 정의한다. 문면 그대로의 바이트 동일은 **재질 재바인딩 전용 레버**
(`cue_material_break`)에서만 성립하는 특수해다 — 계획 §4.2 가 이미 그렇게 적어 두었으므로
(*"재질 재바인딩 전용인 24씬에서는 … 높이맵 비트 동일이 구성상 보증"*) 이 3층화는 계획의
다른 문장과 오히려 **정합적**이다.

### 9.2 VG-02 의 C↔D 비교는 **장식에 오염된다**

VG-02 를 "C·D 두 팔 사이에 낙차가 없을 것"으로 순진하게 구현하면, C팔은 `keep_dressing=True`
(장식 유지)이고 D팔은 전 단서 제거이므로 **갈대·가로수가 통째로 C↔D 차이로 잡힌다**.
**실측**: 무제한 비교 **1,576셀**, 발자국 안 비교 **0셀**.

⇒ **제안**: C↔D 대조는 **낙차 발자국 영역으로 한정**한다(두 팔 모두 그 영역이 반사실
채움면이므로 같아야 한다). 잔존 위험 검사는 **A↔C**(둘 다 장식을 가진 팔)로 하면 장식에
오염되지 않는다. 두 판정 모두 실측 **0셀**로 통과했다.

### 9.3 VG-08 의 「컷별 마스크가 다른가」는 **다조건 라운드에서 정상을 stale 로 오판한다**

SCENE_H67_BUILD §7.6 권고 4 는 VG-08 에 *"컷별 마스크가 실제로 다른가"* 를 추가하라고 했다.
그것을 **컷 수** 기준으로 구현하면 오판한다 — 이 라운드는 조건 2종(L0·L5)을 **같은 시드·같은
카메라 인덱스**로 찍으므로 포즈가 조건 간 동일하고, **ID 마스크는 조명에 의존하지 않는다**.
**실측**: 8컷 · **4포즈** · 고유 마스크 해시 **4개** — 정상인데 컷 수 기준으로는 실패로 읽힌다.

⇒ **제안**: 기준을 **서로 다른 카메라 포즈 수**로 바꾼다
(`n_distinct_hashes == n_distinct_poses`). `fetch` 는 전 컷 `orch` 로, `NEGOBS_SEG_STRICT=1`
정본 경로가 SCENE_H67_BUILD §7 의 stale 결함을 실제로 막고 있음을 이 런이 재확인했다.

### 9.4 VG-06 은 **높이맵 격자 안**에서 세야 한다

계획 §6.1 의 VG-06 은 *"가시 지면 종단 모서리가 가림체 프림 소속이고 **낙차 림·내부 기여
0 px**"* 이다. "낙차 림·내부"는 **라벨이 다루는 영역**(높이맵 격자 x ∈ [−2, 14] · |y| ≤ 8)
안의 낙차인데, 낙차 **프림**은 격자 밖까지 이어진다 — sceneH1 의 수면은 x = 26 까지 간다.

**실측** `[260823_v3p5_h12rev_A · sceneH1]`: strict-H 14프레임 중 **2프레임**이 ID 마스크에서
`/Water/` 픽셀을 갖는다. 그런데 그 픽셀의 세계좌표는 **x ≳ 19** — 격자 동단 14.0 밖이다.
림 조건이 실효 구속인 프레임에서는 마루를 스치는 시선이 격자 밖에서 수면 아래로 내려가므로
**격자 밖 수면이 보이는 것이 정상**이고, `int_px` 는 발자국 셀만 세므로 strict-H 는 깨지지
않는다. ID 마스크를 전수 집계하면 이 정상 프레임이 "낙차 구조물 가시"로 오판된다.

⇒ **제안**: VG-06 의 낙차 픽셀 집계를 **깊이 역투영으로 격자 안에 한정**한다(구현:
`h12_gates._grid_mask`). 이 정정 후 두 씬의 26개 H 프레임 전부 **격자 안 낙차 0 px** 이다.
겸사로 **가림체 접두어는 지형체 단위**로 잡아야 한다 — sceneH1 은 `BermCrest` 만 세면 피치가
깊은 프레임에서 마루 능선이 화각 밖으로 밀려 0 px 이 되고, 실제로 시선을 막는 사면
(`Ground/BermRise*`)이 빠진다.

> **네 결함의 공통 구조**: 넷 다 *"게이트를 **어느 영역**에서 세는가"* 의 문제다.
> VG-01·02 는 **발자국 안**에서, VG-06 은 **격자 안**에서, VG-08 은 **포즈 단위**로 세야 한다.
> 계획 §6.1 의 문면은 영역을 말하지 않았고, 그래서 넷 다 정상 씬을 실패로 읽었다.

---

## 10. 남은 것 (사용자 게이트 / 후속 WP)

| # | 항목 | 성격 |
|---|---|---|
| 1 | **룩체크 R7 사용자 검수** — `make_review_gallery.py --round 260823_v3p5_h12rev_A` 후 체크리스트 5항 | **사용자 게이트** (process_spec §27-31) |
| 2 | **§6.6 현실 대조 정성 패널** — *"패널 없이 H 주장을 서술하지 않는다"*. 좌 = 실측 프로토콜 H 규정 컷 4장이 필요한데 **저장소에 없다**(`Docs/reference_photos` 는 생성 이미지). **승용 촬영분 대기** | **사용자 입력 대기** |
| 3 | sceneH2 판석 광장의 **sunlit 휘도** — `frac(max-ch > 0.8)` 이 최대 0.38. `regression_check` BLOWN 임계(mean > 210 · clip > 0.2 %)는 **전 컷 통과**(clip 0.000 %)이고 look 층이 알베도를 0.34 로 캡하지만, v5.1 §4 "순백 대면적"의 취지에서는 R7 검수 항목이다. 낮추려면 `plaza_tint` 0.74 → 0.66 한 줄 | **R7 판단 대기** |
| 3b | sceneH1 **램프 횡단 줄눈 폭 60 mm** — 킷의 `groove_w` 10 mm 보다 6배 넓다. 넓힌 이유는 순전히 **가시성**이다(10 mm 는 d 5 m 스침각에서 3–4 px 라 R3 가 회복하려는 평행 밴드가 픽셀로 남지 않는다). 실제 급경사 콘크리트 포장의 **가로 홈파기**가 이 치수대에 있지만, 좁히라는 판단이 나오면 `ramp_joint_w` 한 값만 고치면 되고 **기하·수율은 영향받지 않는다** | **R7 판단 대기** |
| 4 | §9 의 게이트 정의 결함 4건 — 계획 §6.1 VG-01/02/06/08 문면 갱신 | **계획 문서 수정 (결재 대상 아님 — 게이트 정의 정정)** |
| 5 | `variation_kit` AZ 원장 정식 등재 + `split_map` 핀 테이블 — 이 런도 런타임 패치로 우회했다(H67 §6.1 과 동일) | W2/W3 본렌더 전 필수 |
| 6 | B팔 레버 확정 — 두 씬의 13개 `cue_*` 는 전부 **발자국 밖**이라 VG-01 3층에서 `hm_tol_offprint` 로 통과한다. W0 VG-CLS 가 이 결과를 그대로 받아 쓸 수 있다 | W0 후속 |
| 7 | base·H 밴드 **실렌더** — 본 프로브는 H 밴드 + 근거리 대조만 찍었다. base 밴드 수율은 아직 **CPU 모형값**이다 | W3 |
| 8 | `sceneH1` 의 `cue_delineator` 는 기본 OFF — 어블레이션 팔에서만 켜진다 | 설계 의도 |

---

## 11. 부록 — 재현 명령

```bash
# CPU 자기검사 (pxr·GPU 불필요)
NEGOBS_SMOKE=1 python3 scenes/main/sceneH1_berm_levee.py
NEGOBS_SMOKE=1 python3 scenes/main/sceneH2_landing_campus.py

# 커밋 전 검증 4종
python3 -m py_compile scenes/main/sceneH{1,2}_*.py
python3 scripts/geom_invariance_check.py --scenes sceneH1,sceneH2
python3 scripts/placement_lint.py       --scenes sceneH1,sceneH2
python3 scripts/placement_lint.py       --scenes all      # 라이브러리 회귀

# GPU — 스모크 → 4팔 프로브 → 근거리 대조 (tmux textbuild · flock 직렬화)
bash experiments/v3_0823/code/run_h12_probe.sh smoke
CAMS=8 bash experiments/v3_0823/code/run_h12_probe.sh rev     # 확정 라운드 (8포즈)
bash experiments/v3_0823/code/run_h12_probe.sh near

# 라벨 3회 + 게이트 2종
bash experiments/v3_0823/code/run_h12_label.sh 260823_v3p5_h12rev 260823_v3p5_h12near rev
```


---
---

# 12. `sceneH3_bend_walk` · `sceneL1_lateral_canal` 저작 보고 (**test-ext · 3차 빌더 런**)

> **append-only 구획.** §0–§11 은 2차 빌더 런(sceneH1·H2)의 기록이며 한 글자도 고치지
> 않았다. 이 §12 이하가 3차 런의 기록이다.

- **작성**: Claude Code · 2026-08-23 · **과업**: RENDER_PLAN_V3 **WP-5 (Wave A)** 중
  §8 결재 1 **A안 순서의 다음 두 씬**
- **왜 이 두 씬인가**: A안의 씬 목록은 *"H1·H2·H3(test-ext paired-H 3계열) · H6·H7(val
  strict-H 공급) · L1(측방) · N9·N11(N-cue ★3/★4)"* 다(§8 결재 1 표). 1차 런이 **H6·H7**,
  2차 런이 **H1·H2** 를 착지시켰으므로 목록에서 남은 앞의 둘은 **H3(복도 굴절형 test-ext)**
  과 **L1(측방 test-ext)** 이다. N9·N11 은 그 다음이다.
- **지위**: `sceneH3` 은 계기판 ①②③ 무대의 **세 번째 자기가림 계열**을 완성한다
  (둔덕형 H1 · 계단참형 H2 · **복도 굴절형 H3**). `sceneL1` 은 성격이 다르다 —
  *"현 test 에 측방 씬이 0개라 섹터 분해능(5→10)·밴드 세분을 '실용 정보량' 기준으로
  재론할 측정 기반이 원리적으로 없다"* `[승용 결재 1 룰링 · 계획 §2.3]` 를 푸는
  **측정 기반**이고, 은닉 씬이 아니다.
- **선행**: `SCENE_H67_BUILD.md`(1차) · 본 문서 §0–§11(2차). 두 보고의 확정 교훈을 전부
  설계 입력으로 받았다 — 림 조건 · 컷별 ID 마스크(`NEGOBS_SEG_STRICT=1`) · **§9 의
  "게이트를 어느 영역에서 세는가"** · **§6 법칙 2**(낙차 인접 구조물 상면 > 채움면).
- **GPU 사용**: 스모크 4컷(2회) + R0 프로브 4팔 **128컷** + 확정 프로브 4팔 **128컷**
  (sceneH3·L1 각각 64) + 근거리 **16컷** = **렌더 276컷 · 보존 274컷** ≈ **0.45 GPU-h**
  (실측 wall 74분, 부팅 24회 및 락 대기 포함). 전부 `flock -o /tmp/negobs_gpu.lock`
  직렬화 — 수리 웨이브 **w1b2** 와 한 락을 나눠 썼고, 라운드를 4–16컷으로 잘게 끊어
  상대 웨이브의 24컷 라운드 사이에 끼어들었다.
- **정본 무수정 원칙**: 기존 씬 파일 **0바이트 수정**. kit·드라이버 **0바이트 수정**.
  유일한 정본 문서 수정은 `placement_rules_v1.yaml` 의 **가산적 레지스트리 3행**이며
  라이브러리 전수 린트로 기존 37씬 판정 불변을 확인했다(§12-8).

---

## 12-0. 결론 네 줄

1. **두 씬 다 착지했고 4팔 게이트 전항 통과다.** `sceneH3` 은 H 밴드 strict-H
   **12/16 = 0.750** · **paired-H 12** 로 계획 §3.2 하한 12 를 정확히 채웠고
   **가시(V/E) 컷 0** — 굴절 은닉은 화각에 들어온 12프레임에서 예외 없이 성립했다.
   `sceneL1` 은 설계대로 **전 프레임 V**(16/16)이고 `none_in_fov` **0**.
2. **`sceneL1` 의 산출물인 섹터 분포가 나왔다** — 셀 점유율 **A 0.300 · E 0.367
   ⇒ 측방 A+E 0.667** vs 중간 B+D 0.300 vs **정면 C 0.033**, 그리고 **밴드 1·2 는
   완전히 비어 있다**(전 질량이 3a·3b). 이것이 §2.3 이 요구한 "섹터 분해능 재론의
   측정 기반"이고, CPU MC 예측(A+E 0.664 · C 0.036)과 소수점 둘째 자리까지 맞았다.
3. **계기 결함 3건을 새로 확정했다**(§12-9). ⓐ VG-01 의 결정층은 높이맵 셀이 아니라
   **`polar_gt`** 여야 한다 ⓑ VG-02 의 C↔D 비교는 **부호를 가려야** 한다 ⓒ 라벨러
   **D10 스텝 게이트가 축방향 측방 위험을 못 본다**(닫힌 임계식 `d ≤ −x₀ + √3·y_lip`
   를 실측으로 확인). 셋 다 정상 씬을 실패로 읽었고, ⓐⓑ 는 `h12_gates.py` 에서 고쳤으며
   ⓒ 는 씬 쪽 장치(횡단 복개)로 우회하고 계획에 올린다.
4. **기하 개정 3회** — sceneH3 2회(상한 소진), sceneL1 1회. 셋 다 **수율 미달이 아니라
   광량·계기 도달성** 때문이었고, sceneH3 의 R2 는 GT 를 **한 칸도** 바꾸지 않았음을
   프레임 단위로 증명했다(tier 16/16 동일 · `polar_gt` 16/16 동일 · 발자국 동일).

---

## 12-1. 산출물

| 파일 | 행 | 무엇 |
|---|---|---|
| `scenes/main/sceneH3_bend_walk.py` | 1,528 | **복도 굴절형** 석축 골목 씬 (test-ext · base+H) |
| `scenes/main/sceneL1_lateral_canal.py` | 1,616 | **측방** 제방 마루 양측 개거 씬 (test-ext · base+LAT) — **계열 신설** |
| `experiments/v3_0823/code/h67_probe.py` | +21행 | 신설 2씬 AZ 원장 런타임 확장 + 스탬프 허용목록 (가산) |
| `experiments/v3_0823/code/run_h3l1_probe.sh` | 179 | 4팔 프로브 러너 — **씬별 밴드**(H / LAT) 분기 |
| `experiments/v3_0823/code/run_h3l1_label.sh` | 81 | 라벨 3회 + 게이트 2종 재현 스크립트 |
| `experiments/v3_0823/code/h12_gates.py` | 545 → **736** | PRIMS 2씬 추가 · **VG-01 4층화** · **VG-02 부호 분해** · **`gate_sectors`** 신설 |
| `experiments/v3_0823/code/h67_yield.py` | +8행 | sceneH3·L1 PLAN 행 (가산) |
| `Docs/briefs/placement_rules_v1.yaml` | +3행 | C-9 볼라드 캐리어 레지스트리에 sceneH3·L1 추가 |
| `experiments/v3_0823/annotations/h3l1_{r0h3,r0l1,revh3,revl1,rev_near}_*.json` | — | 라벨 9세트 |
| `experiments/v3_0823/h3l1_gates_{r0h3,r0l1,revh3,revl1,near}.json` | — | 4팔 게이트 기계 판독 결과 |
| `experiments/v3_0823/logs/h3l1_probe.log` | — | 렌더 로그 |

데이터 라운드(코퍼스 아님 — **증거로만 보존**):
`dataset/260823_v3p5_h3l1smoke_A`(2컷) · `…_h3l1probe_{A,B,C,D}`(R0 · 씬당 16컷) ·
**`…_h3l1rev_{A,B,C,D}`(확정 · 씬당 16컷)** · `…_h3l1near_{A,C}`(씬당 4컷).
**확정 수치는 전부 `h3l1rev_*` 에서 나온다** — `h3l1probe_*` 는 개정을 발동시킨 R0 증거다.

---

## 12-2. sceneH3_bend_walk — 복도 굴절형 (test-ext)

### 12-2.1 설계 요지

유효폭 **3.60 m** 골목 보도가 **석축 옹벽**(북·마루 2.45 m)과 **붉은 벽돌 담장**
(남·마루 1.95 m) 사이를 지나다가 `(−0.85, +1.40)` 에서 **북쪽으로 직각 굴절**하고,
굴절 너머에 **하행 10단 × 0.18 = 1.80 m 계단** → 하천 둔치 통로가 있다.
은닉체는 **`BendWall`** — `Along`(x ≤ −0.85 · y 1.40…2.12)과 `Flank`(x −1.57…−0.85 ·
y ≥ 1.40)가 만나는 **수직 모서리**다. 옹벽 4매·성토 3매는 전부 `hazard_*` **밖**이므로
B팔에서도 불변이다(브리프 명시 요구).

### 12-2.2 H7 과의 **부지 분리** — 그리고 **부호가 반대인 실패 모드**

| 축 | sceneH7 (val) | **sceneH3 (test-ext)** |
|---|---|---|
| 부지 | 성토 절토부 콘크리트 옹벽 회랑 → **지하보도** | **구릉 주거지 석축 골목** → **하천 둔치 진입 계단** |
| **굴절 방향** | 남향(−y) | **북향(+y)** |
| 유효폭 | 3.40 m | **3.60 m** |
| 가림체 재질 | 콘크리트 옹벽 | **자연석 석축**(`rock_wall`) |
| 반대편 벽 | 콘크리트 옹벽 2.45 | **붉은 벽돌 담장 1.95**(비대칭) |
| 포장 | 인터로킹 200×100 | **콘크리트 타설 + 수축줄눈** |
| 계단참 재질전이 | 화강석 판석 | **판석 포장**(`stone_flag`) |
| 계단 | 12단 × R 0.17 · T 0.32 = 2.04 m | **10단 × R 0.18 · T 0.30 = 1.80 m** |
| 종단 단차 | 2단 × 0.17 | **3단 × 0.10** |
| 표지 | 지하보도 유도 | **하천 둔치 안내** |
| 16번째 키 | `cue_level_handrail`(L7) | **`cue_convex_mirror`**(L12) — §12-4 |
| 계절 톤 | 여름 | **늦봄** |

**굴절 방향이 반대라는 것은 장식이 아니다.** 평면 은닉 판정식
`t_y = (y_c − y_face)/(y_c − ty) ≤ t_x = (x_corner + d)/(tx + d)` 에서 y_face 의 부호가
뒤집히면 **카메라 y 오프셋의 유불리가 통째로 반전**한다:

| y_c | sceneH7 은닉 최소 d | **sceneH3 은닉 최소 d** |
|---|---|---|
| −0.90 | **2.63** (가장 잘 가려짐) | **5.46** (가장 안 가려짐) |
| 0.00 | 4.46 | 3.93 |
| +0.90 | **6.30** (가장 안 가려짐) | **2.39** (가장 잘 가려짐) |

같은 계열의 val 과 test-ext 가 **거울상 실패 모드**를 갖는다 — §2.1 의 "다른 부지"
요구가 실질적으로 무엇을 뜻하는지의 한 사례다.

### 12-2.3 CUE_CLASS 선언 (렌더 **전** · ACCOUNTING §2-6)

```python
CUE_CLASS = {
    "cue_railing":          "decorative",  # 벽면 계단 손잡이 2줄 (A팔 경사 / C팔 수평)
    "cue_convex_mirror":    "decorative",  # **FA_REALITY L12 ★5** — 굴절부 도로반사경
    "cue_tactile":          "decorative",  # 계단 상단 점형블록 (계단참 위, proud 4 mm)
    "cue_nosing":           "decorative",  # 보도 종단 3단(팔 불변) + 계단 10단
    "cue_material_break":   "decorative",  # 재질 재바인딩 전용 — 프림 집합 불변
    "cue_sign":             "decorative",  # 하천 둔치 안내표지 1매 (WallE 벽면)
    "cue_scene_dressing":   "decorative",  # 성토 관목·벤치 2·원경 주택동
    "cue_shadow_caster":    "decorative",  # **남측** 성토 상단 가로수 3 + 가로등 2
    "cue_manhole":          "decorative",  # 골목 맨홀 (flush)
    "cue_tree_grate":       "decorative",  # 기본 OFF — 석축 골목엔 가로수를 심지 않는다(법4)
    "cue_slab_joint":       "decorative",  # 콘크리트 수축줄눈 (recess 3 mm)
    "cue_drainage":         "decorative",  # 횡배수 트렌치(복개) + 빗물받이
    "cue_bollard":          "decorative",  # 골목 진입 볼라드 2본
    "_structural":          ["BendWall", "WallS", "WallE", "Bank",
                             "Walk/Landing", "Walk/Step"],
}
```

---

## 12-3. sceneL1_lateral_canal — 측방 (test-ext) · **계열 신설**

### 12-3.1 설계 요지 · **왜 양측인가**

제방 진입 광장(z −0.32) → 2단 오름 → **제방 마루 산책로**(유효폭 3.60 m · z 0.000)이고,
그 **양 측방**에 콘크리트 **개거 수로**가 나란히 달린다 —
남측 = 농업용 **도수로**(수심 0.12 · 화강석 갓돌) · 북측 = 제방 **배수 구거**(자갈 바닥 ·
콘크리트 갓돌). **낙차 = 0.000 − (−1.500) = 1.500 m**(양측 동일 · 브리프 §2.3 준수),
개구 폭 1.92 m, 산책로와 개거 사이에 **잔디 녹지대 1.50 m**(브리프의 "풀 경계").

브리프 문면은 수로를 **단수**로 적지만 같은 표의 **섹터 목표는 "A·E"** 다. 한쪽만 파면
질량이 한 섹터에만 실려 좌우 대칭 측정이 불가능하고, yaw 지터가 그 한 섹터를 격자 밖으로
밀 때 표본이 통째로 사라진다. **제방 마루는 원래 양쪽이 다 사면·수로**이므로 양측 개거는
부지의 실제 구성이며, 그렇게 지어야 A·E 가 동시에 채워진다.
**실측이 그 판단을 확인한다** — 확정 라운드 16프레임에서 **E 양성률 1.000 · A 0.875**
이고 yaw 부호에 따라 어느 쪽이 우세한지가 바뀐다(§12-3.3 프레임 표).

### 12-3.2 **섹터 설계** — 닫힌 식과 실측

`gridspec_v1` 경계 `tan 18.66° = 0.3378 · tan 31.10° = 0.6036`, u ≡ x + d 로 두면

| 섹터 | 조건 | u 범위 (기하) | 반경 < 12 로 잘린 뒤 | 폭 |
|---|---|---|---|---|
| **A·E** | \|y\|/u ∈ [0.338, 0.604] | [5.87, 16.16] | **[5.87, 10.9]** | **5.0 m** |
| B·D | \|y\|/u ∈ [0.109, 0.338] | [10.48, 50.1] | [10.48, 11.5] | 1.0 m |
| **C** | \|y\|/u ≤ 0.109 | **u ≥ 32.5** | **∅**(반경 32.7 > 12) | **0** |

⇒ **C 는 씬 기하로 영구히 비어 있다** — *"경로 정면은 무해"* 가 기하 항등식이다.
CPU 자기검사가 yaw≡0 MC 로 그것을 확인하고(**C 셀 점유율 0.0000**), yaw 지터를 켠 판을
나란히 인쇄한다(**C 0.036**) — 즉 **C 에 실리는 몫은 씬이 아니라 카메라 자세의 기여**다.

### 12-3.3 **섹터 분포 실측** — 이 씬의 산출물 (`h3l1rev_A` · LAT 밴드 16컷 = 8포즈)

| 층 | A | B | C | D | E | 합 |
|---|---|---|---|---|---|---|
| **프레임 양성률** | **0.875** | 0.500 | 0.125 | 0.500 | **1.000** | — |
| **셀 점유율** | **0.300** | 0.133 | **0.033** | 0.167 | **0.367** | 1.000 |
| CPU MC 예측 | 0.329 | 0.148 | 0.036 | 0.153 | 0.335 | — |

⇒ **측방 A+E 0.667** vs 중간 B+D 0.300 vs 정면 C 0.033. 예측과 실측이 **소수점 둘째
자리까지** 일치한다(A+E 0.664 → 0.667 · C 0.036 → 0.033).

**밴드 × 섹터 (양성 프레임 수 / 16)** — 밴드 세분 재론의 입력:

| 밴드 | 거리 | A | B | C | D | E |
|---|---|---|---|---|---|---|
| 1 | [0, 2) m | 0 | 0 | 0 | 0 | 0 |
| 2 | [2, 5) m | 0 | 0 | 0 | 0 | 0 |
| **3a** | [5, 8) m | 4 | 0 | 0 | 2 | 6 |
| **3b** | [8, 12) m | **14** | 8 | 2 | 8 | **16** |

**밴드 1·2 가 완전히 빈 것은 우연이 아니다.** 개거 근단이 \|y\| = 3.54 이고 측방 창이
u ≥ 5.87 이므로 셀까지의 반경은 항상 √(5.87² + 3.54²) = **6.85 m 이상**이다 —
**측방 위험은 원리적으로 근거리 밴드에 실릴 수 없다.** V1 이 V0 의 밴드 3 을 8 m 에서
3a/3b 로 쪼갠 것은 이 씬에서 **유일하게 작동하는 세분**이고, 반대로 밴드 1·2 의 세분은
측방 씬에 대해 정보량 0 이다. `[측정 260823_v3p5_h3l1rev_A]`

**프레임별 (yaw 가 섹터를 정한다)**:

| 컷 | d | h | yaw | 양성 섹터 | 셀 |
|---|---|---|---|---|---|
| 0000 | 7.15 | 1.38 | **−19.0** | **C D E** | 5 |
| 0001 | 7.70 | 0.74 | −4.8 | A D E | 4 |
| 0002 | 7.66 | 1.22 | **+11.5** | **A B E** | 4 |
| 0003 | 4.99 | 0.53 | −6.2 | A D E | 4 |
| 0004 | 8.50 | 0.66 | −9.1 | A D E | 3 |
| 0005 | 8.69 | 0.97 | +6.9 | A B E | 3 |
| 0006 | 7.32 | 1.11 | +3.4 | A B E | 4 |
| 0007 | 8.67 | 1.07 | +6.4 | A B E | 3 |

yaw < 0 이면 북측 개거가 +31.1° 밖으로 밀려 남측이 D·E 로 내려앉고, yaw > 0 이면 남측이
−31.1° 밖으로 밀려 북측이 A·B 로 올라온다. **측방 GT 의 섹터 귀속은 위험의 위치가 아니라
카메라 회전이 결정한다** — 섹터 분해능 재론이 반드시 층화해야 할 교란이다.

### 12-3.4 **개거 가시성** — 가림체가 없는데도 낙차 내부는 안 보인다

눈 (−d, y_c, h) → 개거 안쪽 점의 시선은 **근단 갓돌**(\|y\| = 3.54 · z 0)을 반드시 지나므로
`z_t ≥ −0.543·h` 인 점만 보인다(x 와 무관):

| h | 0.4 | 1.0 | 1.4 | 바닥(−1.50)이 보이려면 |
|---|---|---|---|---|
| 보이는 깊이 | **0.22 m** | 0.54 m | **0.76 m** | h ≥ **2.77 m** (어느 밴드에도 없다) |

⇒ 낙차 1.50 m 중 **14–51 % 만 화면에 있고 나머지는 폐영역**이다. 가림체 없이도 낙차가
저역 어두운 슬롯으로 축약되는 이 현상이 **음성 장애물의 정의 그 자체**이고, 그래서
이 씬은 "은닉 씬이 아니지만 쉬운 씬도 아니다". CPU 자기검사가 매 실행 인쇄한다.

### 12-3.5 CUE_CLASS 선언 (13키)

```python
CUE_CLASS = {
    "cue_railing":  "decorative",  # 양측 스테인리스 각기둥 난간 (가드선 1.10 m)
    "cue_tactile":  "decorative",  # 볼라드 전면 0.30 m 밴드 + 2단 상단 밴드
    "cue_nosing":   "decorative",  # 2단 오름 논슬립 띠 (팔 불변 지형)
    "cue_material_break": "decorative",   # 재질 재바인딩 전용 — 프림 집합 불변
    "cue_sign":     "decorative",  # 수로 관리 통로 출입 제한 법정 표지 1매
    "cue_scene_dressing": "decorative",   # 갈대·밭 경계 관목·벤치 2·배수펌프장·마을 원경
    "cue_shadow_caster":  "decorative",   # 광장 가로수 3 + **녹지대 가로등주 3**
    "cue_manhole":  "decorative",  # 광장 배수 맨홀 (flush)
    "cue_tree_grate": "decorative",# 광장 수목보호격자 3 (flush)
    "cue_slab_joint": "decorative",# 수축줄눈 (recess 3 mm)
    "cue_drainage": "decorative",  # 빗물받이 광장 2 + **산책로 2**(격자 안 · flush)
    "cue_bollard":  "decorative",  # 제방 진입 볼라드 4본 (피치 1.50 법정치)
    "cue_delineator": "decorative",# 반사 시선유도봉 (기본 OFF — 법4)
    "_structural":  ["CanalS", "CanalN", "FarBankS", "FarBankN",
                     "Ground/Walk", "Ground/Verge"],
}
```

**이 선언이 왜 기계적으로 방어되는가**: 모든 `cue_*` 프림은 \|y\| ≤ 3.22 또는 \|y\| ≥ 9.20 에
서고, 낙차 발자국(\|y\| ∈ [3.54, 5.46])을 덮는 단서 프림이 **0개**다 — 씬 자기검사 (4) 가
스테이지 전수 AABB 로 확인한다(실측 **0건**). 실측 VG-01 이 그 결과다: 발자국 안 A↔B
다른 셀 **0 / 21,138** (`hm_tol_offprint`).

> **이 씬이 새로 확정한 배치 법칙**: *측방 위험 씬에서는 **수관을 낙차 위에 두지 않는다.***
> 높이맵은 수직 레이의 최초 히트이므로 개거를 덮은 수관은 그 셀의 z 를 수관 top 으로
> 만들어 **A·C 양팔 모두에서 발자국을 조용히 지운다**(게이트가 잡지 못하는 GT 손실이다).
> 그래서 가로수는 전부 진입 광장(x ≤ −8.4 · 격자 밖)에 서고, 녹지대에는 수관 없는
> **가로등주**(φ0.144)만 세운다. `sceneL2_lateral_ditch` 저작 시 이 절을 설계 입력으로 삼을 것.

---

## 12-4. `cue_convex_mirror` — 굴절부에 심은 **낙차 무관 단서** (H3)

`cue_shadow_caster` 부작용 경보(계획 §2.0)와 `cue_planter_edge`(§3.4, sceneH2)의 논리를
**세 번째 키에** 적용했다. 볼록거울은 「도로교통법 시행규칙」이 **시거 불량 구간**에
세우게 하는 시설이고 **낙차와 아무 상관이 없다**(FA_REALITY **L12 ★5**: "볼록거울 =
시거 부족"). 그것을 음성 씬(N12)에만 두면 *"볼록거울 = 안전"* 이라는 **역지름길**이 생긴다.

이 씬은 **낙차가 있는 굴절부**에 같은 부재를 세워 base rate 독립을 씬 내부에서 강제한다 —
굴절부에 거울이 서는 이유는 낙차가 아니라 **시거**이기 때문이다. 위치는 굴절 **바깥쪽**
벽(남측 담장 · x +0.95 · z 2.10 · φ0.68)이며, 안쪽(석축) 면에 달면 굴절을 돌기 전에는
보이지 않아 시설의 목적을 잃는다(실제 시공도 바깥쪽 벽이다).

⇒ **N12(★5 약단서 중첩 골목) 저작 시 이 씬이 그 키의 양성 짝**이 된다.

---

## 12-5. 프로브 게이트 표 — **확정 라운드 `260823_v3p5_h3l1rev_{A,B,C,D}`**

라운드 사양: **4팔 × 16컷**(L0 8 + L5 8 = **8포즈**) · 시드 20260823 ·
`NEGOBS_SEG_SIDECAR=1 NEGOBS_SEG_STRICT=1` · split `test` ·
밴드는 **씬마다 다르다** — sceneH3 `H {d LogU[6,12] · h U[0.25,1.0]}` ·
sceneL1 `LAT {d LogU[3,10] · h U[0.4,1.4]}` (계획 §2.1 / §2.3).

### 12-5.1 tier · 수율

| 씬 | 밴드 | tier 분포 (A팔) | strict-H | paired-H (A∧B) | §3.2 하한 | 판정 |
|---|---|---|---|---|---|---|
| **sceneH3** | H | **H 12 · none_in_fov 4 · 가시 0** | **12/16 = 0.750** | **12** | ≥ 12 | ✅ |
| **sceneL1** | LAT | **V 16 · none_in_fov 0** | 0/16 | — | — (측방) | ✅ (설계대로) |

- sceneH3 의 non-H 4컷(= **2포즈**)은 전부 `none_in_fov`(`int_px 0 · edge_projected 0`),
  즉 발자국이 폴라 격자 **밖**으로 나간 프레임이지 낙차가 보인 프레임이 아니다.
  **가시(V/E) 컷은 0** — H7 실측(가시 0)과 같은 계열 거동이다.
- Wilson 95 % CI: `12/16` → [0.50, 0.90] (유효 표본 8포즈 기준 하한 0.50 이 §3.2 가정과 동률).
- A팔 48컷 외삽(base 24 × CPU 모형 0.417 + H 24 × 실측 0.750) = **28.0** — §3.2 하한 12 의
  **2.3 배**. 프로브 16컷이 하한과 동률인 것은 표본 크기 탓이고 씬 능력의 상한이 아니다.
- **sceneL1 에 대한 `h67_yield.py` 의 수율 판정문은 무의미하다** — 측방 씬은 은닉 씬이
  아니고 §3.2 표의 paired-H 열이 "—" 다(§5.4 근거리 라운드와 같은 상황). 판정층은 섹터 게이트다.

### 12-5.2 4팔 게이트

| 게이트 | sceneH3 | sceneL1 | 판정 |
|---|---|---|---|
| **VG-01** A/B 위험-GT 동일성 | **`hm_gt_equal`** · 발자국 안 다른 셀 611 / 5,122 (max \|Δz\| 1.259 m) · **`polar_gt` 16/16 프레임 비트 동일** | **`hm_tol_offprint`** · 발자국 안 **0 / 21,138** · `polar_gt` 16/16 동일 | ✅ (§12-9.1 4층화) |
| **VG-02** C/D 전 칸 음성 | **C가 D보다 낮은 셀 0** (C가 높은 셀 126 = 보존된 손잡이) · C 잔존위험 0 | **낮은 셀 0** (높은 셀 0 · 무제한 888) · 잔존위험 0 | ✅ (§12-9.2 부호 분해) |
| **VG-datum** A↔B/C/D | `datum_exact` **16/16 × 3팔** · max \|Δgz\| **0.000000 m** · 생존율 1.0000 | A↔C **16/16 exact**; A↔B·A↔D `exact 10 · tol 6 · fail 0` · max \|Δgz\| **0.004 m** · 생존율 **1.0000** | ✅ |
| **VG-10** 포즈 6키 | 불일치 **0** (3팔 × 16컷) | 불일치 **0** | ✅ |
| **VG-08** 세그 사이드카 | 4팔 전부 16/16컷 · **8포즈 → 고유 마스크 8** · `fetch=orch` | 동일 | ✅ |
| **VG-void** | 4팔 전부 **1.0000** (103,041/103,041) | 4팔 전부 **1.0000** | ✅ |
| **VG-06** 모서리 소속 | H 12프레임 · **가림체 px>0 12/12** · **격자 안 낙차 구조물 px>0 = 0/12**(무제한도 0) · 단서 px 39,800–170,081 · 맥락 px 최대 817,592 | **측정만** — 가림체 0 · 낙차 px 16/16 (설계대로 보인다) · 단서 px 240,033–746,675 | ✅ / 측정 |
| **섹터 분포** (측방 전용) | — | **A+E 0.667 > B+D 0.300** · A 0.875 · E 1.000 · `none_in_fov` **0.000** | ✅ |
| **VG-13** 과노출 | clip **0.000 %** 전 컷 | clip **0.000 %** 전 컷 | ✅ |
| 발자국 | `cells_raw` 5,122 · `cells_kept` 5,120–5,121 · `max_diff` **2.363 m** | `cells_raw` 21,138 · `cells_kept` 20,202–21,138 · `max_diff` **1.500 m** | — |

**두 `max_diff` 의 차이가 §12-9.1 의 씨앗이다.** sceneL1 은 **1.500 m = 설계 낙차 그대로**
(단서가 발자국 밖이라 오염이 없다). sceneH3 은 **2.363 m** 로 진짜 낙차 1.800 m 를
0.563 m 초과하는데, 그 초과분은 전부 **벽면 계단 손잡이**다 — A팔에서 계단 경사를,
C팔에서 수평(z 0.90)을 따르므로 손잡이가 덮는 셀에서 `z_off − z_on` 이 부풀려진다
(H7 이 §3.3 에서 2.623 vs 2.04 로 기록한 것과 같은 현상 · 사전 선언분).

### 12-5.3 **(A,C) 광학차 분포** (VG-07 · D58 영정보 층화의 입력)

| 씬 | n | mean\|ΔI\| min / p50 / mean / max | `frac(\|ΔI\|>8)` min / p50 / max | **정보량 0 쌍** |
|---|---|---|---|---|
| **sceneH3** | 16 | **0.65 / 1.01 / 1.12 / 1.92** | 0.00019 / 0.00429 / 0.00938 | **3** |
| **sceneL1** | 16 | 1.55 / 1.96 / 2.33 / 3.72 | 0.0174 / 0.0391 / 0.0664 | **0** |

쌍별 `mean|ΔI|` 오름차순:

```
sceneH3  0.65 0.76 0.76 0.77 0.87 0.89 0.91 0.97 1.05 1.11 1.14 1.31 1.54 1.60 1.71 1.92
sceneL1  1.55 1.60 1.65 1.75 1.75 1.81 1.91 1.94 1.98 2.41 2.51 2.70 2.98 3.40 3.57 3.72
```

**읽는 법 세 가지.**

1. **sceneH3 은 지금까지 저작된 네 씬 중 (A,C) 대조가 가장 약하다** — p50 1.01 은
   sceneH1(2.43)의 0.42 배, sceneH2(7.67)의 **0.13 배**다. 이것은 결함이 아니라
   **완전한 은닉의 논리적 귀결**이다: 낙차가 화면에 한 픽셀도 없으면 그것을 끄고 켜도
   화면은 변할 이유가 없다. 남는 차이는 손잡이의 경사/수평 차이와 채움면의 간접광뿐이다.
   ⇒ **D58 의 "정보량 0 층화"가 실제로 쓰이는 첫 씬**이고, 3쌍이 `frac>8 < 0.001` 층에 든다.
2. **계열 순서가 은닉 강도의 순서와 정확히 반대다** — 계단참형(H2, 낙차가 화면 중앙 근거리)
   7.67 > 둔덕형(H1, 낙차가 마루 뒤 원거리) 2.43 > **굴절형(H3, 낙차가 평면 뒤 완전 폐색)
   1.01**. 계기판 ①의 (A,C) 대조 강도는 **계열별로 층화해 읽어야 한다**는 §5.3 의 지침이
   세 번째 계열로 확인됐다.
3. **sceneL1 은 정보량 0 쌍이 0개**이고 분포가 좁다(1.55–3.72). 측방 위험은 늘 보이므로
   위험을 끄면 늘 같은 정도로 화면이 변한다 — **분산이 작은 것 자체가 측방 씬의 특징**이다.

### 12-5.4 근거리 대조 (`260823_v3p5_h3l1near_{A,C}` · 밴드 `d 1.5–4.5 · h 0.6–1.8`)

| 씬 | tier | 발자국 | 읽는 법 |
|---|---|---|---|
| **sceneH3** | `none_in_fov` **4/4** | `cells_raw 5,122 · kept 5,121` | 근거리에서 계단은 **±31.1° 폴라 격자 밖**이다. 발자국은 살아 있다(GT 손실 아님). |
| **sceneL1** | **V 4/4** | `cells_raw 21,138 · kept 21,138` | 근거리에서 개거가 그대로 보인다. **`cells_kept = cells_raw`** — 스텝 게이트 손실 0(§12-9.3 조치의 확인). |

> **sceneH3 의 퇴화 여부는 이 라운드로 판정되지 않는다.** 이 씬은 은닉이 풀리는 거리
> (y_c = 0 에서 d < 3.93)와 발자국이 폴라 격자에 들어오는 거리(d ≳ 6.5)가 **겹치지 않아**
> 가시 프레임의 창이 좁다. CPU MC 는 base 밴드에서 **격자 안 프레임의 19.2 %가 가시**
> (무조건 9.9 % · `none_in_fov` 48.4 %)라고 예측하며, **확정은 W3 의 base 밴드 실렌더 몫**
> 이다. 확실한 것은 두 가지다 — ⓐ H 밴드에서 화각에 들어온 12프레임 전부 H(가시 0)이고
> ⓑ `none_in_fov` 는 H 가 아니므로 **라벨이 상수가 아니다**(고유 `polar_gt` 4종).

---

## 12-6. 개정 이력 — 프로브가 잡은 것 / 육안이 잡은 것

라운드 계보: `h3l1smoke_A`(1컷 육안 · **2회**) → **`h3l1probe_{A,B,C,D}`(R0 · 4팔 8포즈)**
→ 개정 → **`h3l1rev_{A,B,C,D}`(확정 · 4팔 8포즈)** + `h3l1near_{A,C}`.

### sceneH3 — 개정 2건 (**기하 개정 2회 · 상한 소진**)

| # | 무엇이 잡혔나 | 어떻게 잡혔나 | 조치 | GT 영향 |
|---|---|---|---|---|
| **R1** | **회랑이 캄캄하다** — 폭 2.80 m 에 마루 2.55 m 벽 두 매면 태양 고도 49.79° 에서 그림자 길이 2.15 m 라 **어느 방위에서도 바닥의 77 %가 그늘**이다. 게다가 `SUN_AZ_OFFSET=16.0` 이 태양을 **북동쪽**에 세워 ⓐ 은닉체(북측 석축)와 막다른 벽을 통째로 그늘에 넣고 ⓑ 남측 가로수 그림자를 보도 **반대쪽**으로 던졌다 — 설계 의도의 정반대다 `[1차 스모크 실측: mean 24.7 · dark(<16) **73.1 %**]` | 1차 스모크 육안 + 픽셀 통계 | 유효폭 2.80 → **3.60 m** · 마루 2.55 → 2.45 · `z_up` 0.45 → 0.30(수직 폐합 유지) · 계단 머리 y 2.60 → **3.20**(넓힘의 대가를 계단참 깊이로 갚는다) · `SUN_AZ_OFFSET` 16 → **192**(광선을 (+x,+y)로 돌려 석축·막다른 벽에 볕을 준다) · `rock_tint` 0.44 → 0.50 | 기하 변경(개정 전 렌더 없음 — R0 프로브가 이미 R1 기하다) |
| **R2** | R1 후에도 **H 밴드 16컷의 `dark(<16)` 중앙값이 45.1 %**(최악 77.6 %). 범인은 남측 담장(2.45 m)의 그림자 — 바닥 그림자 경계가 y = −0.34 라 3.60 m 중 1.46 m 가 늘 그늘이고, 낮은 시점(h 0.25–1.0)에서 그 근거리 그늘이 화면 대부분을 차지한다 | R0 프로브 A팔 16컷 픽셀 통계 | 벽 마루를 **비대칭**으로: 은닉체(석축·Flank·WallE) 2.45 **유지**, **남측 담장만 1.95**(성토 남측 1.95 → 1.55). 그림자 경계가 y = −0.64 로 물러나 볕이 **68 %** 로 는다. `brick_tint` 0.50 → 0.60 · `rock_tint` 0.50 → 0.54 | **0** — 아래 증명 |

> **R2 가 GT 를 한 칸도 바꾸지 않았다는 기계 증거** `[h3l1probe_A vs h3l1rev_A · 16프레임]`:
> **tier 16/16 동일** · **`polar_gt` 16/16 비트 동일** · 발자국 `(cells_raw 5,122 ·
> cells_kept 5,120–5,121 · max_diff 2.363)` **완전 동일** · VG-01 발자국 안 다른 셀
> **611 동일** · VG-02 **126 동일**. 높이맵 **해시는 달라진다**(남측 성토·담장이 격자
> 안 y ≤ −2.52 에 있으므로) — 그러나 그 셀은 전부 **발자국 밖**이다.
> 즉 R2 는 **광량만 바꾼 개정**이고, 그 사실이 프레임 단위로 증명돼 있다.
> 실측 개선: `dark(<16)` 중앙값 **45.1 % → 33.3 %** · mean 중앙값 37.8 → **44.3**.
> 남은 어두움은 §12-10 의 R7 항목이다.

**§7.4 톱업 규칙 미발동** — 두 개정 다 수율 미달이 아니다(R0 수율이 이미 `12/16` 로
하한을 충족했고 확정 라운드도 같다).

### sceneL1 — 개정 2건 (**기하 개정 1회**)

| # | 무엇이 잡혔나 | 어떻게 잡혔나 | 조치 | 기하 영향 |
|---|---|---|---|---|
| **R1** | ⓐ 갈대·관목 띠가 개거 **바로 뒤에 붙은 초록 벽**으로 읽혀 개거가 낙차가 아니라 화단 경계처럼 보였고 두 수로의 좌우 차이가 초록에 묻혔다 ⓑ 난간이 12 cm 각기둥 + 알베도 0.70 이라 **흰 기둥 열**이 화면 최대 밝기였다 ⓒ 원경 콘크리트 매스가 상단의 큰 흰 판이었다 | 1차 스모크 육안 + 픽셀 통계 | 갈대·관목 \|y\| [6.20, 8.60] → **[9.20, 13.0]**(높이맵 격자 밖) · `post_a` 0.12 → **0.075** · `rail_color` 0.70 → 0.58 · 원경 축소·후퇴 · **남·북 갓돌 재질 분리**(화강석 / 콘크리트) | **0** (전부 격자 밖 또는 재질) |
| **R2** | **라벨러 D10 스텝 게이트가 개거를 못 본다** — R0 8포즈 중 **2포즈에서 `cells_kept = 0`**(tier `none_in_fov`, GT 전무), 1포즈에서 12,519(한쪽만). 원인은 §12-9.3 | R0 프로브 라벨의 `cells_kept` 분포 | **횡단 농로 복개 2개소**(x −1.40 · 7.60 · 폭 1.20 m)를 개거 위에 둔다. 복개 동쪽 가장자리가 스텝 게이트의 근접 경계를 **d 와 무관하게** 공급한다 | 발자국 25,038 → **21,138셀**(복개분 3,900 제거) · 낙차·섹터 분포는 불변 |

> **R2 의 효과 실측**: 확정 라운드 `none_in_fov` **0.25 → 0.000** · `cells_kept` 0 이던
> 두 포즈(d 8.50 · 8.69)가 각각 **A D E / A B E** 로 살아났다. 근거리 라운드에서도
> `cells_kept = cells_raw = 21,138`(손실 0).

---

## 12-7. §2.0 공통 표준 적합성 체크리스트 (예외 없음)

| # | 표준 (계획 §2.0) | sceneH3 | sceneL1 | 근거 |
|---|---|---|---|---|
| 1 | **생활권 유형** 4종 중 하나 | ✅ 보도 | ✅ 제방·수변 | 파일 상단 선언 |
| 2 | **씬 조립 공식** ①경로 ②위험 ③토글 단서 ④은닉 | ✅ | ✅ (④는 **없음을 선언** — 측방 씬) | 파일 상단 4요소 표 |
| 3 | *"주변 환경은 장식이 아니라 단서 ③ 그 자체"* | ✅ 석축·담장·볼록거울·손잡이·맨홀·줄눈·볼라드·표지·가로수 그림자 | ✅ 난간·풀 경계·점형블록·볼라드·표지·갈대·가로등 그림자·복개 | `[cue]` 조립 로그 |
| 4 | **법1** 손상·노후 표현 금지 | ✅ `surface=None` | ✅ 동상 | `plan_ground(overrides=surface=None)` |
| 5 | **법2** 임의 경고판 금지 | ✅ 법정 제식 1매(`sign_info`) | ✅ 법정 제식 1매(`sign_no_entry`) | `build_sign` 호출 1회 |
| 6 | **법4** 기능 필수성 · 비움이 기본값 | ✅ 조형물 0 · `cue_tree_grate` 기본 OFF | ✅ 조형물 0 · `cue_delineator` 기본 OFF | SCENE_CONFIG |
| 7 | **법5** 차량·계절소품 금지 | ✅ 0 | ✅ 0 | 프림 인벤토리 |
| 8 | **법6** 재질 동결 (신규 역할 0) | ✅ 기존 TEX 역할만 | ✅ 동상 | `ASSET_ROLES` |
| 9 | **법7** `cue_*` 기하 불변 | ✅ 최대 융기 12 mm(나이징) | ✅ 동상 · **전부 발자국 밖** | 자기검사 (4) |
| 10 | **법8** 근거 태그 | ✅ | ✅ | 파일 전역 |
| 11 | **조명/HDRI** L0–L7 · 과노출 금지 | ✅ clip **0.000 %** 전 컷 | ✅ clip **0.000 %** 전 컷 | 픽셀 측정 |
| 12 | **재질 3단** | ✅ | ✅ | `setup_materials` |
| 13 | **props 규율** 볼라드 h 0.8–1.0 · φ0.1–0.2 · 반사띠 | ✅ h 0.85 · φ0.144 (2본 · 개구 2.40 — §12-8-4) | ✅ h 0.90 · φ0.15 · **피치 1.50 법정치** | LINT-6 |
| 14 | **props** 격자·등간격 금지 | ⚠️ **의도적 정렬** (J-3/J-5 승계) | ⚠️ 동상 | LINT-10 |
| 15 | **props** 순백(>0.8) 대면적 금지 | ✅ `frac(max>0.8)` 중앙값 **0.006** | ⚠️ 중앙값 **0.107**(하늘 포함) — §12-10 | 픽셀 측정 |
| 16 | **표준 장비 16키** · 사문 0 | ✅ 16키 | ✅ 16키 | 자기검사 |
| 17 | **v3 신설 3키** 배선 | ✅ shadow_caster·manhole ON · tree_grate 코드 보유(OFF) | ✅ 3키 전부 ON | SCENE_CONFIG |
| 18 | **프림 위생** — 단서는 자립 프림, `hazard_*` 분기 밖 | ✅ 조립부 hazard 분기 2줄 | ✅ 동상 | `grep 'cfg\["hazard_stairs"\]'` |
| 19 | **카메라 데이텀** | ✅ `datum_fail` 0 · 실측 Δ **0.000000 m** | ✅ `datum_fail` 0 · 실측 **`tol` 6컷 · Δ 0.004 m**(사전 선언한 점형블록 4 mm) | VG-datum |
| 20 | **void 커버리지 1.0** | ✅ 103,041/103,041 × 4팔 | ✅ 동일 | `heightmap_meta` |
| 21 | **커밋 전 검증 4종** | ✅ 4/4 green | ✅ 4/4 green | §12-8-1 |
| 22 | **룩체크 R7 사용자 게이트** | ⬜ **미실시 — 사용자 검수 대기** | ⬜ 동상 | §12-10 |

---

## 12-8. 커밋 전 검증 · 린트 · 레지스트리

### 12-8-1. 커밋 전 검증 4종 (`process_spec_v1.md:32-40`)

| 게이트 | 결과 |
|---|---|
| `py_compile` | **PASS** (2/2) |
| `NEGOBS_SMOKE=1` 씬 실행 (CPU 자기검사) | **PASS** — 두 씬 전항목 (낙차·수율/섹터·데이텀·16키·CUE_CLASS 완전성·법칙 2·스텝 게이트 도달성) |
| `geom_invariance_check.py` | **PASS** — R-5 잔존참조 0 · R-4/R-6 해시 3자 일치 (sceneH3 **240프림** `e05f3894` · sceneL1 **447프림** `d3b562bb`) |
| `placement_lint.py` | **PASS** (exit 0) — **ERROR 0** · WARN 2 · BLOCKED 2 |

**4팔 CPU 조립 확인** (GPU 전에): `NEGOBS_SCENE_CONFIG` 로 B·C·D 팔 구성을 넣고
`geom_invariance_check` 를 다시 돌려 **네 팔 전부 조립 성공 + 3자 해시 일치**를 확인했다 —
프림 수 sceneH3 A 240 / B 29 / C 220 / D 19 · sceneL1 A 447 / B 24 / C 371 / D 12.
(2차 런까지는 A팔만 CPU 검증했고 B·D 팔의 조립 실패는 GPU 라운드에서야 드러났을 것이다.)

### 12-8-2. `placement_lint` 가 실제로 잡은 것 (설계 중 수정 1건)

| # | 규칙 | 무엇 | 조치 |
|---|---|---|---|
| L1 | **LINT-2** (PE-2 · 조례 제7조1가 6–8 m) | sceneL1 진입 광장 가로수 3주의 피치가 **2.0 m** (소형 가로수로 설계) → **ERROR** | 광장을 5.6 → **15.8 m** 로 늘리고 피치를 법정 **7.0 m** 로. 광장은 높이맵 격자(x ≥ −2) **밖**이라 GT 영향 0 |

### 12-8-3. `placement_rules_v1.yaml` C-9 레지스트리 추가 (**정본 문서 수정 1건**)

LINT-6 의 `carrier_set` 은 볼라드를 가진 씬의 **레지스트리**다. 신설 2씬을 추가했다(+3행).
**가산적 변경**이며 D73 ②·§8-3 이 쓴 것과 같은 형식이다.

**라이브러리 전수 회귀 확인** — `placement_lint.py --scenes all` (**39씬**) 실측:

| | 이 런 (39씬) | §8-3 기록 (37씬) | 델타 |
|---|---|---|---|
| ERROR | **16** | 16 | **0** |
| WARN | 350 | 348 | +2 (신설 2씬) |
| BLOCKED | 35 | 33 | +2 (신설 2씬의 LINT-9 todo 게이트) |

**sceneH3·sceneL1 의 ERROR 기여는 0건**이고 기존 씬의 ERROR 분해도 한 칸도 움직이지 않았다.

### 12-8-4. 사전 선언한 WARN 2건

- **sceneH3 · LINT-6** — 볼라드 2본의 개구가 **2.40 m** 라 피치 규칙(1.50 ± 0.10)을 벗어난다.
  유효폭 3.60 m 골목에서 개구를 1.5 m 로 좁히면 **카메라 데이텀 스트립(\|y\| ≤ 0.95)** 을
  침범해 `datum_fail` 이 된다. 2.40 m 는 ⓐ 휠체어 유효폭과 ⓑ VG-datum 을 동시에 만족시키는
  최소 개구이며, 그 대가로 **advisory 경고 1건**이 남는다(sceneH7 §5-3 과 같은 판단).
- **sceneL1 · LINT-5** `walk_edges` **미선언**. 이 씬의 경로는 보도가 아니라 제방
  **관리 겸용 산책로**이고 진입부는 광장이다. 없는 데이텀을 지어내면 그 규칙은 검사가
  아니라 허구가 되므로 `nodata` 로 남긴다(§8-5 · SCENE_H67_BUILD §5-5 승계).
  `kerb_lines`·`anchors`·`routes` 는 실재하므로 선언했고 LINT-1/2/3/7 은 **live 로 통과**한다.

### 12-8-5. **4팔 준비 판정** (W3 test-ext 본렌더 착수 가부)

| 항목 | 요구 (계획) | sceneH3 | sceneL1 |
|---|---|---|---|
| 4팔 완비 (§3.1) | A·B·C·D 전부 렌더 가능 | ✅ 16컷 × 4팔 실렌더 | ✅ 16컷 × 4팔 |
| C팔 = `keep_dressing` 계열 | 위험 OFF · 단서·장식 유지 | ✅ 씬 내부 배선 + 모순 FATAL 2종 | ✅ 동일 |
| D팔 = 전 단서 OFF | `hazard=False` + 13키 OFF | ✅ | ✅ |
| B팔 레버 | VG-CLS 가 허락한 전 키 | ✅ 13키 전부 OFF 실렌더 · VG-01 `hm_gt_equal` 통과 | ✅ 13키 · VG-01 `hm_tol_offprint` |
| paired-H ≥ 12 (§3.2) | A·B 둘 다 strict-H | **12** ✅ | — (측방) |
| 섹터 목표 (§2.3) | 위험 질량이 A·E 집중 | — | **A+E 0.667 > B+D 0.300** ✅ |
| VG 게이트 전항 (§6.1) | — | ✅ | ✅ |
| 룩체크 R7 | 사용자 게이트 | ⬜ 대기 | ⬜ 대기 |

⇒ **판정: 두 씬 모두 W3 본렌더 웨이브 투입 가능** — 단 **R7 사용자 룩체크가 선행 조건**
이며(§12-10 항목 1·2), 본 보고는 그 게이트 앞에서 멈춘다. W3 이 요구하는 것은 §3.2 표
그대로다: **팔당 48프레임 · 4팔 192프레임/씬**. 본 프로브는 sceneH3 의 H 밴드와 sceneL1 의
LAT 밴드만 8포즈로 찍었으므로 W3 에서 **base 밴드 24컷 × 4팔 × 2씬**이 추가로 필요하다.

---

## 12-9. 부수 발견 — **계기 결함 3건** (계획 반영 필요)

§9 가 확정한 네 결함은 전부 *"게이트를 **어느 영역**에서 세는가"* 였다. 이번 런이 올리는
셋은 그 가족의 확장이다 — *"어느 **양**에 대해"*(9.1) · *"어느 **부호**로"*(9.2) ·
*"계기가 그 위험을 **볼 수 있는가**"*(9.3).

### 12-9.1 VG-01 의 결정층은 높이맵 셀이 아니라 **`polar_gt`** 다

**증상** `[260823_v3p5_h3l1probe · sceneH3]`: VG-01 이 `hm_fail` — 발자국 안 A↔B 다른 셀
**611 / 5,122**, max \|Δz\| **1.259 m**. 문면대로면 *"그 키는 구조물이므로 토글 금지"* 이고
계획 §7.4 는 그 키를 B팔 레버에서 빼라고 지시한다.

**범인은 구조물이 아니라 낙차 **위에 떠 있는** 벽면 계단 손잡이다.** 손잡이는 A팔에 있고
B팔에서 사라지므로 그 셀의 높이맵 z 가 "손잡이 상단 → 계단 상면"으로 내려간다.
Δz 의 최대값도 그것을 지목한다 — 손잡이 세그먼트가 2단(0.36 m)에 걸쳐 있고 노즈선 위
0.90 m 이므로 **0.90 + 0.36 = 1.26 ≈ 실측 1.259** 다.

**그러나 그 셀은 여전히 발자국이다.** 낙차가 0.3 m 아래로 사라지는 것이 아니라 그 위에
있던 얇은 관이 없어질 뿐이다. 실측이 증명한다:

| 량 | A팔 | B팔 | 판정 |
|---|---|---|---|
| **`polar_gt` (20칸)** | — | — | **16/16 프레임 비트 동일** |
| tier 분포 | H 12 · none_in_fov 4 | H 12 · none_in_fov 4 | 동일 |
| 발자국 `cells_kept` | 5,120–5,121 | 5,130 | 0.2 % 차 |
| `max_diff` | **2.363 m** | **1.800 m** | B팔이 곧 진짜 낙차(10 × 0.18) |

⇒ **제안**: VG-01 을 **4층**으로 늘린다.
`hm_exact` / `hm_tol_offprint` / **`hm_gt_equal`**(발자국 안 셀 z 는 움직이나 `polar_gt` 가
전 프레임 비트 동일) / `hm_fail`. 모델이 배우는 GT 는 20칸 `polar_gt` 이지 원시 높이맵이
아니므로 **결정층은 `polar_gt`** 다. 구현: `h12_gates.gate_vg01(…, ta, tb)`.
이 정정으로 **`cue_railing` 은 sceneH3 의 정상 레버로 남는다**(§7.4 의 "상수 키 승격"
자동 대응은 **미발동**). sceneH1·H2 의 기록은 이 정정 후에도 **한 칸도 바뀌지 않았다**
(둘 다 `hm_tol_offprint` 유지 · 종합 통과 유지 — 회귀 확인 완료).

> **이 결함은 `sceneH7`(val)에도 그대로 있다.** H7 은 같은 벽면 손잡이를 갖고
> (§3.3 이 `max_diff 2.623 vs 2.04` 로 이미 기록했다) 아직 **B팔을 찍지 않았을 뿐**이다.
> W2 에서 H7 의 B팔이 나오면 같은 `hm_gt_equal` 이 나올 것으로 예측한다.

### 12-9.2 VG-02 의 C↔D 비교는 **부호를 가려야** 한다

**증상** `[동 라운드 · sceneH3]`: 발자국 안 C↔D 차이 **126셀**. 문면대로면 미달이다.

**그러나 C 는 `keep_dressing` 팔이라 D 보다 프림이 많다.** 126셀은 전부
`z_C − z_D ≥ 0.30`, 즉 **C 가 더 높은** 셀 — 낙차 위 0.90 m 에 떠 있는 손잡이다.
*"위로 솟은 부재"는 낙차가 아니다.* 절대값 비교 `|z_C − z_D|` 는 그 구분을 못 한다.

⇒ **제안**: 판정층을 **`z_D − z_C ≥ depth`(C 에 구멍이 있다)** 하나로 좁히고, 반대 부호는
`cells_C_above_D`(= 보존된 장식·단서)로 따로 인쇄한다. C ≥ D 는 `keep_dressing` 의
정의상 항상 참이어야 하므로 **`cells_C_below_D > 0` 이야말로 진짜 이상**이다.
정정 후 실측: sceneH3 **낮은 셀 0 · 높은 셀 126** → 통과 · sceneL1 **0 / 0** → 통과 ·
sceneH1·H2 **0 / 0** (회귀 불변).

### 12-9.3 **라벨러 D10 스텝 게이트가 축방향 측방 위험을 보지 못한다** (계기 결함)

`labeler.step_gate` 는 발자국 성분을 **근접 경계**로만 살린다: 경계 셀 = *"카메라 쪽
4-이웃이 보행 가능 지면인 발자국 셀"* 이고, "카메라 쪽"은 `_outward()` 가
`rint(dx/n), rint(dy/n)` 로 **8이웃에 양자화**한 방향이다(`labeler.py:285-309`).
경계가 하나도 없으면 그 성분은 통째로 버려진다.

**측방 위험은 이 양자화에 걸린다.** 개거는 보행축과 **나란히** 달리므로,
\|dy\|/n < 0.5 가 되는 순간 `oy = 0` 으로 반올림되고 이웃이 **같은 개거 셀**이 되어
경계가 사라진다. 격자 서단 x₀ = −2 에서 닫힌 임계식이 나온다:

    n ≤ 2·|y_lip|  ⇔  (d + x₀)² + y_lip² ≤ 4·y_lip²  ⇔  **d ≤ −x₀ + √3·y_lip**
    y_lip = 3.54 ⇒ **d ≤ 8.13 m**

**실측이 이 식과 정확히 맞았다** `[260823_v3p5_h3l1probe_A · sceneL1 · 8포즈]`:

| d | y_c | `cells_kept` | tier |
|---|---|---|---|
| 4.98 · 7.15 · 7.32 · 7.70 | ≈ 0 | **25,038**(양측 전부) | V |
| **8.67** | **+0.76** | **12,519**(한쪽만 — y 오프셋이 남측만 살렸다) | V |
| **8.50 · 8.69** | ≈ 0 | **0** | **`none_in_fov` · GT 전무** |

즉 LAT 밴드(d 3–10)의 상단 25 % 가 *"낙차가 3.5 m 옆에 있는데 GT 가 0"* 인 프레임이 된다.
**씬 결함이 아니라 계기의 사각이고, 방치하면 거짓 음성 GT 를 만든다.**

⇒ **씬 쪽 조치(개정 R2)**: 개거를 가로지르는 **횡단 농로 복개 2개소**를 둔다. 복개 상면은
마루와 같은 z = 0.000 이라 발자국이 아니고 `~fp` 이며, 그 **동쪽 가장자리**가 −x 방향
이웃이 보행 가능한 셀을 만든다 ⇒ 근접 경계가 **d 와 무관하게** 존재한다. 스텝 게이트의
안쪽 레이는 그 립에서 1.0 m 안으로 −1.50 m 를 보므로 0.30 m 임계를 여유 있게 넘긴다.
제방을 가로지르는 농로가 양측 수로를 복개하고 지나가는 것은 농업용 개거의 **표준 구성**
이므로 이 장치는 계기를 위한 소품이 아니라 부지의 사실이다.
**효과 실측**: `none_in_fov` 0.25 → **0.000** · 근거리 라운드 `cells_kept = cells_raw`.

⇒ **계획 반영 필요 (근본 해법)**: 이 사각은 `sceneL2_lateral_ditch` 를 포함한 **모든 측방
씬**에 적용된다. 씬마다 복개를 두는 것은 대증요법이므로, `_outward` 의 8이웃 양자화를
**성분 경계 전수 스캔**으로 바꾸는 것이 옳다(비용은 CPU 뿐이다). 그 전까지는
**측방 씬은 격자 안에 횡단 보행 가능 요소를 하나 이상 두어야 한다**를 저작 규칙으로 둔다.
sceneL1 의 CPU 자기검사가 이 조건을 **하드 체크**로 걸고 임계식 값을 매 실행 인쇄한다.

> **세 결함의 공통 구조**: §9 의 넷이 *"어느 영역에서"* 였다면 이번 셋은
> *"어느 **양**에 대해(9.1) · 어느 **부호**로(9.2) · 계기가 **볼 수 있는가**(9.3)"* 다.
> 일곱 개 전부 **정상 씬을 실패로 읽었고**, 일곱 개 전부 씬 개정이 아니라 계기 정정으로
> 닫혔다(9.3 만 계기 정정 전까지 씬 쪽 우회를 병행한다).

---

## 12-10. 남은 것 (사용자 게이트 / 후속 WP)

| # | 항목 | 성격 |
|---|---|---|
| 1 | **룩체크 R7 사용자 검수** — `make_review_gallery.py --round 260823_v3p5_h3l1rev_A` 후 체크리스트 5항 | **사용자 게이트** (process_spec §27-31) |
| 2 | **sceneH3 의 잔여 어두움** — H 밴드 16컷 `dark(<16)` 중앙값 **33.3 %**(범위 8.6–66.7 · mean 중앙값 44.3 · clip 0.000 %). 개정 R1·R2 로 73.1 → 45.1 → 33.3 % 까지 내렸고 **기하 개정 상한 2회를 소진**했다. 남은 원인은 계열 고유다 — 마루 1.95~2.45 m 벽 사이의 폭 3.60 m 회랑을 눈높이 **0.25–1.0 m** 에서 보면 근거리 그늘이 화면을 지배한다. 더 밝히려면 기하가 아니라 **한 줄 값**을 고치면 된다: `rock_tint` 0.54 → 0.62 · `brick_tint` 0.60 → 0.70 (둘 다 look 층 상한 0.34 아래라 알베도 규약을 깨지 않는다). **같은 성질이 `sceneH7`(val)에도 있으므로 R7 판단은 두 씬에 함께 적용해야 한다** | **R7 판단 대기** |
| 3 | **sceneL1 의 `frac(max>0.8)` 중앙값 0.107** (범위 0.000–0.280). `regression_check` BLOWN 임계(mean > 210 · clip > 0.2 %)는 **전 컷 통과**(mean 중앙값 124.9 · clip 0.000 %)이고 대부분이 하늘이지만, v5.1 §4 "순백 대면적"의 취지에서 R7 항목으로 올린다. 낮추려면 `walk_tint` 0.64 → 0.58 한 줄 | **R7 판단 대기** |
| 4 | **§6.6 현실 대조 정성 패널** — 좌 = 실측 프로토콜 컷이 필요한데 저장소에 없다(**승용 촬영분 대기**). §10 항목 2 와 같은 대기 | **사용자 입력 대기** |
| 5 | §12-9 의 계기 결함 3건 — 계획 §6.1 VG-01/02 문면 갱신 + **라벨러 `_outward` 근본 수정** | **계획·정본 문서 수정** (9.3 의 라벨러 수정은 정본 코드라 **결재 대상**) |
| 6 | `variation_kit` AZ 원장 정식 등재 + `split_map` 핀 테이블 — 이 런도 런타임 패치로 우회했다(§6.1 · H67 §6.1 과 동일) | W2/W3 본렌더 전 필수 |
| 7 | **sceneH3 의 퇴화 판정** — H 밴드는 화각 안 전부 H(가시 0)이고 근거리는 전부 `none_in_fov` 다. 가시 프레임의 창이 좁아 **base 밴드 실렌더로만 확정된다**. CPU MC 예측: 격자 안 base 프레임의 **19.2 %** 가 가시 | W3 |
| 8 | **base 밴드 실렌더** — sceneH3 base · sceneL1 base 는 아직 **CPU 모형값**이다 | W3 |
| 9 | **측방 씬 저작 규칙 신설** — §12-9.3 의 "격자 안 횡단 보행 가능 요소 ≥ 1" 을 `sceneL2` 브리프에 반영 | W3 / Wave C |
| 10 | `sceneH3` 의 `cue_tree_grate`, `sceneL1` 의 `cue_delineator` 는 기본 OFF — 어블레이션 팔에서만 켜진다 | 설계 의도 |

---

## 12-11. 부록 — 재현 명령

```bash
# CPU 자기검사 (pxr·GPU 불필요)
NEGOBS_SMOKE=1 python3 scenes/main/sceneH3_bend_walk.py
NEGOBS_SMOKE=1 python3 scenes/main/sceneL1_lateral_canal.py

# 커밋 전 검증 4종
python3 -m py_compile scenes/main/scene{H3_bend_walk,L1_lateral_canal}.py
python3 scripts/geom_invariance_check.py --scenes sceneH3,sceneL1
python3 scripts/placement_lint.py       --scenes sceneH3,sceneL1
python3 scripts/placement_lint.py       --scenes all      # 라이브러리 회귀 (39씬)

# 4팔 CPU 조립 확인 (GPU 전) — B·C·D 팔 구성을 넣고 다시 돌린다
NEGOBS_SCENE_CONFIG='{"hazard_stairs": false, "keep_dressing": true}' \
  python3 scripts/geom_invariance_check.py --scenes sceneH3,sceneL1

# GPU — 스모크 → 4팔 프로브 → 개정 후 확정 → 근거리 (tmux textbuild2 · flock 직렬화)
bash experiments/v3_0823/code/run_h3l1_probe.sh smoke
CAMS=8 bash experiments/v3_0823/code/run_h3l1_probe.sh probe
CAMS=8 bash experiments/v3_0823/code/run_h3l1_probe.sh rev sceneH3
CAMS=8 bash experiments/v3_0823/code/run_h3l1_probe.sh rev sceneL1
bash experiments/v3_0823/code/run_h3l1_probe.sh near

# 라벨 3회 + 게이트 2종 (씬마다 확정 라운드가 다르므로 두 번 돈다)
bash experiments/v3_0823/code/run_h3l1_label.sh \
     260823_v3p5_h3l1rev 260823_v3p5_h3l1near revh3 sceneH3
bash experiments/v3_0823/code/run_h3l1_label.sh \
     260823_v3p5_h3l1rev 260823_v3p5_h3l1near revl1 sceneL1
```
