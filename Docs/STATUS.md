# STATUS — 08-11 2판: 갤러리 회신 배치 2 착지(GT-104~107 + GT-73 frosted 자연화) (2026-08-11)

직전 판(08-10 2판: 4씬 재지시 수령·유리 정정)은 git 이력 참조. 이 판 = 08-11 사용자 갤러리
회신 5건(유리 어두움 / s06 난간 유리 / s11 난간 바 / s16 방호·가독성·조명 / 바닥 카펫 노이즈)
일괄 착지 후.

## 한 줄
08-11 회신 5건 전량 코드 착지·라운드·regr·갤러리 완료 — **검수 대기 갤러리 6건 신규**
(s11clean·s06bay14·s13frost_c/d/e·s16under). s06 이음/코벨 동결·frosted 확산 보류는 유지.
바닥 노이즈 중 잔여 1건(아스팔트 매크로 패치 톤)은 **레버1 슬롯으로 이월 선언**(원장 GT-107).

## 읽는 순서 (신규 합류)
1. 이 파일 → 2. `briefs/process_spec_v1.md` → 3. `audit_v4/gt_changes_w3.md` 행 29(GT-73
   08-11(2) frosted 자연화)·60~63(GT-104~107 — 전부 08-11 착지) → 4.
   `reports/critique_intent_map_v1.md`(수정 전 필독)

## 현재 상태
- **검수 대기 갤러리(재촉 금지)** — 08-11 배치 2(신규 6): `_review/260811_w3_s11clean`
  (GT-105 하강 가드 중간 가로대 소거 + GT-107 파일럿) · `_review/260811_w3_s06bay14`
  (GT-104 데크 판폭 재모듈 — 에이프런 2+필드 12, Δ21 mm) · `_review/260811_w3_s13frost_c`
  **(채택안: rough 0.35·유백 0.90)**·`_d`(0.22)·`_e`(0.50) — 부스 젖빛 70→102(+46 %) ·
  `_review/260811_w3_s16under`(GT-106 복개 연장 6.40…31.15·헤드월 2기·지하보도 표지·통로
  조명 5등). 기존 대기: s06glass(2판)·s11piers·s13frost_a/b(참고 보존)·s16road6 + 사용자
  보류 2건(s13fix7·fixqueue2).
- **s06 이음·코벨 동결 유지**(3판 철회 상태 — 재지시 대기). GT-104 는 난간 판열만 접촉.
- **유리**: GT-73 HELD 유지 — frosted 자연화 착지(c 채택·d/e 대체안), **확산은 검수 후**.
- **바닥 데칼 정온(GT-107)**: crack·stain 전 프로파일 미발행(복원 `NEGOBS_DECAL_FULL=1`),
  s11 보행면 재질 오지정 2건 정정, s13 판석 러너 소거. **이월 소견**: 잔여 s13 필드 톤·매크로
  패치 대비 = 룩 레이어 asphalt 클래스 소관 → 레버1 재보정 슬롯에서 취급(씬측 tint 는 승격
  재질에 미도달 — 원장 실측 기록). 잔여 29씬 데칼 정온 육안 = 차기 allview 라운드.
- **결재 상태 유지분**: C4 수막 유지 · 헤지 밀도 확산 보류 · C2 대안 3안 기억만(재개 시 사용자
  상기 필요) · 건물 사실성 방향 재고 지시 유효.
- **비주얼 갱신**: 레버1(재질 재보정) 승인 — GT-107 이월 소견 1건이 첫 작업항목으로 등재됨 ·
  레버2 조건부(실외 노출면만) · 레버3 승인. 적용 라운드는 큐 소화 뒤 선신고.
- **북극성(08-10 2차, 사용자)**: 씬의 목적은 **'있을법한 환경'**이지 화려한 구현이 아니다.

## 다음 (착수 순서)
1. 검수 회신 처리(오면) — 6건 신규 갤러리 + 기존 대기분.
2. **s03 측정 조사**(기하 무변경, CPU 전용): x축 정렬안 — **현실성(실제 시공 개연성) 우선**,
   내부 기준(차폐 35 % 등)은 참고 지표 → 수치 보고 → 판정 후 새 행.
3. **건물 사실성 방향 재고안**(유형학 기반 — 요소 체크리스트 아님) 작성 → 제시.
4. **레버1 재질 재보정**(선신고로): 첫 항목 = GT-107 이월(asphalt 클래스 매크로 패치·등화
   틴트 재보정 — 33씬 blast radius, 룩 클래스 차원).
5. W4 GT 재판정 → W5 생산 렌더 (기존 로드맵 유지).

## 규율
`process_spec_v1.md` §2 정본. floor 4종 / 같은 prim A/B / 선신고 / gallery --out.
**GPU flock 은 반드시 `flock -o`** — 08-11 실사고: -o 누락 시 Isaac 부팅이 띄우는 Omniverse
Hub 데몬이 락 fd 를 상속해 렌더 종료 후에도 락이 안 풀림(대기열 기아). 회수 절차·재발 방지는
메모리·`run_260811_w3_batch2.sh` 헤더 기록. 캡처 대기 `NEGOBS_CAPTURE_WAIT`(기본 160).
기존 신규 규율(08-10 2차) 유지: ① 룰링 기록은 사용자 표현 강도 그대로 ② 검수·결재 재촉 금지
③ 짧은 답변이 애매하면 설명 먼저, 기하 변경은 명시 판정 후 ④ 선택지 UI 금지 — 질문은 본문
텍스트로.
