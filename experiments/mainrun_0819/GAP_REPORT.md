# GAP_REPORT — 사람(승용)이 채워야 할 항목 (08-20 아침 결재용)

밤사이 자율로 진행한 것 중 **제가 대신 정하면 안 되는 것만** 모았습니다. 각 항목은
〈상황 → 밤에 임시로 한 것 → **결정하실 것** → 비용〉 순서이고, 근거는 `DECISIONS.md`의
D번호와 `GATES_REPORT.md`의 절 이름으로 표기했습니다. 급한 순서 **① > ③ > ⑥ > ④ > ⑤ > ② > ⑦**.

## ① 폴라 그리드 상수 — 리포에 아예 없음 (가장 급함) `D3`

- **상황**: 15칸 격자(방위 몇 도씩, 거리 몇 m씩)를 정의한 코드가 리포 어디에도 없었습니다.
  브리프는 "코드가 진실"이라고 했는데 그 코드 자체가 없는 상태였습니다.
- **임시 조치**: 리포에 흩어져 있던 앵커 값으로 `PROVISIONAL-GRID-V0`를 선언하고 밤을
  진행했습니다 — 방위 5섹터를 [+31.1°, −31.1°](명목 hfov 62.2°)에 12.44°씩 균등 분할,
  거리 밴드 0–2 / 2–5 / 5–12 m, 셀 번호 = 밴드×5+섹터. 라벨·모델·결과 파일 전부에
  `PROVISIONAL-GRID-V0` 태그를 박아 뒀습니다.
- **결정하실 것**: 이 상수가 맞는지, 아니면 원래 쓰시던 값이 따로 있는지.
- **비용**: `code/labeling/gridspec_v0.json` 숫자만 바꾸면 라벨 재파생 → 재훈련까지
  원커맨드로 **약 40분**입니다(렌더는 재사용, 다시 안 찍습니다).

## ② V/E/H 계측기 — 사양대로 만들 수 없었음 `D6` · `GATES_REPORT: tau sensitivity`

- **상황**: 브리프 A3의 티어 판정기(semantic ID로 물체를 식별하고 raw 가시성을 저장하는
  방식)는 구현이 리포에 없습니다. 만들려면 씬마다 Semantics 저작이 필요합니다.
- **임시 조치**: depth 재투영 기반 파생으로 대체(`tier_source=derived-depth-v0`). 문턱값은
  걱정 안 하셔도 됩니다 — **strict-H 45프레임이 τ_int{1,50,200} × τ_edge{0.02,0.05,0.10}
  9조합 전부에서 한 프레임도 안 바뀝니다**(사실상 무민감). 흔들리는 건 V/E 경계뿐(V 417~453, E 33~42).
- **결정하실 것**: 이번 결과를 "파생 티어" 표기로 그대로 낼지, 정식 계측기를 후속 과제로
  잡을지. 정식화는 밤 작업 범위 밖입니다.

## ③ HOLD 4씬 — 씬 코드/렌더를 손대야 풀리는 문제 `D15` · `D16` · `D17`

훈련·검증·테스트 어디에도 안 넣고 빼 둔 씬 4개입니다(오염 방지). 각각 사람 결정이 필요합니다.

- **sceneD4**: 밀폐 실내라 ⓐ 노출이 어둡고(평균 휘도 0.5, 노출 위반 48컷 전부 이 씬)
  ⓑ 높이맵 상공 레이가 지붕에 막혀 위험을 못 봅니다.
  → **결정**: 실내 씬용 노출 프리셋을 따로 둘지, 이번 판에서 D4를 뺄지.
- **scene11 / scene19**: hazard 토글을 켜고 끄면 **카메라가 서 있는 지면 자체가 같이
  움직입니다**. "같은 자리에서 위험만 제거"라는 트윈 전제가 깨집니다.
  → **결정**: 씬 코드에서 카메라 기준면을 토글과 무관하게 고정할지, off팔 카메라 높이를
  보정해 다시 렌더할지.
- **scene13**: 보행 근경계가 depth 융합 커버리지 밖이라 GT가 전멸합니다.
  → **결정**: 근경계가 보이는 컷을 추가로 렌더할지, footprint를 해석적으로 구할지.

## ④ 유리 가림물 — 자동 판별 불가, 눈으로 봐 주셔야 합니다 `D6`

- **상황**: 티어 판정기가 "위험이 유리 너머로 보이는 것"과 "그냥 보이는 것"을 구분하지
  못합니다(브리프 A3④). 잘못 두면 H(은닉)여야 할 프레임이 V(가시)로 새어 들어갑니다.
- **확인 대상 3씬**: `scene06`(DeckGlass) · `scene08`(발러스트레이드) · `scene12`.
  오버레이는 `audit_samples/`에 있습니다.
- **결정하실 것**: 세 씬에 실제로 투명 가림물이 있는지 육안 확인 → 있으면 해당 씬 티어를
  손으로 강등할지 여부.

## ⑤ 트윈 포즈 불일치 183쌍 `D15④` · `STATUS 01:50`

- **상황**: 양팔 792쌍 중 **183쌍**에서 두 팔의 카메라가 완전히 같지 않습니다. 샘플러가
  "이 자리는 안 됨"으로 기각하면 다시 뽑는데, 기하가 달라진 off팔에서 다른 자리가 뽑히기
  때문입니다(어긋나는 값은 `cam.ground_z` 한 필드).
- **임시 조치**: 트윈 분석은 **매칭되는 609쌍만** 씁니다. 훈련에는 영향 없습니다. 참고로
  `code/twin_analysis.py`의 기본 포즈 비교 키에 `ground_z`를 넣었습니다 — 브리프의 4개 키
  (d/h_rel/yaw/pitch)만 보면 이 183쌍이 하나도 안 걸러집니다(`--pose-keys`로 전환 가능).
- **결정하실 것**: 609쌍으로 충분한지, "기각 없는 고정 카메라 리스트"를 도입해 100% 트윈을
  만들지(도입 시 전 씬 재렌더 필요).

## ⑥ G5 목표 0.80 — 기하적으로 도달 불가 `GATES_REPORT: G5`

- **상황**: V티어에서 "GT 양성 셀에 실제로 지면 아래 픽셀이 보이는 비율"이 평균 0.552로
  목표 0.80 미달입니다. 밴드별로 보면 원인이 분명합니다 — **밴드1(0–2 m) 0.029 /
  밴드2(2–5 m) 0.475 / 밴드3(5–12 m) 0.715**. 가까운 낙차는 립(모서리) 뒤가 카메라
  시야그림자라 바닥이 원래 안 보입니다. 라벨 결함이 아니라 기하 문제입니다.
- **결정하실 것**: 판정을 **밴드3 기준으로 재정의**할지(그러면 0.715 대 0.80), 목표치를
  내릴지, G5를 "참고 지표"로 강등할지.

## ⑦ 예비탄 3발 — 쏠지 말지 `D12` · `D18`

명령어는 다 준비돼 있고 실행만 안 했습니다. 셋 다 독립이라 골라서 지시하시면 됩니다.

1. **test H씬 보강 렌더**: `scene14`/`scene15`에 카메라 +8대 → test strict-H가 21 →
   **약 40프레임**, 헤드라인 지표(H recall) 신뢰구간이 눈에 띄게 좁아집니다. 씬당 약 3분,
   단 라벨 재파생 필요.
2. **`code/train_oversample.py`**: 양성 프레임 오버샘플링 훈련(적은 H를 손실 쪽에서 보정).
3. **`code/aug_photometric.yaml`**: 밝기/노이즈/JPEG 증강 켜기(실사 일반화용).
