# STATUS — 08-10 2판: 4씬 재지시 수령 · 유리 '금지' 표현 정정(추가 검토로) · 수막 유지 (2026-08-10)

직전 판(08-10 1판: GT-90~93 revert 기록)은 git 이력 참조. 이 판 = 08-10 2차 사용자 회신 반영.

## 한 줄
GT-89 유지·GT-90~93 revert 유지(1판 그대로). **08-10 2차 회신으로 4씬 재지시 수령, 3차
회신으로 판독 확정**: s03(x축 정렬 조사 — **현실성 우선**, 내부 기준은 참고 지표로 격하:
"그 기준들 자체를 네가 정했던 거") · s16(**6차로 이상, 더 커도 됨** + 지하통로 전면 대공사
확정) · s06(대상 판독 정정 — 300° 나선이 아니라 **나선 상부 반원(0~180°) 환형 랜딩 칼라**,
동측 무기능 로브 포함 → "원 1/4" 섹터 연결부로 정리) · s11(**일관성 유지 + 깔끔하게** 확정).
**유리 정정: "전면 금지"는 기록 격상 오류** — 실제는 추가 검토(보류), 원장 GT-73 08-10(2)
정정 참조. 3차: **경계를 애매하게 투영하는 부분은 변경 필요 방향 확인** — 전 씬 간단 고려
결과 실질 대상은 scene13 유리 중 경계 차폐 프림뿐(나머지는 불투명 재질 또는 의도된 단서).
**C4 수막은 hazard 형태로 사실성 유지 확정**(GT-88 행 기록).

## 읽는 순서 (신규 합류)
1. 이 파일 → 2. `briefs/process_spec_v1.md` → 3. `audit_v4/gt_changes_w3.md` 행 45~49
   (45=GT-89 LANDED·08-10(2) old skins 해소 · 46~49=**WITHDRAWN — 구판독 재착지 금지** ·
   행 29=GT-73 유리 08-10(2) 정정) → 4. `reports/critique_intent_map_v1.md`(수정 전 필독)

## 현재 상태
- 씬 상태 = **GT-72~88 착지 직후 복원 유지**(scene03 곡선 고가 + 선언 FAIL 38.9 % 포함 —
  사용자 룰링 귀속의 유효한 선언 상태).
- **검수 대기 2건은 사용자 보류**("일단 내버려둬" — 08-10 2차): `_review/260806_w3_s13fix7` ·
  `_review/260806_w3_fixqueue2`. 재촉 금지. `_review/260807_w3_fixqueue3` 는 revert 상태의
  렌더 — 참고용, 검수 대상 아님.
- **결재 상태(08-10 2~5차)**: ① 유리 — **추가 검토(HELD, '금지' 아님)**. 측정 완료(GT-73
  행 08-10(4)): s13 31프림 중 **낙차 경계 차폐 23 · 무관 8**. 5차 사용자 제안: **frosted
  (블러) 유리** — "유리 적용하되 블러를 좀 넣는 방식… 뭉개지는 편이 차라리 마음 편하고" →
  기술적으로 가능(OmniGlass roughness 거친 투과 — PT 에서 배경이 뭉개짐; 현행 OmniPBR
  opacity 는 배경이 선명하게 비침). 후속안: 차폐 23 = frosted · 무관 8 = 클리어, s13 파일럿
  A/B — 사용자 "천천히 볼게" 슬롯에서. C4 수막은 유지 확정
  ② 헤지 밀도 확산 — **보류**, s16 대공사 안에서 깔끔함에만 집중 ③ C2 대안 3안 — **기억만**
  (3안의 실제 내용은 repo 문서 미기록 — 재개 시 사용자 상기 필요) ④ 건물 사실성 — **방향
  재고 지시**: 실외기 등 요소 나열이 아니라 "빌딩다운 빌딩"(유형·매스·비례) 차원으로 접근;
  시스템 FCU 방식이면 실외기 없음도 정당.
- **결정 대기(설계)**: s03 x축 정렬안 — 측정 수치 보고 후 사용자 판정(기하 변경은 판정 후).
  scene16 row-35 대기항목(하부 유효고 1.75·x=0 종단 포스트)은 s16 대공사에 흡수.
- **비주얼 갱신 판정(08-10 5차)**: 레버1(공통 재질 재보정) **승인**("사실화 작업이면 진행") ·
  레버2(웨더링) **조건부** — 제시 예시가 노출 논리 위반(캐노피 하부=비 안 맞는 공간에 빗물
  스트리크): 채택하더라도 **실외 노출면만**, 지붕 덮인 공간 제외 · 레버3(실자산 재표현)
  **승인**. 적용 라운드는 4씬 큐 뒤 슬롯에서 선신고로.
- GT-89 "old skins" 잔여 질문 **해소**(비주얼 의도 아니었음 확인 — 원장 행 45).
- **북극성(08-10 2차, 사용자)**: 씬의 목적은 **'있을법한 환경'**이지 화려한 구현이 아니다.

## 다음 (착수 순서)
1. **s06 — GT-98 착지(행 54, 검수 대기)**: 8차 지시(폭 불일치·기둥 무쓸모) — **계단 전폭
   광폭화**(r_out 4.5, 상두 에지 = 단부 전폭 x 2.0~5.0, 함정 잔부 소멸 — s=0 GT 계단
   하강형 재분류·W4 후보, 축 3.5 복원) + **기둥 지지 리브 10본**(기둥→소핏 레벨 방사재).
   GT-97(행 53) 위 증축 — 타워 동측 이동·정면 접속은 유지. floor 전판 OK, round
   `260811_w3_s06wide`, regr 전량 재구도 선언 귀속, gallery `_review/260811_w3_s06wide`.
   사용자 검수 대기(천천히).
2. **s11 — GT-96 착지(행 52, 검수 대기)**: 리본 단일 높이 1.25(테이퍼·빌보드 소멸,
   HeadMesh 팬스 정착) + **동측 미러 레그 삭제**(다이아 소거 — 개정 H 3각, 비운 두부면
   L-가드 폐합, −988프림), hazard ① 존치. floor 전판·A/B 델타 0, round
   `260811_w3_s11ribbon` regr 전량 FRAME 선언 귀속, gallery `_review/260811_w3_s11ribbon`.
   사용자 검수 대기(천천히). s06 는 GT-99(행 55, 마감 2건)까지 착지 — 검수 대기.
3. **s16 대공사**(확정): 상부 도로 **6차로 이상(더 커도 됨)** 확폭 → 지하통로 연장·단면·
   출입구·계단·유효고 전면 재유도. row-35 대기항목 흡수. 헤지는 깔끔함만.
4. **s03 측정 조사**(기하 무변경, CPU 전용 — 병행): x축 정렬안 — **현실성(실제 시공
   개연성) 우선**으로 평가, 내부 기준(차폐 35 %·이격 밴드 등)은 참고 지표로만 → 수치 보고
   → 판정 후 새 행
5. **건물 사실성 방향 재고안**(유형학 기반 — 요소 체크리스트 아님) 작성 → 제시. 비주얼
   갱신 = 레버1·3 승인/레버2 노출조건(08-10 5차) — 적용 라운드는 4씬 큐 뒤 선신고로.
   frosted 유리 파일럿(차폐 23 frosted·무관 8 클리어)은 s13 검수 슬롯에 준비.
6. W4 GT 재판정 → W5 생산 렌더 (기존 로드맵 유지)

## 규율
`process_spec_v1.md` §2 정본. floor 4종 / 같은 prim A/B / 선신고 / flock(-o) / gallery --out.
33씬 전부 SMOKE 안전(GT-89) — 구형 씬 특례 폐지. 캡처 대기 `NEGOBS_CAPTURE_WAIT`(기본 160).
**신규(08-10 2차)**: ① 룰링 기록은 **사용자 표현 강도 그대로** — 보류·재검토를 '금지/기각'
으로 격상 금지 ② 검수·결재 **재촉 금지** ③ 짧은 답변이 질문인지 지시인지 애매하면 설명
먼저, 기하 변경은 명시 판정 후(1판 규칙 유지).
